# Project State

> File ini dibaca OTOMATIS oleh agent di awal setiap sesi. JANGAN dihapus.
> Diperbarui oleh agent sebagai langkah TERAKHIR setiap kali sesi/tahap
> selesai atau saat checkpoint. Ini bukan pengganti ROADMAP.md atau
> DECISIONS_LOG.md — ini cuma penunjuk cepat harus mulai dari mana.

STATUS: CODING_DIJEDA_SADAR (audit AUD-3 & review PR putaran13 SELESAI: 27 temuan diverifikasi NYATA & ditutup migrasi `0014`; suite SQL 41/41, mutasi 16/16 & 17/17 MERAH, CI HIJAU; sesi kerja ditutup 2026-09-18 — lanjut di sesi baru lewat `docs/ops/SIAP-LANJUT.md`; PR #1 TETAP JANGAN MERGE)
DETAIL: **FASE 1 DIJEDA DENGAN SADAR DI T1-10 — sedang mematangkan KEAMANAN & KELENGKAPAN UI (permintaan pemilik, pesan ke-14, 2026-09-17).** Pemilik meminta jangan lanjut sebelum dua hal ini matang: **(a)** mekanisme login teraman per peran (kasir hanya bisa buka kasir, dst.), **satu orang dua fungsi = dua akun**, bagian staf **hanya bisa diakses dari perangkat terdaftar**, login staf **mudah tapi aman**, dan antisipasi **perangkat hilang/dicuri**; **(b)** rencana fitur–layar–tombol yang lebih matang karena pengalaman pemilik sebelumnya (dibangun dengan agent AI) banyak **tombol kurang** dan **fungsi yang katanya ada tapi tidak bisa dipakai**. Pemilik juga meminta agent **tidak langsung meng-iya-kan** tetapi meneliti & mengusulkan yang lebih baik, serta memaksimalkan skill yang tersedia. Hasil: naskah usulan `docs/teknis/USULAN_KEAMANAN_DAN_KELENGKAPAN_UI.md` + 6 pertanyaan keputusan → **pemilik menjawab: (1) PIN hanya sah di perangkat terdaftar · (2) kode pendaftaran admin + persetujuan pemilik saat pegawai baru pertama memakai perangkat · (3) satu akun satu peran diserahkan ke agent → ditetapkan KETAT · (4) TOTP diminta saran terbaik → ditetapkan WAJIB untuk pemilik platform, owner pusat, DAN admin cabang + jalan pemulihan · (5) mode dukungan berbatas waktu & tercatat · (6) setuju semua rencana dokumen.** Fondasi sudah diperbarui: **`docs/KEAMANAN.md` (baru, aturan resmi)**, **`docs/SPESIFIKASI_UI.md` (baru)**, **`docs/teknis/BUKU_INSIDEN.md` (baru, 12 bab langkah darurat)**, TECH_SPEC **ART-11…ART-15** + §4.6 + §5.1 + §8 ditulis ulang, PRD M12 diperdalam + **Aturan Bisnis 14–18** + risiko #9–#11, DECISIONS_LOG **+10 entri**, ROADMAP **+28 tugas (179 total)** (kini **193**: +T1-40 kerangka bahasa, +T1-41 RTL/huruf, +T1-42 bantuan tiap laman, +T1-43 Buku Uji Pemilik, +T1-44 perketat paket audit; +T1-45 penutupan temuan putaran verifikasi; +T1-40/T1-41 = keputusan Lee 2026-09-17 Opsi 1): **Fase 1B** (T1-23…T1-30, migrasi 0011–0016: peran tunggal · perangkat terdaftar · sesi & pencabutan · percobaan masuk · audit berantai · mode dukungan · matriks izin · pemeriksa keamanan) dan **Fase 1C** (T1-31…T1-35: peta layar · registri aksi · pemeriksa peta UI · harness uji komponen · naskah jalan). **Nomor migrasi rencana lama bergeser +7** (kas & shift 0011 → **0018**). Buku tunggu kini **6 butir terbuka** (batas 12; T-014 region sudah ditutup: **Singapore**). PR #1 tetap **OPEN** (merge = keputusan pemilik). Fase 1 yang sudah selesai & ber-CI-hijau: **T1-01…T1-10** dan **T1-23** (peran tunggal + PIN 6 angka unik & kuat, migrasi 0011; 24 tabel + 2 tabel rahasia, **21 berkas uji SQL, 21 LULUS · 0 GAGAL (angka saat itu — T1-23, 2026-09-17; kini **41 LULUS · 0 GAGAL** per STATUS.md)** — termasuk uji regresi temuan audit AUD-3: **seluruh K-1 & K-2 TUNTAS**, K-3 F-11 dipagari ke Fase 1B dengan uji yang bisa MERAH; **pagar wajib: T1-24 harus memperketat `supabase/tes/percobaan_pin_perangkat.sql` dan menutup baris F-11 di `docs/uji/AUDIT_RIWAYAT.md` §1b**).
PUTARAN 7 (2026-09-17, setelah T1-23): **verifikasi menyeluruh permintaan Lee** dijalankan (bukan mengandalkan centang) → cacat ketertelusuran ditemukan & ditutup; daftar temuan audit kini per temuan (dijaga `alat/periksa-temuan-audit.py`); 3 pemeriksa baru (`periksa-rujukan`, `periksa-temuan-audit`, `periksa-buku-uji`) + `periksa-panduan --uji-diri`; **npm audit 0**; **Buku Uji Pemilik** dibuat berisi baris "harus Lee lakukan" & "harus Lee coba"; tugas baru T1-42 (bantuan tiap laman) · T1-43 (buku uji + pemeriksa + gema chat) · T1-44 (perketat paket audit). Pratinjau desain dinyalakan (port 5173) untuk baris U-01.
PUTARAN 8 (2026-09-17): cacat "kontrol mati" Nyaman/Padat diperbaiki di sumber desain (prototipe) + penjaga `aplikasi/alat/periksa-kerapatan.py` & `alat/periksa-rahasia.py` (+`--uji-diri`), uji interaksi `kerapatan.test.tsx`, lembar kunci pemilik (formulir ikut Git; berkas kerja terisi tidak ikut Git), Buku Uji Pemilik bertambah baris U-06 & P-03. Paket review PR putaran8 (`7e8c0b9`) tetap berlaku untuk sesi peninjau yang sedang berjalan.
PUTARAN 8b (2026-09-17): CI kiriman pertama GAGAL — enam dokumen merujuk ber-backtick ke berkas kerja yang tidak ikut Git (di komputer sendiri tampak sehat). Diperbaiki (rujukan ke formulir + nama berkas kerja tanpa backtick) dan ditutup kelas cacatnya oleh penjaga baru `alat/periksa-bersih.py` (pohon bersih berisi hanya berkas terlacak; 2 uji mutasi) di `periksa-semua.sh` & CI.
PUTARAN 10 (2026-09-17, pesan ke-28/29): tema & Padat diperbaiki + dijaga (commit `597cc77`) · **laporan 3 sesi peninjau dibaca** — ketiganya TIDAK-BERSIH, ditutup migrasi `0012` + 7 uji + bukti mutasi 12/12 (kini ikut CI) · **PR-07 ditutup**: `periksa-semua.sh` & `alat/uji-database.sh` memasang pustakanya sendiri sehingga janji Buku Uji benar-benar berlaku di salinan baru (bukti: `uji: 28 LULUS · 0 GAGAL`; `SEMUA PEMERIKSAAN LOLOS`, 166 lolos/0 gagal) · **katalog kalibrasi review diacak** (6→10 cacat) + **daftar tunggal temuan luar-cakupan** · **paket review putaran11** terbit untuk tip `750889a` (369 berkas) — Lee: chat baru → salin berkas SIAP-TEMPEL → bilang "Laporan review sudah masuk, periksa". PR #1 tetap **tidak di-merge** (keputusan Lee).
PUTARAN 11 (2026-09-17, pesan ke-30): **hasil review PR diperiksa** — Lee menjalankan 3 sesi peninjau di satu cabang (`arena/01a0b093-resto-barokah`); 2 laporan masuk & LOLOS kontrak (tetapi 1 temuan **PALSU** dan 1 **nyata**). PR-01 ("CI tidak menjalankan 28 uji SQL") **terbantah** dengan eksekusi — `--daftar` tetap menjalankan seluruh uji (`uji: 27 LULUS · 1 GAGAL`, exit 1 saat uji sengaja dirusak) → dicatat di `docs/uji/REVIEW_PR_RIWAYAT.md` §2b. PR-02 (penjaga buku uji lebih longgar dari klaim) **nyata** → penjaga menilai baris petunjuk paket + uji-diri 5 mutasi. Lahir penjaga baru `alat/periksa-gerbang-ci.py` (9 gerbang wajib, tolak `|| true`/`continue-on-error`/ambang turun; uji-diri 6 mutasi), langkah CI `run: node alat/uji-sql.mjs` (penuh, nama "Uji SQL penuh"), protokol §6a (temuan gerbang wajib dibuktikan dengan menjalankan; temuan K-3 wajib dibantah ulang oleh sesi kerja), dan aturan 5b di prompt peninjau. Temuan luar-cakupan peninjau masuk daftar tunggal `L-09`…`L-11`. **Sesi peninjau ke-3 belum mengirim laporan.** PR #1 tetap **tidak di-merge**.
PUTARAN 12 (2026-09-17, pesan ke-31): **3 laporan peninjau lengkap masuk** (hakim 1 ternyata punya laporan penuh 390 baris, verdict TIDAK-BERSIH). Keempat temuannya saya uji ulang dengan probe SQL sendiri dan **keempatnya terbukti nyata**: (1) **K-2 uang** — kasir bisa mencatat "diskon voucher" 100% nota tanpa voucher (batas diskon manual dilewati dengan ganti jenis); (2) **K-3 status** — pesanan bisa lahir `batal`/`lunas` lewat INSERT (penjaga status hanya pada UPDATE); (3) **K-3 jejak** — `nilai_kerugian` pembatalan bisa dikarang klien; (4) **K-4 dokumen** — klaim "21 berkas uji" basi. Ditutup: migrasi baru `supabase/migrations/0013_penutup_celah_putaran11.sql` + 3 uji baru (`diskon_voucher`, `pesanan_status_awal`, `nilai_kerugian`) + 4 mutasi baru (**16/16 WAJIB MERAH**, termasuk M13b yang menjaga jebakan `security definer`) + penjaga angka dokumen di `alat/periksa-panduan.py`. Dua temuan palsu dari sesi lain tetap dicatat di `docs/uji/REVIEW_PR_RIWAYAT.md` §2b. Temuan luar-cakupan → `L-09`…`L-14`. PR #1 tetap **tidak di-merge** (keputusan Lee).
PUTARAN 13 (2026-09-17, pesan ke-32): **tiga keluhan pratinjau ketiga diperbaiki** — (1) mode **Padat** dulu hanya memendekkan kiri-kanan; kini token dipisah **per sumbu** (tinggi kendali 48→44 px, baris tema 56→48 px, pad vertikal 20→12 px, tinggi baris 1,55→1,42) sementara **jarak kiri-kanan & ukuran huruf dikunci sama** — dasarnya riset Material 3 (*density* = tinggi −4 dp/langkah, jarak mendatar tidak berubah, huruf tidak berubah, sasaran sentuh ≥44 px) & Cloudscape; (2) panel "Ganti tema" dulu `<details>` bawaan yang **tidak** menutup saat Esc — kini tombol+panel dengan **Esc (fokus pulang) · klik di luar · fokus keluar · pilih lalu tutup** (WAI-ARIA APG) di komponen baru `aplikasi/src/komponen/PemilihRingkas.tsx` dan di sumber desain `prototipe/js/ui.js`; (3) ujung daftar yang "nabrak" kini punya **jejak pudar** (`mask-image` di elemen penggeser, mengikuti posisi gulir via `animation-timeline: scroll(self block)`, plus cadangan peramban lama). **Ilmunya disimpan sebagai kemampuan** `skills/desain-antarmuka/SKILL.md` (permintaan Lee: *"jangan hanya disimpan, tapi juga harus digunakan"*) → wajib dibaca fase DESAIN + dijaga penjaga baru `aplikasi/alat/periksa-antarmuka.py` (10 uji-diri, masuk `periksa-semua.sh` & CI; gerbang CI 9→11 + mutasi baru). Bukti: 25 uji aplikasi (3 berkas) LOLOS · `tsc -b --force` bersih · `periksa-halaman` 183/183 · `validate_system` PASS. PR #1 tetap **tidak di-merge**.
PUTARAN 13b (2026-09-18): **jalur paket audit diperbaiki** — `alat/audit-independen.py --paket AUD-3 --semua` ternyata **mati total** (`NameError: name 'lingkup' is not defined`) dan cakupan `--fase` melebar ke seluruh 192 tugas (parameter `semua` tertimpa daftar tugas). Diperbaiki + penjaga baru `_uji_pembuat_paket()` di `--uji-diri` (menjalankan pembuat paket di salinan pohon, memeriksa cakupan `menyeluruh` vs `terarah`). Paket siap pakai untuk tip ini: **review** `docs/uji/review-pr/PKT-2026-09-18-pr-01-putaran13-SIAP-TEMPEL.md` (target `57fe689`) dan **audit** `docs/uji/paket-audit/AUD-3-2026-09-18-SIAP-TEMPEL.md`. PR #1 tetap **tidak di-merge** (keputusan Lee).
PUTARAN 14 (2026-09-18, pesan ke-34/35): **putaran13 + audit AUD-3 2026-09-18 DITUTUP.** Lee menjalankan 4 sesi (2 review PR + 2 audit); laporan yang tertimpa diselamatkan lebih dulu (`--ambil-laporan`). **27 temuan diverifikasi NYATA lewat probe sendiri** (12 review + 15 audit) lalu ditutup migrasi `0014_penutup_celah_putaran13.sql`: `hitung_total()` (alur uang tidak buntu) · subtotal selalu peladen & dapur hanya memindahkan status · cap bawaan diskon 100%→50% · jejak pesanan (kasir_id, tanggal, nomor dari sistem, pelayan se-cabang) · pembatalan sah menutup pesanan · stempel diskon wajib bukti PIN · metode bayar wajib · meja (kasir hanya status; meja terpakai tidak bisa dihapus) · arah stok ditentukan jenis · PIN berjenjang. **10 uji SQL baru** + 6 uji lama diselaraskan → **41 LULUS · 0 GAGAL**; `uji-mutasi-0012.py` 16/16 dan `uji-mutasi-0014.py` 17/17 MERAH (penjaga berlapis dibuktikan lewat mutasi gabungan M3k/M5k/M8k). Alat audit sendiri diperkuat: `alat/periksa-paket.py` (paket wajib menunjuk induk commit-nya) & `alat/periksa-angka-bukti.py` (angka ROADMAP wajib punya perintah) & `periksa-komponen-env.py` (F-15 `GOOGLE_CLIENT_ID`) & `rls_semua_tabel.sql` (arah timbal balik). **2 cacat tertangkap CI** (field STATUS.md berubah; T-019 tidak ditandai) — diperbaiki. **Mekanisme lanjut-sesi dibuat**: `alat/lanjut-sesi.py` + `docs/ops/SIAP-LANJUT.md` + `docs/ops/SIAP-TEMPEL-SESI-BARU.md` (permintaan Lee: lanjut di chat baru tanpa kehilangan konteks); alat itu langsung dipakai untuk menguji dirinya sendiri dan **menemukan 2 cacat nyata miliknya** (bagian handoff bisa terduplikasi; pemeriksa kesegaran terlewat di klon dangkal) — keduanya diperbaiki, dan uji-dirinya kini 15 kasus dan membuktikan handoff **basi** benar-benar DITOLAK (repo Git uji nyata); pemeriksa juga tidak lagi menuduh 'belum ter-push' secara palsu saat ref remote-tracking lokal tertinggal (bertanya langsung ke remote). Berkas siap tempel juga kini menjelaskan beda antara **commit keadaan** (induk) dan **ujung cabang** yang disusul sesi baru, supaya tidak ada kebingungan saat pindah chat. Menjawab pertanyaan Lee (2026-09-18): **base branch tidak perlu diatur** dan **prompt penutup disarankan tetapi tidak wajib** — keduanya kini jadi kalimat tetap di buku induk (AL-13), panduan pemilik, dan berkas siap-tempel, dijaga 4 topik wajib pemeriksa panduan + mutasi uji-diri ke-16. **Uji nyata pertama (2026-09-18):** Lee benar-benar memakai berkas siap-tempel di sesi baru; mekanisme bekerja (menyusul dari `main` yang buta ke ujung cabang, KARTU SESI, tanpa menulis sebelum tujuan dikonfirmasi), dan sesi baru menemukan 4 hal — 3 di antaranya cacat mekanisme yang langsung ditutup: kartu sesi buta pada status jeda (1 → 15 skill, + `--uji-diri` 5 kasus + masuk CI), klaim paket peninjau yang basi (diganti baris fakta mesin), dan penjelasan PR #1 vs cabang sesi baru (§2c). Uji-diri lanjut-sesi 16 → 19 kasus. **Permintaan Lee menyusul (2026-09-18):** Lee sendiri yang menentukan sesi mana yang dilanjutkan (kadang ada sesi yang sengaja ingin ditinggalkan). Diterapkan: `--daftar-sesi` (lihat semua sesi di GitHub + jarak commit + catatan sesi lama) dan `--siapkan --lanjut-dari <cabang>` (pilihan Lee tersimpan di handoff); penjaga baru: cabang tujuan WAJIB ada di GitHub, resep berkas tempel WAJIB menunjuk cabang yang sama, berkas tempel WAJIB menjelaskan hak pilih Lee → uji-diri 19 → **23 kasus**. Saat mendata sesi, ditemukan **cabang sesi baru Lee (`arena/01a0b4c3-resto-barokah`) sudah di-push** dengan 1 commit berisi pekerjaan SERUPA (3 cacat mekanisme + pratinjau + penomoran migrasi ROADMAP) — jadi ada DUA garis pekerjaan yang berbeda isinya; pilihan sesi mana yang dilanjutkan = keputusan Lee. Permintaan ini dicatat sebagai pesan ke-9 di `docs/teknis/REKAM_PESAN_PEMILIK.md`. Paket peninjau (AUD-3 2026-09-18 + review PR putaran15) sudah disegarkan ke commit `52e22fc` sehingga putaran verifikasi siap dijalankan (satu berkas `-SIAP-TEMPEL` per chat peninjau). DIBUKTIKAN lewat simulasi nyata: klon baru dari `main` (buta, tanpa berkas handoff) menjalankan resep berkas tempel → langsung mendarat di ujung cabang kerja terakhir dan KARTU SESI mencetak status handoff. **Koreksi Lee 2026-09-18 (bahasa):** agent sempat menjawab dengan bahasa yang salah; aturan "balas selalu dalam bahasa Indonesia sederhana" kini jadi baris pertama Prompt Pembuka + dijaga pemeriksa (Topik wajib baru `ATURAN BAHASA`; berkas siap-tempel wajib memuatnya). Cacat nyata ikut tertangkap: **CI sempat MERAH** (run 35328530232) karena format baris `Waktu pembaruan` di `STATUS.md` menyimpang dari kontrak — dibetulkan; dan bukti bahwa CI benar-benar MENOLAK handoff cacat dikunci lewat commit mutasi `195b708` (CI merah di langkah pemeriksa) lalu dikembalikan (`cacdff3`, CI hijau).
PUTARAN 17 (2026-09-19, sesi lanjutan `arena/01a0b7d1-resto-barokah` — Lee memilih **putaran verifikasi**): ruang kerja sempat di-clone ulang (HEAD mundur ke `main`, berkas kerja utuh) → dipulihkan resmi (`git fetch origin arena/01a0a8a2-resto-barokah:refs/remotes/origin/kerja-terakhir` → `git reset --soft` → `git restore --source`, TANPA `--hard`/`--force`), lalu cabang sesi ini di-push pertama kali. Sambil menyiapkan paket peninjau, **tiga cacat nyata ditemukan & ditutup** (bukan temuan peninjau, jadi ditutup lebih dulu supaya peninjau tidak membuang anggaran): **(1) angka jumlah tugas basi** — `STATUS.md` & `PROJECT_STATE.md` menulis 189 padahal ROADMAP **192** (T1-42…T1-44) → angka disegarkan (rincian per fase diukur ulang, cocok semua) + **Aturan 2** di `alat/periksa-angka-bukti.py` (angka jumlah tugas keadaan-sekarang wajib sama dengan jumlah nyata; uji-diri 3 → **5 kasus**, dua mutasi baru terbukti DITOLAK); **(2) dua penjaga tidak pernah dijalankan sungguhan** — `alat/periksa-paket.py` (F-11/F-12) & `alat/periksa-angka-bukti.py` (F-14) hanya uji-dirinya yang ada di CI, sehingga aturannya tak pernah ditegakkan (padahal docstring penjaga paket menyatakan "Dipakai CI") → kini keduanya dijalankan di CI **dan** `aplikasi/alat/periksa-semua.sh`, checkout diberi `fetch-depth: 0` (riwayat penuh wajib karena pemeriksa paket menyelidiki commit yang ditunjuk paket lewat `git cat-file`), gerbang CI **19 → 22** (`alat/periksa-gerbang-ci.py` +3 mutasi baru); **(3) riwayat review bolong** — paket putaran13/14/15 terbit **tanpa satu pun baris** di `docs/uji/REVIEW_PR_RIWAYAT.md` → baris 8–11 diisi dari fakta mesin (target commit, verdict, kalibrasi, jumlah temuan laporan yang tersimpan) + **Aturan 3** di `alat/periksa-paket.py` (paket review baru wajib punya baris riwayat; uji-diri 4 → **6 kasus**). **Paket peninjau terbit** untuk `93a50ba`: audit `docs/uji/paket-audit/AUD-3-2026-09-19-SIAP-TEMPEL.md` (192 tugas · 117 berkas · mode **menyeluruh**) dan review `docs/uji/review-pr/PKT-2026-09-19-pr-01-putaran16-SIAP-TEMPEL.md` (jalur risiko **Merah** · 420 berkas · +60192/−145) — keduanya menyegarkan paket = menutup tenggat butir tunggu **T-019** untuk putaran ini. **PR #2** dibuka dari cabang sesi ini (base `main`); PR #1 tidak disentuh dan **keduanya JANGAN MERGE** tanpa keputusan Lee. **Cacat keempat (ditemukan saat menyegarkan handoff, dampaknya paling senyap):** handoff lanjut-sesi bisa menunjuk **cabang yang lebih tua** — handoff menyegarkan membawa pilihan cabang dari handoff sebelumnya, dan pilihan itu (sesi lama `01a0a8a2`, berhenti di `0af1cf9`) **tidak memuat** paket peninjau 2026-09-19 + perbaikan putaran ini; sesi baru yang menyusulnya akan **kehilangan pekerjaan terbaru tanpa penolakan apa pun**. Perbaikan: `alat/lanjut-sesi.py` menolak handoff yang cabang tujuannya tidak memuat "commit keadaan kerja" handoff itu (pesan menunjuk jalan keluar: `--lanjut-dari <cabang sesi ini>`, atau `--paksa` bila sesi memang ditinggalkan) + handoff diarahkan ke `arena/01a0b7d1-resto-barokah`; uji-diri **37 → 39 kasus** (mutasi "cabang tertinggal" terbukti DITOLAK + kontrol positif) dan PANDUAN diperbarui pada arti-gagal `--siapkan`. Bukti: `bash aplikasi/alat/periksa-semua.sh` hijau · suite SQL **41 LULUS · 0 GAGAL** · `periksa-paket` 30 paket LOLOS · `periksa-gerbang-ci --uji-diri` 13/13 · `periksa-angka-bukti --uji-diri` 5/5 · `lanjut-sesi --uji-diri` 39 kasus · CI **hijau** di `acf5398` (run push & PR).
PUTARAN 18b (2026-09-19, lanjutan hari yang sama — **Fase 0 dibereskan**): akun pemilik aktif → `T0-00`/`T-018` ditutup; `aplikasi/src/lib/supabase.ts` (+10 uji) · `aplikasi/alat/cek-supabase.mjs` (+`--uji-diri` 5 kasus) · `aplikasi/wrangler.toml` + `npm run deploy` · gerbang CI **50** (`npm run cek:supabase` memakai kunci publik saja) · DECISIONS_LOG dicatat; sisa Fase 0 = keputusan Lee (`T-020` sebar skema 14 migrasi, `T-021` deploy publik); jalur penyebaran **disengaja** siap: `sebar-skema.yml` (dry-run lebih dulu) + `sebar-halaman.yml` + `supabase/config.toml`, menunggu 3 rahasia GitHub dari Lee (`docs/ops/LANGKAH_PEMILIK_SEKARANG.md`); penjaga gerbang kini juga mengawasi alur di luar `ci.yml`. Bukti live: run CI `35432334878` hijau, langkah uji sambung Supabase = success (https://github.com/With-AI-Agent/Resto-Barokah/actions/runs/35432334878).
PUTARAN 18 (2026-09-19, lanjutan putaran verifikasi — penutupan temuan dimulai): **temuan D F-05 DITUTUP dengan izin Lee.** Berkas `docs/uji/kalibrasi/pr-bahan-2026-09-17.diff` (diff dari migrasi NYATA ke versi cacat = kunci jawaban kalibrasi) **dikeluarkan dari repo**; karena riwayat Git tetap menyimpannya, bahan itu **pensiun** — aturan rotasinya ditulis di `docs/uji/PROTOKOL_AUDIT_INDEPENDEN.md` §7. Jalur review PR diubah: `alat/review-pr.py` menyiapkan bahan di LUAR repo (`KAL_DIR_LUAR`, bawaan `/tmp/kalibrasi-pr`) dan paket **menyematkan isi bahan** ke §5, serta **MENOLAK** membuat paket bila bahan/kunci masih ada di dalam repo. Penjaga baru `alat/periksa-kunci-kalibrasi.py` (aturan A–E: tidak ada bahan/kunci di repo · aturan tertulis · alat penyiap menulis di luar repo · paket tidak menunjuk bahan pensiun · aturan rotasi; uji-diri 6 mutasi menolak + 1 kontrol hijau) → gerbang CI 22 → **24** → **49** (setiap perintah CI kini diawasi; temuan **PR-10** gerbang gagal-terbuka ditutup — penjaga kini dua arah + `if:` dilarang, terbukti menolak di salinan `/tmp/gc2`). Jalur yang pensiun berjejak di **daftar pensiun** `docs/uji/BERKAS_PENSIUN.md` (siapa memutuskan, kapan, kenapa, nasib isi di riwayat) — validator memperlakukan jalur terdaftar sebagai sengaja tidak ada, jadi **paket & laporan peninjau TIDAK disunting** (barang bukti). Dua cacat mekanisme ikut ketahuan & ditutup: (1) penjaga paket `alat/periksa-paket.py` rapuh — aturan F-11 memakai "commit terakhir yang mengubah paket" sehingga satu suntingan sah menuduh 22 paket lama melanggar (kini: paket sah bila ADA commit dalam riwayat yang menulisnya tepat sesudah commit target dan targetnya tidak berubah); (2) `--uji-diri` penjaga kunci sempat tumpul karena menguji paket lama sementara aturannya berlaku untuk paket baru (kini aturan & mutasi memakai sumber yang sama). Probe verifikasi 13 berkas juga disimpan permanen di `docs/uji/audit/probe-2026-09-19/` (+BACA-DULU berisi tabel arti). Bukti: percobaan nyata di klon — alat MENOLAK saat bahan masih di repo, lalu menulis bahan ke `/tmp` + paket memuat blok diff; `_sistem/validate_system.py` PASS · `periksa-bersih`/`periksa-paket`/`periksa-gerbang-ci`/`periksa-temuan-audit` LOLOS · CI hijau.
SELANJUTNYA:  **(0) AUDIT AUD-3 SUDAH MASUK (2026-09-17)** — 2 dari 3 sesi auditor mengirim laporan lewat jalur pulang (`--ambil-laporan`): **keduanya LOLOS KONTRAK + TERKALIBRASI 5/5, 0 temuan palsu, verdict TIDAK-BERSIH** (10 dan 17 temuan; K-1: uang lebih bayar diterima saat total 0 · `total_dibayar()`/`izin_efektif_untuk()` SECURITY DEFINER bocor lintas resto). Sesi kerja **menguji ulang sendiri 9 temuan dengan eksekusi** (`node alat/uji-sql.mjs docs/uji/audit/bukti-verifikasi-2026-09-17.sql` → 1 LULUS · 0 GAGAL) — jadi temuan bukan klaim. Berkas audit menunjuk `442913e`; berkas `aplikasi/`, `supabase/`, `prototipe/` tidak berubah sejak itu → temuan berlaku untuk tip. Ketiga sesi **sudah masuk** (temuan Lee: dua sesi berbagi satu cabang & nama berkas yang sama → laporan pertama tertimpa lalu diselamatkan dari riwayat commit; laporan itu mengaudit repo basi sehingga **tidak sah untuk putusan**). **(1) AUDIT MENYELURUH (AUD-3)** (untuk putaran berikutnya) atas keadaan sekarang — paket + bahan kalibrasi siap; auditor = sesi baru (idealnya model berbeda). Base branch **bebas**: paket memuat commit target + LANGKAH 0, dan peninjau memakai `python3 alat/audit-independen.py --verifikasi-lingkup` (cocok → lanjut; beda commit tetapi target ada → `git fetch origin && git checkout --detach <sha>`; target tidak ada → berhenti & lapor). **(2) REVIEW PR INDEPENDEN (RV-2)** untuk PR #1 sebelum dimintakan merge ke Lee: `python3 alat/review-pr.py --siapkan --dasar origin/main --nama pr-01` → peninjau sesi baru → `--periksa-laporan` → `--kartu-keputusan` (Kartu Keputusan 7 baris untuk Lee). **(3)** perbaiki temuan audit/review (K-1/K-2 dulu, gerbang `tahan_semua`) + pekerjaan ulang T1-37 (B.1–B.9) → **(4)** audit ulang kecil → **(5)** Fase 1C (T1-31…T1-35 **+ T1-39 isi PETA_UI semua layar G1**, spesifikasi perilaku/gerakan `docs/SPESIFIKASI_UI.md` §9) → **(6)** Fase 1B (T1-23…T1-30 + T1-36; migrasi 0011–0016b) → **(7)** kembali ke T1-11 (kas & shift, migrasi 0018) → T1-12 → T1-15 → T1-17/T1-18 → T1-19/20 → T1-21 → T1-22. Fase 0 tinggal T0-00 → T0-08 → T0-09 (menunggu akun Lee; region Singapore sudah diputuskan). **Buku pedoman induk v2** (`PANDUAN_PENGGUNA.md`: 12 alur + prompt berlabel + semua perintah dijelaskan) dijaga `alat/periksa-panduan.py`; **permintaan Lee direkam** di `docs/teknis/REKAM_PESAN_PEMILIK.md` (dibaca sebelum mengubah aturan). Panggilan: **Lee** (bukan "Bapak").
RIWAYAT RINGKAS: Tahap 1 Discovery (DISCOVERY.md dikunci) → Tahap 2 PRD (dikunci; 12 kelompok fitur M1-M12, 13 aturan bisnis, 6 peran, voucher + katalog publik masuk MVP) → sisipan Desain & UI ronde 1-3 (10 tema; 5 papan bukti gambar) → Tahap 3 Rancangan Teknis (TECH_SPEC dikunci 2026-09-16; ART-1..ART-10) → Tahap 4 Aturan Kerja Agent → Tahap 5 ROADMAP (151 tugas, 11 fase, 7 atribut; pemeriksa) → Tahap 6 pemeriksaan silang + DECISIONS_LOG → Review independen 2 putaran (gerbang review DITUTUP) → CODING Fase 0 (T0-01…T0-07 + T0-10 selesai; sisa menunggu akun pemilik) → CODING Fase 1 (T1-01…T1-10 selesai: penyewa/cabang/pengguna/izin/pengaturan → katalog & stok → meja → pesanan dengan salinan harga beku → pembayaran/diskon/pembatalan) → **2026-09-17: jeda sadar, pemilik meminta mematangkan keamanan akun/perangkat & kelengkapan UI → dokumen fondasi diperluas + dua fase sisipan (1B & 1C)**.
UPDATE TERAKHIR: 2026-09-20 **(m) TIGA CACAT K-2 AUDIT F DITUTUP (bagian 8 `0015`):** dibuktikan nyata dulu lewat probe sendiri `docs/uji/audit/probe-2026-09-20/aud-3-f03-f05-f06-uang.sql`, lalu diperbaiki sebagai pemeriksaan tambahan pada pemicu yang sudah ada — **F-03** metode bayar nonaktif tidak bisa mencatat uang (uji `supabase/tes/metode_bayar_nonaktif.sql`) · **F-05** satu target pembatalan = satu jejak, kiriman ulang ditolak sehingga kerugian tidak tergandakan (uji `supabase/tes/pembatalan_sekali.sql`; uji lama `supabase/tes/pembayaran.sql` diselaraskan) · **F-06** stempel lifecycle (`dibayar_pada`, `dibatalkan_pada`, `alasan_batal`) tidak bisa dikarang perangkat, jalur peladen tetap bebas (uji `supabase/tes/lifecycle_pesanan.sql`). Bukti: suite SQL **50 LULUS · 0 GAGAL** · `alat/uji-mutasi-0015.py` **20 kasus** (3 baru wajib MERAH, terbukti) · probe ketiga GAGAL = cacat hilang · daftar temuan `docs/uji/AUDIT_RIWAYAT.md` §1c 23 DITUTUP / 26 TERBUKA · keputusan di `docs/DECISIONS_LOG.md`. Sisa: 12 temuan audit F + 16 temuan lama K-3/K-4. *(handoff disegarkan; mekanisme `alat/lanjut-sesi.py` diperkuat: tuduhan "handoff basi" palsu saat ref remote-tracking lokal tertinggal kini ditolak dengan bertanya ke remote lebih dulu — uji-diri 40 kasus; CI commit itu masih antre saat handoff ditulis, sesi berikutnya periksa dulu)* **(l) TIGA CACAT UANG AUDIT F DITUTUP (bagian 6 `0015`):** `hitung_total` ditulis ulang sehingga aturan uang hidup di satu tempat — **(F-01a)** PB1 & service dihitung dari subtotal **setelah** diskon (100.000 − diskon 20.000 → PB1 8.000 · service 4.000 · total **92.000**, dulu 95.000) sesuai aturan terkunci TECH_SPEC §329-331 yang selama ini tidak dijalankan mesin; **(F-01b)** `pengaturan.pembulatan` dibaca lagi dan diterapkan di langkah **terakhir** dengan arah **ke bawah** (31.050 → 31.000 pada langkah 500) — arah pembulatan tak pernah dikunci dokumen (diperiksa ulang PRD §88/§229, TECH_SPEC §331, ROADMAP T1-15/T1-16), jadi arahnya diputuskan sekarang dan dikunci uji (bisa dibalik satu baris); **(F-02)** pesanan `lunas`/`batal` tidak bisa dihitung ulang dari perangkat (panggilan ber-identitas ditolak; jalur pemicu peladen tetap sah lewat pembeda `pg_trigger_depth() = 0`; penanda `set_config` ditolak karena bisa dipalsukan klien) dan baris pesanan dikunci `for update`. Bukti: uji regresi baru `supabase/tes/urutan_uang.sql` · ekspektasi uji lama `supabase/tes/diskon_sesudah_lunas.sql` diselaraskan ke rumus benar (**29.498**, bukan mesin dilemahkan) · `alat/uji-mutasi-0015.py` **17 kasus** (4 mutasi baru mengunci aturan uang, semua terbukti MERAH) · **probe audit lama kini GAGAL = cacat hilang** · suite SQL **47 berkas LULUS · 0 GAGAL** · seluruh 18 temuan audit F punya pemilik di `docs/uji/AUDIT_RIWAYAT.md` **§1c** baru (penjaga `alat/periksa-temuan-audit.py` kini membaca laporan F + 1 mutasi uji-diri baru) · keputusan dikunci di `docs/DECISIONS_LOG.md`. Sisa maraton: 15 temuan F (bantah-balik berurutan) + 16 temuan lama K-3/K-4. **(k) AUDIT INDEPENDEN MASUK — 3 CACAT UANG TERBUKTI NYATA:** laporan AUD-3 sesi `01a0bbd2` LOLOS KONTRAK (kalibrasi 5/5, verdict TIDAK-BERSIH, 18 temuan); bantah-balik sesi kerja membuktikan tiga cacat jalur uang lewat probe sendiri `docs/uji/audit/probe-2026-09-20/aud-3-f01-f02-uang.sql`: pajak/service dihitung dari subtotal SEBELUM diskon (melanggar aturan terkunci TECH_SPEC §329-330), `pengaturan.pembulatan` tidak dibaca, dan RPC `hitung_total` bisa menulis ulang angka pesanan LUNAS. Perbaikannya `0015 bagian 6` + uji + mutasi. Dua butir pemilik dipanen dari review fondasi: `T-022` (email pelanggan tanpa domain) & `T-023` (PIN pelanggan). Sandbox sempat me-reset Git di tengah batch; dipulihkan lewat fetch + reset --soft ke ujung cabang remote (tanpa penulisan ulang riwayat). **(j) CELAH MEKANISME AUDIT DITUTUP:** sesi auditor independen (`arena/01a0b9f2`) TERBLOKIR karena Lee menyalin berkas **cetakan** `PROMPT_AUDIT_INDEPENDEN.md` (memuat baris kosong `<<< TEMPEL ISI … >>>`) dan sesinya bercabang dari `main` sehingga tidak melihat berkas proyek. Perbaikan: berkas siap-tempel bersih + memuat cara mengambil bahan, paket menunjuk commit + cabang, cetakan diberi peringatan, dan `alat/periksa-paket.py` menjaga semuanya (2 mutasi uji-diri, 8 kasus). Riwayat jujur = baris audit E TERBLOKIR. Berkas siap-tempel yang dipakai: `docs/uji/paket-audit/AUD-3-2026-09-19-4830b5a-SIAP-TEMPEL.md`. **(i) SELURUH K-2 TUNTAS:** bagian 4–5 `0015_penutup_celah_putaran16.sql` menutup **PR-03** (hitungan nomor pesanan tidak bocor antar resto — pemeriksa keterlihatan cabang sama seperti angka uang; uji `supabase/tes/nomor_pesanan_isolasi.sql`) dan **PR-04** (pesan PIN kembar netral, tidak lagi memastikan PIN aktif kolega; sisa sifat berhasil-vs-ditolak dibatasi pembatas 20 percobaan/15 menit dan dicatat jujur di `docs/DECISIONS_LOG.md`; uji `supabase/tes/pin_bukan_oracle.sql`). Suite SQL **46 berkas**; `alat/uji-mutasi-0015.py` **15 kasus** (13 wajib MERAH terbukti). Sisa temuan **16** (14 `T1-45`, 2 `T1-44`) — lanjut K-3/K-4. **(h) MARATON JALAN — K-2a/K-2b DITUTUP:** bagian 2–3 migrasi baru `0015_penutup_celah_putaran16.sql`: **PR-02** (void satu item tidak lagi membatalkan seluruh pesanan — pesanan ditutup hanya bila tidak ada item hidup tersisa, dan item pesanan yang ditutup ikut ditandai batal, sehingga pembayaran sisa tidak buntu; uji `supabase/tes/void_satu_item.sql`) dan **audit D F-01** (diskon tidak bisa lagi ditambah/diubah/dihapus sesudah pesanan lunas/batal; uji `supabase/tes/diskon_sesudah_lunas.sql`, urutan pemicu status-sebelum-nilai ikut dikunci mutasi). Suite SQL **44 berkas**; `alat/uji-mutasi-0015.py` **11 kasus** (10 wajib MERAH terbukti). Sisa temuan **18** (16 `T1-45`, 2 `T1-44`). Dikunci di `docs/DECISIONS_LOG.md`. **(g) MARATON JALAN — K-1 DITUTUP:** temuan review PR-01 (penanda pembatalan palsu → void sesudah dapur tanpa PIN/jejak) ditutup lewat migrasi BARU `0015_penutup_celah_putaran16.sql` bagian 1 (bukti = data tabel, bukan pengaturan transaksi yang bisa ditulis klien) + uji regresi 4 serangan + `alat/uji-mutasi-0015.py` (6 mutasi, 5 wajib MERAH) sebagai gerbang CI ke-52; dikunci di `DECISIONS_LOG.md`. Sisa temuan **20**. **(f) FASE 0 TUNTAS — halaman publik naik atas izin Lee:** `T0-09` DITUTUP & `T-021` SELESAI; halaman kerangka publik di <https://resto-barokah.fatrizmubarok.workers.dev> (HTTP 200, dibuktikan otomatis di dalam alur lewat anotasi) · alat `aplikasi/alat/catat-alamat.mjs` (+uji-diri, gerbang CI ke-51) dan penjaga batas mode bimbingan (`alat/periksa-panduan.py`, 2 mutasi baru) · catatan alamat & cara mundur di `docs/ops/ALAMAT_PUBLIK.md`. **Maraton T1-45 dimulai.** **(e) PEMERIKSAAN PRA-MARATON (permintaan Lee):** semua daftar tertunda & temuan diperiksa ulang → **PR-12 ditutup** (label `12/12` basi di `aplikasi/alat/periksa-semua.sh` dibuat tanpa angka) dan **angka sisa temuan dibetulkan 23 → 21 terbuka** (19 milik `T1-45`, 2 milik `T1-44`); **bentrok penomoran** Fase 1B ditemukan & diberi catatan: `T1-24`/`T1-25`/`T1-26` wajib memakai migrasi baru `0015`–`0017` karena `0012`–`0014` sudah terpakai & dibekukan. Aturan mode bimbingan juga ditambah: **pemeriksaan rutin boleh ditunda demi balasan cepat, kecuali saat ada perubahan baru, saat klaim "selesai", dan saat menyentuh §12 (biaya/keamanan/uang/data/tindakan tak bisa dibatalkan)**. **(d) MODE BIMBINGAN ditanam atas permintaan Lee:** alur **AL-14** + **§14** (`AGENT_OPERATING_GUIDE.md`) + `PROFIL_PENGGUNA.md`, pemicu "Tolong bimbing" / "Mode bimbingan" / "Beri arahan step by step", penutup "Sudah beres, lanjut normal" — jawaban agent menjadi pendek (satu tindakan sekali) sementara pemeriksaan & catatan tetap penuh di belakang; batasnya ditegaskan: Stop Conditions §12 dan klaim "selesai" tetap butuh bukti. Penjaga `alat/periksa-panduan.py` naik **MIN_ALUR 13 → 14**. Lee juga menyelesaikan **Langkah B** (rahasia Cloudflare) → sisa `T-021` tinggal izin publik. **(c) SKEMA HIDUP DI PROYEK NYATA — `T0-08` DITUTUP, `T-020` SELESAI:** alur disengaja `.github/workflows/sebar-skema.yml` menyebar 14 migrasi (run `35435248540` hijau: `link` → pratinjau → `db push` → `migration list`) tanpa agent menyentuh kredensial; gerbang CI ke-50 lalu membaca tabel katalog dengan kunci publik (HTTP 200, run `35435414653`) — itulah `select 1` yang selama ini tertahan. Karena database nyata sudah memuat `0001`–`0014`, berkas itu **dibekukan** (penjaga `alat/periksa-migrasi-beku.py`; perubahan skema lewat `0015+`). Sisa Fase 0: `T0-09` menunggu izin Lee. **(b) Jalur penyebaran milik pemilik siap & terbukti aman:** penjaga gerbang `alat/periksa-gerbang-ci.py` kini juga mengawasi dua alur rilis (8 + 5 perintah, **urutan diperiksa**, 9 mutasi alur ditolak) · uji sungguhan hari ini: penanda terpasang → kedua alur **menyala lalu berhenti di gerbang rahasia** tanpa menyentuh proyek (run `35433200326` skema, `35433200375` halaman); penanda dihapus → **hijau tanpa kerja** (run `35433237658`, `35433237656`) · panduan awam 3 rahasia `docs/ops/LANGKAH_PEMILIK_SEKARANG.md` (masuk `BUKU_UJI_PEMILIK` P-04/P-05) · sisa Fase 0 tinggal menunggu Lee memasang rahasia. **(a) Fase 0 lanjut:** Lee membuat akun Supabase/Resend/Cloudflare dan mengisi nilai non-rahasia di repo (`bd68685`) → `T0-00` + butir `T-018` **DITUTUP**; klien Supabase aman + alat uji sambung + `wrangler.toml`/`npm run deploy` siap; uji sambung masuk CI (gerbang 49 → **50**); sisa menunggu Lee: **T-020** (sebar skema) & **T-021** (deploy publik). **(b) Putaran 17 → 18 —** putaran verifikasi: 2 paket peninjau terbit (audit AUD-3 menyeluruh + review PR #2 putaran16, target `93a50ba`) + 3 cacat nyata ditutup — angka jumlah tugas basi 189→192 (Aturan 2 penjaga angka) · penjaga F-11/F-12 & F-14 kini dijalankan sungguhan di CI (+`fetch-depth: 0`, gerbang CI 19→22) · riwayat review putaran13–15 yang bolong diisi (Aturan 3); PR #2 JANGAN MERGE). **Putaran 18 (lanjutan hari ini):** temuan **D F-05 ditutup** — kunci kalibrasi keluar dari repo (pensiun karena riwayat Git), bahan review PR kini di luar repo & disematkan ke paket, penjaga `alat/periksa-kunci-kalibrasi.py` (6 mutasi) → gerbang CI **24** · handoff disegarkan; sisa **23 temuan** T1-45 menunggu (PR-10 gerbang CI gagal-terbuka & D F-05 kunci kalibrasi sudah DITUTUP)
UPDATE SEBELUMNYA: 2026-09-18 (putaran 16 — sesi ditutup atas perintah Lee: mekanisme "kalimat perintah sederhana → alur" diperkuat 4 celah; LOG_SESI CLOSED; handoff disegarkan)

