# agent-skills — catalog index (sumber: Input-Pengguna/agent-skills-main.zip 6.6M, 248 skills di repo meta)

> **Catatan template:** File zip asli `Input-Pengguna/agent-skills-main.zip` **tidak ikut ter-copy** saat kamu `cp -r sistem/sistem-building-aplikasi/*` ke repo baru (sesuai `PANDUAN_PENGGUNA.md` → "Jangan copy Input-Pengguna"). Yang ikut adalah **index katalog** ini + 10K sample di folder ini (hemat 6.6M bengkak). Untuk install skill tambahan di repo baru, **jangan unzip Input-Pengguna** — gunakan jalur baku:
> `npx skills add <owner/repo> --skill <nama>` atau `npx skills find <keyword>` (lihat `skills/README.md` § Cara pakai adaptif). Contoh: `npx skills add vercel-labs/agent-skills --skill <nama>`.

Folder ini adalah hub 248 skills — zip asli tetap di repo meta sebagai provenance, katalog index di sini agar agent baru tahu apa saja yang tersedia tanpa bengkak.
