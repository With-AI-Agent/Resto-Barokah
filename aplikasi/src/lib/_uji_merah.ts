// BERKAS UJI SENGAJA (dihapus lagi di commit berikutnya).
// Gunanya membuktikan gerbang CI benar-benar MERAH kalau kode tidak rapi:
// variabel di bawah sengaja tidak dipakai (dilarang aturan kode).
export function tambah(a: number, b: number): number {
  const tidakDipakai = 1
  return a + b
}
