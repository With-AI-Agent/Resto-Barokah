# LAPORAN AUDIT INDEPENDEN — AUD-4 — 2026-09-25

- **Auditor:** Arena.ai Agent Mode — sesi `arena/01a0d6aa-resto-barokah` (bukan sesi penulis kode; platform tidak mengungkap keluarga model dasar — independensi model dicatat sebagai keterbatasan di §6)
- **Tanggal:** 2026-09-25
- **Tingkat audit:** AUD-4
- **Commit yang diaudit:** `804ed86f3464490d5bc6c466b7e268d138b78e5e` (dibaca sebagai objek Git/salinan sementara unik; working tree bersama tidak pernah di-checkout)
- **Paket audit:** `docs/uji/paket-audit/AUD-4-2026-09-25-keamanan.md` — dibaca utuh dari SHA paket `474e018054645d6927557af80302094dca2270d7` (berkas paket memang tidak ada di pohon target; dicatat apa adanya)
- **Mode cakupan:** bidang-keamanan
- **Alasan commit berbeda:** commit yang diaudit (`804ed86…`) adalah commit target yang ditetapkan paket; HEAD meja kerja sesi ini adalah SHA paket `474e018…` — jadi berkas laporan ini di-commit SESUDAH pohon yang diperiksa oleh mekanisme pengiriman, sehingga HEAD cabang laporan bukan pohon yang saya nilai. Selisih kedua pohon hanya 5 berkas dokumentasi/alat (PROJECT_STATE.md, STATUS.md, alat/uji-mutasi-0031.py, alat/uji-mutasi-0039.py, docs/ops/SIAP-LANJUT.md). Kode yang saya nilai tetap pohon `804ed86…` dari salinan terisolasi.
- **Verdict:** TIDAK-BERSIH

## 1. Cakupan

Cakupan bidang-keamanan: 205 dari 205 berkas

Definisi yang saya pakai: **pindai** = ikut minimal satu pemeriksa mesin yang saya jalankan atas salinan ber-Git commit target (`alat/periksa-rahasia.py` membaca seluruh berkas terlacak; pemeriksa SQL/ACL/RLS membaca seluruh migrasi; pemeriksa rujukan/dokumen membaca dokumen); **baca** = berkas dibuka dan dibaca; **jalan** = perintah/perkakas benar-benar dieksekusi. Semua 205 berkas mencapai tingkat minimum (pindai mesin), berkas yang menopang temuan/klaim dibaca manual, dan pemeriksa yang tak dapat hijau karena keterbatasan lingkungan dicatat di §6 — bukan dihitung sebagai temuan.

Angka lingkup dihitung ulang sendiri dari pohon target (bukan dari paket):

```
git -C /home/user/Resto-Barokah ls-tree -r --name-only 804ed86f3464490d5bc6c466b7e268d138b78e5e > /tmp/lingkup.txt
grep -cE '^(supabase/|\.github/workflows/|docs/KEAMANAN\.md$|alat/periksa-rahasia\.py$|alat/periksa-kunci-kalibrasi\.py$|alat/uji-edge-pin\.mjs$|aplikasi/src/lib/)' /tmp/lingkup.txt
→ 205
```

| # | Grup / Artefak | Jumlah | Diperiksa | Bukti (perintah → hasil nyata) |
|---|---|---|---|---|
| 1 | `aplikasi/src/lib/` (kode aplikasi: lib + uji) | 30 | pindai + baca (5 berkas penuh) | `grep -rn "auth\.\(signIn\|signOut\|getSession\|setSession\)" aplikasi/src` → hanya OAuth/OTP pelanggan + signOut; `sed -n '140,300p' aplikasi/src/lib/auth.ts` → sesi `sessionStorage`; `grep -rn "\.from('" aplikasi/src` → 6 akses tabel langsung |
| 2 | `supabase/migrations/` (migrasi database) | 59 | pindai + baca terarah + jalan | `python3 /tmp/audit/acl2.py` → "TOTAL definisi fungsi: 127 · security definer non-trigger: 69 · FINAL TANPA revoke: 0"; `python3 /tmp/audit/rls2.py` → "TABEL: 41 · RLS tidak aktif: 0 · tanpa policy: 0" |
| 3 | `supabase/tes/` (uji SQL) | 105 | pindai + baca (2 berkas uji) + daftar | `ls supabase/tes \| wc -l` → 105; `grep -n "uji\.klaim" supabase/tes/sesi_kedaluwarsa.sql` → klaim JWT disuntik manual; `sed -n` sesi_dan_perangkat.sql |
| 4 | `supabase/functions/` (Edge Functions) | 2 | pindai + baca penuh + coba jalan | `cat -n supabase/functions/verifikasi_pin/index.ts` → tipis, tanpa `console.*`, POST-only, CORS allowlist, validasi UUID+PIN; `node alat/uji-edge-pin.mjs` → GAGAL-tertutup (esbuild absen) |
| 5 | `supabase (akar)` (`README.md`, `config.toml`) | 2 | pindai + baca penuh | `cat -n supabase/config.toml` → `enable_signup = true`, `enable_anonymous_sign_ins = false`, `jwt_expiry = 3600`, tanpa hook token; `grep -rn "session_id\|perangkat_id" supabase/config.toml` → kosong |
| 6 | `alat` keamanan (`periksa-rahasia.py`, `periksa-kunci-kalibrasi.py`, `uji-edge-pin.mjs`) | 3 | pindai + jalan (2 dari 3) | `python3 alat/periksa-rahasia.py` (klon penuh) → "HASIL: LOLOS"; `python3 alat/periksa-kunci-kalibrasi.py` → "HASIL: LOLOS"; `node alat/uji-edge-pin.mjs` → GAGAL-tertutup |
| 7 | `docs (fondasi)` (`docs/KEAMANAN.md`) | 1 | pindai + baca penuh | dibaca §1–§4 + `grep -n "pencabutan\|sesi" docs/KEAMANAN.md`; klaim §1.4/§4 dipakai sebagai acuan F-03 |
| 8 | `.github/workflows/` (alur CI) | 3 | pindai + baca penuh + jalan | `grep -n "permissions:\|uses:\|pull_request_target\|secrets\." .github/workflows/*.yml` → `permissions: contents: read`, tanpa `pull_request_target`, aksi pakai tag (`actions/checkout@v5`); `git show 804ed86:.github/workflows/ci.yml \| sed -n '270,320p'` → seluruh perintah langkah 58 direplikasi (28 log `bukti/ci58b_*.out`) |
| 9 | Berkas paket + protokol + pengirim (dari SHA paket) | 4 | baca penuh + jalan | `git show 474e018:docs/uji/paket-audit/AUD-4-2026-09-25-keamanan-SIAP-TEMPEL.md` → 757 baris; `PROTOKOL_AUDIT_INDEPENDEN.md`, `PENGIRIMAN_LAPORAN_AMAN.md`, `alat/kirim-laporan.py` dibaca; pengirim dijalankan dengan `--siapkan` |
| 10 | Seluruh berkas target dari SHA (salinan unik) | 2670 | pindai (kerahasiaan) + klasifikasi | `git archive 804ed86f… \| tar -x -C /tmp/audit/target` → `find … -type f \| wc -l` = 2670; `python3 alat/periksa-rahasia.py` di klon penuh → LOLOS |

**Dikecualikan dari lingkup — saya SETUJU dengan alasan:** `skills/` (kumpulan skill pihak ketiga/vendored, bukan kode proyek; 1803 berkas), `_salinan-meta/` (arsip provenance), `_Notes.md` (catatan pribadi pemilik). Ketiganya tetap ikut tersapu `periksa-rahasia.py`/`periksa-bersih.py` sehingga tidak ada wilayah yang sepenuhnya buta.

### 1a. Berkas untuk pengguna

| # | Berkas pengguna | Diperiksa | Bukti / temuan |
|---|---|---|---|
| 1 | `PANDUAN_PENGGUNA.md` (buku induk) + `PROMPT_ENTRI_UNIVERSAL.md` + `docs/uji/PROMPT_AUDIT_INDEPENDEN.md` | ya | `python3 alat/periksa-panduan.py` (klon penuh) → "HASIL: LOLOS — buku induk lengkap, rujukan hidup, prompt identik dengan sumbernya"; hanya 1 rujukan ber-tanda rencana (`supabase/functions/ringkasan_harian/index.ts`) |
| 2 | `docs/ops/DAFTAR_KUNCI_PEMILIK_NONSECRET.md` (+ `.template.md`) | ya | `ls -la docs/ops/` → berkas ada; isi = alamat/kunci publik, bukan rahasia; `periksa-rahasia.py` LOLOS |
| 3 | `docs/PANDUAN_PEMILIK.md`, `docs/ops/LANGKAH_PEMILIK_SEKARANG.md`, `docs/ops/PAPAN_TUGAS.md`, `docs/teknis/*` | ya (sapuan) | `python3 alat/periksa-rujukan.py` → "HASIL: LOLOS — semua rujukan di dokumen pengikat hidup"; `python3 alat/periksa-panduan.py` menolak rujukan ber-backtick yang tidak ada |
| 4 | Berkas panduan lain di akar (`START_DI_SINI.md`, `STATUS.md`, `PROJECT_STATE.md`, `AGENT_SYSTEM.md`) | ya (sapuan) | `python3 _sistem/validate_system.py` → PASS; `python3 alat/periksa-bersih.py` (klon penuh) → LOLOS |
| 5 | Kalibrasi auditor: `docs/uji/kalibrasi/bahan-2026-09-17/04_panduan_singkat.md` | ya | `bash aplikasi/pratinjau.sh` (tidak ada), `python3 alat/periksa-struktur.py` (tidak ada), `docs/PANDUAN_KEAMANAN.md` (tidak ada) → `04_panduan_singkat.md:4-11`; kebijakan PIN bahan salah (lihat §5) — bukti bahwa pemeriksa rujukan pengguna memang bergigi |

## 2. Klaim pembangun yang saya coba falsifikasi

| # | Klaim (lokasi) | Cara uji | Hasil |
|---|---|---|---|
| 1 | **T2-01 `[x]` "Pemasangan Supabase Auth di klien + penyimpanan sesi aman"** (`docs/ROADMAP.md:661`) | `grep -rn "auth\.\(signIn\|setSession\|getSession\|refreshSession\)" aplikasi/src`; baca `aplikasi/src/lib/auth.ts` 112–232 | **DIBANTAH (TERVERIFIKASI).** Tidak ada satu pun pemanggilan Supabase Auth untuk staf; yang ada hanya `signInWithOAuth`/`signInWithOtp` (pelanggan) dan `signOut`. Sesi staf = objek JSON di `sessionStorage` (`simpanSesiLokal`, baris 210). |
| 2 | **T1-24/T2-15 `[x]` perangkat terdaftar dipakai saat masuk** (`docs/TECH_SPEC.md:381`; ROADMAP:794) | `grep -rn "daftarkan_perangkat_dengan_kode\|buat_kode_perangkat\|ikat_sesi_perangkat" aplikasi/src`; baca `auth.ts:38-56,136,176` | **DIBANTAH (TERVERIFIKASI).** Aplikasi tidak memanggil satu pun RPC perangkat. `ambilPerangkatLokal()` membuat UUID acak di peramban dan `masukDenganPin` mengirimkannya sebagai `p_perangkat_id`; `0061_verifikasi_pin_perangkat.sql:60-90` menolak bila perangkat tidak ada → jalur masuk nyata selalu `PERANGKAT_TIDAK_SAH`. |
| 3 | **KEAMANAN §4 / 0058 N F-03: "pencabutan … berlaku seketika di database pada setiap permintaan"** (`docs/KEAMANAN.md` §1.4 & §4; klaim paket #25/#26) | Baca `0058_sesi_masih_aktif.sql:63-103`; `grep -rn "x-perangkat-id"`; `grep -rn "session_id" supabase/config.toml` | **DIBANTAH (TERVERIFIKASI).** Pemeriksaan hanya berjalan bila token membawa klaim `session_id`/`perangkat_id` atau header `x-perangkat-id`; tanpa keduanya fungsi mengembalikan `true`. Tidak ada kode/konfigurasi di pohon ini yang menerbitkan klaim itu (`x-perangkat-id` hanya muncul di 0058; `config.toml` tanpa hook token). |
| 4 | Klaim paket #19 (T1-07) "**(hari ini 26 tabel** — perintah: `select count(*) from pg_tables …`)" | `grep -rh "create table" supabase/migrations/*.sql \| wc -l`; `python3 /tmp/audit/rls2.py` | **DIBANTAH (TERVERIFIKASI).** Ada **41** tabel di `public` pada commit target, bukan 26. Angka "26" muncul dua kali di `docs/ROADMAP.md` (baris 250 & 277). |
| 5 | Klaim paket #1 (T0-00): kunci `service_role` tidak pernah masuk repo | `python3 alat/periksa-rahasia.py` (klon penuh, riwayat penuh) + `grep -rIn "eyJhbGciOi\|SUPABASE_SERVICE_ROLE_KEY=" --include=*.ts --include=*.yml …` | **TIDAK DIBANTAH.** LOLOS; yang ditemukan hanya contoh kosong di `.env.example` dan teks kebijakan — tidak ada nilai kunci. |
| 6 | Klaim paket #2 (T0-01): 19 berkas huruf `.woff2` | `find aplikasi/src/gaya/aset -name '*.woff2' \| wc -l` | **TIDAK DIBANTAH.** Hasil: 19. |
| 7 | Klaim paket #5 (T0-04): `uji-kontras.py` "166 lolos · 0 gagal" | `python3 aplikasi/alat/uji-kontras.py` | **TIDAK DIBANTAH.** "RINGKASAN: 166 lolos, 0 gagal — 10 tema, 130 pemeriksaan warna, 36 pemeriksaan aturan desain". |
| 8 | Klaim paket (AUD-3 F-11 DITUTUP): nama perangkat kiriman klien bukan lagi jalur | `grep -n "perangkat_kunci\|p_perangkat text" 0018/0061`; baca `0061:104-112` | **SEBAGIAN DIBANTAH (TERVERIFIKASI).** Jalur `verifikasi_pin` (0061 RPC terautentikasi) sudah memakai `perangkat_id`+kunci, tetapi jalur masuk anon `verifikasi_pin_perangkat` **tidak mewajibkan** perangkat sama sekali (`p_perangkat_id` opsional; jatuh ke `p_perangkat_nama` — nama kiriman klien). |
| 9 | Klaim paket #22 (T1-22): sapuan RLS wajib menyebut `penyewa_saya()` | `python3 /tmp/audit/rls2.py` | **SEBAGIAN DIBANTAH.** 41/41 tabel ber-RLS & berpolicy, dan seluruh policy ber-`penyewa_id` menyebut penyewa — kecuali 1 policy `using (true)` pada **kamus** `izin_kode` (tanpa kolom `penyewa_id`; saya nilai sah). Tidak ada pelanggaran isolasi penyewa yang terbukti di lapisan policy. |
| 10 | Klaim paket #17 (T1-06): PIN hanya hash + "tidak ada jalur menulis langsung dari klien" | `grep -n "revoke all on public.kredensial_pin\|crypt(pin" 0006`; baca `percobaan_simpan_pin_korban` policy | **TIDAK DIBANTAH.** `revoke all on public.kredensial_pin from public, anon, authenticated`; CHECK bentuk hash; policy `percobaan_simpan_pin` membatasi baca ke pemegang `kelola_pegawai`. |

## 3. Serangan yang dijalankan (kill attempts)

| # | Skenario | Cara | Hasil |
|---|---|---|---|
| 1 | Masuk tanpa pernah membuat sesi Supabase Auth (klien hanya menulis `sessionStorage`) | Baca `auth.ts`; `grep` seluruh pemanggilan `auth.*`; periksa grant RPC `bayar_pesanan` | **BERHASIL (kontrol gagal).** Sesi klien ≠ sesi peladen; `bayar_pesanan` hanya untuk `authenticated` (`0060:205-206`) dan menuntut `auth.uid()` (`0060:46`) → aplikasi tidak dapat memanggilnya. Lihat F-01. |
| 2 | Login staf pada perangkat yang tidak terdaftar | Telusuri alur `ambilPerangkatLokal()` → `masukDenganPin` → `0061:60-90` | **BERHASIL (kontrol gagal).** UUID lokal acak tidak ada di tabel `perangkat` → `PERANGKAT_TIDAK_SAH`; tidak ada jalur pendaftaran di aplikasi. Lihat F-02. |
| 3 | Memakai sesi/perangkat yang sudah dicabut pada permintaan berikutnya | Baca `sesi_masih_aktif()` (0058) + cari penerbit klaim | **BERHASIL (kontrol gagal).** Tanpa klaim/header, fungsi `true`; identitas tetap hidup. Lihat F-03. |
| 4 | Enumerasi akun staf lewat RPC anon | Baca `0061:40-58,136-142`; bandingkan dengan kontrak non-disclosure di `0006`/`0011` | **BERHASIL (kontrol gagal).** Kode jawaban berbeda: `PENGGUNA_TIDAK_DITEMUKAN` vs `PIN_SALAH`. Lihat F-04. |
| 5 | Kunci akun korban 15 menit (DoS) dari anon | Baca `0061:98-102,130-134` vs perbaikan PR-13 di `0012:301,308` | **BERHASIL (kontrol gagal).** Penghitung lintas-pemanggil + `pemanggil_id` diisi korban. Lihat F-04. |
| 6 | Masuk staf tanpa perangkat sama sekali | Baca tanda tangan `0061` (`p_perangkat_id default null`) + blok langkah 3 | **BERHASIL (kontrol gagal).** `PERANGKAT_*` dilewati seluruhnya; login dijawab `LOGIN_SUKSES`. Lihat F-05. |
| 7 | Brute force kode pendaftaran perangkat dari anon | Baca `0030:238-300` (tanpa penghitung), `0030:210` (kode 8 heks), `0030:313` (grant anon) | **BERHASIL (kontrol gagal).** Tidak ada pembatas percobaan/pencatatan gagal pada RPC anon yang menjadi faktor kedua. Lihat F-06. |
| 8 | Baca data resto lain lewat fungsi `security definer` | `python3 /tmp/audit/acl2.py` + `acl3.py` | **DITOLAK (kontrol bekerja).** 127 definisi fungsi; 69 definer non-trigger; **0** yang masih tanpa revoke dari `public/anon/authenticated`; 4 kandidat tanpa pemeriksaan otorisasi di badannya saya periksa satu per satu (`ganti_pin` warisan sudah di-`drop` di 0018; `ganti_pin` pembungkus → `simpan_pin`; `total_dibayar` memakai `pesanan_sepenyewa()`; `tanggal_lokal_cabang` hanya zona waktu). |
| 9 | Panggil RPC istimewa sebagai `anon` | Skrip ACL (grant/revoke berurutan) | **DITOLAK sebagian.** Tersisa 5 fungsi anon yang sah: login PIN, pendaftaran perangkat berkode, ikat sesi (butuh kunci perangkat), `harga_berlaku`, `menu_habis`. `catat_percobaan_masuk` & `periksa_kunci_masuk` sudah dicabut dari anon di 0059. |
| 10 | Baca lintas penyewa lewat policy RLS | `python3 /tmp/audit/rls2.py` | **DITOLAK (kontrol bekerja).** 41/41 tabel RLS aktif + berpolicy; tidak ada policy `penyewa_id is not null`/`true` pada tabel ber-`penyewa_id`. |
| 11 | Karang angka uang dari klien (INSERT pesanan) | Baca `picu_pesanan_jaga_uang` (`0010:206-243`) | **DITOLAK (kontrol bekerja).** Trigger menolak `subtotal/pajak/service/total_diskon/total` bukan-nol dari klien; `peran_peladen()` tidak `SECURITY DEFINER` (jebakan `current_user` diakui di komentar & diperbaiki). |
| 12 | Ubah status pesanan menjadi `lunas` tanpa uang | Baca `picu_pesanan_jaga_status` (`0009:144-188`) | **DITOLAK (kontrol bekerja).** Hanya transisi `draf→dikirim` dan `dikirim/dimasak→dimasak/siap` yang sah dari perangkat; `dikirim_ke_dapur_pada` tidak bisa dihapus. |
| 13 | Lebih bayar / dobel bayar lewat INSERT langsung ke `pembayaran` | Baca `picu_pembayaran_jujur` (`0010:246-329`) + policy `pembayaran_tambah` | **DITOLAK (kontrol bekerja).** `v_sebelum + new.jumlah > total` ditolak; total pesanan harus > 0; kasir tidak bisa mengaku orang lain; jejak audit wajib (0060). |
| 14 | Ubah/hapus/`TRUNCATE` catatan audit | Baca `0029:33-45` + `0057` | **DITOLAK (kontrol bekerja).** Trigger `BEFORE UPDATE OR DELETE` + trigger `BEFORE TRUNCATE` (koreksi N F-02). |
| 15 | Curi rahasia dari repo/riwayat | `periksa-rahasia.py` (klon penuh) + grep manual `eyJhbGciOi`/`service_role=`/`.env` | **DITOLAK (kontrol bekerja).** Tidak ada kunci; `.gitignore` memuat `.env`/`.env.*`/`*.local*`; hanya `.env.example` kosong. |
| 16 | Panggil Edge Function PIN dengan payload aneh (null/array/UUID palsu, pin < 6) | Baca `index.ts:79-126`; coba jalankan `alat/uji-edge-pin.mjs` | **DITOLAK (kontrol bekerja, uji tidak jalan).** Handler memvalidasi bentuk JSON/UUID/PIN dan menjawab seragam; uji runtime gagal-tertutup karena `esbuild` tidak ada (lihat §6). |
| 17 | Baca daftar pegawai/penyewa lain lewat fungsi identitas | Baca `0003` (`sepenyewa`, `penyewa_saya`) + `0025` (`jalur_peladen_terverifikasi`) | **DITOLAK (kontrol bekerja).** `auth.uid()` NULL tidak lagi dianggap jalur peladen; jalur peladen butuh role + klaim JWT `service_role` cocok. |
| 18 | Pakai `izin_kode` (policy `using(true)`) untuk membocorkan data penyewa | Baca definisi tabel `izin_kode` (`0005:17-24`; policy `izin_kode_pilih` di `0005:277-279`) | **DITOLAK.** Tabel kamus tanpa `penyewa_id`; hanya `select` untuk `authenticated`. |
| 19 | Panggil `sesi_masih_aktif()` / `periksa_kunci_masuk()` dari anon (probe STATUS.md K F-05) | Skrip ACL net-grant | **DITOLAK (kontrol bekerja).** Keduanya tidak lagi anon; `periksa_kunci_masuk` menolak lintas penyewa (0059). |
| 20 | Jalankan seluruh perintah langkah CI ke-58 ulang di klon penuh untuk mencari penyebab CI merah | `git fetch --unshallow` + jalankan seluruh daftar perintah langkah 58 | **SEBAGIAN.** Semua perintah yang tidak butuh PGlite hijau (yang merah hanya yang butuh `@electric-sql/pglite` yang tak bisa dijalankan di lingkungan auditor) — penyebab pasti CI merah tidak dapat saya tunjukkan (§6). Pada klon dangkal `periksa-paket.py` merah 44× → artefak klon dangkal, bukan temuan. |

## 4. Temuan

### [F-01] Sesi & wewenang staf hidup di klien — tidak pernah menjadi sesi Supabase Auth
- **Tingkat:** K-2
- **Artefak:** `aplikasi/src/lib/auth.ts:141-172,176-181,210`; `aplikasi/src/lib/supabase.ts:44-53`; `aplikasi/src/hook/useSesi.ts:20-41`; `docs/ROADMAP.md:661`
- **Klaim yang dilanggar:** ROADMAP **T2-01 `[x]`** "Pemasangan Supabase Auth di klien + penyimpanan sesi aman"; `docs/KEAMANAN.md` §1.2 "Layar bukan penjaga … keputusan sebenarnya ada di database (RLS + `boleh()`)" dan §1.4 "semua pemeriksaan … di database pada setiap permintaan".
- **Bukti:** `grep -rn "auth\.\(signIn\|signOut\|getSession\|setSession\|refreshSession\|onAuthStateChange\)" aplikasi/src` → hanya
  `auth.ts:236 signInWithOAuth` (pelanggan), `auth.ts:263 signInWithOtp` (pelanggan), `auth.ts:287 signOut`.
  `sed -n '140,232p' aplikasi/src/lib/auth.ts` → setelah RPC `verifikasi_pin_perangkat`, hasil disimpan dengan `simpanSesiLokal(sesi)` (baris 210) ke `sessionStorage` (`auth.ts:73-95`); peran/cabang dipakai layar dari objek itu.
  `grep -rno "\.from('[a-z_]*'" aplikasi/src` → 6 akses tabel langsung: `metode_bayar` (useBayar.ts:125), `pesanan` (App.tsx:74; useTiketDapur.ts:172), `pesanan_item` (App.tsx:97), `stok_bahan` (useStok.ts:135), `stok_pergerakan` (useStok.ts:137), sedangkan grant/RPC peladen hanya untuk `authenticated` (`0060_pembayaran_audit.sql:205-206`; `bayar_pesanan` melempar `'Anda harus masuk dulu.'` bila `auth.uid()` NULL, baris 46).
  `aplikasi/src/lib/auth.test.ts:68-74` → tes "berhasil masuk … dalam mode simulasi / offline" menuntut `berhasil: true` tanpa Supabase — tes mengunci jalur simulasi, bukan jalur produksi.
- **Skenario gagal:** Kasir membuka aplikasi versi terbit (env Supabase terisi) → masuk dengan email+PIN → layar menampilkan "Berhasil masuk". Setiap pembacaan tabel mengembalikan 0 baris (role `anon`, RLS menolak) dan setiap RPC (`bayar_pesanan`, `buka_shift`, `pilih_cabang`, `tutup_shift`) gagal `permission denied`/`'Anda harus masuk dulu.'` → uang tidak bisa dicatat dari aplikasi. Skenario kedua: bila env kosong/salah, cabang simulasi (baris 143-171) memberi sesi dengan peran dari **teks email** — alamat apa pun tanpa kata kunci khusus menjadi `owner_pusat`, dan kata "platform" menjadi `pemilik_platform` — sehingga wewenang yang dilihat layar dapat dipalsukan sepenuhnya di peramban.
- **Dugaan penyebab:** T2-01 dikerjakan setengah: alur masuk staf dibuat sebagai RPC `security definer` + objek sesi lokal (cukup untuk demo/uji komponen) tanpa langkah `signInWithPassword`/penukaran token; tidak ada penerbit JWT berklaim sehingga RLS tidak pernah melihat identitas staf.
- **Cara membuktikan perbaikan:** `grep -rn "signInWithPassword\|setSession" aplikasi/src/lib/auth.ts` → ada jalur sesi Supabase Auth untuk staf; dan uji baru: `cd aplikasi && npm test -- auth` yang menuntut RPC `authenticated` berhasil setelah masuk (mis. `buka_shift`) tanpa jalur simulasi.
- **Status verifikasi:** TERVERIFIKASI

### [F-02] Jalur masuk staf selalu berakhir `PERANGKAT_TIDAK_SAH` — perangkat tidak pernah didaftarkan aplikasi
- **Tingkat:** K-2
- **Artefak:** `aplikasi/src/lib/auth.ts:38-56,136,176-181`; `supabase/migrations/0061_verifikasi_pin_perangkat.sql:60-90`; `aplikasi/src/layar/pengaturan/Perangkat.tsx:1-33`; `docs/ROADMAP.md:794,813`
- **Klaim yang dilanggar:** `docs/TECH_SPEC.md:381` "peran staf … **wajib** memakai perangkat terdaftar (… ada sesi aktif di `sesi_perangkat`)"; ROADMAP T2-15/T2-17 `[x]` (pendaftaran perangkat + layar perangkat).
- **Bukti:** `grep -rn "buat_kode_perangkat\|daftarkan_perangkat_dengan_kode\|setujui_perangkat_pegawai\|ikat_sesi_perangkat\|simpanPerangkatLokal" aplikasi/src` → **tidak ada hasil** (hanya definisi fungsi di `auth.ts`).
  `auth.ts:38-56` (`ambilPerangkatLokal`) membuat `crypto.randomUUID()` lokal dan menyimpannya di `localStorage`; `auth.ts:136,176-181` mengirimkannya sebagai `p_perangkat_id`.
  `0061:60-90`: bila UUID itu tidak ada di `public.perangkat` (pasti, karena tak pernah didaftarkan) → `{"kode":"PERANGKAT_TIDAK_SAH","pesan":"Perangkat tidak terdaftar atau telah dicabut."}`.
- **Skenario gagal:** Staf mana pun tidak bisa masuk dari aplikasi terbit: langkah pertama pendaftaran perangkat (kode dari owner → perangkat menyimpan kunci → `ikat_sesi_perangkat`) tidak pernah dipanggil aplikasi, jadi tidak ada `perangkat_id` sah yang bisa dikirim dan `sesi_perangkat` selalu kosong.
- **Dugaan penyebab:** pekerjaan perangkat (T1-24/T2-15) mendarat di database + layar presentasional, tetapi penyambungan aplikasi (pemanggil RPC) belum ada; `auth.ts` memakai id perangkat lokal sebagai tempat id perangkat peladen.
- **Cara membuktikan perbaikan:** uji integrasi: `masukDenganPin` harus gagal `PERANGKAT_TIDAK_SAH` pada perangkat yang belum didaftarkan **dan** berhasil setelah alur `buat_kode_perangkat → daftarkan_perangkat_dengan_kode → ikat_sesi_perangkat` dijalankan; `grep -rn "daftarkan_perangkat_dengan_kode" aplikasi/src` harus menemukan pemanggil.
- **Status verifikasi:** TERVERIFIKASI

### [F-03] "Pencabutan seketika" tidak ditegakkan: `sesi_masih_aktif()` no-op tanpa klaim JWT yang tidak pernah diterbitkan
- **Tingkat:** K-2
- **Artefak:** `supabase/migrations/0058_sesi_masih_aktif.sql:25-111,116-166`; `supabase/config.toml:47-55`; `supabase/tes/sesi_kedaluwarsa.sql:36-72`
- **Klaim yang dilanggar:** `docs/KEAMANAN.md` §1.4 & §4 "pencabutan … pada detik berikutnya … di database pada setiap permintaan"; komentar kepala `0058` butir 2; klaim paket #25/#26 (T1-25).
- **Bukti:** `sed -n '63,103p' supabase/migrations/0058_sesi_masih_aktif.sql` → pemeriksaan sesi hanya `if v_session_id is not null` / `if v_perangkat_id is not null` / `if v_header_perangkat is not null`; tanpa ketiganya → `return true`.
  `grep -rn "x-perangkat-id" .` (tanpa `skills/`) → hanya 3 kemunculan, semuanya di berkas 0058 → tidak ada klien yang mengirim header itu.
  `grep -rn "session_id\|perangkat_id" supabase/config.toml` → kosong; `config.toml:47-55` tanpa hook token (`[auth.hook]`), dan `grep -rn "perangkat_id" alat/ aplikasi/src` tidak menemukan penerbit klaim.
  `grep -n "uji\.klaim" supabase/tes/sesi_kedaluwarsa.sql` → uji menyuntik klaim sendiri (`{"session_id": "sess_uji_nf03_01"}`) sehingga lulus hanya pada keadaan yang tidak pernah terjadi di produksi.
- **Skenario gagal:** Perangkat hilang dicabut owner (`cabut_perangkat`) atau sesi lewat umur (12 jam/30 hari): permintaan berikutnya dari token lama tetap lolos karena `sesi_masih_aktif()` tidak melihat apa pun (klaim kosong) dan identitas (`penyewa_saya`, `peran_saya`, `cabang_ids_saya`) tetap terisi → RLS tetap memberi akses penuh sampai token kedaluwarsa sendiri (jam/berhari-hari). Penyerang cukup **tidak** mengirim header `x-perangkat-id`.
- **Dugaan penyebab:** pemeriksaan sesi disandarkan pada klaim JWT yang seharusnya dipasang Auth Hook + pada header yang dikendalikan klien; tidak ada verifikasi bahwa penegakan benar-benar aktif (tidak ada uji negatif "tanpa klaim harus ditolak").
- **Cara membuktikan perbaikan:** tambah uji `supabase/tes/sesi_masih_aktif.sql` yang menuntut `false`/identitas NULL untuk token tanpa klaim & tanpa header (bukan hanya saat klaim disuntik), dan penerbit klaim nyata (hook/pemicu) wajib ada sebelum klaim "seketika" dipakai ulang di `docs/KEAMANAN.md`.
- **Status verifikasi:** TERVERIFIKASI

### [F-04] RPC masuk anon `verifikasi_pin_perangkat`: oracle enumerasi akun + penguncian lintas-pemanggil (DoS) + atribusi pelaku salah
- **Tingkat:** K-2
- **Artefak:** `supabase/migrations/0061_verifikasi_pin_perangkat.sql:40-58,98-102,114-120,130-134,170`
- **Klaim yang dilanggar:** aturan anti-oracle yang sudah berlaku di fungsi kembarnya (`0006_pin.sql`/`0011_peran_tunggal.sql`/`0012_penutup_celah_review.sql`: "Jangan membocorkan apakah pegawai itu ada: jawab seperti PIN salah"); perbaikan **PR-13** (`0012:296-311`) yang mengubah penghitung menjadi per-pemanggil; `docs/KEAMANAN.md` §2 ("PIN ditebak → batas 5×/15 menit per akun & 12×/15 menit per perangkat").
- **Bukti:** baris 40-58 → pengguna tak dikenal dijawab `{"kode":"PENGGUNA_TIDAK_DITEMUKAN"}`, sedangkan PIN salah dijawab `{"kode":"PIN_SALAH"}` (baris 136-142) — pembeda eksplisit bagi pemanggil **anon** (grant baris 170: `to anon, authenticated, service_role`).
  baris 98-102 menghitung `percobaan_pin` untuk `pengguna_id` **tanpa** penyaring pemanggil; baris 114-120 mengunci setelah 5 kegagalan; baris 130-134 mencatat `pemanggil_id = v_pengguna.id` (korban), bukan penyerang.
  Bandingkan `0012:301,308` (`and pp.pemanggil_id = v_saya`) — jalur autentikasi sudah diperbaiki, jalur masuk anon tidak.
- **Skenario gagal:** Penyerang tanpa akun mengirim 5 kali PIN asal untuk email `kasir@resto` tiap 15 menit → akun itu terkunci terus-menerus (kasir tidak bisa masuk, persetujuan void/diskon berhenti), dan jejak `percobaan_pin` menuduh korban sebagai pelakunya sehingga investigasi diarahkan ke orang yang salah. Ditambah enumerasi email pegawai tanpa memakai jatah tebakan.
- **Dugaan penyebab:** `verifikasi_pin_perangkat` ditulis pada 0061 untuk kebutuhan masuk cepat dan tidak mewarisi kontrak anti-oracle/atribusi yang sudah dikunci di 0006/0011/0012.
- **Cara membuktikan perbaikan:** uji baru `supabase/tes/verifikasi_pin_perangkat_oracle.sql`: (a) email tak dikenal & PIN salah harus mengembalikan kode+pesan identik; (b) 5 kegagalan dari pemanggil A tidak boleh mengunci pemanggil B (pemilik PIN); (c) `percobaan_pin.pemanggil_id` harus berisi pencoba; jalankan `node alat/uji-sql.mjs supabase/tes/verifikasi_pin_perangkat_oracle.sql`.
- **Status verifikasi:** TERVERIFIKASI

### [F-05] Dua faktor dipatahkan: RPC masuk anon menerima ketiadaan perangkat sama sekali
- **Tingkat:** K-2
- **Artefak:** `supabase/migrations/0061_verifikasi_pin_perangkat.sql:10-15,104-112,150-168`; `docs/TECH_SPEC.md:381`
- **Klaim yang dilanggar:** "masuk staf = **perangkat terdaftar** + PIN" (`docs/KEAMANAN.md` §1.3, §4; TECH_SPEC §381); status AUD-3 F-11/K F-03 "DITUTUP 2026-09-21".
- **Bukti:** tanda tangan fungsi `p_perangkat_id uuid default null, p_perangkat_nama text default null` (baris 10-15) dan blok langkah 3 dibungkus `if p_perangkat_id is not null` (baris 104); bila null, `v_alat_nama := coalesce(p_perangkat_nama, 'tidak-diketahui')` — memakai **nama kiriman klien**, persis kelas cacat yang dulu ditutup. Jalur sukses tetap `LOGIN_SUKSES` (baris 150-168).
- **Skenario gagal:** Penyerang yang memiliki email + PIN (mis. PIN ditebak/dibagikan) masuk tanpa perangkat terdaftar dan tanpa jejak `perangkat_id`; bila hanya nama perangkat yang dikirim, batas 12×/perangkat pun dimatikan (`v_gagal_alat := 0`, baris 110-112) sehingga penebakan PIN hanya tersisa batas 5×/akun — dan penguncian itu bisa dipakai untuk menyerang korban (F-04).
- **Dugaan penyebab:** parameter perangkat dibuat opsional agar layar masuk lama tetap jalan; tidak ada pemeriksaan "wajib" di sisi database maupun Edge.
- **Cara membuktikan perbaikan:** ubah kontrak menjadi `p_perangkat_id uuid` **wajib** (tanpa default) + uji `supabase/tes/verifikasi_pin_perangkat.sql`: panggilan tanpa `perangkat_id` harus `PERANGKAT_TIDAK_SAH` dan **tidak** menambah jatah tebak; `grep -n "default null" supabase/migrations/0061_verifikasi_pin_perangkat.sql` pada tanda tangan harus kosong.
- **Status verifikasi:** TERVERIFIKASI

### [F-06] Pendaftaran perangkat (faktor kedua) dapat ditebak dari anon tanpa pembatas percobaan
- **Tingkat:** K-3
- **Artefak:** `supabase/migrations/0030_sesi_dan_persetujuan_perangkat.sql:210,238-300,313`; `aplikasi/src/layar/pengaturan/Perangkat.tsx:212`
- **Klaim yang dilanggar:** `docs/KEAMANAN.md` §4 ("perangkat baru wajib lewat kode + persetujuan"; kode sekali pakai 15 menit) — tanpa pembatas, kontrol kode tidak setara kontrol PIN (5×/12×).
- **Bukti:** `0030:210` kode = `upper(substr(replace(gen_random_uuid()::text,'-',''),1,8))` → 8 karakter heksadesimal (≈32 bit); `0030:238-300` tidak ada penghitung percobaan/pencatatan gagal (bandingkan `percobaan_pin`/`percobaan_masuk` yang ada untuk PIN); `0030:313` memberi hak eksekusi ke **anon**; `0030:265` hanya menjawab "salah/dipakai/kedaluwarsa" (baik untuk non-oracle, tetapi tanpa biaya tebak).
- **Skenario gagal:** Skrip anon menembak kode acak terus-menerus selama 15 menit masa berlaku; peluangnya kecil per percobaan tetapi tanpa pembatas/biaya dan tanpa jejak, dan satu keberhasilan menghasilkan perangkat **aktif** (faktor kedua) untuk peran `['kasir','pelayan','dapur']` bawaan. Layar juga menyebut "kode 6 digit" (Perangkat.tsx:212) padahal peladen menerbitkan 8 karakter — alur pendaftaran tidak konsisten bila nanti disambungkan.
- **Dugaan penyebab:** pembatas percobaan hanya dipasang untuk PIN; alur kode dianggap cukup aman karena kode acak-32-bit.
- **Cara membuktikan perbaikan:** tabel `percobaan_kode_perangkat` + batas (mis. 10 percobaan/15 menit/IP-sesi) dan uji `supabase/tes/perangkat_registrasi.sql` yang menuntut penolakan setelah batas; selaraskan panjang kode di layar & peladen.
- **Status verifikasi:** TERVERIFIKASI

### [F-07] Higiene CI: aksi belum dipin ke SHA dan kata sandi DB lewat argumen perintah
- **Tingkat:** K-4
- **Artefak:** `.github/workflows/ci.yml:23,33`; `.github/workflows/sebar-skema.yml:60-69`
- **Klaim yang dilanggar:** tidak ada janji eksplisit; ini higiene rantai pasok pada repo publik yang menjalankan alur rilis.
- **Bukti:** `grep -n "uses:" .github/workflows/*.yml` → `actions/checkout@v5`, `actions/setup-node@v5` (tag bergerak, bukan SHA commit); `sebar-skema.yml:66-69` → `npx --yes supabase@2.117.0 --yes link … --password "$SUPABASE_DB_PASSWORD"` (kata sandi tampil di daftar argumen proses runner; masking log GitHub tidak melindungi `ps`). Sisi baik: `permissions: contents: read`, tanpa `pull_request_target`, rahasia lewat `secrets.` context.
- **Skenario gagal:** Tag aksi dibajak/di-retag → kode berjalan dengan izin repositori; atau proses lain di runner yang sama membaca argumen `--password` (kata sandi DB proyek).
- **Dugaan penyebab:** kepraktisan; standar pinning SHA belum dipakai di proyek ini.
- **Cara membuktikan perbaikan:** `grep -n "uses: actions/checkout@" .github/workflows/*.yml` → SHA penuh + komentar versi; gunakan `SUPABASE_DB_PASSWORD` lewat stdin/env resmi CLI (`supabase db push --password` diganti `PGPASSWORD`/prompt non-interaktif).
- **Status verifikasi:** TERVERIFIKASI

## 5. Kalibrasi cacat tanaman

Folder: `docs/uji/kalibrasi/bahan-2026-09-17/` (5 berkas; membaca `CARA-PAKAI.md`, tidak mencari kunci di luar repo).

**Ditemukan: 5 dari 5** — setiap berkas mengandung minimal satu cacat sengaja; temuan palsu: **0**.

| # | Berkas | Kelas | Cacat yang saya temukan | Bukti |
|---|---|---|---|---|
| 1 | `01_gerbang_izin.sql` | K-2 (hak istimewa) | Fungsi `boleh()` di-`grant execute … to authenticated` **tanpa** `revoke … from public`, padahal komentarnya sendiri menuntut "hak execute dicabut dari public"; akibatnya PUBLIC (termasuk `anon`) tetap boleh memanggil gerbang izin tunggal. Bandingkan konvensi proyek di `0005_izin_berjenjang.sql:301-309` (revoke lalu grant). | `grep -n "grant\|revoke" bahan-2026-09-17/01_gerbang_izin.sql` → hanya baris grant; `grep -n "revoke all on function public.boleh" supabase/migrations/0005_izin_berjenjang.sql` → ada 3 baris revoke |
| 2 | `02_policy_pengaturan.sql` | K-1 (bocor lintas penyewa) | Policy SELECT `pengaturan_pilih … using (penyewa_id is not null)` → setiap pengguna terautentikasi membaca pengaturan **semua** resto; proyek nyata memakai `penyewa_id = penyewa_saya()` (`0004_pola_rls.sql:83-85`). | `cat bahan-2026-09-17/02_policy_pengaturan.sql` vs `sed -n '83,85p' supabase/migrations/0004_pola_rls.sql` |
| 3 | `03_fungsi_terima_bayar.sql` | K-2 (uang) | Pemeriksaan lebih bayar salah tempat: `if v_sebelum > v_pesanan.total` (memeriksa jumlah lama, bukan `v_sebelum + p_jumlah`), lalu INSERT tanpa cek → pembayaran melebihi total diterima dan pesanan langsung ditandai `lunas`. Proyek nyata: `0010_pembayaran.sql:313-317` memakai `v_sebelum + new.jumlah > total`. | `cat -n bahan-2026-09-17/03_fungsi_terima_bayar.sql` (baris 15-24) vs `grep -n -A3 "v_sebelum := public.total_dibayar" supabase/migrations/0010_pembayaran.sql` |
| 4 | `04_panduan_singkat.md` | K-2 (dokumen pengguna tak bisa dijalankan) | Tiga rujukan mati — `bash aplikasi/pratinjau.sh` (yang ada: `aplikasi/alat/pratinjau.sh`), `python3 alat/periksa-struktur.py` (yang ada: `aplikasi/alat/periksa-struktur.py`), `docs/PANDUAN_KEAMANAN.md` §4 (tidak ada di repo) — dan kebijakan PIN salah: "Salah PIN **10 kali** → terkunci 15 menit", sedangkan proyek menetapkan 5×/15 menit per akun & 12×/15 menit per perangkat. | `for f in aplikasi/pratinjau.sh alat/periksa-struktur.py docs/PANDUAN_KEAMANAN.md; do [ -e "$f" ] && echo ADA || echo "TIDAK ADA"; done` → tiga "TIDAK ADA"; `grep -n "5×/15 menit per akun · 12×/15 menit per perangkat" docs/KEAMANAN.md` |
| 5 | `05_pemeriksa_ambang.py` | K-3 (pemeriksa tumpul/gagal-terbuka) | Ambang diturunkan (`MIN_LAYAR_DIPERIKSA = 5  # ambang nyata proyek: 20`), jalur di bawah ambang `return 0` (SKIP = lulus), dan `AKAR` menunjuk `docs/uji/kalibrasi` sehingga glob layar tak pernah menemukan berkas nyata → pemeriksa selalu hijau tanpa memeriksa apa pun. | `cat -n bahan-2026-09-17/05_pemeriksa_ambang.py` (baris 7-8, 12, 19-21); jalankan `python3 bahan-2026-09-17/05_pemeriksa_ambang.py` → "SKIP: layar baru 0 …" dengan exit 0 |

Catatan kejujuran: saya menghitung satu butir per berkas; berkas 04 dan 05 masing-masing punya beberapa gejala dari satu cacat dasar yang sama, sehingga "5 dari 5" adalah hitungan yang saya yakini kebenarannya (bukan tebakan jumlah kunci). Saya tidak mencari/memakai kunci di luar repo.

## 6. Yang tidak bisa saya verifikasi

- **Suite uji SQL tidak bisa dijalankan di lingkungan auditor.** `node alat/uji-sql.mjs`, `alat/periksa-keamanan-sql.py`, `alat/periksa-matriks-izin.py`, dan semua `alat/uji-mutasi-*.py` gagal dengan `Error [ERR_MODULE_NOT_FOUND]: Cannot find package '@electric-sql/pglite'`, dan `alat/uji-edge-pin.mjs` menolak jalan karena `esbuild` tidak ada ("GAGAL: esbuild tidak ditemukan"). Paket menyatakan mesin "selalu siap", tetapi klon bersih tanpa `node_modules` tidak siap; saya **tidak memasang apa pun** (aturan hanya-baca + arahan paket). Semua klaim berangka "N LULUS" di ROADMAP karena itu tidak dapat saya reproduksi; verdict saya tidak bersandar pada klaim itu.
- **Uji unit/komponen (`cd aplikasi && npm test`, vitest) tidak bisa dijalankan** (tanpa `node_modules`). Yang bisa saya buktikan tentang mutu uji: jumlah berkas uji dan isi tes yang saya baca (`auth.test.ts` — lihat F-01; `sesi_kedaluwarsa.sql` — lihat F-03; `Perangkat.test.tsx` memakai mock).
- **Penyebab pasti CI merah commit target tidak dapat saya tunjukkan.** `gh api repos/With-AI-Agent/Resto-Barokah/actions/runs?head_sha=804ed86…` → 1 run, `failure` (run `36088466076`), langkah ke-58 "Pemeriksa fondasi, roadmap, struktur, komponen, uji & kontras". Unduhan log gagal dua kali (`gh run view --log-failed` → `EOF` dari results-receiver). Saya menjalankan ulang seluruh perintah langkah itu di klon penuh: semua hijau kecuali perintah yang butuh PGlite (butir di atas). Pada klon dangkal, `alat/periksa-paket.py` merah 44×, tetapi itu artefak klon dangkal — di klon beriwayat penuh hasilnya "HASIL: LOLOS".
- **Tidak ada peramban/tablet, tidak ada kredensial Supabase/Cloudflare** → klaim waktu-nyata (deploy, CORS di Cloudflare, kunci proyek nyata, uji perangkat sungguhan) tidak saya uji; `docs/ops/DAFTAR_KUNCI_PEMILIK_NONSECRET.md` saya baca sebagai dokumen, bukan sebagai bukti layanan hidup.
- **Jalur kalibrasi mesin tidak bisa dinilai di sesi ini:** `alat/periksa-kunci-kalibrasi.py` melaporkan sendiri bahwa katalog cacat luar-repo (`/home/user/.kalibrasi/kalibrasi-cacat.json`) tidak ada, jadi `--kalibrasi-siapkan`/`--kalibrasi-nilai` tidak dapat dijalankan; saya tidak membuat/mencari kunci.
- **Independensi model:** saya sesi/konteks berbeda dari penulis kode, tetapi platform tidak mengungkap keluarga model dasar; bila modelnya sekeluarga, korelasi cacat tetap mungkin (dicatat sebagai keterbatasan, bukan klaim independensi penuh).
- **Kedalaman lensa UI:** tanpa `npm test`/peramban, pemeriksaan keadaan layar (kosong/memuat/gagal/…) dan target sentuh saya batasi pada pemeriksa statis (`uji-kontras.py` → "166 lolos, 0 gagal"; `aplikasi/alat/periksa-struktur.py` → "27 OK · 7 INFO · 0 GAGAL") + pembacaan kontrak `layar.ts`; belum saya buktikan di perangkat.

## 7. Pernyataan tidak mengubah apa pun

Saya tidak mengubah apa pun di repositori ini — saya hanya-baca. SATU-SATUNYA berkas yang saya buat di dalam repositori adalah laporan ini (draf di `.laporan-lokal/`); tidak ada berkas proyek lain yang saya ubah, dan saya tidak memakai `git add` atas folder, tidak merge/rebase/reset/force-push, tidak menyentuh cabang sumber/main. Semua pengujian saya jalankan pada salinan sementara unik (`/tmp/audit/target`, `/tmp/audit/tr`) dan skrip analisis saya simpan di `/tmp/audit/`. Bukti: `git status --short` pada checkout bersama (`/home/user/Resto-Barokah`, HEAD tetap `474e018`) hanya menampilkan berkas laporan ini (`.laporan-lokal/` diabaikan Git sehingga tidak muncul sebagai berkas proyek); commit/push laporan dilakukan alat `alat/kirim-laporan.py` dari SHA paket dengan index sementara terpisah.

```
$ git status --short
?? .laporan-lokal/          # draf laporan ini — diabaikan Git (.gitignore)
$ git rev-parse HEAD
474e018054645d6927557af80302094dca2270d7
```

## 8. Temuan di luar cakupan

| # | Temuan | Mengapa di luar cakupan | Bukti | Syarat dilanjutkan ke audit lain |
|---|---|---|---|---|
| G-01 | **Angka tabel basi di dokumen:** `docs/ROADMAP.md` dua kali menulis "hari ini **26 tabel**", padahal commit target memiliki **41** tabel di `public`. | Dokumen roadmap bukan berkas lingkup keamanan (lingkup: `docs/KEAMANAN.md`). | `grep -rh "create table" supabase/migrations/*.sql \| wc -l` → 41; `python3 /tmp/audit/rls2.py` → "TABEL: 41"; `grep -n "26 tabel" docs/ROADMAP.md` → baris 250 & 277 | segarkan angka + jalankan `python3 alat/periksa-angka-bukti.py` (kini LOLOS karena angka tanpa perintah dianggap "ditandai jujur"; pertimbangkan agar klaim "hari ini N tabel" wajib berperintah) |
| G-02 | **Layar perangkat & sesi belum tersambung database, tetapi T2-15/T2-17 `[x]`:** `LayarPerangkat`/`DaftarPerangkat` hanya komponen presentasional (props + `onBuatKode`), tidak ada pemanggil RPC perangkat di `aplikasi/src`; tesnya memakai mock. | Berkas `aplikasi/src/layar/**` di luar prefiks lingkup (`aplikasi/src/lib/`). | `grep -rn "buat_kode_perangkat\|daftarkan_perangkat_dengan_kode\|cabut_perangkat" aplikasi/src` → kosong; `head -33 aplikasi/src/layar/pengaturan/Perangkat.tsx` (props/kallbacks); `Perangkat.test.tsx` (`vi.fn().mockResolvedValue`) | sambungkan layar ke RPC 0030 lalu buktikan dengan uji integrasi nyata (bukan mock) sebelum T2-15/T2-17 boleh tetap `[x]` |
| G-03 | **Dokumen privasi basi:** `docs/PRIVASI_PELANGGAN.md` §0 berbunyi "migrasi yang hidup saat ini (`supabase/migrations/`, nomor 0001–0019)", padahal kini 0001–0061. Substansinya (belum ada tabel/data pelanggan) masih benar. | Dokumen privasi bukan berkas lingkup keamanan. | `head -40 docs/PRIVASI_PELANGGAN.md`; `ls supabase/migrations \| tail -3` | segarkan kalimat rentang migrasi atau ubah menjadi rujukan tak-bernomor |
| G-04 | **Nama berkas paket di paket audit menyimpang dari yang benar-benar ada:** paket menulis `docs/uji/paket-audit/AUD-4-2026-09-25-keamanan.md` (tanpa `-SIAP-TEMPEL`) dan menyatakan berkasnya "dibuat SETELAH angka dihitung", sementara SHA paket memuat `AUD-4-2026-09-25-keamanan-SIAP-TEMPEL.md` (blob `93eb2f07…`) dan `AUD-4-2026-09-25-keamanan.md` (blob `5c478f47…`) — keduanya ada; tidak ada cacat isi, hanya penamaan ganda yang bisa membingungkan auditor berikutnya. | Berkas paket hidup di commit paket, bukan di pohon target. | `git ls-tree 474e018:docs/uji/paket-audit/` → dua berkas itu ada | tidak wajib ditindaklanjuti; catat di riwayat agar auditor memakai path `…-SIAP-TEMPEL.md` |