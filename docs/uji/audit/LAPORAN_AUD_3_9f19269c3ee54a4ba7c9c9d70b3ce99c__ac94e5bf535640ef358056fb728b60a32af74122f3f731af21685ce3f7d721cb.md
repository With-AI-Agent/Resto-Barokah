# LAPORAN AUDIT INDEPENDEN — AUD-3 — 2026-09-24

- **Auditor:** sesi auditor terpisah (Arena.ai Agent Mode, cabang `arena/01a0d3d8-resto-barokah`). Model tidak dapat dipilih manual pada sesi ini → dicatat sebagai keterbatasan (§6, PROTOKOL §3.2).
- **Tanggal:** 2026-09-24
- **Tingkat audit:** AUD-3
- **Commit yang diaudit:** `e7818e2f8c51d4b6503ce6becd1cbc3b7d11be6b`
- **Paket audit:** `docs/uji/paket-audit/AUD-3-2026-09-24-SIAP-TEMPEL.md` @ `289db4f3036dea942058bea175ad922371f832c0` (SHA-256 paket `361b62fd51029fe64319f189fd2446fe5075ec8fd7ace91336fde568397397d9`). Berkas paket **tidak ada** di pohon commit target — sesuai §0a paket; diambil lewat `git fetch --no-write-fetch-head origin 289db4f3…` + `git show`.
- **Alasan commit berbeda:** klon kerja HEAD berada di SHA paket `289db4f3036dea942058bea175ad922371f832c0`; audit dilakukan pada commit target `e7818e2f8c51d4b6503ce6becd1cbc3b7d11be6b` sesuai paket (objek target diambil terpisah lewat `git fetch --no-write-fetch-head`, bukan dengan berpindah HEAD).
- **Mode cakupan:** menyeluruh
- **Verdict:** TIDAK-BERSIH

## 1. Cakupan

Cakupan menyeluruh: 839 dari 839 berkas — bukti: `git ls-tree -r --name-only e7818e2f8c51d4b6503ce6becd1cbc3b7d11be6b | wc -l` → 2645 (pohon), `… | grep -v '^skills/' | grep -v '^_salinan-meta/' | grep -v '^_Notes.md$' | wc -l` → 839 (lingkup).
Bukti per grup (tabel di bawah) memakai perintah/berkas:baris: `git ls-tree -r --name-only e7818e2f8c51d4b6503ce6becd1cbc3b7d11be6b | wc -l` → 2645; `git ls-tree -r --name-only e7818e2… | grep -v '^skills/' | grep -v '^_salinan-meta/' | grep -v '^_Notes.md$' | wc -l` → 839.

Cara hitung: pohon commit target (`git ls-tree -r --name-only e7818e2f8c51d4b6503ce6becd1cbc3b7d11be6b`) = 2.645 berkas; dikecualikan sesuai §0c: `skills/` (1.803), `_salinan-meta/` (2), `_Notes.md` (1) → 839 berkas lingkup, cocok persis dengan tabel §0 paket. Setiap berkas lingkup dibaca mesin (sapuan seragam: sha256/ukuran + pola rujukan berkas, rahasia, klaim angka, TODO) dan tiap grup punya pemeriksa/uji khusus; berkas pengguna, berkas uang, migrasi, dan layar dibaca manual. Satu berkas `__pycache__` hasil kerja lokal saya sendiri dikeluarkan dari hitungan; bukan isi commit.

| # | Artefak | Diperiksa | Bukti (perintah/baris) |
|---|---|---|---|
| 1 | `aplikasi/src` (219) | 219/219 | `npx vitest run` → 84 berkas/671 uji LULUS · `npx eslint .` → 0 error/21 warning · `npx tsc -b --noEmit` bersih · `python3 aplikasi/alat/periksa-uji.py` → 23 berkas logika beruji, 14 uji interaksi nyata, 656 uji terbaca |
| 2 | `aplikasi/alat` (14) | 14/14 | `uji-kontras.py` 166 lolos/0 gagal · `periksa-bahasa.py` paritas 269 kunci ×4 · `periksa-arah.py` 461 KB < 650 KB · `periksa-struktur.py` 27 OK/0 GAGAL · `periksa-komponen-env.py` 10 OK/0 GAGAL (+`--uji-diri` LOLOS) · `periksa-node.py`, `periksa-kerapatan.py`, `periksa-antarmuka.py` LULUS |
| 3 | aplikasi (konfigurasi) (20) | 20/20 | `npm run format:check` / `lint` / `typecheck` / `build` LULUS · `npm audit` 0 kerentanan · baca `package.json`, `vite.config.ts`, `tsconfig*.json`, `.env.example` (9 nama variabel TECH_SPEC §6) |
| 4 | `supabase/migrations` (53) | 53/53 | `node alat/uji-sql.mjs` → 98 LULUS/0 GAGAL · `periksa-keamanan-sql.py` 2 LULUS · `periksa-migrasi-beku.py` LOLOS · `periksa-audit.py` OK |
| 5 | `supabase/tes` (99) | 99/99 | `node alat/uji-sql.mjs` → `uji: 98 LULUS · 0 GAGAL`; `ls supabase/tes | wc -l` → 99 (+1 berkas pendamping) |
| 6 | `supabase/functions` (2) | 2/2 | `ls supabase/functions/*/` → `verifikasi_pin/index.ts`; `wc -c supabase/functions/verifikasi_pin/index.ts` → 8351 — hanya **satu** Edge Function |
| 7 | supabase (akar) (2) | 2/2 | `ls supabase/` → `README.md`, `config.toml`; `sed -n '1,20p' supabase/config.toml` dibaca |
| 8 | `alat` (96) | 96/96 | `for f in alat/uji-mutasi-*.py; do python3 "$f"; done` → semua LOLOS; `python3 alat/periksa-roadmap.py` → LOLOS |
| 9 | `_sistem` (15) | 15/15 | `python3 _sistem/validate_system.py` → PASS |
| 10 | docs (fondasi) (15) | 15/15 | `validate_system.py` + baca `PRD.md`, `TECH_SPEC.md`, `KEAMANAN.md`, `ROADMAP.md` (193 tugas), `DECISIONS_LOG.md`, `SPESIFIKASI_UI.md`, `AGENT_OPERATING_GUIDE.md` |
| 11 | `docs/uji` (132) | 132/132 | `periksa-roadmap.py` LOLOS (+`--uji-diri`) · `periksa-panduan.py` LOLOS (+`--uji-diri`) · `periksa-buku-uji.py` LOLOS (+`--uji-diri`) · `periksa-rujukan.py` LOLOS · `periksa-temuan-audit.py` · `periksa-kunci-kalibrasi.py` |
| 12 | `docs/teknis` (6) | 6/6 | `git ls-tree -r --name-only e7818e2… -- docs/teknis | wc -l` → 6; baca `BUKU_INSIDEN.md` (`:166` menandai `PEMULIHAN.md` belum dibuat → bukan temuan) |
| 13 | `docs/ops` (13) | 13/13 | `git ls-tree -r --name-only e7818e2… -- docs/ops | wc -l` → 13; baca `SIAP_AKUN_PEMILIK.md`, `DAFTAR_KUNCI_PEMILIK_NONSECRET.md`, `SIAP-LANJUT.md` |
| 14 | `docs/desain` (59) | 59/59 | sapuan seragam + pemeriksa UI (`periksa-kerapatan.py`, `periksa-antarmuka.py`, `uji-kontras.py`) |
| 15 | `prototipe` (58) | 58/58 | `ls prototipe/alat/gambar.js prototipe/alat/mockup.js` → ADA; rujukan relatif “alat/…” di `prototipe/README.md:22` sah — bukan temuan |
| 16 | `_log-sesi` (14) | 14/14 | `git ls-tree -r --name-only e7818e2… -- _log-sesi | wc -l` → 14; baca `_log-sesi/LOG_SESI_2026-09-22.md` |
| 17 | berkas pengguna di akar (19) | 19/19 | `periksa-panduan.py` → 803 baris/16 alur/4 blok prompt/30 perintah/13 mekanisme/89 rujukan · baca `PANDUAN_PENGGUNA.md`, `STATUS.md`, `PROJECT_STATE.md`, `PROMPT_ENTRI_UNIVERSAL.md`, `AGENT_SYSTEM.md`, `START_DI_SINI.md`, `PROFIL_PENGGUNA.md`, `PANDUAN_PEMILIK.md` |
| 18 | `.github/workflows` (3) | 3/3 | `grep -c '^      - name:' .github/workflows/ci.yml` → 57 langkah (70 baris perintah); baca `sebar-halaman.yml`, `sebar-skema.yml` |
| 19 | Dikecualikan: `skills/` (1.803), `_salinan-meta/` (2), `_Notes.md` (1) | tidak diperiksa | `git ls-tree -r --name-only e7818e2… \| grep -c '^skills/'` → 1803; **setuju** dikecualikan: `skills/` = pustaka skill pihak ketiga/katalog (26 MB) yang dipakai sebagai **referensi wajib** §5 paket, bukan produk; `_salinan-meta/PLATFORM_LMARENA.md` = salinan meta platform; `_Notes.md` = catatan kerja. Tidak ada kode produk, migrasi, uji, atau kontrol keamanan proyek di dalamnya |

### 1a. Berkas untuk pengguna

| # | Berkas untuk pengguna | Diperiksa dengan cara pengguna | Bukti |
|---|---|---|---|
| 1 | `PANDUAN_PENGGUNA.md` | prompt bisa disalin apa adanya? langkah menunjuk berkas/perintah yang ada? | `python3 alat/periksa-panduan.py` → LOLOS (803 baris, 16 alur, 89 rujukan); cacat struktur C6/C7 & H1/H3/H4 → F-07 |
| 2 | `STATUS.md` + `PROJECT_STATE.md` | angka & klaim "selesai" bisa dipercaya hari ini? | `npx vitest run` → 84/671 vs klaim 45/213; `ls supabase/tes/*.sql \| wc -l` → 98 vs klaim 72 → F-05 |
| 3 | `docs/ops/SIAP_AKUN_PEMILIK.md` + `DAFTAR_KUNCI_PEMILIK_NONSECRET.md` | nilai yang tersimpan pantas? rahasia bocor? | `python3 alat/periksa-rahasia.py` → LOLOS; email ditutup sesuai K F-06 |
| 4 | `docs/uji/BUKU_UJI_PEMILIK.md` | tiap butir bisa dikerjakan/dinilai orang non-teknis? | `python3 alat/periksa-buku-uji.py` → 6 lakukan + 12 coba, semua berlangkah & berharapan |
| 5 | `PROMPT_ENTRI_UNIVERSAL.md` + `docs/uji/PROMPT_AUDIT_INDEPENDEN.md` | prompt identik dengan sumber kanonik? | `python3 alat/periksa-panduan.py` → blok Prompt Pembuka & Prompt Auditor identik |
| 6 | `docs/ops/SIAP-LANJUT.md` + `docs/teknis/BUKU_INSIDEN.md` | langkah darurat bisa diikuti? rujukan hidup? | `python3 alat/periksa-rujukan.py` → LOLOS; `0015_audit.sql`/`0016_…` bernama baru + bercatatan (bukan cacat) |

Catatan naratif (bukan pengganti tabel): 
- **`PANDUAN_PENGGUNA.md`** — prompt & langkah utama sehat (`python3 alat/periksa-panduan.py` → LOLOS; blok Prompt Pembuka identik dengan `PROMPT_ENTRI_UNIVERSAL.md`; 1 rujukan ditandai `(rencana)` = `supabase/functions/ringkasan_harian/index.ts`, jadi tidak menyesatkan). **Tetapi struktur Bagian C–H cacat** (lihat F-07): `### C7` (baris 533) mendahului `### C6` (baris 547); dua sub-bagian bernomor `### H1` (baris 713 & 739); `### H3` (baris 766) kosong dan `### H4` (baris 768) berjudul sama. Bagi Lee yang menavigasi dengan nomor bagian, ini langkah buntu kecil yang berulang.
- **`STATUS.md:11` + `PROJECT_STATE.md:61`** — banner "…SELESAI LENGKAP 100% (2026-09-22) … Seluruh 100 gerbang CI otomatis, 72 berkas uji SQL lokal (100% LULUS), dan 45 berkas uji Vitest (213 unit test) lolos 100%" adalah **bukti utama** yang Lee baca. Angkanya sekarang salah (`ls supabase/tes/*.sql | wc -l` → 98; `npx vitest run` → 84 berkas/671 uji) dan memuat klaim TOTP/E2E/beban yang tidak ada wujudnya di commit ini (F-01…F-05). Ini jenis cacat "buku pedoman basi = temuan" (PROTOKOL §2b.5).
- **`docs/ops/SIAP_AKUN_PEMILIK.md` + `docs/ops/DAFTAR_KUNCI_PEMILIK_NONSECRET.md`** — nilai non-rahasia nyata (alamat proyek Supabase, kunci `publishable`, akun Cloudflare) tersimpan; email ditutup sesuai K F-06; `python3 alat/periksa-rahasia.py` → LOLOS. **Tidak ada temuan** (rahasia sesungguhnya tetap di kotak rahasia CI).
- **`docs/uji/BUKU_UJI_PEMILIK.md`** — `python3 alat/periksa-buku-uji.py` → LOLOS: 6 baris "lakukan" + 12 baris "coba" = 18 baris, semua punya langkah + harapan + kolom hasil, dan tiap baris menunjuk tugas ROADMAP nyata.
- **`docs/uji/PROMPT_AUDIT_INDEPENDEN.md` + `PROMPT_ENTRI_UNIVERSAL.md`** — blok auditor/pembuka identik dengan sumber kanonik (dijaga `periksa-panduan.py`; prompt bisa disalin apa adanya). **Tidak ada temuan.**
- **`docs/ops/SIAP-LANJUT.md` + `docs/teknis/BUKU_INSIDEN.md`** — `python3 alat/periksa-rujukan.py` → LOLOS; langkah darurat & lanjutan terbaca awam. Satu catatan §1b paket (`0015_audit.sql`, `0016_mode_dukungan.sql`) ternyata **memang bernama baru** dan sudah diberi catatan penomoran di ROADMAP — bukan cacat.

## 2. Klaim pembangun yang saya coba falsifikasi

| # | Klaim (lokasi) | Cara uji | Hasil |
|---|---|---|---|
| 1 | "T2-13 Kunci kedua (TOTP) … uji lulus" + `[x]` + Bukti 2026-09-22 (`docs/ROADMAP.md:776`) | `ls supabase/functions`; `find . -name 'Totp.tsx' -o -name 'mfa.sql' -o -name 'atur_ulang_mfa'`; `grep -rn 'butuhTotp' aplikasi/src` | **DIBANTAH** — tidak ada implementasi TOTP/MFA sama sekali (F-01) |
| 2 | "T2-04 … 'Daftar dengan Google' bekerja … uji manual dua jalur lulus" (`docs/ROADMAP.md:688`) | `grep -rn 'signInWithOAuth\|google' aplikasi/src`; `ls aplikasi/src/lib/google.ts`; `grep -rn '<LayarMasukPelanggan' aplikasi/src` | **DIBANTAH** — hanya logo; tidak ada `google.ts`; layar tak pernah dirender (F-02) |
| 3 | "T3-14 Uji alur kasir ujung-ke-ujung … lulus di CI" (`docs/ROADMAP.md:967`) | `ls aplikasi/uji`; `git ls-tree -r --name-only e7818e2 \| grep -c 'uji/e2e'`; devDeps | **DIBANTAH** — tidak ada berkas/harness E2E (F-04) |
| 4 | "T3-16 Uji beban ringan kasir … < 1 detik per aksi" (`docs/ROADMAP.md:985`) | `ls aplikasi/uji/beban`; devDeps | **DIBANTAH** — tidak ada uji beban (F-04) |
| 5 | Banner "seluruh Fase 2 … dan komponen utama Fase 3 … selesai dikerjakan dan diverifikasi penuh" (`STATUS.md:11`, `PROJECT_STATE.md:61`) | Uji #1–#4 + `grep -n '^\s*case ' aplikasi/src/App.tsx` | **DIBANTAH** — TOTP, Google, E2E, beban tidak ada; T3-05/T3-09/T3-13 masih `[ ]` |
| 6 | "45 berkas uji Vitest (213 unit test) lolos 100%" (`STATUS.md:11`) | `npx vitest run` | **DIBANTAH** — nyatanya 84 berkas / 671 uji LULUS (F-05) |
| 7 | "72 berkas uji SQL lokal (100% LULUS)" (`STATUS.md:11`) | `ls supabase/tes/*.sql \| wc -l`; `node alat/uji-sql.mjs` | **DIBANTAH** — nyatanya 98 berkas (F-05) |
| 8 | "Seluruh 100 gerbang CI otomatis" lolos (`STATUS.md:11`) | `gh api …/check-runs` untuk commit target; `grep -c '^\s*- name:' .github/workflows/ci.yml` | **DIBANTAH** — run `36009322838` / check `107665621957` = **failure**; berkas CI memuat 57 langkah/70 perintah, tidak ada definisi "100 gerbang" (F-05) |
| 9 | Paket §2 T1-32: "`aksi.ts` memuat **32** registri aksi" | `grep -cE "^  '[^']+': \{" aplikasi/src/lib/aksi.ts`; ekstraktor `alat/peta-ui.py` | **DIBANTAH** — nyatanya **39** entri (F-06) |
| 10 | Paket §2 T1-39: "PETA_UI disegarkan menjadi 11 layar & **38** aksi" | `grep -n 'Total Aksi Terdaftar' docs/PETA_UI.md` | **DIBANTAH** — `docs/PETA_UI.md:12` menulis **39 aksi** (F-06) |
| 11 | Paket §2 T1-38: "4 kamus … 100% paritas **102 kunci**" | `python3 aplikasi/alat/periksa-bahasa.py` | **DIBANTAH (angka)** — paritas 100% benar, tetapi acuannya **269** kunci (F-06) |
| 12 | Paket §0a: "CI commit target BELUM-HIJAU (1 run: failure)" | `gh api repos/…/commits/e7818e2…/check-runs` | **TIDAK DIBANTAH** — benar; conclusion `failure` |
| 13 | Paket §1b: 31 jalur bertanda "sudah [x] — berkasnya TIDAK ADA: laporkan!" | Loop keberadaan tiap jalur + cek catatan/`Bukti` di ROADMAP | **SEBAGIAN DIBANTAH** — 4 memang cacat penuh (F-01…F-04), sisanya bernama baru & bercatatan resmi atau berkas pengganti hidup (F-08) |
| 14 | T1-05 "10 kode izin" (`supabase/migrations/0005_izin_berjenjang.sql:31`) | `sed -n '31,45p' 0005_izin_berjenjang.sql` | TIDAK DIBANTAH — tepat 10 baris nilai |
| 15 | T1-06 "PIN hanya hash + tolak 5 gagal/akun & 12 gagal/perangkat per 15 menit" | `grep -n 'crypt(\|5 kali\|12 kali' 0006_pin.sql` (13-14, 384) | TIDAK DIBANTAH |
| 16 | T1-27 "rantai hash sha256 atomik + `verifikasi_rantai_audit()` + trigger tolak UPDATE/DELETE" | `sed -n '1,40p' 0029_audit_kekal_rantai.sql`; `node alat/uji-sql.mjs` (98/98) | TIDAK DIBANTAH |
| 17 | "Lebih bayar ditolak (`BY-301`)" | `sed -n '120,135p' 0039_bayar_pesanan.sql`; uji `uang_kunci.sql`, `uang_peladen.sql` | TIDAK DIBANTAH |
| 18 | T1-41 "461 KB < 650 KB" & T1-43 "18 butir (6 lakukan, 12 coba)" | `periksa-arah.py`; `periksa-buku-uji.py` | TIDAK DIBANTAH — 461,0 KB; 6/12/18 |
| 19 | T1-32 "`TombolAksi.test.tsx` 7 uji lulus" | `npx vitest run src/komponen/TombolAksi.test.tsx` | TIDAK DIBANTAH — 7 lulus |

## 3. Serangan yang dijalankan (kill attempts)

| # | Skenario | Cara | Hasil |
|---|---|---|---|
| 1 | Uji SQL penuh (RLS, uang, shift, audit, PIN, perangkat) | `node alat/uji-sql.mjs` | 98 LULUS · 0 GAGAL |
| 2 | Jaring uji aplikasi | `npx vitest run` | 84 berkas / 671 uji LULUS |
| 3 | Gerbang mutasi perilaku aplikasi | `node alat/uji-mutasi-app.mjs` (+`--uji-diri`) | 77 mutasi semua MERAH; harness `--uji-diri` LOLOS |
| 4 | Uji mutasi SQL/aturan (0005, 0011, 0012, 0015, 0018, 0020, 0025, 0028, 0029, 0030, 0031, 0039, …) | jalankan semua `alat/uji-mutasi-*.py` | semua LOLOS; mis. 0012: 16/16 MERAH; 0015: LOLOS |
| 5 | Sapuan `security definer` (fungsi istimewa yang bisa dipanggil siapa saja) | grep semua migrasi + klasifikasi 96 fungsi unik & grant | Tidak ada definer uang/PIN yang terbuka; 5 RPC `anon` memang dijanjikan (`harga_berlaku`, `menu_habis`, `daftarkan_perangkat_dengan_kode`, `catat_percobaan_masuk`, `periksa_kunci_masuk`); `using (true)` hanya 1 di `0005_izin_berjenjang.sql:278` (katalog izin) — bukan cacat |
| 6 | Fungsi `picu_*` tanpa `revoke` (calon temuan L1) | klasifikasi 64 fungsi `picu_*` | Semuanya `returns trigger` → tidak dapat dipanggil lewat API; **bukan temuan** |
| 7 | Sapuan RLS semua tabel | `supabase/tes/rls_semua_tabel.sql` (blok 1–6) | LULUS |
| 8 | Matriks izin 6 peran | `periksa-matriks-izin.py` + `matriks_izin_6_peran.sql` | LOLOS |
| 9 | Brute force PIN (5×/akun, 12×/perangkat) & identitas perangkat palsu | `0006_pin.sql:13-14`; `percobaan_pin_perangkat.sql`; `uji-mutasi-0018.py` | LULUS; perangkat tak terdaftar dilayani 0 kali |
| 10 | Lebih bayar & pembayaran dobel | `0039_bayar_pesanan.sql:126-130`; uji `uang_kunci.sql`/`uang_peladen.sql`; `uji-mutasi-0039.py` | Ditolak `BY-301`; kunci baris + idempotensi; bukan temuan |
| 11 | Ubah/hapus jejak audit | `0029_audit_kekal_rantai.sql` + `verifikasi_rantai_audit()` + `uji-mutasi-0029.py` | Ditolak; rantai putus terdeteksi; bukan temuan |
| 12 | Ubah migrasi yang sudah dibekukan | `periksa-migrasi-beku.py` | LOLOS |
| 13 | Rahasia masuk repo | `periksa-rahasia.py` (di klon Git) | LOLOS |
| 14 | Pohon kerja kotor / artefak liar | `periksa-bersih.py`, `periksa-gerbang-ci.py` | LOLOS |
| 15 | Rujukan dokumen mati (sapuan otomatis) | `periksa-rujukan.py`, `periksa-panduan.py`, `periksa-buku-uji.py`, sapuan 819 rujukan ber-backtick | LOLOS (1 rujukan `(rencana)`); manual: F-07, F-08 |
| 16 | Peta UI & tombol liar | `peta-ui.py --periksa` (+`--uji-diri`) | LOLOS — **tetapi** gerbang ini tidak memeriksa sambungan nyata → F-03 |
| 17 | Perilaku UI (kontras, arah, bahasa, kerapatan, struktur, komponen/env) | 8 pemeriksa UI | Semua LOLOS di klon Git; 2 di antaranya GAGAL di pohon tanpa `.git` → F-09 |
| 18 | Rantai CI lokal (format → lint → tipe → uji → mutasi → build → audit) | replika langkah CI di klon SHA target | Semua hijau; hanya `npm run cek:supabase` merah karena `.env` kosong (rahasia hanya ada di CI) → §6 |
| 19 | Berkas hilang yang dirujuk klaim `[x]` | loop 31 jalur §1b + cek keberadaan tiap `File:` | 4 cacat penuh, sisanya rujukan basi bercatatan → F-01…F-04, F-08 |
| 20 | Alur nyata kasir (buka kas → simpan → bayar) di kode | baca `App.tsx` + `aksi.ts` + `useBayar.ts` | Shift & simpan pesanan = sukses palsu di klien → F-03 |

## 4. Temuan

### [F-01] Kontrol wajib TOTP tidak ada, tetapi ROADMAP dan laporan status menyatakannya selesai

- **Tingkat:** K-2
- **Artefak:** `docs/ROADMAP.md:776`; `docs/KEAMANAN.md:22,100-102`; `docs/PRD.md:276`; `docs/TECH_SPEC.md:392`; `STATUS.md:11`; `PROJECT_STATE.md:61`; `aplikasi/src/layar/masuk/MasukPengelola.tsx:12-15,48-68`; `docs/uji/AUDIT_RIWAYAT.md:146`
- **Klaim yang dilanggar:** PRD Aturan Bisnis 16 & M12 ("TOTP wajib untuk Pemilik Platform, Owner Pusat, Admin Cabang"); KEAMANAN §2/§5; TECH_SPEC §5.1 + uji wajib "admin tanpa TOTP tidak bisa masuk"; DoD ROADMAP T2-13
- **Bukti:** `ls supabase/functions` → hanya `.gitkeep` + `verifikasi_pin/` (tak ada `atur_ulang_mfa/`) · `find . -name 'Totp.tsx' -o -name 'mfa.sql' -o -name 'periksa-fungsi-mfa.py'` → **kosong** · `grep -rn 'butuhTotp' aplikasi/src` → hanya `MasukPengelola.tsx:15` (tipe) & `:68` (pemakaian); **tidak ada pemasok nyata** — nilai itu hanya datang dari mock uji · `grep -rn '<MasukPengelola' aplikasi/src` → hanya berkas uji (layar tak pernah dirender di `App.tsx`) · `grep -n 'TOTP' docs/uji/AUDIT_RIWAYAT.md:146` → baris F F-08 "DITUTUP sebagian · TERBUKA sisa … MFA/TOTP (`T2-13` `[ ]`)" — dokumen repo sendiri masih menyatakan terbuka, sementara kotak `[x]` di ROADMAP berkata selesai
- **Skenario gagal:** admin cabang/owner/pemilik platform masuk memakai kata sandi saja → aturan dua faktor untuk peran yang menyentuh uang & pegawai tidak ditegakkan; laporan "100% selesai" membuat pemilik percaya kontrol ini aktif. (Layar pengelolanya pun tidak dirender, jadi jalur masuk ber-pasangan pun tidak tersedia.)
- **Dugaan penyebab:** UI tahap-2 (`MasukPengelola`) dibuat lebih dulu, Edge Function/RPC pengaturan ulang MFA + uji SQL tidak pernah dikerjakan; centang `[x]` diberikan atas dasar uji komponen yang memakai tiruan.
- **Cara membuktikan perbaikan:** ada `supabase/functions/atur_ulang_mfa/index.ts` + `supabase/tes/mfa.sql`; `grep -rn 'butuhTotp: true' aplikasi/src` pada jalur nyata (bukan mock); uji SQL "admin tanpa TOTP ditolak" MERAH saat penjaga dilepas; `python3 alat/periksa-fungsi-mfa.py` ada & hijau.
- **Status verifikasi:** TERVERIFIKASI

### [F-02] Jalur masuk pelanggan (Google utama + tautan email) tidak tersambung, layar tidak dirender

- **Tingkat:** K-2
- **Artefak:** `docs/ROADMAP.md:688`; `aplikasi/src/layar/masuk/LayarMasukPelanggan.tsx:8-14,26-38`; `docs/PRD.md` M12; `docs/TECH_SPEC.md:305,389`
- **Klaim yang dilanggar:** PRD M12 ("pelanggan = Google (utama) / email terverifikasi"); DoD T2-04 "Daftar dengan Google bekerja; jalur email mengirim verifikasi; uji manual dua jalur lulus"
- **Bukti:** `grep -rn 'signInWithOAuth\|challengeAndVerify' aplikasi/src` → **kosong** · `grep -rn 'google' aplikasi/src` → hanya `public/logo-google.svg` di `LayarMasukPelanggan.tsx:103-105` · `ls aplikasi/src/lib/google.ts` → tidak ada · `grep -rn '<LayarMasukPelanggan' aplikasi/src` → hanya di `LayarMasukPelanggan.test.tsx` (layar tidak pernah dirender) · "Bukti 2026-09-22" T2-04 = uji komponen yang memakai `vi.fn()` (`LayarMasukPelanggan.test.tsx:25-43`)
- **Skenario gagal:** pelanggan menekan "Masuk Google" → `await onMasukGoogle?.()` tidak melakukan apa pun (prop opsional tak pernah diisi aplikasi) lalu layar berpikir sukses tanpa sesi; jalur email juga tak punya pengirim. Janji pelanggan inti M12 tidak ada; "Bukti" hijau hanya karena mock.
- **Dugaan penyebab:** pola sama dengan F-01 — komponen jadi lebih dulu, sambungan (Supabase OAuth / kanal email) tidak pernah dibuat; uji komponen bertiruan dihitung sebagai bukti.
- **Cara membuktikan perbaikan:** ada pemanggilan OAuth nyata pada jalur produksi + uji integrasi yang MERAH bila `onMasukGoogle` tidak memanggil `klien.auth.signInWithOAuth`; `grep -rn '<LayarMasukPelanggan' aplikasi/src/App.tsx` menemukan pemakaian.
- **Status verifikasi:** TERVERIFIKASI

### [F-03] Tombol uang mengembalikan "sukses" tanpa menyentuh peladen (buka/tutup shift & simpan pesanan)

- **Tingkat:** K-2
- **Artefak:** `aplikasi/src/App.tsx:44,59-66,74-101`; `aplikasi/src/lib/aksi.ts:191-223`; `aplikasi/src/layar/kasir/BukaKas.tsx:6`; `supabase/migrations/0045_buka_shift.sql:22-72`
- **Klaim yang dilanggar:** registri aksi `kasir.buka_shift`/`kasir.tutup_shift` menyatakan `rpc: 'buka_shift'` + `audit: true`; komentar `BukaKas.tsx:6` "Peladen memproses buka shift lewat `onBukaShift` (RPC `buka_shift`)"; DoD T1-11 (kas & shift, ART-6) dan prinsip ART-3 (angka uang dari peladen)
- **Bukti:** `sed -n '74,101p' aplikasi/src/App.tsx` → `id: \`shift-${Date.now()}\``, `uangSeharusnya`/`selisih` dihitung di klien, lalu `return { sukses: true }` · `sed -n '59,66p' aplikasi/src/App.tsx` → `onSimpanPesanan` mengembalikan `{ sukses: true, pesananId: … ?? 'ord-new' }` · `sed -n '44p'` → pembayaran memakai id literal `'ord-current'` bila belum ada pesanan · `grep -rn "rpc('buka_shift')\|rpc('tutup_shift')" aplikasi/src` → **kosong** (hanya terjemahan/label) · RPC nyata memang ada & teruji: `0045_buka_shift.sql` + uji `tutup_shift.sql`/`wajib_shift.sql` LULUS · `python3 alat/peta-ui.py --periksa` → LOLOS karena gerbang hanya mencocokkan nama RPC di registri dengan migrasi, bukan sambungan nyata
- **Skenario gagal:** kasir menekan "Tutup Kas" → kartu sukses + angka selisih muncul, padahal **tidak ada** baris `shift_kas`/`kas_pergerakan` dan tidak ada jejak audit; pemilik tidak bisa mengaudit uang shift. "Simpan pesanan" melaporkan sukses untuk pesanan yang tidak tersimpan (`ord-new`). Kasir tidak punya cara membedakan "tersambung" dari "melapor sukses".
- **Dugaan penyebab:** integrasi Fase 5 belum dikerjakan (T1-11 & T3-05 memang masih `[ ]`), tetapi layar/registri tidak menandai "belum tersambung" sehingga sukses palsu tampak sama dengan sukses nyata.
- **Cara membuktikan perbaikan:** `App.tsx` memanggil `klien.rpc('buka_shift')`/`('tutup_shift')`/`('simpan_pesanan')`; uji komponen yang MERAH bila perekam RPC harness tidak dipanggil; uji SQL membuktikan baris `shift_kas` bertambah; sampai itu, handler tidak boleh mengembalikan `sukses: true`.
- **Status verifikasi:** TERVERIFIKASI

### [F-04] Uji E2E kasir & uji beban diklaim selesai, tidak ada wujudnya

- **Tingkat:** K-3
- **Artefak:** `docs/ROADMAP.md:967,985`; `STATUS.md:11`; `PROJECT_STATE.md:61`
- **Klaim yang dilanggar:** DoD T3-14 ("uji otomatis … lulus di CI"), DoD T3-16 ("< 1 detik per aksi utama; tidak ada kebocoran memori"), banner status "Uji E2E Kasir & Beban Ringan … selesai dikerjakan dan diverifikasi penuh"
- **Bukti:** `ls aplikasi/` → tidak ada `uji/` · `git ls-tree -r --name-only e7818e2 \| grep -c 'uji/e2e\|uji/beban'` → **0** · `python3 -c "import json;d=json.load(open('aplikasi/package.json'));print([k for k in d['devDependencies'] if k in ('playwright','@playwright/test','cypress','k6')])"` → `[]` · `grep -n 'Bukti' (blok T3-14/T3-16)` → tidak ada baris Bukti sama sekali
- **Skenario gagal:** alur kasir ujung-ke-ujung dan ambang performa tidak pernah dijalankan mesin; regresi jalur utama (mis. F-03) tidak tertangkap, sementara pemilik diberi tahu keduanya "diverifikasi penuh".
- **Dugaan penyebab:** centang dipakai sebagai penanda niat/ruang lingkup, bukan hasil; tidak ada gerbang yang menolak `[x]` tanpa bukti yang bisa dijalankan.
- **Cara membuktikan perbaikan:** ada harness E2E yang dijalankan CI dan lulus, atau centang dicabut + banner status dikoreksi.
- **Status verifikasi:** TERVERIFIKASI

### [F-05] Angka & klaim "100% selesai" di berkas pengguna sudah basi dan menyesatkan

- **Tingkat:** K-3
- **Artefak:** `STATUS.md:11`; `PROJECT_STATE.md:61`; `.github/workflows/ci.yml`
- **Klaim yang dilanggar:** "Seluruh 100 gerbang CI otomatis, 72 berkas uji SQL lokal (100% LULUS), dan 45 berkas uji Vitest (213 unit test) lolos 100%"; PROTOKOL §2b.5 "buku pedoman yang basi = temuan"
- **Bukti:** `npx vitest run` → `Test Files 84 passed (84)` / `Tests 671 passed (671)` · `ls supabase/tes/*.sql \| wc -l` → **98** (bukan 72) · `node alat/uji-sql.mjs` → 98 LULUS/0 GAGAL · `grep -c '^\s*- name:' .github/workflows/ci.yml` → 57 langkah (70 baris perintah) — tidak ada berkas yang mendefinisikan "100 gerbang" · `gh api …/commits/e7818e2…/check-runs` → conclusion **failure** (run 36009322838) sementara banner menyatakan semua gerbang lolos
- **Skenario gagal:** Lee menjadikan banner sebagai bukti utama (memang begitu niatnya) → ia menerima laporan "100%" padahal CI commit itu merah dan angka-angkanya tidak cocok dengan mesin; kepercayaan pada mekanisme bukti rusak.
- **Dugaan penyebab:** angka ditulis tangan pada 2026-09-22 dan tidak diperbarui mesin; `alat/periksa-angka-bukti.py` ada tetapi tidak menjaga dua banner ini.
- **Cara membuktikan perbaikan:** banner menyebut angka yang dihasilkan runner (98/84/671) dan status CI commit itu apa adanya; `periksa-angka-bukti.py` diperluas ke `STATUS.md`/`PROJECT_STATE.md` sehingga GAGAL bila angka tidak cocok.
- **Status verifikasi:** TERVERIFIKASI

### [F-06] Angka klaim yang tidak cocok dengan kode (registri aksi & kamus bahasa)

- **Tingkat:** K-3
- **Artefak:** `docs/ROADMAP.md:524,562`; `docs/PETA_UI.md:11-12`; `aplikasi/src/lib/aksi.ts`; `aplikasi/src/bahasa/*.ts`; paket §2 baris T1-32/T1-38/T1-39
- **Klaim yang dilanggar:** "memuat 32 registri aksi" (T1-32), "11 layar & 38 aksi" (T1-39), "100% paritas 102 kunci" (T1-38)
- **Bukti:** `grep -cE "^  '[^']+': \{" aplikasi/src/lib/aksi.ts` → **39** · ekstraktor `alat/peta-ui.py` (`ekstrak_objek_ts`) → **39** aksi & 11 layar · `docs/PETA_UI.md:12` → "**39** aksi" (dokumen kanonik sudah benar; klaim paket yang tertinggal) · `python3 aplikasi/alat/periksa-bahasa.py` → "Kamus acuan (id.ts) memiliki **269** kunci… en/zh/ar 100% paritas (269 kunci cocok)"
- **Skenario gagal:** angka ini dipakai pembaca & penilai untuk mengukur cakupan UI/bahasa; 32/38/102 membuat pekerjaan tampak jauh lebih kecil/berbeda dari kenyataan, dan menghidupkan kembali "fungsi yang katanya ada" sebagai soal hitungan.
- **Dugaan penyebab:** klaim disalin tangan dari keadaan lama; sebagian angka (39 aksi) memang dihasilkan mesin di `PETA_UI.md`, tetapi klaim ROADMAP/paket tidak ikut diperbarui.
- **Cara membuktikan perbaikan:** klaim menyebut angka yang dihasilkan `peta-ui.py`/`periksa-bahasa.py` hari itu; uji/gate yang GAGAL bila angka di klaim ≠ angka keluaran pemeriksa.
- **Status verifikasi:** TERVERIFIKASI

### [F-07] Buku pedoman pengguna: urutan bagian & penomoran cacat

- **Tingkat:** K-3
- **Artefak:** `PANDUAN_PENGGUNA.md:533,547,713,739,766,768`
- **Klaim yang dilanggar:** PROTOKOL §2b.5 (langkah pengguna yang tak bisa dijalankan apa adanya / buku pedoman basi = temuan); kelengkapan & kerapian buku induk yang dijaga `periksa-panduan.py`
- **Bukti:** `grep -nE '^### ' PANDUAN_PENGGUNA.md` → `### C7` di baris **533** mendahului `### C6` di baris **547**; `### H1. Peta berkas penting` (713) dan `### H1. Cara pakai sebagai template` (739) = **dua H1**; `### H3. Kebiasaan yang perlu dijaga` (766) kosong & `### H4. Kebiasaan yang perlu dijaga` (768) berjudul sama. `periksa-panduan.py` tetap LOLOS karena memeriksa keberadaan bagian & identitas blok prompt, bukan urutan/keunikan nomor.
- **Skenario gagal:** Lee merujuk "Bagian C6" atau mengikuti daftar isi → menemukan nomor tidak berurutan dan judul kembar; saat menautkan bagian, dua kandidat "H1/H4" membuat rujukan ambigu.
- **Dugaan penyebab:** penyuntingan/penyisipan bagian tanpa aturan urutan yang ditegakkan mesin.
- **Cara membuktikan perbaikan:** `periksa-panduan.py` menambah aturan "nomor sub-bagian unik & berurutan" (GAGAL saat ini) lalu hijau setelah C6↔C7 ditukar, H1 ganda disatukan, H3 kosong diisi/dihapus.
- **Status verifikasi:** TERVERIFIKASI

### [F-08] 31 jalur `File:` ROADMAP untuk tugas berstatus selesai tidak ada di commit ini

- **Tingkat:** K-3
- **Artefak:** `docs/ROADMAP.md` (blok tugas T1-27, T1-28, T2-03/04/05/09/10/12/13/14/15/19, T3-02/03/06/07/08/10/14/16, T6-01/02/03); paket §1b
- **Klaim yang dilanggar:** PROTOKOL §2b.5 (rujukan salah = temuan); §1b paket sendiri menandai 31 jalur ini "laporkan!"
- **Bukti:** loop keberadaan tiap jalur → 31/31 **TIDAK ADA**. Disposisi saya setelah memeriksa konteks tiap tugas: (a) **cacat penuh** → T2-13, T2-04, T3-14, T3-16 (F-01…F-04); (b) **bernama baru + bercatatan resmi, bukan cacat** → T1-27 (`0015_audit.sql` → `0029_audit_kekal_rantai.sql`, "Catatan penomoran" di blok), T1-28 (`0016_…` → `0031_mode_dukungan_platform.sql`, tertulis di baris `File:`), T6-01 (`lib/printer-escpos.ts` → `aplikasi/src/lib/printer/expos.ts`, "Rujukan berkas diselaraskan"); (c) **rujukan basi** (berkas pengganti hidup disebut di baris `Bukti` yang sama) → T2-03 (`undang_pegawai/index.ts` → `KelolaPegawai.tsx`), T2-05, T2-09 (`useKunciSesi.ts` → `useKunciOtomatis.ts`), T2-10, T2-12, T2-14, T2-15, T2-19, T3-02 (`lib/uang.ts`; DoD juga dicatat jujur sebagai belum ditepati di blok T3-02 + `T-027` di `docs/TERTANGGUH.md`), T3-03, T3-06, T3-07, T3-08, T3-10, T6-02, T6-03. Perintah: `for p in <31 jalur>; do [ -e "$p" ] || echo "TIDAK ADA: $p"; done` → 31 baris; konteks: `grep -n 'File:\|Catatan penomoran\|Rujukan berkas diselaraskan' docs/ROADMAP.md`.
- **Skenario gagal:** agen/pemilik berikutnya mengikuti jalur `File:` untuk melanjutkan pekerjaan dan menemukan berkas yang tidak ada — persis kelas cacat yang dikejar §1b paket; biaya paling besar ada pada empat tugas tanpa berkas pengganti (F-01…F-04).
- **Dugaan penyebab:** `File:` diisi saat perencanaan lalu tidak disinkronkan lagi; `alat/periksa-roadmap.py` (LOLOS) tidak memeriksa keberadaan jalur `File:`.
- **Cara membuktikan perbaikan:** `periksa-roadmap.py` menambah aturan "jalur di `File:` wajib ada, kecuali ditandai `(rencana)`" → GAGAL 27 kali sebelum penyelarasan, hijau sesudahnya.
- **Status verifikasi:** TERVERIFIKASI

### [F-09] Pemeriksa memberi verdict GAGAL ketika dijalankan di pohon tanpa `.git`

- **Tingkat:** K-4
- **Artefak:** `aplikasi/alat/periksa-komponen-env.py`
- **Klaim yang dilanggar:** pemeriksa seharusnya menilai isi repo, bukan bentuk checkout (bandingkan `aplikasi/alat/periksa-struktur.py` yang dengan jujur mencetak "bukan repositori Git: pemeriksaan … dilewati")
- **Bukti:** di pohon tanpa `.git` (hasil `git archive`): `python3 aplikasi/alat/periksa-komponen-env.py` → `komponen & rahasia: 8 OK · 2 GAGAL` — "aplikasi/.env TIDAK diabaikan Git (rahasia bisa ikut ke repo)" & "aplikasi/.env.example justru diabaikan Git"; di klon Git SHA yang sama: `10 OK · 0 GAGAL`; isi `.gitignore:9-11` memang `.env`, `.env.*`, `!.env.example` (`aplikasi/.gitignore:4-6` sama)
- **Skenario gagal:** siapa pun menjalankan pemeriksa dari salinan arsip/tanpa Git melihat "GAGAL" palsu tepat di area rahasia — memicu kepanikan atau, lebih buruk, kebiasaan mengabaikan warna merah. (`periksa-bersih.py` juga gagal-tertutup di pohon tanpa Git, tetapi dengan pesan jelas — itu perilaku benar, tidak saya hitung cacat.)
- **Dugaan penyebab:** jalur `.git`/`git check-ignore` diasumsikan selalu ada; tidak ada cabang "bukan repo" seperti di `periksa-struktur.py`.
- **Cara membuktikan perbaikan:** `periksa-komponen-env.py` mendeteksi "bukan repo Git" lalu memeriksa pola `.gitignore` sebagai teks (atau mencetak "dilewati"), sehingga hasilnya sama hijau di pohon arsip maupun klon Git.
- **Status verifikasi:** TERVERIFIKASI

## 5. Kalibrasi cacat tanaman

Ditemukan: 11 dari 11

Saya tidak diberi tahu jumlah/kelas cacat; kunci di luar repo tidak dicari (tidak ada upaya). Cacat yang saya yakini ada, semuanya dibuktikan terhadap kode/dokumen nyata di pohon target:

1. `01_gerbang_izin.sql` — **K-2** · hak `EXECUTE` default ke PUBLIC tidak dicabut: baris 17 hanya `grant execute … to authenticated`, padahal komentar baris 20 sendiri menuntut "hak execute dicabut dari public". Kode nyata: `supabase/migrations/0005_izin_berjenjang.sql:300-306` (`revoke all on function public.boleh(text, uuid) from public;` baru `grant … to authenticated, service_role`). Rujukan aturan: PostgreSQL "by default, execute privilege is granted to PUBLIC for newly created functions" (<https://www.postgresql.org/docs/current/sql-createfunction.html>) dan Supabase RLS "revoke execute on function … from public" (<https://supabase.com/docs/guides/database/postgres/row-level-security>). Akibat: peran tanpa login (`anon`) dapat memanggil fungsi **SECURITY DEFINER** ini.
2. `01_gerbang_izin.sql` — **K-2** · `set search_path = public` (baris 7) tanpa `pg_temp`, melanggar standar yang repo ini tegakkan sendiri: `alat/periksa-keamanan-sql.py` (mode `T130-path`) menandai `set search_path=public,pg_temp` sebagai penyimpangan dan kode nyata memakai `set search_path = public, pg_temp` (`0005:243`). Risiko pembayangan objek oleh skema sementara: <https://www.cybertec-postgresql.com/en/abusing-security-definer-functions/>.
3. `02_policy_pengaturan.sql` — **K-1** · policy SELECT `using (penyewa_id is not null)` (baris 4-6) membuat **setiap** pengguna `authenticated` membaca `pengaturan` semua penyewa. Kode nyata: `0004_pola_rls.sql:83-85` dan `0027_initplan_policy_rls.sql:248-250` memakai `penyewa_id = public.penyewa_saya()`.
4. `03_fungsi_terima_bayar.sql` — **K-1** · penjaga lebih bayar di sisi yang salah: baris 15 memeriksa `v_sebelum > v_pesanan.total` (kondisi LAMA) alih-alih `v_sebelum + p_jumlah > total`, sehingga pembayaran melebihi total diterima dan langsung ditandai `lunas` (baris 22). Kode nyata: `0039_bayar_pesanan.sql:126-130` (`BY-301` atas `total_dibayar` + nominal baru). Jalur yang sama juga tidak memvalidasi `p_jumlah > 0`. Bahan 04 baris 14 mengklaim sebaliknya ("selisih lebih bayar ditolak sistem") → dokumen dan kode saling bertentangan.
5. `03_fungsi_terima_bayar.sql` — **K-2** · fungsi **SECURITY DEFINER** jalur uang tanpa pemeriksaan wewenang/penyewa di dalam badan (tidak ada `boleh(…)`/cek penyewa pesanan), lalu `grant execute … to authenticated` (baris 29) → siapa pun yang login dapat mencatat pembayaran ke pesanan mana pun (UUID). Kode nyata: gerbang peran + penyewa + metode + `for update` + jejak audit.
6. `04_panduan_singkat.md` — **K-2** · langkah `bash aplikasi/pratinjau.sh` (baris 4): berkas **tidak ada** (`aplikasi/pratinjau.sh` → TIDAK ADA; yang ada `aplikasi/alat/pratinjau.sh`). Langkah tak bisa dijalankan apa adanya.
7. `04_panduan_singkat.md` — **K-2** · `python3 alat/periksa-struktur.py` (baris 6): jalur salah (`alat/periksa-struktur.py` → TIDAK ADA; nyatanya `aplikasi/alat/periksa-struktur.py`).
8. `04_panduan_singkat.md` — **K-2** · rujukan `docs/PANDUAN_KEAMANAN.md` §4 (baris 11): berkas **tidak ada** (`docs/KEAMANAN.md` yang ada) → jalan buntu bagi pembaca.
9. `04_panduan_singkat.md` — **K-3** · "Salah PIN **10 kali** berturut-turut → akun terkunci 15 menit" (baris 10) berbeda dari aturan nyata **5** gagal/akun & **12** gagal/perangkat per 15 menit (`0006_pin.sql:13-14,384`; `KEAMANAN.md:40`).
10. `05_pemeriksa_ambang.py` — **K-2** · pemeriksa yang tidak dapat MERAH: `AKAR` (baris 7) menunjuk `docs/uji/kalibrasi/`, sehingga `AKAR/"aplikasi"/"src"/"layar"` tidak ada → `layar = []` → `SKIP … return 0` (baris 19-21). Dijalankan apa adanya: `SKIP: layar baru 0 — di bawah ambang 5` dengan exit 0. Diperkuat ambang `MIN_LAYAR_DIPERIKSA = 5` padahal komentarnya menyebut ambang nyata **20** (baris 8).
11. `05_pemeriksa_ambang.py` — **K-3** · `glob("*.tsx")` hanya satu tingkat (baris 12): layar nyata berada di subfolder (`aplikasi/src/layar/kasir/`, `dapur/`, …), jadi walau akarnya diperbaiki pemeriksa ini akan melewatkan hampir semua layar; uji keadaan juga hanya pencocokan substring longgar.

Temuan palsu: **0**. Saya hanya mencatat cacat yang bisa saya buktikan pada bahan/kode nyata; dugaan yang tidak terbukti tidak saya masukkan (mis. saya **tidak** menyebut `stable` pada 01 atau ketiadaan policy INSERT/DELETE pada 02 sebagai cacat karena kode nyata juga begitu).

## 6. Yang tidak bisa saya verifikasi

- **Penyebab CI merah pada commit target** (run `36009322838`, check `107665621957`): log tidak dapat diunduh (dua kali `EOF`), ringkasan check-run `output` null. Seluruh perintah pada langkah yang dilaporkan gagal ("Pemeriksa fondasi, roadmap, struktur, komponen, uji & kontras", `ci.yml:242-306`) saya jalankan ulang di klon Git pada SHA yang sama — dengan dan tanpa `dist/` hasil build — dan **semuanya hijau**; saya juga mengulang delapan pemeriksa inti dengan `HOME` kosong → hijau. Jadi saya **tidak** dapat menyimpulkan penyebabnya; saya juga tidak dapat mengesampingkan perbedaan lingkungan runner (versi Python/OS) atau efek samping langkah-langkah sebelumnya yang tidak terlihat dari log.
- **Perilaku layanan nyata Supabase/Cloudflare**: sebar skema, halaman publik `*.workers.dev`, `npm run cek:supabase` (butuh `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`), dan `catat-alamat.mjs` mode hidup — tidak ada kredensial di sesi saya. `node aplikasi/alat/catat-alamat.mjs --uji-diri` LOLOS 6/6, tetapi itu uji mandiri mesin, bukan bukti layanan nyata.
- **Uji perangkat nyata**: printer Bluetooth/USB 58 & 80 mm, tablet Android, aplikasi authenticator TOTP (tidak ada perangkat), dan uji manual "dua jalur" pelanggan (F-02) — hanya bisa dibuktikan di perangkat/akun nyata.
- **`uji-konkuren`** (butuh `python3 -m pip install pgserver "psycopg[binary]"`) tidak saya jalankan; CI yang menjalankannya.
- **Angka "100 gerbang CI"** tidak dapat saya lumpuhkan/verifikasi: tidak ada berkas yang mendefinisikan istilah "gerbang" secara terhitung (yang ada: 57 langkah, 70 baris perintah).
- **Model berbeda** untuk audit tidak dapat dipilih pada sesi ini (PROTOKOL §3.2 → keterbatasan yang dicatat, bukan diklaim lebih kuat).

## 7. Pernyataan tidak mengubah apa pun

Saya hanya-baca. Saya **tidak mengubah apa pun** pada pohon commit yang diaudit: SATU-SATUNYA berkas yang saya buat adalah laporan ini; tidak ada berkas proyek lain yang saya ubah.
Bukti: `git status --short` di `/home/user/Resto-Barokah` menampilkan **tidak ada perubahan berkas terlacak** (keluaran kosong); draf laporan hidup di `.laporan-lokal/` yang diabaikan Git (`git check-ignore -v .laporan-lokal/…` → `.gitignore:33:.laporan-lokal/`) dan dikirim lewat `python3 alat/kirim-laporan.py --jenis audit --sumber arena/01a0d09b-resto-barokah` (commit lewat index sementara + push ke cabang auditor sendiri, tanpa menyentuh pohon kerja). Semua data audit dibuat di salinan `/tmp` (`audit-target-e7818e2`, `audit-work`, `audit-clone`), bukan di pohon repo.

## 8. Temuan di luar cakupan (WAJIB — boleh "tidak ada")

| # | Temuan | Mengapa di luar cakupan | Bukti | Syarat dilanjutkan ke audit lain |
|---|---|---|---|---|
| — | tidak ada | — | — | — |