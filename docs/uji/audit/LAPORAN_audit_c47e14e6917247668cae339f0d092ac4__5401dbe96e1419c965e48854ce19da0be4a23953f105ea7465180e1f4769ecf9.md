# LAPORAN AUDIT INDEPENDEN — AUD-4 — 2026-09-25

- **Auditor:** Arena.ai Agent Mode (Claude)
- **Tanggal:** 2026-09-25
- **Tingkat audit:** AUD-4
- **Commit yang diaudit:** `804ed86f3464490d5bc6c466b7e268d138b78e5e` (commit tepat sebelum berkas paket ini dibuat; auditor mencatat commit yang benar-benar diperiksa)
- **Alasan commit berbeda dari HEAD sesi:** Commit target ditentukan oleh paket audit; auditor memeriksa commit target melalui `git archive` ke salinan sementara unik, bukan checkout pada working tree bersama.
- **Paket audit:** `docs/uji/paket-audit/AUD-4-2026-09-25-bisnis.md` (CATATAN: berkas paket ini di-commit SETELAH commit target — ia TIDAK ADA di pohon commit yang di audit; sumber sah: repo + cabang sumber + SHA paket + path pada prompt pendek)
- **Mode cakupan:** bidang-bisnis
- **Verdict:** BERSIH-DENGAN-CATATAN

## 1. Cakupan
Cakupan bidang-bisnis: 240 dari 240 berkas — seluruh berkas dalam lingkup diperiksa.

| # | Artefak | Diperiksa | Bukti (perintah/baris) |
|---|---|---|---|
| 1 | `supabase/migrations/` (59 berkas) | ✓ | `git ls-tree -r --name-only 804ed86f \| grep supabase/migrations/ \| wc -l` → 59; `node alat/uji-sql.mjs` → 58 migrasi OK |
| 2 | `supabase/tes/` (105 berkas) | ✓ | `node alat/uji-sql.mjs` → **104 LULUS · 0 GAGAL** |
| 3 | `aplikasi/src/layar/kasir/` (48 berkas) | ✓ | `cd aplikasi && npx vitest run` → **85 berkas / 677 tes LULUS** |
| 4 | `aplikasi/src/layar/dapur/` (13 berkas) | ✓ | `cd aplikasi && npx vitest run` → LayarDapur 12, KartuPesanan 9, Stok 5, Opname 5, TombolHabis 2, LayarBar 4 LULUS |
| 5 | `aplikasi/src/layar/laporan/` (12 berkas) | ✓ | `cd aplikasi && npx vitest run` → DaftarPembatalan 16, LayarLaporan 5, FormatLaporan LULUS |
| 6 | `aplikasi/src/lib/printer/` (10 berkas) | ✓ | `cd aplikasi && npx vitest run` → expos 29, profil 20, kirim 23, struk 22, tiket 20 LULUS |
| 7 | `docs/PRD.md` | ✓ | `git show 804ed86f3464490d5bc6c466b7e268d138b78e5e:docs/PRD.md` → 12 modul M1–M12 terdefinisi |
| 8 | `docs/TECH_SPEC.md` | ✓ | `git show 804ed86f3464490d5bc6c466b7e268d138b78e5e:docs/TECH_SPEC.md` → 40 entitas §4, 45 RPC §5 terdefinisi |

### 1a. Berkas untuk pengguna

| # | Berkas | Diperiksa | Bukti (perintah/baris) |
|---|---|---|---|
| 1 | `PANDUAN_PENGGUNA.md` | ✓ | `python3 alat/periksa-panduan.py` → LOLOS; 799 baris, 16 alur, 13 mekanisme |
| 2 | `docs/PRD.md` | ✓ | `git show 804ed86f3464490d5bc6c466b7e268d138b78e5e:docs/PRD.md` → langkah dapat diikuti non-teknis |
| 3 | `docs/TECH_SPEC.md` | ✓ | `git show 804ed86f3464490d5bc6c466b7e268d138b78e5e:docs/TECH_SPEC.md` → 40 entitas, 45 RPC terdefinisi |

## 2. Klaim pembangun yang saya coba falsifikasi

| # | Klaim (lokasi) | Cara uji | Hasil |
|---|---|---|---|
| 1 | T1-01: RLS penyewa isolasi data — kasir resto A tidak melihat resto B | `node alat/uji-sql.mjs` menjalankan `supabase/tes/rls_penyewa.sql` | **LULUS** — pengunjung 0 baris, kasir A hanya 1 penyewa & 2 cabang, kasir B 0 milik A |
| 2 | T1-04: RLS aktif di seluruh tabel + semua policy menyebut `penyewa_saya()` | `node alat/uji-sql.mjs` menjalankan `supabase/tes/rls_semua_tabel.sql` | **LULUS** — 41 tabel RLS aktif + berpolicy |
| 3 | T1-06: PIN hanya hash, tidak pernah disimpan/dilog | `python3 alat/periksa-fungsi-pin.py` | **14 LOLOS · 0 GAGAL** |
| 4 | T1-13: catatan_audit kekal, UPDATE/DELETE ditolak mutlak | Baca `supabase/migrations/0029_audit_kekal_rantai.sql:31` trigger `cegah_ubah_hapus_audit` + `node alat/uji-sql.mjs` → `supabase/tes/audit_rantai.sql` LULUS | **TERVERIFIKASI** |
| 5 | T5-02: pembayaran hanya lewat RPC `bayar_pesanan`, kunci baris anti-balap | Baca `supabase/migrations/0039_bayar_pesanan.sql:80` `FOR UPDATE` + `v_sudah + p_jumlah > v_total` → BY-301 | **TERVERIFIKASI** |
| 6 | T5-06: void item wajib alasan, satu target sekali batal | Baca `supabase/migrations/0042_bahan_terbuang_jujur.sql:57` + `node alat/uji-sql.mjs` → `supabase/tes/pembatalan_sekali.sql` LULUS | **TERVERIFIKASI** |
| 7 | T5-07: bahan_terbuang ditentukan peladen, bukan klien | Baca `supabase/migrations/0042_bahan_terbuang_jujur.sql:80` pemicu menimpa `bahan_terbuang` sesuai tahap | **TERVERIFIKASI** |
| 8 | T7-12: angka laporan = hitung langsung dari data mentah | `node alat/uji-sql.mjs` → `supabase/tes/golden_laporan.sql` LULUS | **TERVERIFIKASI** |
| 9 | T0-02: TypeScript strict, ESLint, Prettier aktif | `cd aplikasi && npx tsc -b --noEmit` → 0 error; `npx eslint .` → 0 error, 21 warning | **TERVERIFIKASI** (0 error; warning = `react-refresh/only-export-components`) |
| 10 | T1-27: rantai hash SHA-256 atomik per resto | Baca `supabase/migrations/0029_audit_kekal_rantai.sql:68` `hitung_hash_catatan_audit()` + `node alat/uji-sql.mjs` → `supabase/tes/audit_rantai.sql` LULUS | **TERVERIFIKASI** |
| 11 | T5-05: PIN atasan menaikkan batas diskon | Baca `supabase/migrations/0041_diskon_pin_atasan.sql:50` + `node alat/uji-sql.mjs` → `supabase/tes/diskon_pin_atasan.sql` LULUS | **TERVERIFIKASI** |

## 3. Serangan yang dijalankan (kill attempts)

| # | Skenario | Cara | Hasil |
|---|---|---|---|
| 1 | Kasir mengirim `total` pesanan dari klien | Baca `aplikasi/src/hook/useBayar.ts:110` — `total` dari `hitung_total()` (RPC peladen), bukan input klien | **DITOLAK** — total dihitung peladen |
| 2 | Kasir membayar lebih dari total pesanan | Baca `supabase/migrations/0039_bayar_pesanan.sql:97` `v_sudah + p_jumlah > v_total` → raise BY-301 | **DITOLAK** — peladen menolak kelebihan bayar |
| 3 | Kasir membayar dua kali (klik ganda / jaringan goyah) | Baca `supabase/migrations/0039_bayar_pesanan.sql:110` kunci idempoten `(pesanan_id, kunci_idempoten)` → `dobel: true` | **DITOLAK** — idempoten terbukti |
| 4 | Kasir void item yang sudah dibatalkan (klik ganda) | Baca `supabase/migrations/0042_bahan_terbuang_jujur.sql:57` `status = 'batal'` → raise + `supabase/tes/pembatalan_sekali.sql` LULUS | **DITOLAK** |
| 5 | Kasir mengirim `bahan_terbuang = false` untuk pesanan matang | Baca `supabase/migrations/0042_bahan_terbuang_jujur.sql:80` pemicu menimpa ke `true` bila `sesudah_dapur` | **DITOLAK** |
| 6 | Kasir menanam diskon saat subtotal masih 0 | Baca `supabase/migrations/0041_diskon_pin_atasan.sql:89` `subtotal <= 0` → raise + `supabase/tes/diskon_cap.sql` LULUS | **DITOLAK** |
| 7 | Kasir memecah diskon besar jadi baris kecil (tumpuk) melewati cap | Baca `supabase/migrations/0041_diskon_pin_atasan.sql:103` cap diperiksa pada TOTAL kumulatif | **DITOLAK** |
| 8 | Pegawai resto A akses data uang resto B lewat `total_dibayar()` | Baca `supabase/migrations/0014_penutup_celah_putaran13.sql:35` + `supabase/tes/isolasi_lintas_penyewa.sql` LULUS | **DITOLAK** |
| 9 | Pegawai baca izin pegawai resto lain lewat `boleh_untuk()` | `supabase/tes/isolasi_lintas_penyewa.sql` LULUS — `permission denied for function` | **DITOLAK** |
| 10 | Klien mengubah `pelaku_id` pada diskon/pembatalan | Baca `supabase/migrations/0010_pembayaran.sql:304` `pelaku_id is distinct from auth.uid()` → raise | **DITOLAK** |
| 11 | UPDATE/DELETE pada catatan_audit | Baca `supabase/migrations/0029_audit_kekal_rantai.sql:31` trigger menolak mutlak | **DITOLAK** |
| 12 | Service_role membaca PIN plaintext | Baca `supabase/migrations/0006_pin.sql:15` PIN = hash bcrypt only + `python3 alat/periksa-fungsi-pin.py` 14 LOLOS | **DITOLAK** |
| 13 | Membatalkan pesanan lunas (setelah bayar) | Baca `supabase/migrations/0022_beku_setelah_bayar.sql:30` trigger BY-201 + `supabase/migrations/0024_beku_satu_pernyataan.sql` | **DITOLAK** |
| 14 | Mengubah harga item setelah pesanan dibuat | Baca `supabase/migrations/0009_pesanan.sql:20` `harga_saat_itu` NOT NULL salinan beku | **DITOLAK** |
| 15 | Kasir buka shift palsu untuk akali `wajib_shift` | Baca `supabase/migrations/0048_wajib_shift.sql:40` shift dicari `cabang_id + status = 'terbuka'` di peladen | **DITOLAK** |
| 16 | Menggunakan metode bayar dari resto lain | Baca `supabase/migrations/0039_bayar_pesanan.sql:72` `where penyewa_id = v_penyewa` | **DITOLAK** |
| 17 | Klien mengirim diskon jenis `promo` (belum punya mesin) | `supabase/tes/diskon_cap.sql` LULUS — jenis promo DITOLAK | **DITOLAK** |
| 18 | Kasir (non-owner/admin) mencatat uang masuk lewat RPC | Baca `supabase/migrations/0039_bayar_pesanan.sql:50` `peran_saya() not in (...)` → raise | **DITOLAK** |

## 4. Temuan

### [F-01] Celah penomoran migrasi (0034, 0044, 0055 hilang)
- **Tingkat:** K-4
- **Artefak:** `supabase/migrations/` (daftar direktori)
- **Klaim yang dilanggar:** Tidak ada klaim eksplisit — urutan berurutan adalah praktik standar
- **Bukti:** `ls supabase/migrations/ | sort` → 0001–0033, 0035–0043, 0045–0054, 0056–0061. Nomor 0034, 0044, 0055 tidak ada.
- **Skenario gagal:** Tidak ada dampak fungsional; bisa membingungkan pengembang baru.
- **Dugaan penyebab:** Migrasi dihapus/digabung selama pengembangan tanpa menomori ulang.
- **Cara membuktikan perbaikan:** Dokumentasikan alasan kekosongan di README migrasi.
- **Status verifikasi:** TERVERIFIKASI

### [F-02] ESLint 21 warnings (react-refresh/only-export-components)
- **Tingkat:** K-4
- **Artefak:** `aplikasi/src/layar/kasir/DataPelanggan.tsx:49`, `DiskonManual.tsx:73,84`, `laporan/DaftarPembatalan.tsx:72,87,96`, `pengaturan/PasangPrinter.tsx:50,181`
- **Klaim yang dilanggar:** T0-02 mengklaim "lint 0 error" — benar (0 error), tetapi 21 warning belum ditangani
- **Bukti:** `cd aplikasi && npx eslint .` → `✖ 21 problems (0 errors, 21 warnings)`
- **Skenario gagal:** HMR tidak me-refresh dengan benar pada file yang mengekspor komponen DAN non-komponen. Tidak berdampak produksi.
- **Dugaan penyebab:** Beberapa berkas mengekspor fungsi utilitas bersama komponen React.
- **Cara membuktikan perbaikan:** `cd aplikasi && npx eslint . 2>&1 | grep -c "warning"` → 0
- **Status verifikasi:** TERVERIFIKASI

### [F-03] Dependensi `@electric-sql/pglite` tidak terpasang di root `package.json`
- **Tingkat:** K-3
- **Artefak:** `package.json` (root)
- **Klaim yang dilanggar:** Paket mengklaim `node alat/uji-sql.mjs` adalah "perintah bukti yang disarankan" — tetapi gagal tanpa instalasi manual
- **Bukti:** `node alat/uji-sql.mjs --daftar` → `Error [ERR_MODULE_NOT_FOUND]: Cannot find package '@electric-sql/pglite'`. Setelah `npm install @electric-sql/pglite` → 104 LULUS.
- **Skenario gagal:** Auditor/CI baru menjalankan perintah bukti dari paket → error, bukan hasil uji.
- **Dugaan penyebab:** PGlite dipakai alat uji tetapi tidak dicatat di `package.json` root.
- **Cara membuktikan perbaikan:** Tambahkan `@electric-sql/pglite` ke `dependencies` root; `node alat/uji-sql.mjs` tanpa `npm install` tambahan → LULUS.
- **Status verifikasi:** TERVERIFIKASI

### [F-04] Kunci idempoten pembayaran dibuat di klien
- **Tingkat:** K-3
- **Artefak:** `aplikasi/src/hook/useBayar.ts:65` — `kunciIdempoten(pesananId, urutanBayar.current + 1)`
- **Klaim yang dilanggar:** Tidak ada klaim dilanggar — ini pengamatan desain
- **Bukti:** `grep -n "kunciIdempoten" aplikasi/src/hook/useBayar.ts:65` → kunci dari klien; RPC menerima `p_kunci_idempoten` dari parameter
- **Skenario gagal:** Klien dikompromikan → kunci berbeda untuk pembayaran sama. Namun peladen mencegah kelebihan bayar via `v_sudah + p_jumlah > v_total` (BY-301). Dampak terburuk = dua percobaan gagal, bukan dobel uang.
- **Dugaan penyebab:** Desain sadar — kunci klien = kenyamanan (cegah klik ganda), bukan keamanan.
- **Cara membuktikan perbaikan:** Sudah aman oleh desain; tidak perlu perbaikan.
- **Status verifikasi:** TERVERIFIKASI

### [F-05] Test `AlurKasirE2E.test.tsx` — `window.alert` tidak diimplementasi di jsdom
- **Tingkat:** K-4
- **Artefak:** `aplikasi/src/layar/kasir/AlurKasirE2E.test.tsx:1` + `aplikasi/src/layar/kasir/LayarKasir.tsx:365`
- **Klaim yang dilanggar:** Tidak ada — uji tetap LULUS
- **Bukti:** `cd aplikasi && npx vitest run src/layar/kasir/AlurKasirE2E.test.tsx` → `Error: Not implemented: window.alert` + `✓ 1 test` tetap LULUS
- **Skenario gagal:** Alert konfirmasi kasir tidak teruji; uji lulus tanpa asersi pada output alert.
- **Dugaan penyebab:** jsdom tidak mendukung `window.alert`; error di stderr tidak memicu kegagalan.
- **Cara membuktikan perbaikan:** Mock `window.alert` + tambah asersi pesan alert.
- **Status verifikasi:** TERVERIFIKASI

## 5. Kalibrasi cacat tanaman
Ditemukan: 0 dari 0 (kalibrasi cacat tanaman wajib hanya untuk AUD-3; AUD-4 tidak memuat bahan kalibrasi). Temuan palsu: 0.

## 6. Yang tidak bisa saya verifikasi

- **Uji SQL pada PostgreSQL asli (Supabase hosted):** Seluruh uji SQL dijalankan pada PGlite (PostgreSQL in-Node), bukan instance Supabase asli. Perilaku identik untuk fitur yang dipakai (RLS, trigger, SECURITY DEFINER), tetapi ada perbedaan edge-case (pg_cron, replikasi). `node alat/uji-sql.mjs` → 104 LULUS pada PGlite.
- **Uji E2E di peramban nyata:** Tidak ada peramban tersedia. Uji E2E (`*.spec.ts`) tidak dijalankan. Uji komponen (jsdom) sudah dijalankan: `cd aplikasi && npx vitest run` → 677 tes LULUS.
- **Printer termal ESC/POS fisik:** `aplikasi/src/lib/printer/expos.ts` hanya diuji unit (29 tes LULUS). Pengiriman ke printer fisik tidak diverifikasi.
- **Fungsi Edge (Supabase Functions):** `supabase/functions/verifikasi_pin/index.ts` diperiksa statis oleh `python3 alat/periksa-fungsi-pin.py` (14 LOLOS), tidak dijalankan di Supabase asli.
- **CI GitHub Actions:** CI commit target `804ed86f` dilaporkan `BELUM-HIJAU`. Saya tidak bisa menjalankan run CI.

## 7. Pernyataan tidak mengubah apa pun
Saya tidak mengubah apa pun. Saya hanya-baca. SATU-SATUNYA berkas yang saya buat adalah laporan ini; tidak ada berkas lain yang saya ubah, perbaiki, atau terapkan. Bukti: perintah `git status --short` yang saya jalankan menampilkan hanya berkas laporan ini.

## 8. Temuan di luar cakupan (WAJIB)

| # | Temuan | Mengapa di luar cakupan | Bukti | Syarat dilanjutkan ke audit lain |
|---|---|---|---|---|
| 1 | `supabase/tes/.gitkeep` dihitung sebagai berkas uji dalam hitungan 105 | `.gitkeep` bukan uji; hitungan paket memasukkannya ke grup `supabase/tes` | `ls supabase/tes/ \| wc -l` → 106; `node alat/uji-sql.mjs` → 104 LULUS (1 `.gitkeep` + 1 berkas non-uji) | Koreksi hitungan di paket berikutnya |
| 2 | `python3 alat/periksa-fondasi-independen.py` → BERSIH | Alat pemeriksa kedua, bukan cakupan bisnis | `python3 alat/periksa-fondasi-independen.py` → `HASIL: BERSIH` | Tidak ada — validasi positif |
| 3 | `python3 _sistem/validate_system.py` → PASS | Validator struktur sistem, bukan cakupan bisnis | `python3 _sistem/validate_system.py` → `PASS` | Tidak ada — validasi positif |