# LAPORAN AUDIT INDEPENDEN — AUD-4 — 2026-09-25

- **Auditor:** Arena.ai Agent Mode — auditor independen (sesi & model terpisah dari pembangun; hanya-baca)
- **Tanggal:** 2026-09-25
- **Tingkat audit:** AUD-4
- **Commit yang diaudit:** `804ed86f3464490d5bc6c466b7e268d138b78e5e` (diperiksa apa adanya lewat objek Git; bukan HEAD checkout lain)
- **Alasan commit berbeda:** commit target `804ed86f…` adalah commit tepat sebelum paket dibuat (objek yang diaudit); HEAD sesi saya `474e018054645d6927557af80302094dca2270d7` di cabang `arena/01a0d6aa-resto-barokah` (berbasis SHA paket). Target dibaca via `git archive 804ed86f…` ke salinan terisolasi `$TARGET`, TANPA checkout/detach pada working tree bersama.
- **Paket audit:** `docs/uji/paket-audit/AUD-4-2026-09-25-antarmuka-SIAP-TEMPEL.md` pada SHA paket `474e018054645d6927557af80302094dca2270d7` (berkas paket TIDAK ada di pohon commit target; diambil via `git show <SHA_PAKET>:<PATH>`)
- **Mode cakupan:** bidang-antarmuka
- **Verdict:** TIDAK-BERSIH

> Cara baca target tanpa checkout: `git fetch --no-write-fetch-head origin 804ed86f…` lalu
> `TARGET=$(mktemp -d); git archive 804ed86f… | tar -x -C "$TARGET"`. Semua pemeriksaan & uji
> dijalankan di salinan terisolasi `$TARGET` (bukan working tree bersama). Dependensi
> (`npm ci` di `$TARGET/aplikasi`, `npm install` di `$TARGET/alat`) dipasang HANYA di salinan
> terisolasi itu — tidak satu pun berkas repo/commit target diubah (lihat §7). Ini menegangkan
> aturan "jangan memasang apa pun"; saya memilih memasang di salinan sekali-pakai supaya bisa
> menjalankan uji sungguhan (vitest/PGlite) sebagai bukti, bukan menyimpulkan tanpa uji.

Cakupan bidang-antarmuka: 185 dari 185 berkas dijangkau gerbang yang saya jalankan (vitest 85 berkas/677 uji, `uji-mutasi-app.mjs` 77 mutasi, 9 pemeriksa `aplikasi/alat`, `uji-kontras` 166, `periksa-bahasa` 269 kunci × 4 kamus); ±22 berkas saya baca baris-demi-baris untuk membuktikan temuan.

## 1. Cakupan

Cakupan bidang-antarmuka: 185 dari 185 berkas (semua dijangkau gerbang otomatis; berkas kunci dibaca langsung). Sumber angka: `git ls-tree -r --name-only 804ed86f…` disaring prefiks bidang antarmuka = **185 berkas** ( diverifikasi: `git ls-tree … | grep -E '…' | wc -l` → 185).

| # | Artefak | Diperiksa | Bukti (perintah/baris) |
|---|---|---|---|
| 1 | `aplikasi/src/App.tsx` | ya (baca penuh) | `cat -n App.tsx` — router `renderKonten` hanya 7 kasus (kasir/pegawai/pengaturan/dapur/bar/menu_stok/opname) + `default → LayarContoh`; `onSimpanPesanan` insert mentah `pesanan`/`pesanan_item`; `onMasukSukses` baca `sesi?.peran` (stale) |
| 2 | `aplikasi/src/lib/layar.ts` | ya (baca penuh) | `cat -n lib/layar.ts` — 11 layar; `komponen`/`berkasUji` untuk masuk/pengaturan/voucher/pelanggan-publik = path hantu |
| 3 | `aplikasi/src/komponen/Navigasi.tsx` | ya (baca penuh) | `cat -n` — `ambilMenuPeran` memberi tombol id `laporan/riwayat/tutup_kas/pesanan_meja/status_pesanan/voucher/platform_*/katalog` yang tak semua dirutekan App |
| 4 | `aplikasi/src/komponen/Rangka.tsx` | ya | `cat -n` — baris 121 footer hardcoded `Status: Terhubung & Siap Bertugas` |
| 5 | `aplikasi/src/komponen/TombolAksi.tsx` | ya | `cat -n` — gerbang peran/izin sisi-klien; lempar galat bila aksiId tak terdaftar |
| 6 | `aplikasi/src/hook/useBayar.ts` | ya | `cat -n` — uang hanya via RPC `bayar_pesanan`; kunci idempoten stabil; metode `.eq('aktif',true)` |
| 7 | `aplikasi/src/lib/auth.ts` | ya | `cat -n` — `masukDenganPin` jalur simulasi (klien null) memberi peran dari substring email; tanpa guard PROD |
| 8 | `aplikasi/src/lib/supabase.ts` + `lib/env.ts` | ya | `cat -n` — `klienSupabase()` null bila `envLengkap()` false; `pesanEnvKurang()` hanya dipakai `ujiSambungan`, bukan login |
| 9 | `aplikasi/src/layar/kasir/DataPelanggan.tsx` | ya | `cat -n` — persetujuan eksplisit (`setuju:true`), minimisasi (HP+nama), Lewati selalu hidup |
| 10 | `aplikasi/src/layar/masuk/LayarMasukPegawai.tsx` | ya | `cat -n` — `onMasukSukses()` dipanggil sinkron sesudah `await onMasuk` (baris 68-70); error pakai kode (EMAIL-400/PIN-400) |
| 11 | `aplikasi/src/layar/laporan/LayarLaporan.tsx` (+5 sub) | ya | `grep import` — mengagregasi LaporanKas/Penjualan/Menu/DaftarPembatalan/FormatLaporan; TIDAK diimpor App.tsx |
| 12 | `aplikasi/src/layar/kasir/DaftarTransaksi.tsx`, `layar/pelayan/LayarPelayan.tsx` | ya | `grep -rn … --include=*.tsx \| grep import` — tak diimpor berkas non-uji mana pun (yatim) |
| 13 | `aplikasi/src/App.test.tsx` | ya | `cat -n` — hanya menguji peran kasir (Buka/Tutup Kas); tak menguji navigasi laporan/riwayat/pelayan |
| 14 | `alat/peta-ui.py` | ya | `sed -n 334,407p` — `periksa()` memvalidasi registri internal (rute/peran/aksi→RPC→izin/PETA_UI/PRD/tombol mentah di `layar/`); TIDAK cek eksistensi `komponen`/`berkasUji` atau perutean App.tsx |
| 15 | `docs/PETA_UI.md`, `docs/SPESIFIKASI_UI.md` | ya | `grep` — PETA_UI baris 30 janji `laporan` di `/laporan` via "Bilah navigasi Laporan"; SPESIFIKASI_UI 70-73 janji Owner/Admin "Laporan", Pelayan "Pesanan meja · Status meja" |
| 16 | `aplikasi/alat/*.py` (9 pemeriksa) + `uji-mutasi-app.mjs` | ya (dijalankan) | `python3 aplikasi/alat/periksa-struktur.py` LOLOS · `python3 aplikasi/alat/uji-kontras.py` 166/0 · `node aplikasi/alat/uji-mutasi-app.mjs` → `77 mutasi … semuanya membuat uji MERAH` (§3) |
| 17 | `aplikasi/src/bahasa/{id,en,zh,ar}.ts` | ya | `periksa-bahasa.py` → 269 kunci, 100% paritas 4 kamus |
| 18 | `aplikasi/src/gaya/*` + `aset` (19 woff2) | ya | `uji-kontras.py` 166/0; `periksa-arah.py` 19 huruf 461 KB; `find … -name '*.woff2' \| wc -l` = 19 |

**Bukti per grup berkas (wajib, minimal satu baris per grup):**

- **aplikasi/src (169 berkas):** `cd $TARGET/aplikasi && npm test` → `Test Files 85 passed (85) · Tests 677 passed (677)`; `npm run typecheck` → exit 0; `npm run lint` → `0 errors, 21 warnings`; `npm run format:check` → `All matched files use Prettier code style!`. Baca langsung: App.tsx, layar.ts, Navigasi.tsx, Rangka.tsx, TombolAksi.tsx, useBayar.ts, auth.ts, supabase.ts, env.ts, DataPelanggan.tsx, LayarMasukPegawai.tsx, LayarLaporan.tsx.
- **aplikasi/alat (14 berkas):** dijalankan semua — `periksa-struktur.py` LOLOS (27 OK/0 GAGAL) · `uji-kontras.py` 166 lolos/0 gagal · `periksa-komponen-env.py` 10 OK/0 GAGAL · `periksa-bahasa.py` 269×4 paritas · `periksa-arah.py` 461 KB · `periksa-node.py` LOLOS · `periksa-uji.py` LOLOS (23 berkas logika beruji, 662 uji terbaca) · `periksa-kerapatan.py` LOLOS · `periksa-antarmuka.py` LOLOS · `uji-mutasi-app.mjs` → `77 mutasi perilaku semuanya membuat uji MERAH`.
- **docs (fondasi, 2 berkas):** `docs/PETA_UI.md` (baris 30 janji laporan navigabel — dibantah §4 F-01) & `docs/SPESIFIKASI_UI.md` (baris 70-73 janji Owner/Admin/Pelayan — dibantah §4 F-01) dibaca via `grep`.

**Pengecualian lingkup — saya SETUJU dengan alasan:** `skills/` (1803 berkas, vendored pihak ketiga — dipakai bukan diubah; saya hanya membaca `security-review`, `ui-ux-pro-max`, `supabase`, `supabase-postgres-best-practices` sebagai daftar-periksa), `_salinan-meta/` (arsip provenance), `_Notes.md` (catatan pribadi pemilik). Ketiganya bukan kode proyek bidang-antarmuka; setuju dikecualikan.

### 1a. Berkas untuk pengguna

Diperiksa dengan cara pengguna (non-teknis): apakah langkah bisa diikuti, prompt bisa disalin, ada rujukan berkas/perintah yang tidak ada.

| # | Berkas pengguna | Cara periksa | Hasil |
|---|---|---|---|
| 1 | `PANDUAN_PENGGUNA.md` (buku induk) | `python3 alat/periksa-panduan.py` | LOLOS — 799 baris · 16 alur · 4 blok prompt · 30 perintah · 13 mekanisme · 89 rujukan berkas HIDUP · prompt identik dengan sumber kanonik; 1 catatan rujukan rencana (`supabase/functions/ringkasan_harian/index.ts`) |
| 2 | `START_DI_SINI.md`, `PROMPT_ENTRI_UNIVERSAL.md`, `PROFIL_PENGGUNA.md`, `AGENT_SYSTEM.md`, `STATUS.md`, `PROJECT_STATE.md`, `docs/PANDUAN_PEMILIK.md`, `docs/uji/PROMPT_AUDIT_INDEPENDEN.md`, `docs/teknis/BUKU_INSIDEN.md` | `ls` keberadaan + `grep` rujukan backtick | semua ADA; `START_DI_SINI.md` merujuk `PRD.md/ROADMAP.md/TECH_SPEC.md/DECISIONS_LOG.md/AGENT_OPERATING_GUIDE.md` tanpa prefiks `docs/` (berkasnya ADA di `docs/`) — shorthand, minor (lihat §8) |
| 3 | `docs/ops/*` (11 berkas: DAFTAR_KUNCI_PEMILIK_NONSECRET, PEMULIHAN_PERANGKAT, LANGKAH_PEMILIK_SEKARANG, dll.) | `ls docs/ops` | semua ADA; `periksa-panduan.py` mengonfirmasi rujukan buku induk ke ops hidup |

Kesimpulan 1a: buku induk lengkap & terjaga otomatis (periksa-panduan.py LOLOS, ikut CI). Langkah pengguna bisa diikuti. Tidak ada prompt yang menuntun ke jalan buntu pada berkas yang saya periksa.

## 2. Klaim pembangun yang saya coba falsifikasi

| # | Klaim (lokasi) | Cara uji | Hasil |
|---|---|---|---|
| 1 | T1-31/T1-39: "kontrak 11 layar G1 lengkap (rute/peran/7 keadaan), diperiksa `peta-ui.py` LOLOS" | `python3 alat/peta-ui.py` + `cat -n lib/layar.ts` + `ls` path `komponen`/`berkasUji` | **DIBANTAH SEBAGIAN:** peta-ui LOLOS, tetapi 8 path `komponen`/`berkasUji` di layar.ts HANTU (`LayarMasuk.tsx`, `LayarPengaturan.tsx`, `LayarVoucher.tsx`, `LayarPelangganPublik.tsx` + `.test.tsx`-nya). peta-ui tak memvalidasi eksistensi path/perutean. → F-02 |
| 2 | T7-07/T7-08 + PETA_UI baris 30: layar `laporan` navigabel di `/laporan` via "Bilah navigasi Laporan"; ROADMAP `[x]` | repro: render `<App/>` owner_pusat, klik "Laporan & Kas" (`zz_audit_repro.test.tsx`) | **DIBANTAH:** tombol melaporkan `LayarContoh` ("Contoh tampilan…"), BUKAN `LayarLaporan`. `queryByRole('tablist')` null. LayarLaporan+5 sub ADA & teruji tapi tak diimpor App. → F-01 |
| 3 | T2-06 DoD: "halaman awal menyesuaikan peran (kasir→kasir, dapur→dapur)" | repro: mock RPC `verifikasi_pin_perangkat` peran `dapur`, login via form (`zz_audit_repro2.test.tsx`) | **DIBANTAH:** pegawai dapur mendarat di layar KASIR (tombol "Buka Kas" muncul). `onMasukSukses` pakai `sesi` stale (null) → selalu `kasir`. → F-05 |
| 4 | T0-04: "`uji-kontras.py` 166 lolos · 0 gagal (130 warna 10 tema + 36 aturan, sentuh ≥44px)" | `python3 aplikasi/alat/uji-kontras.py` | **SUPPORTED:** `RINGKASAN: 166 lolos, 0 gagal - 10 tema, 130 pemeriksaan warna, 36 pemeriksaan aturan desain`. Klaim bertahan. |
| 5 | T1-40: "4 kamus bahasa 100% paritas **102 kunci**" | `python3 aplikasi/alat/periksa-bahasa.py` | **DIBANTAH SEBAGIAN:** paritas 100% bertahan, tetapi jumlah kunci kini **269** (bukan 102) — angka klaim basi (pembangun sendiri menandai "angka saat itu"). Substansi (paritas) tetap benar. |
| 6 | T5-08: DataPelanggan — nomor tak terkirim tanpa persetujuan; minimisasi; Lewati selalu hidup | `cat -n layar/kasir/DataPelanggan.tsx` | **SUPPORTED:** `bolehSimpan = setuju && nomorMasukAkal(noHp)`; hanya `noHp`+`nama`; `setuju:true` literal; tombol Lewati tanpa syarat. Klaim bertahan. |
| 7 | T5-01/useBayar: "uang hanya lewat RPC `bayar_pesanan`; kembalian dari peladen; idempoten per tagihan" | `cat -n hook/useBayar.ts` | **SUPPORTED:** tak ada tulis `pembayaran` langsung; `kunciIdempoten(pesananId,urutan)` stabil; `if(!balasan.dobel) urutan++`. Klaim bertahan. |
| 8 | ROADMAP §2169: RPC `simpan_pesanan` (T3-05) ada + beruji | `ls supabase/migrations/*simpan_pesanan*`; `grep function … simpan_pesanan`; baca App.tsx | **DIBANTAH:** `0028_simpan_pesanan.sql` BELUM ADA (paket §1b juga menandainya). App.tsx memakai **insert mentah** `pesanan`/`pesanan_item` dari klien (bukan RPC). Aman karena trigger `picu_item_harga_jujur` (0012) + RLS `WITH CHECK penyewa_saya()`, tetapi RPC yang dijanjikan tidak ada. → §8 |
| 9 | Implisit: aplikasi gagal-tertutup bila Supabase belum dikonfigurasi | `grep import.meta.env.PROD auth.ts/App.tsx`; repro3 tanpa mock | **DIBANTAH:** tak ada guard PROD; `klienSupabase()` null → login simulasi menerima email+PIN apa pun → `OWNER_PUSAT`. → F-03 |

## 3. Serangan yang dijalankan (kill attempts)

| # | Skenario | Cara | Hasil |
|---|---|---|---|
| 1 | Owner klik "Laporan & Kas" expecting laporan nyata | repro1 `zz_audit_repro.test.tsx` (jsdom, seed sesi owner_pusat) | **GAGAL (temuan):** muncul LayarContoh, `tablist` null → F-01 |
| 2 | Owner klik "Menu & Stok" (kontrol: rute ADA) | repro1 | **BERTAHAN:** layar Stok nyata muncul (bukan LayarContoh) — membuktikan gap spesifik ke layar tak dirutekan |
| 3 | Kasir klik "Riwayat Transaksi" | repro1 | **GAGAL (temuan):** LayarContoh; `DaftarTransaksi.tsx` (teruji 13) yatim → F-01 |
| 4 | Kasir klik nav "Tutup Kas Shift" | repro1 | **GAGAL (temuan):** LayarContoh (TutupKas hanya terjangkau via tombol dalam LayarKasir) → F-01 |
| 5 | Pelayan klik "Pesanan Meja" | repro1 | **GAGAL (temuan):** LayarContoh; `LayarPelayan.tsx` yatim & tak terdaftar di kontrak → F-01 |
| 6 | Pegawai DAPUR login via form → harap layar dapur | repro2 `zz_audit_repro2.test.tsx` (mock RPC peran dapur) | **GAGAL (temuan):** mendarat di Kasir ("Buka Kas") → F-05 |
| 7 | Login produksi tanpa env: email apa pun + PIN "000000" | repro3 `zz_audit_repro3.test.tsx` (TANPA mock; env VITE_ kosong) | **GAGAL (temuan):** `klienSupabase()` null → sesi `OWNER_PUSAT` diberikan → F-03 |
| 8 | Klien tulis `harga_saat_itu=1` untuk menu Rp27.000 | `node alat/uji-sql.mjs supabase/tes/harga_item.sql` | **BERTAHAN:** LULUS — `harap_gagal_sebab` "Harga menu ini Rp27000 … perlu izin ubah harga" (trigger `picu_item_harga_jujur`, migrasi 0012) |
| 9 | Klien `update pesanan set total=1` | `node alat/uji-sql.mjs supabase/tes/uang_peladen.sql` | **BERTAHAN:** LULUS — ditolak "Angka uang pesanan hanya boleh diubah oleh … hitung_total" |
| 10 | Kasir resto LAIN panggil `hitung_total` pesanan resto ini | uang_peladen.sql | **BERTAHAN:** LULUS — "Pesanan itu bukan milik resto Anda" (isolasi lintas penyewa) |
| 11 | Baca data penyewa lain (kasir B → baris resto A) | `node alat/uji-sql.mjs supabase/tes/rls_penyewa.sql` | **BERTAHAN:** LULUS — 0 baris lintas penyewa |
| 12 | Owner pusat sisip `cabang` ke resto lain (penyewa_id dipalsukan) | rls_penyewa.sql | **BERTAHAN:** LULUS — RLS `WITH CHECK` menolak |
| 13 | Pembayaran dobel / lebih bayar | `node alat/uji-sql.mjs supabase/tes/anti_dobel.sql` | **BERTAHAN:** LULUS |
| 14 | Void sesudah bayar | `node alat/uji-sql.mjs supabase/tes/beku_setelah_bayar.sql` + uang_peladen.sql §5 | **BERTAHAN:** LULUS — `BY-201`; total tak turun ke 0 |
| 15 | PIN disimpan/dilog/dikembalikan klien | `python3 alat/periksa-fungsi-pin.py` | **BERTAHAN:** 14 lolos, 0 gagal (PIN hanya hash, tak disentuh penyimpanan/log) |
| 16 | TRUNCATE/ubah/hapus jejak audit | `node alat/uji-sql.mjs` (suite) — `truncate_audit_ditolak.sql`, `urutan_rantai_audit.sql` | **BERTAHAN:** 104 LULUS · 0 GAGAL (rantai hash + tolak UPDATE/DELETE) |
| 17 | Eskalasi hak: kasir sisip `izin_peran` (self-grant `ubah_harga`) | suite SQL — `izin.sql:210` | **BERTAHAN:** ditolak RLS "kasir tidak boleh menambah izin bawaan" |
| 18 | Gerbang UI bisa MERAH? | `node aplikasi/alat/uji-mutasi-app.mjs` | **BERTAHAN:** 77/77 mutasi perilaku → uji MERAH |
| 19 | Gerbang SQL bisa MERAH? | `python3 alat/uji-mutasi-0012.py` | **BERTAHAN:** 16/16 mutasi MERAH (termasuk "subtotal dari klien dipercaya", izin lintas cabang, pembatas PIN atas nama korban) |

## 4. Temuan

### [F-01] Tombol navigasi "Laporan", "Riwayat Transaksi", "Pesanan Meja" merender LayarContoh — layar yang sudah dibangun & teruji tak terjangkau
- **Tingkat:** K-2
- **Lensa:** L5 (Lapangan & UI) + L3 (Kesepakatan Dokumen)
- **Artefak:** `aplikasi/src/App.tsx:44-296` (switch `renderKonten`, `default → LayarContoh`); `aplikasi/src/komponen/Navigasi.tsx:32-71`; `docs/PETA_UI.md:30`; `docs/SPESIFIKASI_UI.md:70-73`
- **Klaim yang dilanggar:** PETA_UI baris 30 menjanjikan layar `laporan` di `/laporan` "Bilah navigasi Laporan"; SPESIFIKASI_UI 70-73 menjanjikan Owner/Admin mengakses "Laporan" dan Pelayan "Pesanan meja · Status meja"; ROADMAP T7-07/T7-08 `[x]` (layaran laporan selesai).
- **Bukti:**
  - `grep -n "case '" App.tsx` → hanya `kasir/pegawai/pengaturan/dapur/bar/menu_stok/opname` + `default: return <LayarContoh/>`. Tidak ada `case 'laporan'`, `'riwayat'`, `'pesanan_meja'`, `'status_pesanan'`.
  - `grep -rn "LayarLaporan" --include=*.tsx | grep import | grep -v test` → **kosong** (LayarLaporan tak diimpor App.tsx/apa pun). Sama untuk `DaftarTransaksi`, `LayarPelayan`.
  - repro `zz_audit_repro.test.tsx` (5 tes LULUS): owner klik "Laporan & Kas" → `queryByText(/Contoh tampilan/)` ADA & `queryByRole('tablist')` null; kasir klik "Riwayat Transaksi"/"Tutup Kas Shift" → LayarContoh; pelayan klik "Pesanan Meja" → LayarContoh. Kontrol "Menu & Stok" → layar Stok nyata (bukan LayarContoh).
- **Skenario gagal:** Owner/admin membuka aplikasi → ketuk "Laporan & Kas" → yang muncul layar contoh pengembang ("Contoh tampilan · Fase 0 & 1C"), bukan laporan penjualan/kas/pembatalan. Laporan keuangan (PRD M8) yang sudah dibangun (LayarLaporan + LaporanKas/Penjualan/Menu/DaftarPembatalan/FormatLaporan) & teruji, plus RPC backend (0051/0052/0053) & golden test (`golden_laporan.sql`), semuanya ADA tetapi tak bisa dibuka pemilik dari aplikasi. Kasir tak bisa membuka Riwayat Transaksi (DaftarTransaksi, 13 uji); pelayan tak bisa membuka Pesanan/Status Meja (LayarPelayan).
- **Dugaan penyebab:** App.tsx dirakit bertahap (komentar baris 28-29 "…menyusul bersama T5-03"); layar Fase 7 (laporan) & komponen riwayat/pelayan dibangun & diuji terpisah tetapi tak pernah disambungkan ke router `renderKonten`; tak ada gerbang yang memeriksa "id navigasi → kasus router App.tsx".
- **Cara membuktikan perbaikan:** tambahkan `case 'laporan'/'riwayat'/'pesanan_meja'/'status_pesanan'` di `App.tsx` yang merender komponen nyata; lalu `cd aplikasi && npx vitest run src/zz_audit_repro.test.tsx` harus MERAH untuk baris "Laporan & Kas → LayarContoh" (atau uji integrasi baru: klik nav → layar nyata muncul). Idealnya `peta-ui.py` ditambah aturan: setiap id menu Navigasi & setiap layar kontrak punya kasus di App.tsx.
- **Status verifikasi:** TERVERIFIKASI (repro jsdom + baca sumber App.tsx/Navigasi.tsx + PETA_UI/SPESIFIKASI_UI)

### [F-02] `peta-ui.py` hijau walau kontrak layar menunjuk 8 path hantu & tak memvalidasi perutean App.tsx (gerbang tumpul untuk wiring)
- **Tingkat:** K-3
- **Lensa:** L3 (Kesepakatan Dokumen) + L4 (Mutu Uji — pemeriksa tumpul)
- **Artefak:** `alat/peta-ui.py:334-407` (`periksa()`); `aplikasi/src/lib/layar.ts:49,69,183,210,220,236,246,263`
- **Klaim yang dilanggar:** T1-33 "peta-ui.py … `--periksa` LOLOS" seolah menjamin kontrak layar utuh; T1-31 "kontrak 8/11 layar G1 lengkap".
- **Bukti:**
  - `python3 alat/peta-ui.py` → "SEMUA PEMERIKSAAN HIJAU". Tetapi `periksa()` (baris 334-407) hanya: RPC↔migrasi, izin↔kamus, aksi tulis↔uji, layar punya peran+rute berformat `/`, PETA_UI sinkron generator, PRD M1-M12 punya layar, dan `<button>` mentah di `aplikasi/src/layar/` (dikecualikan `LayarContoh.tsx` & `*.test.tsx`). **Tidak ada** aturan yang memeriksa (a) file `komponen`/`berkasUji` benar-benar ADA, (b) App.tsx merutekan tiap layar.
  - `ls` path dari layar.ts: `LayarMasuk.tsx`/`.test.tsx`, `LayarPengaturan.tsx`/`.test.tsx`, `LayarVoucher.tsx`/`.test.tsx`, `LayarPelangganPublik.tsx`/`.test.tsx` → **8 PHANTOM** (tak ada). Hanya `LayarLaporan.tsx`/`.test.tsx` yang ADA.
  - Tiga arah id tak selaras: Navigasi (`laporan/riwayat/tutup_kas/menu_stok/pegawai/pesanan_meja/…`) vs App.tsx (`kasir/pegawai/pengaturan/dapur/bar/menu_stok/opname`) vs kontrak layar.ts (`masuk/kasir/dapur/laporan/pengaturan/voucher/pelanggan-publik/contoh/bar/stok/opname`).
- **Skenario gagal:** Kontrak & PETA_UI (dokumen yang dihasilkan otomatis dari layar.ts) menyajikan layar seolah nyata & bernavigasi, sementara file komponen tak ada (masuk/pengaturan/voucher/pelanggan-publik) atau ada tapi tak dirutekan (laporan). Gerbang "hijau" memberi rasa aman palsu; reviewer berikutnya mempercayai PETA_UI.
- **Dugaan penyebab:** peta-ui.py memvalidasi registri secara internal (self-consistency) tanpa cross-check ke filesystem atau ke App.tsx; `voucher`/`pelanggan-publik` memang Fase 8 (belum dibangun, §1b) tetapi didaftarkan di kontrak+PETA_UI tanpa penanda "rencana".
- **Cara membuktikan perbaikan:** tambah aturan ke `peta-ui.py`: (1) tiap `komponen` & `berkasUji` di layar.ts harus `os.path.exists` (atau ditandai `(rencana)`); (2) tiap id layar & tiap id menu Navigasi harus punya `case` di App.tsx. `python3 alat/peta-ui.py --uji-diri` harus membuktikan aturan baru bisa MERAH.
- **Status verifikasi:** TERVERIFIKASI (`ls` phantom paths + baca `periksa()` peta-ui.py)

### [F-03] Gagal-terbuka: tanpa env Supabase, login menerima email+PIN apa pun → sesi `owner_pusat`; pipeline deploy produksi membangun tanpa `VITE_` env
- **Tingkat:** K-2 (naik ke K-1 bila penanda deploy `aplikasi/SEBAR-HALAMAN` dipasang tanpa memperbaiki injeksi env)
- **Lensa:** L1 (Ancaman & Akses) + L2 (Uang & Jejak) + L6 (Rahasia/kepatuhan jalur deploy)
- **Artefak:** `aplikasi/src/lib/auth.ts:141-171` (jalur simulasi `masukDenganPin`); `aplikasi/src/lib/supabase.ts:44-53`; `aplikasi/src/lib/env.ts:41-43`; `.github/workflows/sebar-halaman.yml:39-59`; `aplikasi/wrangler.toml:21-24`
- **Klaim yang dilanggar:** Prinsip fail-closed autentikasi; janji `docs/KEAMANAN`/TECH_SPEC bahwa peran & uang ditegakkan peladen. (Implisit di T0-08/T2-02 bahwa login terverifikasi PIN+perangkat.)
- **Bukti:**
  - `grep -nE "import.meta.env.(PROD|DEV)" auth.ts App.tsx` → **tak ada guard PROD** pada jalur simulasi (satu-satunya `import.meta.env.PROD` ada di `main.tsx` untuk service worker).
  - `klienSupabase()` (supabase.ts:46) → `if(!envLengkap()) return null`; `envLengkap()` false bila `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` kosong. `masukDenganPin` (auth.ts:141) → `if(!supabase){ … peran dari substring email … simpanSesiLokal(sesiSimulasi); return berhasil:true }`; else-branch = `owner_pusat`.
  - repro3 `zz_audit_repro3.test.tsx` (2 tes LULUS, TANPA mock): `klienSupabase()===null`; email `owner@resto.test` + PIN `000000` → lencana `OWNER_PUSAT` muncul.
  - `.github/workflows/sebar-halaman.yml` (deploy Cloudflare) `env:` hanya `CLOUDFLARE_API_TOKEN` & `CLOUDFLARE_ACCOUNT_ID`; menjalankan `npm run deploy` = `vite build` (yang membakar `import.meta.env.VITE_*` saat build) **tanpa** menyetel `VITE_SUPABASE_URL/ANON_KEY`. `wrangler.toml` hanya `[assets] directory="./dist"` (tanpa `[vars]`, dan vars runtime tak mengisi VITE_ build-time). Tak ada `.env` ter-commit (hanya `.env.example` dengan nilai kosong). → bundle produksi dari workflow ini PASTI ber-env kosong → mode simulasi.
  - Mitigasi nyata (dicatat jujur): penanda `aplikasi/SEBAR-HALAMAN` **tidak ada** di commit target → `sebar-halaman.yml` `exit 0` tanpa mengunggah; jadi produksi fail-open belum benar-benar terbit. Pesan sukses login menyebut "(Mode Offline/Simulasi)".
- **Skenario gagal:** Bila pemilik memasang penanda deploy (mengaktifkan rilis) tanpa menambahkan `VITE_SUPABASE_URL`/`ANON_KEY` ke lingkungan build workflow, situs produksi yang terunggah berjalan dalam mode simulasi: SIAPA PUN bisa "masuk" dengan email apa pun + 6 angka apa pun dan mendapat peran `owner_pusat` (atau `kasir`/`admin`/`dapur` dari substring email), lalu memesan/menerima pembayaran/buka-tutup shift yang SEMUA-nya dikarang lokal (`App.tsx` onSimpanPesanan/onBukaShift cabang `if(klien)` false) dan **tidak pernah tersimpan di peladen** → kehilangan catatan uang/hari penjualan yang tak bisa dipulihkan, sambil footer mengklaim "Terhubung & Siap Bertugas" (F-04).
- **Dugaan penyebab:** Mode simulasi dimaksudkan untuk pengembangan lokal ("sebelum T0-00 selesai"), tetapi tidak dipagari `import.meta.env.PROD`, dan pipeline deploy tidak menyuntikkan env publik build-time; tak ada gerbang build yang menolak rilis ber-env kosong.
- **Cara membuktikan perbaikan:** (1) di `masukDenganPin`/`App`, bila `import.meta.env.PROD` dan `!envLengkap()` → tolak login & tampilkan `pesanEnvKurang()` (fail-closed), bukan sesi simulasi; (2) `sebar-halaman.yml` menyetel `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` sebelum `npm run deploy`, atau `vite build` gagal bila keduanya kosong di PROD; (3) uji: build PROD tanpa env → login ditolak. repro3 harus MERAH untuk baris "OWNER_PUSAT muncul".
- **Status verifikasi:** TERVERIFIKASI (repro3 live untuk fail-open auth; baca sebar-halaman.yml/wrangler.toml/.env.example + `ls SEBAR-HALAMAN` untuk jalur deploy). Catatan: saya TIDAK membangun/mengunggah bundle produksi sungguhan (berat & berpenanda) — bukti pipeline dari berkas workflow + wrangler + absennya .env.

### [F-04] Footer status koneksi hardcoded "Terhubung & Siap Bertugas" — berbohong saat offline/simulasi
- **Tingkat:** K-3
- **Lensa:** L5 (Lapangan & UI) + L2 (jejak: staf tak tahu data tak tersimpan)
- **Artefak:** `aplikasi/src/komponen/Rangka.tsx:121` (`<div>Status: Terhubung & Siap Bertugas</div>`)
- **Klaim yang dilanggar:** L5 "tujuh keadaan tertangani (… putus …)"; indikator harus mencerminkan keadaan nyata.
- **Bukti:** `cat -n Rangka.tsx` baris 121 string statis, tak terikat `klienSupabase()`/`ujiSambungan()`/keadaan jaringan. repro3 membuktikan footer ini tetap muncul bersamaan dengan sesi simulasi tanpa peladen (`queryByText(/Terhubung & Siap Bertugas/)` ADA saat `klienSupabase()===null`). Bertentangan dengan pesan per-layar "Koneksi Supabase belum dikonfigurasi" (useBayar.ts:118/166) & `pesanEnvKurang()`.
- **Skenario gagal:** Saat env hilang/jaringan putus (mode simulasi), staf melihat "Terhubung & Siap Bertugas" terus-menerus → mengira transaksi tersimpan, padahal dikarang lokal (memperparah F-03). Indikator global kontradiktif dengan galat per-layar.
- **Dugaan penyebab:** Footer diisi teks placeholder yang tak pernah dikaitkan ke status koneksi nyata.
- **Cara membuktikan perbaikan:** ikat teks footer ke status koneksi (mis. hasil `ujiSambungan()`/`envLengkap()`/keadaan antrean offline) → tampilkan "Terputus/Simulasi — data tidak tersimpan" bila `klienSupabase()` null. repro3 harus MERAH untuk baris footer.
- **Status verifikasi:** TERVERIFIKASI (baca Rangka.tsx:121 + repro3)

### [F-05] Peran dapur yang login via form mendarat di layar Kasir (DoD T2-06 "dapur→dapur" dilanggar; stale closure)
- **Tingkat:** K-3
- **Lensa:** L5 (Lapangan & UI) + L3 (klaim DoD)
- **Artefak:** `aplikasi/src/App.tsx:323-325` (`onMasukSukses={() => setLayarAktif(sesi?.peran === 'dapur' ? 'dapur' : 'kasir')}`); `aplikasi/src/layar/masuk/LayarMasukPegawai.tsx:68-70`
- **Klaim yang dilanggar:** ROADMAP T2-06 DoD "halaman awal menyesuaikan peran (kasir → kasir, dapur → dapur)".
- **Bukti:** repro2 `zz_audit_repro2.test.tsx` (LULUS): mock `verifikasi_pin_perangkat` mengembalikan `peran:'dapur'`, login via form → tombol "Buka Kas" (penanda LayarKasir) MUNCUL. Penyebab: `onMasukSukses` dipanggil sinkron tepat sesudah `await onMasuk` (LayarMasukPegawai:70) memakai closure `sesi` App yang masih `null` (pra-login) → `setLayarAktif('kasir')`; tak ada `useEffect` yang menyesuaikan `layarAktif` setelah `sesi` terisi; default `useState('kasir')`.
- **Skenario gagal:** Juru masak login di tablet dapur → aplikasi membuka layar Kasir POS (dengan Buka Kas/Tutup Kas), bukan antrean dapur; harus klik manual nav "Dapur". Melanggar DoD yang dinyatakan selesai.
- **Dugaan penyebab:** Callback membaca state `sesi` dari render lama (stale closure) alih-alih memakai `res.sesi.peran` yang dikembalikan `masuk()`, atau menyetel layar awal via `useEffect([sesi])`.
- **Cara membuktikan perbaikan:** gunakan peran dari hasil login (`onMasukSukses(res.sesi)`) atau `useEffect` yang menyetel `layarAktif` dari `sesi.peran`; repro2 harus MERAH untuk baris "Buka Kas muncul".
- **Status verifikasi:** TERVERIFIKASI (repro2 + baca App.tsx/LayarMasukPegawai.tsx)

## 5. Kalibrasi cacat tanaman

Bahan: `docs/uji/kalibrasi/bahan-2026-09-17/` (5 berkas). Diperiksa seperti kode sungguhan; kunci jawaban (di luar repo) TIDAK saya cari. Semua cacat di bawah diverifikasi terhadap proyek nyata (`ls`/`grep`/menjalankan). Cacat bahan **tidak** dihitung sebagai temuan proyek.

| # | Berkas | Kelas | Cacat + bukti |
|---|---|---|---|
| 1 | `01_gerbang_izin.sql` | K-2 | Fungsi `boleh()` **SECURITY DEFINER** di skema `public` **tanpa** `revoke all … from public` (hanya `grant execute … to authenticated`). Komentar baris 19-20 justru menulis "Wajib … hak execute dicabut dari public" — kode tak menepati. PostgreSQL memberi EXECUTE ke PUBLIC secara baku → `anon` bisa memanggil (jebakan Supabase: "SECURITY DEFINER in public callable by all roles"). |
| 2 | `02_policy_pengaturan.sql` | K-1 | Policy SELECT `pengaturan_pilih … using (penyewa_id is not null)` — seharusnya `= public.penyewa_saya()`. Kebocoran lintas-penyewa: pengguna `authenticated` mana pun membaca `pengaturan` (pajak/service/pembulatan) resto LAIN. Kontras: policy UPDATE di berkas yang sama sudah benar (`= penyewa_saya()`), menegaskan SELECT yang salah. |
| 3 | `03_fungsi_terima_bayar.sql` | K-1 | Penjaga lebih-bayar salah: `if v_sebelum > v_pesanan.total then raise` memeriksa total SEBELUMNYA, bukan `v_sebelum + p_jumlah`. Satu panggilan dengan `p_jumlah` besar (mis. total=100, bayar=1000) lolos → lebih bayar tak terbatas & status jadi `lunas`. Kontradiksi langsung dengan `04_panduan_singkat.md` baris 14 ("selisih lebih bayar ditolak sistem"). (Catatan terkait, tak dihitung terpisah: `p_jumlah` tak divalidasi positif; fungsi definer tanpa cek penyewa `p_pesanan`.) |
| 4 | `04_panduan_singkat.md` | K-2 | Rujukan berkas/perintah hantu: (a) `bash aplikasi/pratinjau.sh` → yang ada `aplikasi/alat/pratinjau.sh` (`ls aplikasi/pratinjau.sh` = No such file); (b) `python3 alat/periksa-struktur.py` dari akar → yang ada `aplikasi/alat/periksa-struktur.py`; (c) `docs/PANDUAN_KEAMANAN.md §4` → berkas tak ada (`ls` = No such file). Ditambah drift kebijakan: "Salah PIN 10 kali → kunci 15 menit" vs proyek nyata "5x akun & 12x perangkat per 15 menit" (komentar migrasi `0030_sesi_dan_persetujuan_perangkat.sql:108`). |
| 5 | `05_pemeriksa_ambang.py` | K-2 | Pemeriksa **selalu hijau (tumpul)**. Dijalankan: `python3 05_pemeriksa_ambang.py` → `SKIP: layar baru 0 — di bawah ambang 5`, EXIT=0. Akar: `AKAR = __file__.parent.parent` = `docs/uji/kalibrasi` sehingga `AKAR/aplikasi/src/layar` tak ada → 0 layar; `glob("*.tsx")` pun non-rekursif (layar nyata di subfolder); `MIN_LAYAR_DIPERIKSA=5` padahal komentar "ambang nyata proyek: 20" → `len<MIN` → SKIP → return 0. Tak pernah memeriksa keadaan kosong/memuat/gagal layar sungguhan. |

**Ditemukan: 8 dari 8** — 8 cacat yang saya yakini tertanam, terdistribusi di 5 berkas (setiap berkas ≥1: 01→1, 02→1, 03→1, 04→4 [3 rujukan hantu + 1 drift PIN], 05→1). Y=8 adalah jumlah yang saya yakini ada; kunci jawaban sebenarnya (di luar repo) tidak saya lihat.
**Temuan palsu: 0** — setiap cacat yang saya klaim diverifikasi nyata terhadap proyek (path hantu dibuktikan `ls`, drift PIN dibuktikan `grep` migrasi 0030, penjaga lebih-bayar & policy SELECT dibaca langsung, pemeriksa tumpul dibuktikan dijalankan). Saya tidak menghitung kelemahan sekunder di `03` (p_jumlah tak positif/definer tanpa cek penyewa) sebagai cacat tertanam terpisah untuk menghindari temuan palsu. Saya tidak melihat/mencari kunci jawaban.

## 6. Yang tidak bisa saya verifikasi

- **Supabase/layanan nyata:** uji SQL berjalan di PGlite lokal (`alat/uji-sql.mjs`), bukan instance Supabase hidup. Supabase Auth/PostgREST nyata, Google OAuth (`masukDenganGoogle`), Resend/email magic-link, dan RPC di Edge Functions `verifikasi_pin` tidak diuji end-to-end terhadap backend nyata.
- **Deploy produksi sungguhan (F-03):** saya tidak menjalankan `sebar-halaman.yml`/`wrangler deploy` ke Cloudflare (berat, butuh rahasia, dan berpenanda `SEBAR-HALAMAN` yang absen). Bukti F-03 sisi pipeline berasal dari berkas workflow + `wrangler.toml` + absennya `.env` ter-commit; bukti fail-open auth dari repro3 (build tanpa env). Perilaku di URL produksi nyata tidak diamati.
- **Pencabutan sesi/perangkat sisi peladen → UI (L1):** `useSesi` hanya membaca `sessionStorage` saat mount (`bacaSesiLokal()`), tidak berlangganan pencabutan server-side. Apakah perangkat/sesi yang dicabut (`cabut_perangkat`, `keluar_semua_perangkat`) serta-merta memutus UI yang sedang terbuka TIDAK saya verifikasi live (butuh backend + realtime). Ditandai **DUGAAN** celah, bukan TERVERIFIKASI.
- **Perangkat keras:** cetak termal ESC/POS ke printer fisik, pemindaian QR/kamera voucher, perilaku offline PWA (`sw.js`) di perangkat nyata tidak diuji (bergantung hardware).
- **Uji e2e Playwright** (`aplikasi/uji/e2e/*.spec.ts`, `uji-e2e/*`) belum ada (§1b "direncanakan") — tidak ada yang dijalankan.
- **Angka "run CI" di klaim T0-07/T0-08** (mis. run 35121292973) tidak saya buka dari GitHub Actions; saya memverifikasi gerbang lokal (tsc/lint/format/vitest/SQL/mutasi), bukan riwayat run CI spesifik.

## 7. Pernyataan tidak mengubah apa pun

Saya hanya-baca. SATU-SATUNYA berkas yang saya buat adalah laporan ini; tidak ada berkas lain yang saya ubah. Saya tidak mengubah apa pun pada kode proyek. Draf laporan ini hidup di `.laporan-lokal/` (diabaikan Git).
Bukti: `cd /home/user/Resto-Barokah && git status --short` → **kosong** (working tree bersih); `git check-ignore .laporan-lokal/LAPORAN_audit_3c238491…md` → ignored.
Berkas uji-repro (`zz_audit_repro*.test.tsx`) dan `node_modules` HANYA dibuat di salinan terisolasi `$TARGET=$(mktemp -d)` dari `git archive 804ed86f…` (salinan uji terisolasi yang diizinkan paket), TIDAK di repo/checkout bersama dan TIDAK di objek commit target.

## 8. Temuan di luar cakupan (WAJIB — boleh "tidak ada")

| # | Temuan | Mengapa di luar cakupan | Bukti | Syarat dilanjutkan ke audit lain |
|---|---|---|---|---|
| 1 | RPC `simpan_pesanan` (T3-05) yang dijanjikan ROADMAP §2169 **belum ada**; App.tsx memakai insert mentah `pesanan`/`pesanan_item` dari klien | Backend/migrasi (`supabase/`), bukan bidang-antarmuka | `ls supabase/migrations/*simpan_pesanan*` = tak ada; paket §1b menandai `0028_simpan_pesanan.sql` BELUM ADA; `App.tsx:74-101` insert `.from('pesanan')`/`.from('pesanan_item')` | Audit backend/L2: pastikan insert mentah tetap aman (sudah dijaga trigger `picu_item_harga_jujur` 0012 + RLS `WITH CHECK penyewa_saya()` — serangan #8/#9/#12 BERTAHAN), atau ganti ke RPC atomik bersertakan uji |
| 2 | `LayarPelayan.tsx` (dibangun+teruji 3) tidak terdaftar di kontrak `layar.ts` (tak ada id `pelayan`) DAN tidak dirutekan App.tsx | Menyentuh kontrak/backend layar di luar prefiks antarmuka murni | `grep -n "pelayan:" layar.ts` = tak ada entri layar; `periksa-struktur.py` menyebut "layar tambahan (bukan pelanggaran): ['contoh','pelayan']" | Gabung ke perbaikan F-01/F-02 (daftarkan pelayan di kontrak + ruting) |
| 3 | `Navigasi.tsx:104` memakai `<button>` mentah (bukan `TombolAksi`), tak tertangkap `peta-ui.py` Aturan 7 yang hanya memindai `aplikasi/src/layar/` (bukan `komponen/`) | Konsistensi arsitektur; bukan cacat fungsi (tab navigasi tak butuh gerbang izin) | `cat -n Navigasi.tsx:104`; `peta-ui.py:396-403` (rglob hanya `dir_layar`) | Audit mutu-kode: putuskan apakah tab nav wajib `TombolAksi`; bila ya, perluas Aturan 7 ke `komponen/` |
| 4 | `START_DI_SINI.md` merujuk `PRD.md`/`ROADMAP.md`/`TECH_SPEC.md`/`DECISIONS_LOG.md`/`AGENT_OPERATING_GUIDE.md` tanpa prefiks `docs/` (berkas ADA di `docs/`) | Berkas pengguna akar; dijaga sebagian oleh `periksa-panduan.py` (yang hanya memvalidasi `PANDUAN_PENGGUNA.md`) | `grep -oE '`[^`]+\.md`' START_DI_SINI.md`; `find` → semua ada di `docs/` atau `_sistem/templates/` | Audit dokumen-pengguna: seragamkan path (`docs/PRD.md`) agar非-teknis tak mencari di akar |
| 5 | Klaim jumlah kunci bahasa basi: ROADMAP/T1-40 "102 kunci", nyata 269 | Dokumen ROADMAP di luar prefiks antarmuka | `periksa-bahasa.py` → 269 kunci × 4 kamus paritas | Audit L3 dokumen: segarkan angka klaim atau tandai "angka saat itu" (paritas tetap benar) |
