# LAPORAN REVIEW PR INDEPENDEN — pr-01-putaran8 — 2026-09-17

- **Paket review:** `docs/uji/review-pr/PKT-2026-09-17-pr-01-putaran8-SIAP-TEMPEL.md`
- **Commit yang direview:** `7e8c0b952744fd4b604743022acfb1b643c94a9a`
- **Tingkat risiko:** Merah
- **Verdict:** TIDAK-BERSIH

> Peninjau: sesi baru `arena/01a0afdb-resto-barokah` (bukan sesi penulis PR), hanya-baca.
> Lensa yang dipakai sesuai §3 protokol untuk Jalur Merah: **L1 (ancaman & akses)** — RLS/policy/izin/hak
> fungsi, identitas & klaim token, jalur pintas RPC; **L2 (uang & jejak)** — diskon, pembayaran, void,
> harga baris, jejak pelaku, pengaturan uang; **L4 (mutu uji)** — apakah 21 berkas uji SQL + 51 uji unit
> benar-benar bisa MERAH (10 uji mutasi dijalankan sendiri).
> Cara kerja: repo disalin ke `/tmp/rb-review` lalu `git checkout 7e8c0b9` (salinan, bukan repo ini);
> salinan kedua `/tmp/pristine` dari `git archive 7e8c0b9` untuk uji "ruang kerja segar";
> salinan ketiga `/tmp/kalib` untuk bahan kalibrasi; `/tmp/rb-head` untuk kepala PR `f59debd`.
> Repo `/home/user/Resto-Barokah` tidak disentuh selain berkas laporan ini.

## 1. Cakupan diff

**Jumlah berkas berubah: 338** (`git diff --shortstat origin/main...7e8c0b9` → `338 files changed, 41655 insertions(+), 132 deletions(-)`),
**95 commit** (`git rev-list --count origin/main..7e8c0b9` → `95`). Kepala PR sekarang `f59debd` (340 berkas;
selisih 2 berkas = paket putaran8 + baris riwayat, keduanya berkas netral — `python3 alat/review-pr.py --kesiapan`
di kepala → `SIAP … (target 7e8c0b95 + hanya berkas paket)`).

Rincian menurut mesin paket: Merah 55 · Kuning 110 · Hijau 173. Yang **tidak** saya periksa baris-per-baris
dinyatakan terus terang di kolom "Diperiksa".

| # | Berkas | Jalur risiko | Diperiksa | Bukti (perintah/baris) |
|---|---|---|---|---|
| 1 | `supabase/migrations/0001_penyewa_cabang.sql` | Merah | ya, baca penuh | `cat` 69 baris; grant `anon/authenticated` hanya `select`; RLS nyala tanpa policy (deny by default) |
| 2 | `supabase/migrations/0002_pengguna_izin_pengaturan.sql` | Merah | ya, baca penuh | baris 92–93 `batas_maks_potongan_*`, baris 98 `diubah_oleh`, baris 120 `grant select, update on public.pengaturan to anon, authenticated` → PR-02, PR-05 |
| 3 | `supabase/migrations/0003_helper_identitas.sql` | Merah | ya, baca penuh + mutasi | baris 66 `join … and p.aktif` di `cabang_saya()`; baris 69 `auth.jwt() ->> 'cabang_id'` → PR-12, PR-15 |
| 4 | `supabase/migrations/0004_pola_rls.sql` | Merah | ya, baca penuh + mutasi | baris 6–7 klaim "TIDAK ada subquery langsung" vs baris 50–58 `pengguna_pilih`; baris 81–84 `pengaturan_ubah` → PR-05, PR-19 |
| 5 | `supabase/migrations/0005_izin_berjenjang.sql` | Merah | ya, baca penuh + mutasi | baris 10 "Tidak ada rumus izin kedua"; baris 300–309 revoke/grant `boleh(...)`; matriks 50 baris izin bawaan → PR-11, bahan kalibrasi D3 |
| 6 | `supabase/migrations/0006_pin.sql` | Merah | ya, baca penuh | baris 135 `select pc.peran` di dalam `izin_efektif_untuk`, baris 179 "satu rumus, dua pintu", baris 212–215 revoke hanya `service_role` → PR-11 |
| 7 | `supabase/migrations/0007_katalog.sql` | Merah | ya, bagian harga/stok/policy | baris 150 `cabang_pantau_saya()` (memakai `cabang_ids_saya()`, bukan klaim token); policy 406–467; `catat_stok` hanya-buku-besar |
| 8 | `supabase/migrations/0008_meja.sql` | Merah | ya, baca penuh (65 baris) | policy `meja_hapus` ada + `grant … delete … to authenticated` (baris 63) — hapus meja tidak berjejak, dicatat sebagai sisa risiko, bukan temuan (belum ada uang terkait) |
| 9 | `supabase/migrations/0009_pesanan.sql` | Merah | ya, baca penuh | baris 60 `harga_saat_itu` (klien isi), baris 66 `subtotal`, baris 276–282 `pesanan_item_tambah` untuk kasir/pelayan; penjaga status 144–188 → PR-03 |
| 10 | `supabase/migrations/0010_pembayaran.sql` | Merah | ya, baca penuh (561 baris) | baris 313 penjaga `total <= 0`; 351–364 cabang `jenis` (tanpa `promo`); 377–388 tumpuk & cap subtotal; 415–466 bukti PIN void; 510–546 policy → PR-01, PR-02, PR-04, PR-16 |
| 11 | `supabase/migrations/0011_peran_tunggal.sql` | Merah | ya, baca penuh (443 baris) | baris 14 `drop column … peran`; 62–122 `izin_efektif` ditulis ulang (tidak lagi memanggil `izin_efektif_untuk`); 128–152 tabel `percobaan_simpan_pin` (deny-by-default); 165–211 `pin_lemah`; 241–339 `simpan_pin`; 346–435 `verifikasi_pin` → PR-10, PR-11, PR-13 |
| 12 | `supabase/functions/verifikasi_pin/index.ts` | Merah | ya, baca penuh (96 baris) | `Deno.serve`; hanya POST; tanpa `console.*`; tanpa `service_role`; meneruskan `Authorization`; `p_perangkat` diambil dari badan permintaan (baris 58) → lapisan perangkat tetap kiriman klien (sudah diakui `docs/KEAMANAN.md:40`) |
| 13 | `.github/workflows/ci.yml` | Merah | ya, baca penuh (85 baris) + riwayat | 13 langkah = isi `periksa-semua.sh`; `git log -p origin/main..7e8c0b9 -- .github/workflows/ci.yml \| grep '^-'` → tidak ada langkah/ambang yang dihapus; `npm audit --audit-level=low` ada (baris 53) |
| 14 | `_sistem/validate_system.py` | Merah | ya, diff dibaca penuh | `git diff origin/main...7e8c0b9 -- _sistem/validate_system.py` → pemindaian diperluas ke seluruh `docs/**/*.md`; pengecualian sempit `docs/uji/kalibrasi/bahan-*` dan `LAPORAN_*` (bukti auditor) — bukan pelemahan gerbang |
| 15 | `alat/review-pr.py` | Kuning | ya, dibaca penuh (827 baris) + ditembak 3 laporan uji | baris 109 (klaim kosong), 372–388 (baris kepala tabel ikut terhitung), 457–461 (verdict), 464–470 (kata kunci), 477 (paket), 665–720 (pembuat bahan kalibrasi) → PR-06, PR-09, PR-14, PR-17, PR-18 |
| 16 | `alat/uji-sql.mjs` | Kuning | ya, dibaca penuh (297 baris) | tiruan `crypt()`/`gen_salt()` berlabel "BUKAN pengganti produksi"; `uji.klaim()` mengarang `request.jwt.claims` → PR-15; rollback per berkas uji benar |
| 17 | `alat/periksa-panduan.py` · `periksa-rujukan.py` · `periksa-temuan-audit.py` · `periksa-buku-uji.py` · `audit-independen.py` · `periksa-fondasi-independen.py` · `periksa-roadmap.py` · `periksa-fungsi-pin.py` · `bantu_uji_diri.py` | Merah/Kuning | dijalankan semua + `--uji-diri`; `periksa-temuan-audit.py` dibaca bagian bukti (baris 98–127) | `periksa-temuan-audit.py` hanya memeriksa **keberadaan berkas** bukti (`jalur_ada`), bukan isinya → PR-08 |
| 18 | `alat/kalibrasi-cacat.json` | Kuning | **TIDAK dibuka** (sengaja) | hanya `ls -l` (3923 byte) + peran menurut kode (`KATALOG` di `review-pr.py:29`, `audit-independen.py:29`) — melihat isinya berisiko membocorkan kunci kalibrasi |
| 19 | `supabase/tes/*.sql` (21 berkas) | Kuning | ya: `helper`, `izin`, `kredensial_pin`, `pin`, `pin_batas_pasang`, `percobaan_pin_perangkat`, `persetujuan_void`, `diskon_persen`, `pembayaran`, `gerbang_uang`, `penjaga_stok`, `status_pesanan`, `jejak_pelaku`, `rls_semua_tabel`, `rls_pengguna`, `rls_penyewa`, `isolasi_lintas_penyewa`, `peran_tunggal`, `katalog`, `meja`, `pesanan` dijalankan; `helper.sql` & `persetujuan_void.sql` & `diskon_persen.sql` & `rls_semua_tabel.sql` dibaca penuh | `node alat/uji-sql.mjs` → `uji: 21 LULUS · 0 GAGAL`; `grep -n "cabang_saya" supabase/tes/helper.sql` → tidak ada satu pun assertions `cabang_saya()` untuk akun nonaktif → PR-12 |
| 20 | `alat/sql/data-uji.sql` | Kuning | ya, baca penuh | fixture 2 resto/3 cabang/7 akun; pesanan `eeee…0010` subtotal 54.000 total 62.100 — dipakai semua probe saya |
| 21 | `docs/uji/kalibrasi/pr-bahan-2026-09-17.diff` | Kuning | ya, baca penuh (51 baris) + 4 cara pasang | `git apply` → exit 128 `corrupt patch at line 39`; `git apply --recount` → `patch does not apply`; `patch -p1 --fuzz=5` → exit 2 → PR-06 |
| 22 | `docs/uji/BUKU_UJI_PEMILIK.md` | Kuning | ya, baca penuh (7 baris uji) + **dijalankan** | U-02 → `sh: 1: prettier: not found` rc=127; U-03 → `ERR_MODULE_NOT_FOUND @electric-sql/pglite` rc=1; U-04 menunjuk paket putaran7 → PR-07, PR-10 |
| 23 | `docs/KEAMANAN.md` · `docs/TECH_SPEC.md` · `docs/PRD.md` · `docs/ROADMAP.md` · `docs/DECISIONS_LOG.md` | Merah/Kuning | ya, bagian yang menyatakan perilaku (prinsip §2 KEAMANAN, §4.1/§9 TECH_SPEC, Aturan Bisnis & M3 PRD, bukti tugas T0-01/T1-03/T1-06/T1-10/T1-15/T1-23) | `KEAMANAN.md:24` "Tidak ada angka uang dari perangkat … sudah berlaku sejak T1-10" vs probe P3; `TECH_SPEC.md:318` vs `0003:69`; `PRD.md:261`/`267`/`99`; `ROADMAP.md:61` (57 woff2) → PR-03, PR-05, PR-08, PR-15 |
| 24 | `docs/uji/AUDIT_RIWAYAT.md` · `REVIEW_PR_RIWAYAT.md` · `PROTOKOL_*` · `PROMPT_*` · laporan audit · paket audit | Hijau/Kuning | ya, dibaca (riwayat temuan per baris, status DITUTUP/TERBUKA) | `AUDIT_RIWAYAT.md:58` B F-13 **DITUTUP** dengan angka 57 → PR-08; `REVIEW_PR_RIWAYAT.md` §2 kalibrasi review PR = "belum ada" → bagian 8 baris 2 |
| 25 | `aplikasi/src/**` (67 berkas) + `aplikasi/alat/**` (6) + konfigurasi aplikasi (22) | Kuning | **tidak baris-per-baris** — diperiksa lewat gerbang + `periksa-struktur/komponen-env/uji/kontras` | `cd aplikasi && npm test` → `Test Files 7 passed (7) · Tests 51 passed (51)`; `npm run lint`/`typecheck`/`build` bersih; `uji-kontras.py` → `166 lolos, 0 gagal`. Belum ada layar uang/izin (semua folder layar hanya `.gitkeep`), jadi tidak ada logika klien yang bisa menyelundupkan uang |
| 26 | `prototipe/**` (58 berkas) + `docs/desain/**` (59 berkas, 55 biner) + 19 font `.woff2` + `_log-sesi`, `_salinan-meta`, berkas akar (9) | Hijau | struktur + rujukan saja (bukan kode produksi) | `periksa-rujukan.py` → `11 dokumen pengikat · 147 rujukan … LOLOS`; `git ls-tree -r --name-only 7e8c0b9 \| grep -c '\.woff2$'` → 38 (19 aplikasi + 19 prototipe) → PR-08 |
| 27 | `docs/uji/review-pr/PKT-*putaran5/6/7*` (6 berkas) | Hijau | ya, dibandingkan dengan paket putaran8 yang saya terima | `git show f59debd:…putaran8.md` identik isinya dengan yang ditempel Lee; putaran7 mengikat `183a3c1` → PR-10 |

**Yang tidak diperiksa sama sekali:** isi `alat/kalibrasi-cacat.json` (sengaja, alasan kalibrasi), 55 berkas
gambar/biner `docs/desain` (hanya dihitung & diverifikasi lewat pemeriksa), dan `skills/` (0 berkas di diff ini).

## 2. Klaim yang dibantah

| # | Klaim | Cara membantah | Hasil nyata |
|---|---|---|---|
| 1 | *(kosong di paket — baris "1." tidak berisi apa pun)* | `python3 -c` memuat `alat/review-pr.py` sebagai modul lalu memanggil `klaim_dari_commit("origin/main","7e8c0b9…")` dan mencetak panjang tiap klaim | `1. [0 huruf] ''` — klaim pertama **memang string kosong** (bug presedens `if not b or b.startswith("- ") and len(b) > 12` di `review-pr.py:109`). Tidak ada yang bisa dibantah dari klaim kosong → PR-17 |
| 2 | "Paket baru docs/uji/review-pr/PKT-2026-09-17-pr-01-putaran7-SIAP-TEMPEL.md" | `git show f59debd:docs/uji/review-pr/ \| cat`; `git show f59debd:docs/uji/BUKU_UJI_PEMILIK.md \| grep -n PKT-` | Benar berkas itu ada, tetapi **sudah digantikan** putaran8 di kepala PR (`REVIEW_PR_RIWAYAT` baris 3 "digantikan baris 4") sementara buku uji pemilik masih menyuruh Lee menempel putaran7 (target `183a3c1`) → **DIBANTAH sebagai rujukan yang masih berlaku**: PR-10 |
| 3 | "REVIEW_PR_RIWAYAT baris 3 (paket berlaku) + baris 2 ditandai digantikan" | `git show f59debd:docs/uji/REVIEW_PR_RIWAYAT.md \| head -20` | **BASI di kepala PR**: yang jadi "paket berlaku" sekarang **baris 4** (putaran8, target `7e8c0b9`); baris 3 justru ditandai digantikan. Klaim ini disalin dari badan commit `a34c8d4`, bukan keadaan commit yang direview → bukti bahwa §1/§3 paket menggambarkan commit lama, bukan `7e8c0b9` |
| 4 | Uji otomatis membuktikan perilaku baru/bebas regresi pada commit ini (bukan commit sebelumnya) | `git checkout 7e8c0b9` di salinan → `node alat/uji-sql.mjs`; `cd aplikasi && npm test`; `gh api repos/…/commits/7e8c0b9…/check-runs`; lalu 10 uji mutasi + 7 probe serangan | **SEBAGIAN DIBANTAH.** Uji memang jalan di commit ini (`21 LULUS · 0 GAGAL`, `51 passed`, CI `conclusion: success` ×2 di `7e8c0b9`). Tetapi "bebas regresi" tidak terbukti untuk 4 hal: (a) mutasi D1 (hapus `p.aktif` di `cabang_saya()`) → **21 LULUS · 0 GAGAL** (PR-12); (b) probe P1/P3/P4/P5/P6/P7 menembus penjaga uang & jejak yang diklaim (PR-01…PR-05); (c) `izin_efektif_untuk` rusak runtime sejak 0011 tanpa satu pun uji yang menyentuhnya (PR-11); (d) 2 dari 5 baris Buku Uji Pemilik gagal dijalankan di ruang kerja segar (PR-07) |
| 5 | Tidak ada gerbang keamanan/CI yang dilemahkan (ambang diturunkan, uji dimatikan, revoke/hak dicabut dihapus) | `git log -p origin/main..7e8c0b9 -- .github/workflows/ci.yml \| grep -E '^- +(run|name):'`; `git log --oneline origin/main..7e8c0b9 \| wc -l`; `grep -n 'MIN_' alat/periksa-panduan.py alat/review-pr.py`; `npm audit` di 3 tempat; mutasi dokumen (hapus AL-9) | **TERBUKTI untuk CI & ambang**: tidak ada langkah CI yang dihapus (95 commit, hanya penambahan); `MIN_ALUR = 12` = jumlah nyata (`grep -cE '^### AL-[0-9]+' PANDUAN_PENGGUNA.md` → 12) dan menghapus AL-9 → `HASIL: GAGAL — alur terlalu sedikit: 11 (minimum 12)` rc=1, pulihkan → `LOLOS` rc=0; `npm audit` → `found 0 vulnerabilities` di `aplikasi`, akar, dan `alat`. **TETAPI** gerbang laporan review-nya sendiri longgar di 3 titik (verdict, kalibrasi, kata kunci) → PR-09, PR-14; dan gerbang penutup temuan audit hanya memeriksa keberadaan berkas bukti → PR-08 |
| 6 | Perubahan pada jalur uang/keamanan/data pelanggan tidak bisa dilewati lewat pemanggilan langsung (RPC/API) | 7 probe SQL sebagai `authenticated` (kasir Rina `…0004`, owner `…0002`) lewat `node alat/uji-sql.mjs /tmp/probe/*.sql` — persis jalur PostgREST (grant + policy, tanpa `service_role`) | **DIBANTAH.** (a) `insert into diskon_transaksi … jenis='promo', nilai=54000` sebagai kasir berbatas 25.000/5% → **DITERIMA** (PR-01); (b) `insert into pesanan_item … harga_saat_itu=1, subtotal=5` untuk menu 27.000 → **DITERIMA**, `boleh('ubah_harga')` = false (PR-03); (c) 20 diskon @2.700 dengan `tumpuk_diskon=true` → total **54.000 = 100% subtotal** walau owner memasang cap 10%/5.000 (PR-02); (d) 1 bukti PIN → **3 pembatalan** di 2 pesanan (PR-04); (e) `update pengaturan set pajak=0, diubah_oleh=<kasir>` → **DITERIMA** (PR-05); (f) 5 tebakan PIN salah atas nama owner → PIN benar owner **terkunci 15 menit** (PR-13). Yang **tidak** bisa ditembus (saya coba, gagal): mengubah `total`/`subtotal` pesanan langsung, `delete from pesanan`, membaca `kredensial_pin`, memanggil `izin_efektif_untuk`/`boleh_untuk` sebagai klien, menulis `percobaan_pin` langsung, memalsukan `kasir_id`/`pelaku_id` |
| 7 | Dokumen yang menyatakan perilaku (fondasi, buku induk, panduan Lee) sudah ikut diperbarui — tidak ada klaim basi | `find aplikasi -name '*.woff2' \| wc -l` di `git archive 7e8c0b9`; `grep -n '57 berkas' docs/ROADMAP.md`; `grep -n 'Tidak ada angka uang' docs/KEAMANAN.md`; `sed -n '315p' docs/ROADMAP.md`; `sed -n '92p;318p' docs/TECH_SPEC.md`; `sed -n '5,7p' supabase/migrations/0004_pola_rls.sql` | **DIBANTAH (5 klaim basi/salah):** `ROADMAP.md:61` menulis **57** `.woff2`, perintah yang sama menghasilkan **19** (PR-08); `KEAMANAN.md:24` "tidak ada angka uang dari perangkat … sudah berlaku sejak T1-10" padahal `harga_saat_itu`/`subtotal` baris item dari perangkat (PR-03); `TECH_SPEC.md:318` menyebut cabang diambil dari tabel `pengguna`, padahal `0003:69` mengambilnya dari klaim JWT (PR-15); `ROADMAP.md:315` (DoD T1-15) masih menyebut celah "total = 0" yang sudah ditutup `0010:313` di PR ini (PR-19); `0004:6–7` mengklaim policy tanpa subquery langsung padahal `0004:52–57` memakainya (PR-19) |
| 8 | Setiap berkas baru benar-benar dipakai (tidak ada berkas mati / rujukan menggantung) | `for f in mulai-sesi.py pulihkan-git.sh bantu_uji_diri.py kalibrasi-cacat.json …; do grep -rl "$f" --include='*.md' --include='*.sh' --include='*.yml' --include='*.py' .; done`; `python3 alat/periksa-rujukan.py`; `grep -rn 'napi-rs/canvas' prototipe/alat/gambar.js` | **SEBAGIAN DIBANTAH.** Semua alat baru punya pemakai (`mulai-sesi.py` 12 rujukan, `pulihkan-git.sh` 13, `bantu_uji_diri.py` diimpor 4 pemeriksa, dependensi akar dipakai `prototipe/alat/gambar.js:16`); `periksa-rujukan.py` → `147 rujukan … LOLOS`. **Tetapi ada kode mati yang rusak:** `izin_efektif_untuk()` masih berisi cabang `select pc.peran` atas kolom yang dihapus 0011 → `column pc.peran does not exist` (PR-11); dan dua kolom `pengaturan.batas_maks_potongan_persen/nominal` tidak dibaca satu pun baris kode (`grep -rn batas_maks_potongan --include='*.sql' --include='*.ts'` → hanya definisinya di `0002:92–93`) → PR-02 |

## 3. Pemeriksaan gerbang

Semua dijalankan di salinan `/tmp/rb-review` pada `git checkout 7e8c0b9` (bukan di repo peninjau).

| # | Perintah | Hasil nyata (ringkas) |
|---|---|---|
| 1 | `bash aplikasi/alat/periksa-semua.sh` | **`SEMUA PEMERIKSAAN LOLOS.`** rc=0. Rinci: prettier bersih · eslint bersih · `tsc -b --noEmit` bersih · vitest `Test Files 7 passed (7) / Tests 51 passed (51)` · vite build `✓ built in 1.26s` · `npm audit` `found 0 vulnerabilities` · `uji: 21 LULUS · 0 GAGAL` · `validate_system` `PASS` · `periksa-fungsi-pin` `9 lolos, 0 gagal` · roadmap `192 tugas … LOLOS` · fondasi `BERSIH` · audit `--uji-diri` `TERKALIBRASI 3/3` + `LOLOS` · panduan `680 baris · 12 alur … LOLOS` + 4 mutasi OK · rujukan `147 rujukan … LOLOS` + 3 mutasi OK · temuan audit `27 temuan terlacak semua … LOLOS` + 4 mutasi OK · buku uji `7 baris … LOLOS` + 4 mutasi OK · `review-pr --uji-diri` `LOLOS` (3 contoh) · struktur/komponen-env/uji/kontras `166 lolos, 0 gagal` |
| 2 | `node alat/uji-sql.mjs` | `Menerapkan 11 migrasi: OK` semua · `uji: 21 LULUS · 0 GAGAL` · `HASIL: LOLOS` · rc=0 |
| 3 | `git diff origin/main...origin/arena/01a0a8a2-resto-barokah` | kepala = `f59debd`; `--shortstat` → `340 files changed, 41983 insertions(+), 132 deletions(-)`; selisih dengan commit yang direview (`338`) = `docs/uji/review-pr/PKT-…putaran8{,-SIAP-TEMPEL}.md` + 1 baris `REVIEW_PR_RIWAYAT.md`. **Yang tidak disebut di deskripsi:** diff ini sebenarnya **seluruh proyek** (95 commit, 67 berkas `aplikasi/src`, 11 migrasi, 21 uji SQL, 58 berkas `prototipe`, 59 aset desain) — §1 paket hanya memuat 12 judul commit terakhir (`review-pr.py:158` `[:12]`) → PR-18 |
| 4 | `python3 _sistem/validate_system.py` | `SYSTEM-BUILDING-APLIKASI VALIDATOR: PASS` rc=0 |
| 5 | `python3 alat/periksa-roadmap.py` | `Tugas: 192 … HASIL: LOLOS — semua pemeriksaan roadmap terpenuhi.` (⚠️ DECISIONS 101 tugas, ❓ 7 rujukan tertangguh "semua sah") |
| 6 | `python3 alat/periksa-panduan.py` | `680 baris · 12 alur · 4 blok prompt · 25 perintah · 12 mekanisme · 71 rujukan … HASIL: LOLOS` |
| 7 | `python3 alat/audit-independen.py --uji-diri` | `NILAI KALIBRASI AUDITOR ditemukan 3/3 (100%) · temuan palsu 0 · HASIL: TERKALIBRASI` lalu contoh buruk ditolak (`kalibrasi-sebagian.md: hasil=1`) dan `HASIL: LOLOS — … jalur pulang laporan … terbukti bekerja` rc=0 |
| 8 | `python3 alat/review-pr.py --uji-diri` | `[OK] bagus.md hasil=0 · [OK] buruk-tanpa-bukti.md hasil=1 · [OK] buruk-klaim-kurang.md hasil=1 · HASIL: LOLOS` rc=0 — **tetapi** 3 laporan uji bikinan saya yang melanggar protokol justru **LOLOS** (baris 15) → PR-09, PR-14 |
| 9 | **Uji mutasi RLS/izin — bahan kalibrasi dipasang satu per satu** (`python3 /tmp/probe/mutasi.py`, tiap mutasi `git checkout -- supabase/migrations` dulu, lalu `node alat/uji-sql.mjs`) | D2 `pengaturan_pilih using (penyewa_id is not null)` → `19 LULUS · 2 GAGAL` (`rls_pengguna.sql`, `rls_semua_tabel.sql`) = **uji izin/RLS GAGAL saat dilonggarkan** ✔; D3 hapus `revoke … boleh(text, uuid)` → `20 LULUS · 1 GAGAL` (`izin.sql`) ✔; D4 `> total + 100000` → `19 LULUS · 2 GAGAL` (`gerbang_uang.sql`, `pembayaran.sql`) ✔; **D1 hapus `and p.aktif` di `cabang_saya()` → `21 LULUS · 0 GAGAL` rc=0 = gerbang BUTA** ✘ → PR-12; keempatnya sekaligus → `16 LULUS · 5 GAGAL` |
| 10 | **Uji mutasi tambahan milik peninjau (M1–M5)** | M1 batas persen diskon dimatikan → `18 LULUS · 3 GAGAL`; M2 bukti PIN void dibalik → `19 LULUS · 2 GAGAL`; M3 `BATAS_AKUN 5→500` → `19 LULUS · 2 GAGAL`; M4 `pin_lemah` selalu null → `19 LULUS · 2 GAGAL`; M5 pembatas anti-oracle `20→1000000` → `20 LULUS · 1 GAGAL`; **pulihkan → `21 LULUS · 0 GAGAL` rc=0** (gerbang uang/PIN terbukti bisa MERAH) |
| 11 | **Uji mutasi gerbang dokumen** (klaim `MIN_ALUR = 12` di commit ini) | hapus blok `### AL-9` (983 huruf) dari `PANDUAN_PENGGUNA.md` → `PERIKSA BUKU PEDOMAN INDUK — 669 baris · 11 alur … HASIL: GAGAL — [X] alur terlalu sedikit: 11 (minimum 12)` rc=1; pulihkan → `HASIL: LOLOS` rc=0; `git status --short` di salinan → kosong |
| 12 | `cd aplikasi && npm test` · `npm run lint` · `npm run typecheck` · `npm run build` | `Tests 51 passed (51)` · lint 0 error · tipe bersih · build rc=0 (47 modul) |
| 13 | `npm audit --audit-level=low` di `aplikasi`, akar repo, dan `alat` | `found 0 vulnerabilities` di ketiganya |
| 14 | `gh api repos/With-AI-Agent/Resto-Barokah/commits/7e8c0b9…/check-runs` | 4 run: **2 `conclusion: success`** (14:22:49→14:23:27 dan 14:22:46→14:23:33) + 2 `cancelled` (dibatalkan `concurrency` karena push beruntun) → CI **hijau pada commit yang direview**; `gh api …/commits/253d129…/check-runs` → `total_count 0` (base belum pernah CI) |
| 15 | 3 laporan uji untuk menembak pemeriksa laporan (`python3 alat/review-pr.py --periksa-laporan /tmp/probe/laporan-uji/*.md`) | A: temuan **K-1 TERVERIFIKASI** + verdict `BERSIH-DENGAN-CATATAN` → **`HASIL: LOLOS` rc=0** (protokol §6 mewajibkan TIDAK-BERSIH); B: paket menunjuk berkas tidak ada + kalibrasi "Tidak dijalankan" → **`LOLOS` rc=0** hanya `[catatan]`; C: Jalur Merah, baris gerbang berisi kata "mutasi/RLS/izin/L1 L2 L4/pemulihan/rollback" **tanpa satu pun keluaran perintah** → **`LOLOS` rc=0** → PR-09, PR-14 |
| 16 | `python3 alat/review-pr.py --kesiapan` (di kepala `f59debd`) | `SIAP: commit f59debd2 punya paket review berlaku: PKT-…putaran8… (target 7e8c0b95 + hanya berkas paket)` rc=0 → mekanisme berkas-netral bekerja; jadi "kepala bergerak 1 commit karena commit paket itu sendiri" **bukan** temuan |
| 17 | `git apply docs/uji/kalibrasi/pr-bahan-2026-09-17.diff` (+ `--recount`, `-C0 --recount`, `patch -p1 --fuzz=5`) | `error: corrupt patch at line 39` exit 128 · `patch failed: supabase/migrations/0010_pembayaran.sql:293 … does not apply` exit 1 · exit 1 · `malformed patch at line 39` exit 2 → **bahan kalibrasi tidak bisa dipasang** → PR-06 |
| 18 | 7 probe serangan di salinan (`node alat/uji-sql.mjs /tmp/probe/pN_*.sql`) | P1 diskon `promo` 54.000 oleh kasir → `LULUS` (insert diterima) · P2 `izin_efektif_untuk(…, cabang)` → `GAGAL column pc.peran does not exist` · P3 `harga_saat_itu=1` → `LULUS` (diterima) · P4 3 void dari 1 bukti PIN → `LULUS` (diterima semua) · P5 kunci PIN owner oleh kasir → `LULUS` (PIN benar owner ditolak, pesan "terkunci sementara") · P6 jejak pengaturan dikarang/dihapus → `LULUS`; kontrol pembayaran ber-`kasir_id` karangan → `DITOLAK` · P7 20 diskon tumpuk = 54.000 → `LULUS` |
| 19 | `python3 alat/periksa-buku-uji.py` + menjalankan barisnya sendiri | pemeriksa → `2 baris lakukan · 5 baris coba · 7 baris total … LOLOS` (+4 mutasi OK); **tetapi** dijalankan sungguh di `git archive 7e8c0b9` (ruang kerja segar): U-02 `sh: 1: prettier: not found` rc=127, U-03 `Error [ERR_MODULE_NOT_FOUND]: Cannot find package '@electric-sql/pglite'` rc=1 → PR-07 |

### Rencana pemulihan & sisa risiko (wajib Jalur Merah, bahasa sederhana)

**Kalau perubahan ini salah, apa yang dilakukan supaya aman (rencana pemulihan):**
1. **Jangan merge dulu** PR #1. Semua temuan K-2 di bawah ada di lapisan database; sekali migrasi dijalankan di
   Supabase nyata, berkas migrasi **tidak boleh disunting** (aturan repo sendiri, `0001:11–12`) — perbaikannya
   wajib berupa **migrasi baru** (0012+), bukan mengubah 0010/0011.
2. Urutan pemulihan bila sudah terlanjur dipasang di proyek nyata: (a) pasang migrasi baru yang menutup PR-01…PR-05
   sebelum satu pun transaksi nyata masuk; (b) bila sudah ada transaksi yang memakai diskon `promo`/harga karangan,
   **jangan dihapus** — tandai, hitung ulang selisihnya, dan catat di `docs/teknis/BUKU_INSIDEN.md`; (c) bila perlu
   menarik seluruh perubahan, satu-satunya jalur aman adalah `git revert` pada cabang baru (bukan `reset --hard`
   /`push --force`, dilarang `docs/AGENT_OPERATING_GUIDE.md:54`) lalu jalankan ulang
   `bash aplikasi/alat/periksa-semua.sh` sampai `SEMUA PEMERIKSAAN LOLOS.`
3. Pemulihan untuk PR-06/PR-07 (mekanisme & buku uji) tidak menyentuh data: jalankan ulang
   `python3 alat/review-pr.py --kalibrasi-pr-siapkan` **setelah** pembuatannya diuji dengan `git apply --check`,
   dan tambahkan langkah pemasangan pustaka ke baris U-02/U-03.

**Apa yang bisa salah kalau tetap di-merge apa adanya:** kasir bisa membuat pesanan "Rp 1" untuk menu Rp 27.000,
mencatat diskon 100% tanpa persetujuan atasan, membatalkan banyak pesanan dengan satu persetujuan PIN, dan
perubahan pajak/service bisa tercatat atas nama orang lain. Pegawai mana pun bisa mengunci PIN owner selama 15
menit. Tidak ada satu pun dari itu yang membuat CI merah.

**Sisa risiko yang tetap ada sesudah perbaikan (jujur):** (1) uji SQL berjalan di **PGlite** dengan `crypt()` tiruan
bukan bcrypt asli (`alat/uji-sql.mjs:88–118`) — kekuatan hash & perilaku `pgcrypto` nyata belum teruji, perlu
diulang di Supabase nyata (T0-08); (2) lapisan pembatas PIN **per perangkat** masih memakai nama perangkat kiriman
klien (sudah diakui `docs/KEAMANAN.md:40`, menunggu T1-24); (3) sapuan anti-bocor `rls_semua_tabel.sql` hanya
menyapu tabel berkolom `penyewa_id` (temuan B F-14 masih **TERBUKA** → T1-22), jadi `pesanan_item`, `pembayaran`,
`diskon_transaksi`, `pembatalan` belum tersapu otomatis; (4) Edge Function belum pernah ditembak sungguhan (Deno
tidak tersedia di ruang kerja); (5) pembayaran bersamaan (dua kasir, satu pesanan) belum bisa diuji di PGlite → PR-16
berstatus DUGAAN; (6) kunci/bahan kalibrasi hidup di dalam repo yang sama (`alat/kalibrasi-cacat.json`) — kontrol
proses, bukan kontrol teknis (diakui `PROTOKOL_AUDIT_INDEPENDEN`/§9.3 protokol review).

## 4. Temuan

### [PR-01] Diskon `jenis='promo'` melewati seluruh pemeriksaan izin — kasir bisa mencatat diskon 100% subtotal
- **Tingkat:** K-2
- **Artefak:** `supabase/migrations/0010_pembayaran.sql:351-364` (cabang `if new.jenis = 'manual' … elsif new.jenis = 'voucher'`, tidak ada cabang `promo`) + `0010:531-536` (policy `diskon_transaksi_tambah` mengizinkan `kasir`)
- **Klaim yang dilanggar:** klaim paket #6 "Perubahan pada jalur uang/keamanan/data pelanggan tidak bisa dilewati lewat pemanggilan langsung (RPC/API)"; `0010:17-19` "DISKON TIDAK BOLEH MELEBIHI BATAS … batas kasir (25.000 / 5%) dan admin (50.000 / 10%) berlaku di sini"
- **Bukti:** `node alat/uji-sql.mjs /tmp/probe/p1_diskon_promo.sql` → `LULUS` (rc=0). Isi probe: sebagai kasir Rina (`uji.klaim('90000000-…-0004')`, `set local role authenticated`) kontrol `izin_efektif('beri_diskon', cabang)` → `batas_nominal 25000`, lalu `insert into public.diskon_transaksi (pesanan_id, jenis, persen, nominal, nilai, alasan) values ('eeee…0010','promo',null,54000,54000,'promo karangan kasir')` → **diterima**, `select d.nilai … = 54000`. Pembanding: `jenis='manual'` dengan nilai sama **ditolak** (`supabase/tes/diskon_persen.sql` harapan 1 & 2)
- **Skenario gagal:** kasir yang batas diskonnya Rp 25.000/5% memilih `jenis='promo'` saat mengirim `POST /rest/v1/diskon_transaksi` (grant `insert` ke `authenticated` di `0010:550`, policy hanya memeriksa penyewa+peran) → pesanan Rp 54.000 tercatat berdiskon Rp 54.000 tanpa persetujuan atasan dan tanpa `alasan` (CHECK alasan hanya untuk `manual`, `0010:120`). Begitu `hitung_total` (T1-15) membaca `diskon_transaksi`, uang itu benar-benar hilang; barisnya **append-only** (tidak ada grant `update/delete` ke `authenticated`) sehingga tidak bisa dikoreksi diam-diam — tapi juga tidak bisa dihapus
- **Dugaan penyebab:** pemicu ditulis mengikuti dua jenis diskon yang sudah punya alur (manual & voucher); `promo` ada di enum CHECK (`0010:111`) tetapi belum punya mesin (voucher/promo = T1-19/T1-20/T5-04), sehingga cabang `else`-nya kosong dan "belum ada aturannya" menjadi "tidak ada aturannya"
- **Cara membuktikan perbaikan:** probe yang sama harus GAGAL — `node alat/uji-sql.mjs /tmp/probe/p1_diskon_promo.sql` → `GAGAL` dengan pesan penolakan; dan uji baru di `supabase/tes/` (mis. `diskon_promo.sql`) berisi `uji.harap_gagal(insert … jenis='promo' …)` sebagai kasir, ikut `node alat/uji-sql.mjs` → `22 LULUS · 0 GAGAL`
- **Status verifikasi:** TERVERIFIKASI

### [PR-02] `pengaturan.batas_maks_potongan_persen/nominal` tidak pernah dibaca — dengan `tumpuk_diskon` nyala, kasir menumpuk diskon sampai 100% subtotal
- **Tingkat:** K-2
- **Artefak:** `supabase/migrations/0002_pengguna_izin_pengaturan.sql:92-93` (dua kolom cap) · `0010_pembayaran.sql:377-388` (hanya `tumpuk_diskon` dan `≤ subtotal` yang diperiksa)
- **Klaim yang dilanggar:** `docs/PRD.md:261` Aturan Bisnis 2 "pengaturan dapat mengizinkan tumpuk dengan **batas maksimal total potongan**"; `docs/TECH_SPEC.md:152` (kolom itu didaftar sebagai pelaksana "Aturan Bisnis 1, 2"); klaim paket #8 "setiap berkas baru benar-benar dipakai (tidak ada berkas mati)"
- **Bukti:** `grep -rn "batas_maks_potongan" --include='*.sql' --include='*.ts' --include='*.tsx' .` → **hanya 2 kecocokan, keduanya definisi kolom** (`0002:92`, `0002:93`); tidak ada satu pun fungsi/pemicu yang membacanya. Lalu `node alat/uji-sql.mjs /tmp/probe/p7_tumpuk_diskon.sql` → `LULUS`: owner menyetel `tumpuk_diskon=true, batas_maks_potongan_persen=10, batas_maks_potongan_nominal=5000`, kemudian kasir menyisipkan 20 diskon `manual` @Rp 2.700 (masing-masing 5% = di dalam batasnya) → `sum(nilai) = 54000` = **100% subtotal**, cap resto 10%/Rp 5.000 diabaikan
- **Skenario gagal:** owner menyalakan "boleh tumpuk diskon" (fitur sah menurut PRD) dan memasang cap total 10%. Kasir memecah satu diskon besar menjadi banyak diskon kecil — tiap baris lolos pemeriksaan per-baris karena persen efektif dihitung `nilai/subtotal` **per baris**, bukan kumulatif (`0010:355-361`) — sehingga pesanan gratis tanpa pernah menyentuh batas izinnya dan tanpa PIN atasan
- **Dugaan penyebab:** penjaga diskon ditulis per-baris (`new.nilai`) dan cap tingkat resto dijadwalkan di tugas lain (`T5-04` "Diskon: satu per transaksi (bawaan) + opsi tumpuk dengan batas", masih `[ ]`), sementara setengah fitur yang berbahaya (tumpuk boleh) sudah dipasang lebih dulu tanpa cap
- **Cara membuktikan perbaikan:** `node alat/uji-sql.mjs` dengan uji baru yang (a) menyalakan `tumpuk_diskon` + cap 10%/5.000, (b) menyisipkan diskon kedua sehingga total > cap → `uji.harap_gagal`, dan probe `p7_tumpuk_diskon.sql` berubah jadi `GAGAL`; `grep -rn batas_maks_potongan supabase/migrations/*.sql` menunjukkan kolom itu **dibaca** di dalam pemicu (bukan hanya didefinisikan)
- **Status verifikasi:** TERVERIFIKASI

### [PR-03] `pesanan_item.harga_saat_itu` & `subtotal` bebas diisi klien; izin `ubah_harga` tidak pernah ditegakkan di mana pun
- **Tingkat:** K-2
- **Artefak:** `supabase/migrations/0009_pesanan.sql:60` (`harga_saat_itu` NOT NULL, diisi pemanggil), `0009:66` (`subtotal`), `0009:276-282` (policy `pesanan_item_tambah` untuk `kasir`/`pelayan`), `0009:193-219` (pemicu `picu_item_pesanan_konsisten` hanya memeriksa kesamaan resto, **bukan** harga); penjaga uang hanya dipasang untuk kolom pada tabel `pesanan`
- **Klaim yang dilanggar:** `docs/KEAMANAN.md:24` prinsip 5 "**Tidak ada angka uang dari perangkat.** Semua nominal dihitung di peladen (`hitung_total()`), klien hanya menampilkan (**sudah berlaku sejak T1-10**)"; `docs/TECH_SPEC.md:92` "Fungsi SQL memeriksa izin, **mengunci harga saat itu** (`harga_saat_itu`)"; `docs/PRD.md:97-98` (izin `ubah_harga` + "tindakan di luar izin tidak bisa dilakukan"); klaim paket #6
- **Bukti:** `node alat/uji-sql.mjs /tmp/probe/p3_harga_item.sql` → `LULUS` (rc=0). Probe sebagai kasir Rina: kontrol `boleh('ubah_harga')` → **false**; kontrol `harga_berlaku(Nasi Goreng, Pusat)` → **27000**; lalu `insert into pesanan (… kunci_idempoten 'probe-harga-1')` + `insert into pesanan_item (…, harga_saat_itu, qty, subtotal) values (…, 1, 5, 5)` → **diterima**, `harga_saat_itu = 1`, `subtotal = 5`. `grep -rn "ubah_harga" supabase/migrations/*.sql` → hanya di kamus izin (`0005:32`) & matriks bawaan (`0005:79,90,101,112,123`) — **tidak ada satu pun gerbang yang memanggilnya**
- **Skenario gagal:** kasir menjual Nasi Goreng Rp 27.000 seharga Rp 1 (atau menambah `subtotal` baris sesukanya) lewat `POST /rest/v1/pesanan_item`. `TECH_SPEC.md:329` mengunci rumus "subtotal = jumlah `harga_saat_itu × qty`", jadi begitu `hitung_total` (T1-15) mendarat angka karangan itu **menjadi** total pesanan, masuk struk, masuk laporan omzet/laba, dan jejaknya beku (pemicu `picu_item_salinan_beku` menolak koreksi — `0009:224-242`). Izin `ubah_harga` yang dicentang owner di layar M3 tidak berpengaruh apa pun
- **Dugaan penyebab:** keputusan "salinan harga wajib diisi pemanggil" (komentar di `0009:56-60`) diterapkan tanpa pasangan penjaganya: tidak ada pemicu yang membandingkan `harga_saat_itu` dengan `harga_berlaku(menu_item_id, cabang_id)` dan tidak ada pemeriksaan `boleh('ubah_harga')` bila keduanya berbeda
- **Cara membuktikan perbaikan:** probe `p3_harga_item.sql` harus `GAGAL`; tambah uji `supabase/tes/harga_item.sql`: (a) kasir menyisipkan `harga_saat_itu <> harga_berlaku(...)` → `uji.harap_gagal`, (b) pemegang `ubah_harga` (admin cabang) boleh → diterima, (c) mutasi penjaga dimatikan → uji itu MERAH. `grep -rn "boleh('ubah_harga'" supabase/migrations/*.sql` harus menampilkan minimal satu gerbang
- **Status verifikasi:** TERVERIFIKASI

### [PR-04] Bukti persetujuan PIN untuk void tidak terikat pesanan dan tidak dipakai sekali — 1 PIN menyetujui banyak pembatalan
- **Tingkat:** K-2
- **Artefak:** `supabase/migrations/0010_pembayaran.sql:415` (`JENDELA_SETUJU_MENIT constant integer := 5`), `0010:459-466` (`if not exists (select 1 from percobaan_pin … aksi='void_sesudah_dapur' … waktu > now()-5 menit)`)
- **Klaim yang dilanggar:** `docs/uji/AUDIT_RIWAYAT.md:46` "B F-02 · A F-03 … **DITUTUP** … persetujuan wajib TERBUKTI (PIN benar untuk aksi itu, jendela 5 menit, tercatat)"; `docs/PRD.md:98` "untuk membatalkan setelah dapur mulai … **wajib PIN atasan/owner**"; klaim paket #6
- **Bukti:** `node alat/uji-sql.mjs /tmp/probe/p4_void_replay.sql` → `LULUS` (rc=0). Urutan: owner memasang PIN (`simpan_pin('246810')` → `PIN tersimpan.`), owner **satu kali** `verifikasi_pin(owner, '246810','void_sesudah_dapur')` → `berhasil=true`; lalu sebagai kasir: (1) `insert pembatalan` pesanan `eeee…0010` tahap `sesudah_dapur` `disetujui_oleh=owner` → diterima; (2) kasir membuat pesanan **kedua** `dddd…0002` (status `dikirim`, `dikirim_ke_dapur_pada=now()`, item 3×27.000) dan membatalkannya dengan penyetuju yang sama → diterima; (3) pembatalan tingkat item di pesanan pertama → diterima. Pemeriksaan akhir: `count(*) percobaan_pin (berhasil, aksi='void_sesudah_dapur') = 1` sementara `count(*) pembatalan = 3`. Uji yang ada (`supabase/tes/persetujuan_void.sql`) menguji tanpa-bukti, aksi-salah, dan **kedaluwarsa** (baris 76-96) — **tidak** menguji pemakaian berulang
- **Skenario gagal:** kasir minta owner memasukkan PIN sekali ("batal satu mie ya"), lalu dalam 5 menit membatalkan pesanan mana pun di cabangnya — termasuk pesanan yang tidak pernah dilihat owner — dan semuanya tercatat "disetujui atasan". Kerugian bahan (`nilai_kerugian`, `bahan_terbuang`) ikut tercatat atas nama persetujuan yang tidak pernah diberikan; jejak audit menjadi bukti palsu yang sah
- **Dugaan penyebab:** bukti persetujuan dicari sebagai "ada tidaknya catatan PIN yang cocok" (keberadaan), bukan sebagai **kupon sekali pakai** yang terikat ke `pesanan_id`/`pembatalan` tertentu; kolom `percobaan_pin` memang tidak punya tautan ke pesanan, jadi tidak ada yang bisa dicocokkan
- **Cara membuktikan perbaikan:** probe `p4_void_replay.sql` harus `GAGAL` pada pembatalan ke-2; uji baru di `supabase/tes/persetujuan_void.sql`: satu bukti PIN → pembatalan pertama diterima, pembatalan kedua (pesanan lain/item lain) `uji.harap_gagal('bukti PIN sudah dipakai')`; mutasi (penanda "sudah dipakai" dimatikan) → uji itu MERAH
- **Status verifikasi:** TERVERIFIKASI

### [PR-05] Jejak pelaku perubahan `pengaturan` uang bisa dikarang dan bisa dihapus (pajak, service, cap potongan, tumpuk diskon)
- **Tingkat:** K-2
- **Artefak:** `supabase/migrations/0004_pola_rls.sql:81-84` (`pengaturan_ubah` hanya memeriksa `penyewa_saya()` + `peran_saya()='owner_pusat'`) · `0002:98-99` (`diubah_oleh`, `diubah_pada` kolom biasa) · tidak ada pemicu apa pun di `pengaturan` (`grep -rn "on public.pengaturan" supabase/migrations/*.sql` → hanya grant & policy)
- **Klaim yang dilanggar:** `docs/PRD.md:267` Aturan Bisnis 8 "Semua tindakan sensitif tercatat (jejak audit): … **perubahan pengaturan penting**"; `docs/PRD.md:99`; prinsip yang dipasang PR ini sendiri di `0010:305-308` & `0010:392-395` ("Jejak pelaku TIDAK boleh dikarang klien")
- **Bukti:** `node alat/uji-sql.mjs /tmp/probe/p6_jejak_pengaturan.sql` → `LULUS` (rc=0). Sebagai owner: `update public.pengaturan set pajak_pb1_persen=0, service_persen=0, batas_maks_potongan_persen=100, tumpuk_diskon=true, diubah_oleh='90000000-…-0004' (kasir Rina)` → diterima, terbaca `0.00/90000000-…-0004`; lalu `update … set diubah_oleh=null, diubah_pada='2020-01-01+07'` → diterima (jejak hilang & waktu dimundurkan). Kontrol di berkas yang sama: `insert into pembayaran … kasir_id=<owner>` sebagai kasir → **DITOLAK** `Nama kasir diisi sistem — tidak boleh menyebut orang lain` (jadi prinsipnya sudah ada, hanya tidak diterapkan ke `pengaturan`)
- **Skenario gagal:** pajak PB1 diturunkan ke 0%, service ke 0%, cap potongan dinaikkan ke 100%, tumpuk diskon dinyalakan (membuka PR-02) — lalu jejaknya ditulis atas nama kasir atau dikosongkan. Tidak ada tabel riwayat `pengaturan`, `diubah_pada` bisa ditulis bebas, jadi tidak ada cara membuktikan siapa yang mengubah; laporan selisih kas akan menunjuk orang yang salah
- **Dugaan penyebab:** perbaikan jejak pelaku (temuan AUD-3 K-2) dipasang per tabel yang punya `pelaku_id`/`kasir_id` (pembayaran, diskon, pembatalan) dan terlewat di `pengaturan` yang memakai nama kolom berbeda (`diubah_oleh`); policy UPDATE-nya juga tidak punya penjaga kolom
- **Cara membuktikan perbaikan:** probe `p6_jejak_pengaturan.sql` harus `GAGAL`; pemicu `before update on public.pengaturan` yang (a) menolak `new.diubah_oleh is distinct from auth.uid()`, (b) memaksa `diubah_pada := now()`, plus uji `uji.harap_gagal(update … diubah_oleh=<orang lain>)` dan uji mutasi yang membuktikan uji itu bisa MERAH
- **Status verifikasi:** TERVERIFIKASI

### [PR-06] Bahan kalibrasi review PR tidak bisa dipasang: kepala hunk salah hitung + konteksnya versi pra-perbaikan → RV-3 wajib Jalur Merah tidak bisa dijalankan
- **Tingkat:** K-2
- **Artefak:** `docs/uji/kalibrasi/pr-bahan-2026-09-17.diff:31` (`@@ -298,7 +298,7 @@` padahal isi hunk 7 baris lama / **6** baris baru) dan `:43` (`@@ -293,7 +293,7 @@` dengan konteks `if coalesce(v_pesanan.total, 0) > 0 then` yang sudah tidak ada di `0010` sejak commit `b936ddd`); pembuatnya `alat/review-pr.py:691-700`
- **Klaim yang dilanggar:** paket §5 "salin repo ke folder sementara (`cp -r` ke /tmp lalu `git apply <berkas diff>` di salinan itu)"; `docs/uji/PROTOKOL_REVIEW_PR_INDEPENDEN.md` §2 gerbang merge butir 4 (Jalur Merah wajib "hasil kalibrasi terakhir tidak gagal") dan §6 ("tanpa bagian kalibrasi (bila paket memintanya) → ditolak")
- **Bukti:** di salinan `/tmp/kalib` pada `7e8c0b9`: `git apply docs/uji/kalibrasi/pr-bahan-2026-09-17.diff` → `error: corrupt patch at line 39` **exit 128**; `git apply --recount` → `error: patch failed: supabase/migrations/0010_pembayaran.sql:293 … patch does not apply` **exit 1**; `git apply -C0 --recount` → exit 1; `patch -p1 --dry-run --fuzz=5` → 3 berkas pertama "checking file" lalu `malformed patch at line 39` **exit 2**. Hitungan saya atas berkas itu (skrip python membaca tiap hunk): hunk 3 `lama_diklaim 7 / baru_diklaim 7` tetapi `lama_nyata 7 / baru_nyata 6`. Sumber konteks basi: `git log --oneline -S "if coalesce(v_pesanan.total, 0) > 0 then" -- supabase/migrations/0010_pembayaran.sql` → `b936ddd`, `1ab54a4`; `git log -S "Total pesanan belum dihitung"` → `b936ddd` (commit yang mengganti pola itu, bagian dari PR ini). Akar masalah di pembuatnya: `review-pr.py:691-700` membuang baris `+` yang memuat penanda `SENGAJA` (`if ln.strip() in ("+", "++", ""): continue`) **tanpa menghitung ulang kepala `@@`** — sehingga jumlah baris baru di kepala hunk tinggal satu terlalu banyak. Efek samping pembersih itu sendiri benar: `grep -ni "SENGAJA\|kalibrasi\|KUNCI" docs/uji/kalibrasi/pr-bahan-2026-09-17.diff` → kosong (tidak ada bocoran kunci)
- **Skenario gagal:** peninjau Jalur Merah mana pun yang mengikuti paket §5 mentok di langkah pertama. Tiga akibatnya sama-sama buruk: (a) kalibrasi dilewati dan skor `Ditemukan: X dari Y` diisi tanpa dasar; (b) peninjau menambal sendiri (seperti saya) sehingga yang dinilai bukan bahan resmi; (c) gerbang merge butir 4 tidak pernah bisa dipenuhi, dan `docs/uji/REVIEW_PR_RIWAYAT.md` §2 tetap "belum ada" selamanya — artinya ketajaman peninjau PR **tidak pernah terukur** walau mekanismenya diklaim sudah dipasang
- **Dugaan penyebab:** `--kalibrasi-pr-siapkan` tidak pernah diuji ujung-ke-ujung setelah diff ditulis (tidak ada `git apply --check` di akhir pembuatan), dan bahan tidak pernah disegarkan ketika `0010` berubah di commit `b936ddd`; `--uji-diri` `review-pr.py` hanya menguji pemeriksa laporan, bukan pembuat bahan kalibrasi
- **Cara membuktikan perbaikan:** `python3 alat/review-pr.py --kalibrasi-pr-siapkan` lalu di salinan bersih `git apply --check docs/uji/kalibrasi/pr-bahan-<tanggal>.diff` → exit 0, dan `git apply` + `node alat/uji-sql.mjs` → `HASIL: GAGAL` (cacat tanaman tertangkap/atau terbukti tidak tertangkap untuk dikejar); tambah langkah `git apply --check` itu ke dalam `kalibrasi_pr_siapkan()` sebelum berkas ditulis
- **Status verifikasi:** TERVERIFIKASI

### [PR-07] Dua baris Buku Uji Pemilik tidak bisa dijalankan apa adanya di ruang kerja segar, padahal kolom harapannya menjanjikan "SEMUA PEMERIKSAAN LOLOS"
- **Tingkat:** K-2
- **Artefak:** `docs/uji/BUKU_UJI_PEMILIK.md:39` (U-02) dan `:40` (U-03); `aplikasi/alat/periksa-semua.sh:5,10-11` (`set -euo pipefail` lalu langsung `npm run format:check`) dan `:23-24` (pemasangan `alat/node_modules` yang **tidak pernah tercapai**)
- **Klaim yang dilanggar:** `docs/uji/PROTOKOL_AUDIT_INDEPENDEN.md:52` "setiap langkah pengguna yang **tidak bisa dijalankan apa adanya** … diperlakukan minimal **K-2**. Buku pedoman yang basi = temuan, bukan kerapian"; judul commit `7e8c0b9` "Buku Uji Pemilik (permintaan Lee) … tiap baris: langkah <=5, 'yang seharusnya terjadi'"; klaim paket #7
- **Bukti:** dari `git archive 7e8c0b9` ke `/tmp/pristine` (tanpa `node_modules` sama sekali, persis keadaan klon baru): `bash aplikasi/alat/periksa-semua.sh` → `== aplikasi: kerapian kode == … sh: 1: prettier: not found` **rc=127**; `node alat/uji-sql.mjs` → `Error [ERR_MODULE_NOT_FOUND]: Cannot find package '@electric-sql/pglite' imported from /tmp/pristine/alat/uji-sql.mjs` **rc=1**. `grep -n "periksa-semua" PANDUAN_PENGGUNA.md docs/uji/BUKU_UJI_PEMILIK.md` → tidak satu pun menyebut `npm ci`/`npm install` lebih dulu; pemasangan pustaka hanya disebut untuk **pratinjau** (`PANDUAN_PENGGUNA.md:277` → `aplikasi/alat/pratinjau.sh`). Pemeriksaan mesin tetap hijau (`periksa-buku-uji.py` → `2 baris lakukan · 5 baris coba · 7 baris total … LOLOS`) karena yang diperiksa bentuk barisnya, bukan keterjalanannya
- **Skenario gagal:** Lee mengerjakan U-02/U-03 sendiri (itu tujuan bukunya: "yang butuh mata manusia", tanpa bertanya ke agent) di ruang kerja yang baru dinyalakan ulang — kejadian yang sudah pernah nyata (`_log-sesi/LOG_SESI_2026-09-17.md:168` "Insiden ruang kerja: salinan Git mundur ke `main` (klon dangkal)"; `PANDUAN_PENGGUNA.md:277` "Kalau ruang kerja baru dinyalakan ulang: pustaka aplikasi bisa hilang"). Yang ia lihat bukan `SEMUA PEMERIKSAAN LOLOS.` melainkan `prettier: not found`, jadi ia mengisi `GAGAL` pada kolom Hasil untuk sistem yang sebenarnya sehat — sinyal palsu ke pemilik, persis yang mekanisme ini ada untuk hindari
- **Dugaan penyebab:** baris buku ditulis & diverifikasi di ruang kerja pembangun yang `node_modules`-nya sudah ada. Skripnya sebenarnya **sudah** memasang `alat/node_modules` sendiri (baris 23-24), tetapi `set -euo pipefail` (baris 5) membuatnya mati di baris 11 (`prettier: not found`) sebelum sampai ke sana — jadi kegagalan pertama menutupi kegagalan kedua; dan penjaga `periksa-buku-uji.py` hanya memvalidasi struktur baris (≤5 langkah, ada harapan, id tugas benar), tidak pernah menjalankannya di pohon bersih
- **Cara membuktikan perbaikan:** di `git archive` commit baru: `bash aplikasi/alat/periksa-semua.sh` → baris terakhir `SEMUA PEMERIKSAAN LOLOS.` dan `node alat/uji-sql.mjs` → `HASIL: LOLOS` **tanpa** langkah pemasangan manual (skripnya yang memasang, seperti yang sudah ia lakukan untuk `alat/`), atau langkah 0 "pasang pustaka" ditulis eksplisit di U-02/U-03 dan diuji di pohon bersih
- **Status verifikasi:** TERVERIFIKASI

### [PR-08] Klaim bukti T0-01 "57 berkas `.woff2`" tidak reproduktif (nyata 19) dan dipakai untuk menutup temuan audit B F-13; penjaga penutup temuan hanya memeriksa keberadaan berkas
- **Tingkat:** K-2
- **Artefak:** `docs/ROADMAP.md:61` · `docs/uji/AUDIT_RIWAYAT.md:58` (baris `B F-13 … **DITUTUP 2026-09-17** … diganti angka terhitung + perintah hitungnya`) · `alat/periksa-temuan-audit.py:112-120`
- **Klaim yang dilanggar:** butir 3 badan commit `7e8c0b9` "Klaim bukti T0-01 '31 berkas huruf pindah' tidak bisa direproduksi -> **dicabut, diganti angka terhitung + perintah hitungnya**"; `docs/uji/PROTOKOL_AUDIT_INDEPENDEN.md:52` (langkah yang tidak bisa dijalankan apa adanya ≥ K-2); klaim paket #7
- **Bukti:** perintah yang persis ditulis di ROADMAP, dijalankan di pohon bersih `git archive 7e8c0b9`: `find aplikasi -name '*.woff2' | wc -l` → **19**; `find . -name '*.woff2' -not -path './skills/*' | wc -l` → **38**; `git ls-tree -r --name-only 7e8c0b9 | grep '^aplikasi/.*\.woff2$' | wc -l` → **19**; `… | grep -c '\.woff2$'` → **38**. Tidak ada kombinasi yang menghasilkan 57 (57 = 3×19, kemungkinan terhitung `src` + `dist` hasil build + satu salinan lain). Penjaga: `python3 alat/periksa-temuan-audit.py` → `HASIL: LOLOS — 27 temuan terlacak semua, tiap DITUTUP punya bukti hidup` karena yang diperiksa hanya `rujukan_berkas` ada/tidak (`jalur_ada`, baris 118-120) — **angka di dalam bukti tidak pernah diuji**
- **Skenario gagal:** temuan audit dinyatakan tertutup dengan bukti yang salah, jadi tidak ada yang kembali memeriksanya; pola yang sama akan berulang di 26 temuan lain karena gerbangnya hanya memeriksa "berkasnya ada". Pemilik yang membaca `AUDIT_RIWAYAT` menyimpulkan "klaim tidak reproduktif sudah dibereskan" padahal kelas cacatnya masih ada — dan ini terjadi di commit yang judulnya justru "verifikasi permintaan Lee"
- **Dugaan penyebab:** angka pengganti dihitung di ruang kerja yang berisi artefak build (`aplikasi/dist/` hasil `npm run build`, 19 `.woff2` tambahan — saya sendiri menghasilkannya saat menjalankan `periksa-semua.sh`), bukan di pohon bersih; tidak ada aturan "perintah bukti wajib dijalankan di `git archive`/klon bersih"
- **Cara membuktikan perbaikan:** `find aplikasi -name '*.woff2' | wc -l` di pohon bersih menghasilkan angka yang **sama** dengan yang tertulis di `docs/ROADMAP.md:61`; dan `periksa-temuan-audit.py` (atau pemeriksa baru) mengekstrak perintah ber-backtick di kolom bukti lalu membandingkan keluarannya dengan angka yang diklaim — dimutasi (angka bukti diubah) → pemeriksa MERAH
- **Status verifikasi:** TERVERIFIKASI

### [PR-09] Pemeriksa laporan review menerima verdict `BERSIH-DENGAN-CATATAN` padahal ada temuan K-1 TERVERIFIKASI — gerbang merge #2 bisa lolos dengan cacat kritis terbuka
- **Tingkat:** K-2
- **Artefak:** `alat/review-pr.py:459` (`if angka.get("berat_terverifikasi", 0) > 0 and v_m and v_m.group(1) == "BERSIH":` — hanya menolak verdict **persis** `BERSIH`)
- **Klaim yang dilanggar:** `docs/uji/PROTOKOL_REVIEW_PR_INDEPENDEN.md` §6 "ada K-1/K-2 berstatus TERVERIFIKASI → verdict **wajib** `TIDAK-BERSIH`"; §2 gerbang merge butir 2–3 (verdict BERSIH/BERSIH-DENGAN-CATATAN **dan** tidak ada K-1/K-2 terbuka); klaim paket #5 "tidak ada gerbang keamanan/CI yang dilemahkan"; gate §4.5 "mekanisme tidak boleh tumpul"
- **Bukti:** `python3 alat/review-pr.py --periksa-laporan /tmp/probe/laporan-uji/A_k1_verdict_naik.md` (laporan berisi `### [PR-01] … **Tingkat:** K-1 … **Status verifikasi:** TERVERIFIKASI` dan `- **Verdict:** BERSIH-DENGAN-CATATAN`, Jalur Merah, paket nyata) → `verdict: BERSIH-DENGAN-CATATAN · jalur risiko: Merah · temuan: 1 · **HASIL: LOLOS — laporan memenuhi kontrak review PR.**` **rc=0**. Bandingkan `--uji-diri`: `buruk-tanpa-bukti.md` ditolak justru karena memakai verdict `BERSIH` — fixture uji tidak menutup celah `BERSIH-DENGAN-CATATAN`
- **Skenario gagal:** peninjau (atau agent yang merapikan laporan) menulis temuan K-1 uang/keamanan lalu memilih verdict tengah `BERSIH-DENGAN-CATATAN`; mesin mencetak `LOLOS`, Kartu Keputusan dibuat dari laporan itu (`--kartu-keputusan`), dan syarat merge butir 2 terpenuhi. Butir 3 ("tidak ada K-1/K-2 terbuka") tinggal bergantung pada manusia yang membaca — padahal seluruh mekanisme ini ada karena pemilik tidak bisa menilai sendiri
- **Dugaan penyebab:** pemeriksaan konsistensi ditulis untuk satu nilai verdict, bukan untuk himpunan verdict yang diizinkan ketika ada temuan berat; fixture `--uji-diri` dibuat dari kesalahan yang terpikir saat itu, bukan dari aturan protokol
- **Cara membuktikan perbaikan:** `python3 alat/review-pr.py --periksa-laporan /tmp/probe/laporan-uji/A_k1_verdict_naik.md` → rc≠0 dengan alasan "verdict wajib TIDAK-BERSIH"; tambahkan fixture itu ke `alat/contoh-laporan-review/` sehingga `--uji-diri` menutupnya permanen
- **Status verifikasi:** TERVERIFIKASI

### [PR-10] Buku Uji Pemilik (baris U-04) menyuruh Lee menempel paket review yang sudah digantikan; di kepala PR paket itu mengikat commit `183a3c1` yang selisih 27 berkas non-netral
- **Tingkat:** K-2
- **Artefak:** `docs/uji/BUKU_UJI_PEMILIK.md:41` (di kepala PR `f59debd`: "Salin SELURUH isi `docs/uji/review-pr/PKT-2026-09-17-pr-01-putaran7-SIAP-TEMPEL.md`") vs `docs/uji/REVIEW_PR_RIWAYAT.md` baris 3 ("**digantikan** baris 4") dan baris 4 ("**Paket berlaku:** … putaran8")
- **Klaim yang dilanggar:** `docs/uji/AUDIT_RIWAYAT.md:55` (`B F-09` K-2 "Paket … menunjuk commit lain", status **TERBUKA** → `T1-44`); klaim paket #7 (dokumen panduan Lee tidak basi); tujuan BUKU_UJI sendiri ("Dokumen ini sumber kebenaran")
- **Bukti:** `git show f59debd:docs/uji/BUKU_UJI_PEMILIK.md | grep -n PKT-` → baris 41 masih `putaran7`; `git show f59debd:docs/uji/review-pr/PKT-…putaran7.md | grep -n "Commit yang direview"` → `183a3c11aa24f865d6e3140820e64574aea15ff6`; `git diff --name-only 183a3c1 f59debd | wc -l` → **27** berkas, di antaranya `.github/workflows/ci.yml`, `alat/periksa-buku-uji.py`, `alat/periksa-rujukan.py`, `alat/periksa-temuan-audit.py`, `aplikasi/package.json`, `docs/KEAMANAN.md`, `docs/ROADMAP.md` — jelas **bukan** berkas netral, jadi mekanisme `_selisih_hanya_netral` tidak menyelamatkannya. Catatan adil: di commit yang saya review (`7e8c0b9`) baris itu masih benar (putaran7 memang berlaku saat itu); ia menjadi basi oleh commit berikutnya di PR yang sama
- **Skenario gagal:** Lee mengikuti satu-satunya buku panduannya, menempel paket putaran7, dan sesi peninjau mereview `183a3c1` — tanpa 3 penjaga baru, tanpa Buku Uji, tanpa `npm audit` di CI, tanpa migrasi 0011 versi final. Laporan yang masuk dinyatakan `LOLOS` oleh mesin (SHA-nya nyata), Kartu Keputusan ditulis atas commit yang bukan kepala PR, dan keputusan merge diambil atas dasar review commit lain. Ini persis kejadian `B F-09` yang belum ditutup
- **Dugaan penyebab:** nama berkas paket di-hardcode di dokumen pemilik, padahal paket dibuat ulang setiap putaran; tidak ada penjaga yang memeriksa bahwa rujukan paket di dokumen pemilik = paket yang berlaku di `REVIEW_PR_RIWAYAT` (rujukannya "hidup" jadi `periksa-rujukan.py` LOLOS)
- **Cara membuktikan perbaikan:** `grep -n "PKT-" docs/uji/BUKU_UJI_PEMILIK.md` menunjuk berkas yang sama dengan baris "Paket berlaku" di `docs/uji/REVIEW_PR_RIWAYAT.md`, dan penjaga baru (`periksa-buku-uji.py`/`periksa-rujukan.py`) menolak bila keduanya berbeda → dibuktikan dengan uji mutasi (nama paket di buku diubah → MERAH)
- **Status verifikasi:** TERVERIFIKASI

### [PR-11] `izin_efektif_untuk()` masih membaca kolom `pengguna_cabang.peran` yang dihapus migrasi 0011 → error runtime; klaim "satu rumus izin, dua pintu" batal
- **Tingkat:** K-3
- **Artefak:** `supabase/migrations/0006_pin.sql:135-142` (`select pc.peran into v_peran from public.pengguna_cabang pc …`) vs `supabase/migrations/0011_peran_tunggal.sql:14` (`alter table public.pengguna_cabang drop column if exists peran`) dan `0011:60-123` (`izin_efektif` ditulis ulang **tanpa** memanggil `izin_efektif_untuk`)
- **Klaim yang dilanggar:** `0006:186` "izin_efektif (0005) kini memakai fungsi umum yang sama — **satu rumus, dua pintu**"; `0005:10` "Hasil akhirnya dihitung SATU fungsi … **Tidak ada rumus izin kedua di tempat lain**"; ROADMAP T1-23 DoD "peran tunggal ditegakkan database; **matriks peran tidak berubah setelah migrasi**"
- **Bukti:** `node alat/uji-sql.mjs /tmp/probe/p2_izin_efektif_untuk_cabang.sql` → `GAGAL … column pc.peran does not exist` (rc=1). Probe: `set local role service_role; select * from public.izin_efektif_untuk('90000000-…-0004','beri_diskon','a1a1a1a1-…-0001');`. Pemanggil hidup saat ini semuanya memakai 2 argumen (`0010:451` `boleh_untuk(new.disetujui_oleh,'void_sesudah_dapur')`, `0011:425`), jadi cabang itu belum terpicu; `grep -rn "izin_efektif_untuk\|boleh_untuk" supabase/ alat/ aplikasi/src` → tidak ada satu pun pemanggil yang mengirim `p_cabang_id`, dan tidak ada satu pun uji yang menyentuh cabang itu (`supabase/tes/isolasi_lintas_penyewa.sql:49-54` memanggilnya 2 argumen dan hanya berharap **ditolak** karena hak klien dicabut)
- **Skenario gagal:** begitu ada RPC/Edge Function yang menilai izin **per cabang** (mis. `batal_pesanan`/`set_izin` di T3-13/T9-08 yang memang menerima `cabang_id`), panggilan itu meledak dengan `column pc.peran does not exist` di tengah transaksi uang; atau lebih halus, developer "memperbaikinya" dengan menghidupkan lagi peran per cabang — persis yang dibongkar ART-12. Sementara itu dua rumus izin sekarang hidup berdampingan (0011 untuk `boleh()`, 0006 untuk `boleh_untuk()`), jadi keputusan izin untuk pemanggil yang sama bisa berbeda
- **Dugaan penyebab:** 0011 membongkar kolom dan menulis ulang `izin_efektif`, tetapi `izin_efektif_untuk` (dibuat 0006) tidak ikut ditulis ulang; PL/pgSQL tidak memvalidasi referensi kolom saat `CREATE`, dan migrasi 0006 sengaja dibekukan sehingga tidak ada yang menyentuhnya
- **Cara membuktikan perbaikan:** `node alat/uji-sql.mjs /tmp/probe/p2_izin_efektif_untuk_cabang.sql` → `LULUS` (mengembalikan baris izin, bukan error) setelah migrasi baru menulis ulang `izin_efektif_untuk` memakai `pengguna.peran` + penjaga keanggotaan cabang, **dan** `izin_efektif` kembali menjadi pembungkusnya (satu rumus); tambah uji yang memanggil keduanya dengan `p_cabang_id` dan membandingkan hasilnya
- **Status verifikasi:** TERVERIFIKASI

### [PR-12] Penjaga "akun nonaktif kehilangan cabang" di `cabang_saya()` tidak punya uji regresi — mutasi menghapusnya membuat seluruh gerbang tetap HIJAU
- **Tingkat:** K-3
- **Artefak:** `supabase/migrations/0003_helper_identitas.sql:66` (`join public.pengguna p … and p.aktif`) · `supabase/tes/helper.sql:89-97` (blok akun nonaktif hanya menguji `penyewa_saya`, `peran_saya`, `cabang_ids_saya`)
- **Klaim yang dilanggar:** `docs/ROADMAP.md:210` (bukti T1-03) "akun nonaktif kehilangan seluruh identitas. **Uji ini menangkap satu cacat nyata** pada rancangan awal (`cabang_saya()`/`cabang_ids_saya()` masih memberi cabang ke akun nonaktif) yang langsung diperbaiki"; klaim paket #4 (uji membuktikan bebas regresi)
- **Bukti:** mutasi D1 (bahan kalibrasi, hunk pertama): `join public.pengguna p on p.id = pc.pengguna_id and p.aktif` → `join public.pengguna p on p.id = pc.pengguna_id`, lalu `node alat/uji-sql.mjs` → **`uji: 21 LULUS · 0 GAGAL / HASIL: LOLOS` rc=0** (dipulihkan → tetap `21 LULUS`). Penyebab: `grep -n "cabang_saya\|uji.klaim" supabase/tes/helper.sql` → `cabang_saya()` diuji di baris 79-85 (klaim sah & klaim resto lain) tetapi di blok nonaktif (baris 91) klaimnya `uji.klaim('…0005')` **tanpa** `cabang_id`, jadi memanggil `cabang_saya()` di situ pun akan null karena sebab lain — pola "lulus karena sebab yang salah" yang diakui pembangun sendiri di `ROADMAP.md:273`
- **Skenario gagal:** cacat yang sama masuk lagi (refactor, migrasi berikutnya, atau "perbaikan" yang menyederhanakan join) dan tidak ada satu pun dari 21 uji/CI yang merah. `cabang_saya()` dipakai `pengguna_pilih` (`0004:56`) untuk lingkup admin cabang; gabungan dengan celah lain (mis. policy yang kelak hanya bergantung pada `cabang_saya()`) membuat akun yang sudah dinonaktifkan tetap punya identitas cabang — padahal `KEAMANAN.md:49` menjanjikan "nonaktif = cabut seketika"
- **Dugaan penyebab:** uji regresi ditulis untuk gejala yang terlihat saat itu (`cabang_ids_saya` mengembalikan 2 cabang) tanpa mengunci varian klaim-token untuk `cabang_saya()`
- **Cara membuktikan perbaikan:** `node alat/uji-sql.mjs` dengan tambahan di `supabase/tes/helper.sql`: nonaktifkan akun, `uji.klaim(id, '{"cabang_id":"a1a1a1a1-…-0002"}')`, lalu `uji.sama(public.cabang_saya(), null::uuid, 'pegawai nonaktif tidak punya cabang aktif walau klaim token sah')`; buktikan bisa MERAH dengan mutasi D1 (`21 LULUS` → `20 LULUS · 1 GAGAL`)
- **Status verifikasi:** TERVERIFIKASI

### [PR-13] Pegawai mana pun semeja bisa mengunci PIN kolega/owner (5 tebakan salah atas nama korban) → persetujuan void & diskon macet
- **Tingkat:** K-3
- **Artefak:** `supabase/migrations/0011_peran_tunggal.sql:394-399` (penghitung kegagalan per `p_pengguna_id` = **target**, bukan per pemanggil) dan `0011:406-412` (setelah batas tercapai, tiap percobaan menambah baris gagal baru → jendela 15 menit bergulir)
- **Klaim yang dilanggar:** `docs/KEAMANAN.md:22` (PIN sebagai faktor kedua untuk semua yang menyentuh uang) tanpa pengakuan risiko penolakan layanan; `docs/PRD.md:98` (PIN atasan sebagai jalur persetujuan)
- **Bukti:** `node alat/uji-sql.mjs /tmp/probe/p5_pin_dos.sql` → `LULUS` (rc=0). Urutan: owner memasang PIN dan PIN-nya benar (`berhasil=true`); kasir memanggil `verifikasi_pin(owner, '111223'…'555666', null, 'alat-1…alat-5')` 5 kali (nama perangkat diputar, jadi lapis perangkat tidak pernah tercapai — sudah diakui `KEAMANAN.md:40`); lalu owner memanggil `verifikasi_pin(owner,'246810',null)` → pesan **`PIN terkunci sementara karena terlalu banyak percobaan salah. Coba lagi setelah 15 menit.`** padahal PIN-nya benar
- **Skenario gagal:** kasir yang sedang berkonflik (atau akun kasir yang dibajak) menebak 5 kali PIN owner setiap 14 menit. Owner tidak bisa menyetujui void sesudah dapur, tidak bisa menyetujui diskon di atas batas, dan (karena `percobaan_pin` juga jadi bukti persetujuan PR-04) seluruh alur persetujuan resto berhenti — dengan jejak yang menuduh owner "terlalu banyak percobaan salah". Kunci ini juga tidak bisa dibuka owner sendiri: tidak ada jalur reset kecuali menunggu atau lewat `service_role`
- **Dugaan penyebab:** pembatas dirancang anti-tebak (menghitung kegagalan per akun target) tanpa memisahkan "percobaan dari pemanggil yang sama" vs "dari pemanggil lain"; `p_perangkat` yang bisa diputar membuat lapis kedua tidak menambah perlindungan
- **Cara membuktikan perbaikan:** uji baru: 5 kegagalan yang dibuat **pegawai lain** atas nama korban tidak mengurangi jatah korban (`verifikasi_pin` oleh korban dengan PIN benar → `berhasil=true`), sementara 5 kegagalan dari **pemanggil yang sama** tetap mengunci pemanggil itu; mutasi (penghitung dikembalikan ke per-target saja) → uji MERAH
- **Status verifikasi:** TERVERIFIKASI

### [PR-14] Pemeriksa laporan review tumpul di tiga titik lain: kalibrasi bisa dilewati, kedalaman Jalur Merah hanya pencocokan kata, ambang minimum menghitung baris kepala tabel
- **Tingkat:** K-3
- **Artefak:** `alat/review-pr.py:475-489` (kalibrasi hanya diwajibkan bila berkas paket **ditemukan**), `:463-470` (kedalaman Merah = keberadaan kata `mutasi` / `RLS|izin|hak` / `pemulihan|rollback|dikembalikan` / `L1|L2|L4|lensa`), `:372-388` (`_tabel()` membuang baris pemisah tetapi **tidak** membuang baris kepala, lalu `:422-431` membandingkannya dengan `MIN_ARTEFAK/MIN_KLAIM/MIN_GERBANG = 5`)
- **Klaim yang dilanggar:** gate paket §4.5 "`python3 alat/review-pr.py --uji-diri` (mekanisme tidak boleh tumpul)"; `docs/uji/PROTOKOL_REVIEW_PR_INDEPENDEN.md` §6 ("tanpa bagian kalibrasi (bila paket memintanya) → ditolak"; §3 kedalaman wajib per jalur); §6b anti-teater ("ambang minimum = LANTAI")
- **Bukti:** (a) `python3 alat/review-pr.py --periksa-laporan /tmp/probe/laporan-uji/B_kalibrasi_dilewati.md` → hanya `[catatan] paket review tidak ditemukan — kedalaman/kalibrasi tidak bisa dicocokkan`, lalu **`HASIL: LOLOS` rc=0** padahal bagian 5-nya berisi "Tidak dijalankan." untuk PR Jalur Merah; (b) `… /tmp/probe/laporan-uji/C_merah_kata_kunci.md` (baris gerbang ke-5 literally berbunyi "saya menyebut kata mutasi, RLS, izin, lensa L1 L2 L4, dan rencana pemulihan/rollback (tanpa keluaran perintah apa pun)") → **`HASIL: LOLOS` rc=0**; (c) hitungan mesin: laporan uji saya berisi 6 baris cakupan / 5 klaim / 5 gerbang, tetapi alat mencetak `cakupan/klaim: 7 berkas / 6 klaim / 6 gerbang` → baris kepala ikut terhitung, jadi lantai nyata = **4** baris isi, bukan 5; (d) `--uji-diri` hanya memakai 3 fixture (`bagus.md`, `buruk-tanpa-bukti.md`, `buruk-klaim-kurang.md`) sehingga ketiga celah ini tidak tertutup
- **Skenario gagal:** laporan malas atau laporan yang sengaja disusun untuk lolos (yang justru dilarang §6b) tetap dicap `LOLOS — laporan memenuhi kontrak review PR`. Karena Kartu Keputusan untuk Lee diturunkan dari laporan yang sudah "LOLOS", pemilik menerima ringkasan yang tampak sah tanpa bukti mutasi/RLS/kalibrasi apa pun
- **Dugaan penyebab:** validasi ditulis sebagai pemeriksaan keberadaan (kata/bagian/berkas), bukan pemeriksaan isi; jalur "paket tidak ditemukan" diperlakukan sebagai catatan agar laporan lama tetap bisa dinilai, tetapi efeknya jadi pintu belakang
- **Cara membuktikan perbaikan:** ketiga laporan uji di atas harus ditolak (rc≠0) dengan alasan yang tepat; `_tabel()` membuang baris kepala sehingga `angka['artefak']` = jumlah baris isi; `--uji-diri` memuat ketiganya sebagai fixture baru sehingga regresi ini tidak bisa kembali
- **Status verifikasi:** TERVERIFIKASI

### [PR-15] `cabang_saya()` bergantung pada klaim JWT tingkat-atas `cabang_id` yang tidak diterbitkan di mana pun dalam repo; TECH_SPEC menyebut sumbernya tabel `pengguna`
- **Tingkat:** K-3
- **Artefak:** `supabase/migrations/0003_helper_identitas.sql:69` (`and pc.cabang_id = nullif(auth.jwt() ->> 'cabang_id', '')::uuid`) · `docs/TECH_SPEC.md:318` · `alat/uji-sql.mjs:121-126` (`uji.klaim()` menyetel `request.jwt.claims` sesukanya; `:68` `auth.jwt()` tiruan membacanya) · `supabase/tes/helper.sql:79-85` (klaim diarang oleh `uji.klaim(id, jsonb)`)
- **Klaim yang dilanggar:** `docs/TECH_SPEC.md:318` "Nilai penyewa & cabang diambil dari **tabel `pengguna` lewat id Auth** (fungsi `penyewa_saya()` / `cabang_saya()`), **bukan** dari `user_metadata` yang bisa diubah klien"; `docs/DECISIONS_LOG.md:136` "Cabang aktif diambil dari klaim token" (dua dokumen ini tidak sejalan soal sumbernya)
- **Bukti:** `grep -rn "cabang_id" --include='*.ts' --include='*.tsx' --include='*.md' . | grep -i "klaim\|jwt\|token"` → hanya `ROADMAP.md:210` (bukti uji) dan `TECH_SPEC.md:207` (tabel `sesi_perangkat`, tugas T1-24 yang masih `[ ]`); **tidak ada** kode/Edge Function/migrasi yang menerbitkan klaim `cabang_id`, dan `grep -rn -i "custom access token\|app_metadata\|user_metadata" docs/*.md` → hanya larangan memakainya (`KEAMANAN.md:214`, `TECH_SPEC.md:383`). Uji lokal tidak bisa menangkap ini karena `uji.klaim()` menyetel `request.jwt.claims` sesukanya
- **Skenario gagal:** di Supabase nyata, JWT bawaan tidak memuat klaim tingkat-atas `cabang_id` (isinya `sub`, `role`, `app_metadata`, `user_metadata`, `session_id`, …), jadi `cabang_saya()` selalu `null` → policy `pengguna_pilih` cabang `admin_cabang` (`0004:44-59`) mati: admin cabang tidak pernah melihat pegawai di cabangnya. Gagalnya **ke arah aman** (fail-closed), tetapi diam-diam; godaan perbaikannya berbahaya: jalan pintas "pakai `user_metadata.cabang_id`" justru melanggar `KEAMANAN.md:214`/`TECH_SPEC.md:383` dan bisa diubah klien
- **Dugaan penyebab:** rancangan mengasumsikan ada lapisan penerbit klaim (token hook / `sesi_perangkat` T1-24) yang belum ada; fungsi identitas ditulis lebih dulu, dan harness uji menutupi ketiadaannya karena bisa mengarang klaim
- **Cara membuktikan perbaikan:** (a) `docs/TECH_SPEC.md:318` dikoreksi agar menyebut sumber cabang yang sebenarnya, atau (b) `cabang_saya()` diturunkan dari tabel saja (`pengguna_cabang` + pilihan cabang tersimpan) tanpa klaim token; dibuktikan dengan uji yang **tidak** menyetel klaim apa pun (`uji.klaim(id)` polos) lalu mengharapkan cabang aktif yang benar — dan di Supabase nyata (T0-08) `select public.cabang_saya()` untuk admin cabang tidak null
- **Status verifikasi:** TERVERIFIKASI untuk kontradiksi dokumen-kode dan tidak adanya penerbit klaim di repo; **DUGAAN** untuk isi JWT Supabase nyata (tidak ada akun/kredensial di sesi ini, jadi tidak bisa ditembak sungguhan)

### [PR-16] Batas lebih bayar diperiksa tanpa mengunci baris pesanan → dua pembayaran bersamaan bisa sama-sama lolos
- **Tingkat:** K-3
- **Artefak:** `supabase/migrations/0010_pembayaran.sql:257-260` (`select p.id, p.status, p.total, p.penyewa_id into v_pesanan from public.pesanan p where p.id = new.pesanan_id` — tanpa `for update`) dan `:317-320` (`v_sebelum := public.total_dibayar(...) + new.jumlah; if v_sebelum > v_pesanan.total`)
- **Klaim yang dilanggar:** `0010:12-14` pagar 2 "SATU PEMBAYAN = SATU BARIS TERCATAT … total pembayaran tidak boleh melebihi total pesanan"; `docs/ROADMAP.md:273` (bukti T1-10) "total pembayaran **tidak boleh melebihi total pesanan** (50.000 + 20.000 > 62.100 → ditolak)"
- **Bukti:** baca kode: `total_dibayar()` (`0010:190-201`) hanya `sum(pb.jumlah)` atas baris yang **sudah ter-commit/terlihat**; tidak ada `select … for update`, tidak ada constraint/trigger tingkat pesanan yang mengunci, dan `kunci_idempoten` unik hanya per `(pesanan_id, kunci_idempoten)` (`0010:96-97`) sehingga dua permintaan dengan kunci berbeda tidak saling menghalangi. **Tidak bisa saya buktikan secara empiris di sini**: PGlite menjalankan satu koneksi dalam satu proses, jadi dua transaksi sungguh-sungguh bersamaan tidak bisa dibuat (`node alat/uji-sql.mjs` memakai satu `PGlite.create()`)
- **Skenario gagal:** dua kasir (atau satu kasir yang menekan dua kali di dua perangkat dengan `kunci_idempoten` berbeda) mencatat pembayaran untuk pesanan yang sama pada saat hampir bersamaan; masing-masing melihat `total_dibayar` yang belum memuat baris pihak lain → keduanya lolos → uang tercatat melebihi total pesanan, dan karena baris `pembayaran` append-only, kelebihannya tidak bisa dikoreksi tanpa pembatalan berjejak
- **Dugaan penyebab:** penjaga ditulis sebagai pemeriksaan nilai pada `before insert` tanpa memikirkan isolasi transaksi (READ COMMITTED); uji yang ada (`gerbang_uang.sql`, `pembayaran.sql`) semuanya sekuensial
- **Cara membuktikan perbaikan:** `select … for update` pada baris `pesanan` di `picu_pembayaran_jujur` (atau trigger constraint/`serializable`), lalu uji dua sesi nyata di Supabase/PostgreSQL: dua koneksi menyisipkan pembayaran secara bersamaan untuk satu pesanan → salah satu **ditolak**; `ROADMAP.md:360` (DoD `pakai_voucher`) sudah menuntut "uji lulus termasuk dua pemanggilan bersamaan" — syarat yang sama diterapkan di sini
- **Status verifikasi:** DUGAAN (cacat desain terbaca jelas di kode; pembuktian empirisnya butuh dua koneksi bersamaan yang tidak tersedia di harness PGlite)

### [PR-17] Klaim pertama di setiap paket review kosong, dan klaim yang muncul bukan dari commit yang direview
- **Tingkat:** K-4
- **Artefak:** `alat/review-pr.py:109` (`if not b or b.startswith("- ") and len(b) > 12:` → karena presedens Python dibaca `not b or (startswith and len>12)`, **baris kosong lolos** dan ikut ditambahkan sebagai klaim) dan `:120` (`gabung = klaim[:8] + bawaan`)
- **Klaim yang dilanggar:** paket §3 "Klaim yang wajib kamu bantah" (butir 1 kosong); `docs/uji/PROTOKOL_REVIEW_PR_INDEPENDEN.md` §4 (paket berisi "klaim yang harus dibantah")
- **Bukti:** memuat modul lalu memanggil fungsinya: `klaim_dari_commit("origin/main","7e8c0b9…")` → `jumlah klaim: 8` dengan `1. [0 huruf] ''`; `git log -1 --format=%b a34c8d4` → baris `- Paket baru docs/uji/review-pr/PKT-…putaran7-SIAP-TEMPEL.md` dan `- REVIEW_PR_RIWAYAT baris 3 …` = persis klaim #2 dan #3 paket. Jadi dari 95 commit, klaim yang diangkat adalah **1 klaim kosong + 2 klaim milik commit sebelumnya** (badan commit `7e8c0b9` sendiri berbentuk prosa/daftar bernomor, tidak ada baris `- `, sehingga tidak terangkat sama sekali) + 5 klaim bawaan
- **Skenario gagal:** peninjau menghabiskan satu baris wajib (dari lantai 5 klaim) untuk "membantah" klaim kosong, dan klaim nyata commit yang direview (mis. "3 penjaga baru", "npm audit 0", "MIN_ALUR = 12") tidak pernah masuk daftar yang wajib dibantah — padahal justru itu yang paling perlu diuji
- **Dugaan penyebab:** ekspresi boolean tanpa tanda kurung + strategi "ambil 8 baris pertama dari log terbalik" yang tidak menyaring commit mana yang jadi target
- **Cara membuktikan perbaikan:** `klaim_dari_commit(...)` tidak pernah mengembalikan string kosong (uji unit/`--uji-diri` baru), dan klaim commit **target** diutamakan; paket putaran berikutnya tidak lagi memuat butir kosong
- **Status verifikasi:** TERVERIFIKASI

### [PR-18] Paket review menggambarkan PR 95 commit sebagai 12 judul commit + 3 contoh berkas per jalur
- **Tingkat:** K-4
- **Artefak:** `alat/review-pr.py:158` (`tujuan = [...][:12]`) dan `:167-175` (tabel jalur risiko hanya memuat 3 "Contoh berkas")
- **Klaim yang dilanggar:** `docs/uji/PROTOKOL_REVIEW_PR_INDEPENDEN.md` §4 butir 1 (paket memuat "**daftar berkas per jalur risiko**" dan "ringkasan perubahan **per tujuan** (bukan per berkas)")
- **Bukti:** `git rev-list --count origin/main..7e8c0b9` → **95**; paket §1 memuat **12** judul; `git diff --shortstat origin/main...7e8c0b9` → **338** berkas, paket §2 memuat `Merah 55 · Kuning 110 · Hijau 173` dengan 3 contoh masing-masing (total 9 berkas bernama dari 338). Untuk mengetahui 55 berkas Merah saya harus menghitung sendiri (`git diff --name-status` + pola di `review-pr.py:33-45`)
- **Skenario gagal:** peninjau yang percaya §1 akan mengira PR ini soal "verifikasi permintaan Lee + Buku Uji + 3 penjaga" dan melewatkan bahwa yang dikirim adalah **seluruh aplikasi** (11 migrasi, 21 uji SQL, 67 berkas `aplikasi/src`, Edge Function, CI) — lingkup review menyempit tanpa disadari, dan itu tidak terdeteksi oleh pemeriksa apa pun
- **Dugaan penyebab:** pembatas `[:12]` dipasang supaya paket tidak panjang, tanpa peringatan bahwa daftarnya terpotong; tidak ada lampiran daftar berkas lengkap
- **Cara membuktikan perbaikan:** paket menyebut jumlah commit & berapa yang ditampilkan ("12 dari 95 judul commit — sisanya di `git log`"), dan melampirkan daftar penuh berkas per jalur (atau berkas pendamping `PKT-…-berkas.txt`)
- **Status verifikasi:** TERVERIFIKASI

### [PR-19] DoD T1-15 masih menjanjikan menutup celah yang sudah ditutup PR ini, dan komentar pola RLS 0004 bertentangan dengan isinya sendiri
- **Tingkat:** K-4
- **Artefak:** `docs/ROADMAP.md:315` (DoD T1-15: "…menutup celah sementara T1-10 ('batas lebih bayar dilewati saat total = 0')") vs `supabase/migrations/0010_pembayaran.sql:313-315` (`if coalesce(v_pesanan.total, 0) <= 0 then raise exception 'Total pesanan belum dihitung…'`); dan `supabase/migrations/0004_pola_rls.sql:6-7` ("Policy hanya memakai fungsi identitas dari 0003 … **TIDAK ada subquery langsung ke tabel lain**") vs `0004:44-59` (`pengguna_pilih` berisi `exists (select 1 from public.pengguna_cabang pc …)`)
- **Klaim yang dilanggar:** klaim paket #7 "Dokumen yang menyatakan perilaku … sudah ikut diperbarui — tidak ada klaim basi"
- **Bukti:** `grep -n "menutup celah sementara T1-10" docs/ROADMAP.md` → baris 315; `grep -n "coalesce(v_pesanan.total, 0)" supabase/migrations/0010_pembayaran.sql` → baris 313 bentuk `<= 0` + `raise exception` (celah sudah ditutup, dibuktikan `supabase/tes/gerbang_uang.sql` LULUS); `sed -n '5,7p' 0004_pola_rls.sql` + `sed -n '44,59p'` → subquery langsung ke `pengguna_cabang` ada di dalam policy. Kedua berkas ikut berubah di PR ini (`docs/ROADMAP.md` M, `0004_pola_rls.sql` A)
- **Skenario gagal:** sesi berikutnya membaca DoD T1-15 dan mengira penjaga `total = 0` belum ada → menulis ulang penjaga yang sudah ada (migrasi dobel) atau, lebih buruk, "membuka sementara" celah yang sudah ditutup. Komentar pola RLS yang tidak sesuai isinya membuat peninjau berikutnya salah menyimpulkan aturan (mis. menolak policy baru yang sah, atau menyalin pola subquery sambil mengira itu dilarang)
- **Dugaan penyebab:** teks DoD ditulis sebelum perbaikan K-1 audit (`b936ddd`) dan tidak disegarkan; komentar 0004 ditulis saat policy-nya masih sederhana lalu `pengguna_pilih` diperluas tanpa memperbarui komentar
- **Cara membuktikan perbaikan:** `grep -n "total = 0" docs/ROADMAP.md` tidak lagi menjanjikan penutupan celah yang sudah tertutup (atau menandainya "sudah ditutup di 0010:313"); komentar `0004:6-7` disesuaikan dengan kenyataan (atau subquery di `pengguna_pilih` diganti fungsi identitas baru)
- **Status verifikasi:** TERVERIFIKASI

## 5. Kalibrasi cacat tanaman

**Ditemukan: 4 dari 4 · temuan palsu: 0**

- **Bahan:** `docs/uji/kalibrasi/pr-bahan-2026-09-17.diff` (51 baris, 4 berkas, 4 hunk).
- **Angka `Y` = 4 adalah hitungan saya sendiri** atas perubahan yang tertanam di bahan itu (satu per hunk). Saya **tidak**
  diberi tahu jumlah/kelas resminya dan **tidak mencari kunci jawaban**: `alat/kalibrasi-cacat.json` (katalog yang
  dipakai pembuat bahan) sengaja **tidak saya buka**, dan `/tmp/KUNCI-KALIBRASI-PR-*.md` tidak saya cari. Kalau kunci
  resmi menyebut jumlah lain, angka di atas harus dikoreksi oleh sesi kerja.
- **Bahan tidak bisa dipasang seperti diperintahkan paket §5** (`git apply` exit 128 `corrupt patch at line 39`;
  `--recount` → `patch does not apply` di `0010:293`; `patch --fuzz=5` exit 2) → **PR-06**. Karena itu keempat cacat
  saya pasang **dengan tangan** di salinan `/tmp/kalib` (pengganti teks persis seperti isi hunk; untuk hunk ke-4 saya
  sesuaikan dengan `0010` versi sekarang: `if v_sebelum > v_pesanan.total + 100000 then`), satu per satu dan
  sekaligus, lalu `node alat/uji-sql.mjs` dijalankan tiap kali dan `git checkout -- supabase/migrations` untuk memulihkan.

Daftar cacat yang saya temukan (semua **K-1/K-2 kelas keamanan & uang** menurut penilaian saya):

| # | Berkas:baris bahan | Cacat yang ditanam | Kelas menurut saya | Tertangkap gerbang? |
|---|---|---|---|---|
| D1 | `0003_helper_identitas.sql` (hunk `@@ -63,7`) | `join public.pengguna p … and p.aktif` di dalam `cabang_saya()` dihapus → akun nonaktif tetap mendapat identitas cabang | K-2 (akses; gagal-aman sebagian karena `penyewa_saya()`/`peran_saya()` tetap menyaring `aktif`) | **TIDAK** — `21 LULUS · 0 GAGAL` rc=0 → saya laporkan sebagai PR-12 |
| D2 | `0004_pola_rls.sql` (hunk `@@ -76,7`) | `pengaturan_pilih using (penyewa_id = penyewa_saya())` → `using (penyewa_id is not null)` = semua resto membaca pengaturan resto lain | K-1 (bocor lintas penyewa) | ya — `19 LULUS · 2 GAGAL` (`rls_pengguna.sql`, `rls_semua_tabel.sql`) |
| D3 | `0005_izin_berjenjang.sql` (hunk `@@ -298,7`) | `revoke all on function public.boleh(text, uuid) from public;` dihapus → EXECUTE bawaan PUBLIC hidup lagi | K-3 (pertahanan berlapis; `boleh()` tanpa identitas tetap menjawab `false`) | ya — `20 LULUS · 1 GAGAL` (`izin.sql`) |
| D4 | `0010_pembayaran.sql` (hunk `@@ -293,7`) | batas lebih bayar dilonggarkan jadi `> v_pesanan.total + 100000` | K-1 (uang) | ya — `19 LULUS · 2 GAGAL` (`gerbang_uang.sql`, `pembayaran.sql`) |

Keempatnya sekaligus → `16 LULUS · 5 GAGAL` (`gerbang_uang`, `izin`, `pembayaran`, `rls_pengguna`, `rls_semua_tabel`);
dipulihkan → `21 LULUS · 0 GAGAL`.

**Temuan palsu: 0** — saya tidak mengklaim cacat yang tidak ada di bahan. Catatan jujur: D1 saya temukan dengan
membaca diff (bukan karena gerbang merah), dan justru karena gerbang tetap hijau saya mengubahnya menjadi temuan
PR-12 tentang mutu uji.

## 6. Yang tidak bisa saya verifikasi

- **Perilaku di Supabase/PostgreSQL nyata.** Tidak ada akun, URL proyek, atau kredensial (`docs/uji/BUKU_UJI_PEMILIK.md`
  P-01 masih kosong; T0-00/T0-08/T0-09 masih `[ ]`). Semua uji SQL saya berjalan di **PGlite** dengan `crypt()`/`gen_salt()`
  **tiruan** (`alat/uji-sql.mjs:88-118`, berlabel "BUKAN pengganti produksi") — jadi biaya/kekuatan bcrypt asli, perilaku
  `pgcrypto`, `request.jwt.claims` sungguhan dari PostgREST, dan isi JWT Supabase (PR-15) belum teruji.
- **Edge Function secara runtime.** Deno tidak tersedia di ruang kerja; `supabase/functions/verifikasi_pin/index.ts`
  hanya saya baca dan diverifikasi statis lewat `python3 alat/periksa-fungsi-pin.py` (`9 lolos, 0 gagal`). Permintaan
  HTTP sungguhan (termasuk perilaku `CORS: Access-Control-Allow-Origin: *` dan balasan 200 untuk kegagalan) belum ditembak.
- **Kondisi bersamaan ( concurrency ).** PR-16 berstatus DUGAAN karena harness PGlite satu koneksi; butuh dua sesi
  PostgreSQL nyata untuk membuktikannya.
- **Isi `alat/kalibrasi-cacat.json`.** Sengaja tidak dibuka (berpotensi memuat resep/kunci cacat tanaman). Perannya
  saya simpulkan hanya dari kode pemakainya (`KATALOG` di `review-pr.py:29` dan `audit-independen.py:29`).
- **Jumlah cacat kalibrasi resmi (`Y`).** Kunci jawaban tidak saya cari; `Y = 4` di bagian 5 adalah hitungan saya atas
  bahan, sehingga skor `4 dari 4` harus dinilai ulang oleh sesi kerja terhadap kunci aslinya.
- **Lingkungan GitHub Actions.** Saya hanya membaca `check-runs` lewat `gh api` (2 `success` di `7e8c0b9`); log langkah
  per langkah di runner tidak saya buka, jadi kesamaan persis keluaran CI vs lokal saya simpulkan dari daftar langkah
  di `ci.yml` yang saya jalankan ulang semua di lokal.
- **67 berkas `aplikasi/src` dan 58 berkas `prototipe` baris-per-baris.** Diperiksa lewat gerbang (lint/tipe/51 uji unit/
  build/kontras 166) dan struktur, bukan dibaca satu per satu; belum ada layar uang/izin (semua folder layar `.gitkeep`).

## 7. Pernyataan tidak mengubah apa pun

Saya **hanya-baca** dan **bukan sesi penulis PR** ini (sesi saya `arena/01a0afdb-resto-barokah`, berbasis `main`
`253d129`; PR ditulis di `arena/01a0a8a2-resto-barokah`). Saya **tidak mengubah** berkas apa pun di repo ini:
SATU-SATUNYA berkas yang saya buat adalah laporan ini (`docs/uji/review-pr/LAPORAN_2026-09-17_pr-01-putaran8__01a0afdb.md`).
Semua percobaan — checkout commit, pemasangan dependensi, 10 uji mutasi, 7 probe serangan, pemasangan bahan kalibrasi,
3 laporan uji pemeriksa — dijalankan di **salinan di luar repo** (`/tmp/rb-review`, `/tmp/pristine`, `/tmp/kalib`,
`/tmp/rb-head`, `/tmp/probe`); repo ini tidak pernah di-`checkout` ke commit lain dan tidak ada berkas kerjanya yang
disunting. Bukti: `git status --short` di repo ini menampilkan hanya berkas laporan ini.

## 8. Temuan di luar cakupan diff (WAJIB — boleh "tidak ada")

| # | Temuan | Mengapa di luar cakupan diff | Bukti | Saran ditindaklanjuti |
|---|---|---|---|---|
| 1 | Kepala PR sudah bergerak melewati commit yang direview: paket putaran8 mengikat `7e8c0b9`, sedangkan `headRefOid` PR #1 = `f59debd`. Selisihnya **hanya 3 berkas netral** (2 paket + 1 baris riwayat), jadi secara substansi review ini tetap berlaku untuk kepala PR — tetapi gerbang merge butir 1–2 ("CI hijau **pada commit PR terakhir**" + "RV-2 dijalankan **pada commit itu**") harus dinilai ulang terhadap `f59debd`, bukan `7e8c0b9` | bukan isi diff `origin/main...7e8c0b9`; ini keadaan ref GitHub **sesudah** commit yang direview (commit yang membuat paket itu sendiri) | `gh pr view 1 --json headRefOid` → `f59debd25785fa7cbc2a7c9b890d6faf57c24262`; `git diff --shortstat origin/main...f59debd` → `340 files`; `git diff --name-only 7e8c0b9 f59debd` → 3 berkas (`docs/uji/REVIEW_PR_RIWAYAT.md` + 2 `PKT-…putaran8`); `python3 alat/review-pr.py --kesiapan` di kepala → `SIAP … (target 7e8c0b95 + hanya berkas paket)`; `gh pr view 1 --json statusCheckRollup` → 2 check `SUCCESS` selesai 14:26:20/14:26:25 (sesudah push `f59debd` 14:25:34) | sesi kerja mencatat di `REVIEW_PR_RIWAYAT` baris 4 bahwa RV-2 dijalankan pada `7e8c0b9` dan kepala `f59debd` hanya menambah berkas netral; bila ada commit non-netral berikutnya, paket **wajib** disegarkan sebelum Lee diminta merge |
| 2 | Riwayat kalibrasi review PR (`docs/uji/REVIEW_PR_RIWAYAT.md` §2) masih `(belum ada)` — jadi untuk PR Jalur Merah ini gerbang merge butir 4 ("hasil kalibrasi terakhir tidak 'gagal'") **tidak bisa dinilai sama sekali**, bukan "lulus" | ini keadaan **mekanisme/proses** (baris riwayat yang kosong + bahan yang rusak), bukan baris kode di diff; akibatnya baru terlihat saat keputusan merge diambil | `git show f59debd:docs/uji/REVIEW_PR_RIWAYAT.md \| sed -n '17,21p'` → `\| — \| (belum ada) \| — \| — \| — \| — \| Bahan dibuat dengan --kalibrasi-pr-siapkan`; ditambah PR-06 (bahan satu-satunya tidak bisa dipasang) | perbaiki pembuat bahan (PR-06), jalankan RV-3 sekali, isi §2 dengan `Ditemukan: X dari Y` + temuan palsu; sampai itu terisi, Kartu Keputusan wajib menulis "kalibrasi: belum pernah dijalankan" (jangan dibiarkan kosong/dianggap lulus) |
| 3 | Berkas yang berpotensi memuat resep cacat kalibrasi (`alat/kalibrasi-cacat.json`, 3923 byte) **ter-commit di dalam repo yang sama** dengan bahan & laporan, sementara `docs/uji/kalibrasi/CARA-PAKAI.md` menetapkan kunci jawaban berada "di luar repo". Saya **tidak** membuka isinya, jadi tidak bisa memastikan apakah ia memuat jawaban bahan PR ini | berkasnya ada di diff, tetapi **risikonya** menyangkut seluruh mekanisme kalibrasi lintas sesi (bukan perubahan kode PR ini) dan hanya bisa dinilai oleh pihak yang boleh membacanya | `git ls-files alat/kalibrasi-cacat.json` → tercetak (terlacak); `ls -l alat/kalibrasi-cacat.json` → `3923` byte; `grep -n KATALOG alat/review-pr.py` → baris 29; `docs/uji/kalibrasi/CARA-PAKAI.md` baris tabel "Kunci jawaban: berkas di luar repo"; `docs/uji/PROTOKOL_REVIEW_PR_INDEPENDEN.md` §9.3 mengakui "Kunci kalibrasi ada di sistem berkas — kontrol proses, bukan kontrol teknis" | sesi kerja (yang boleh membacanya) memastikan katalog itu **tidak** memuat pasangan `cari/ganti` yang sama persis dengan bahan `pr-bahan-2026-09-17.diff`; kalau ternyata memuat, pindahkan katalog ke luar repo atau acakkan cacat per-putaran supaya peninjau berikutnya tidak bisa menebak dari katalog |
| 4 | Cabang dasar (`main` = `253d129`) belum pernah dilewatkan CI sama sekali, jadi seluruh gerbang otomatis baru pertama kali berlaku bila PR ini di-merge | `main` bukan bagian dari diff PR ini (ia dasarnya); ini catatan untuk keputusan merge, bukan cacat kode PR | `gh api repos/With-AI-Agent/Resto-Barokah/commits/253d1297a3b81433d7f5809afd257d8a1b40958f/check-runs --jq '.total_count'` → `0`; `git ls-remote origin` → `refs/heads/main` = `253d129…` | setelah merge, jalankan `bash aplikasi/alat/periksa-semua.sh` sekali di `main` dan simpan keluarannya sebagai titik dasar; temuan B F-17 ("commit yang diaudit tidak pernah dilewatkan CI", masih TERBUKA → T1-44) diperluas agar mencakup commit dasar |
