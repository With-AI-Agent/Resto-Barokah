# LAPORAN REVIEW PR INDEPENDEN — pr-01-putaran11 — 2026-09-17

- **Paket review:** `docs/uji/review-pr/PKT-2026-09-17-pr-01-putaran11.md`
- **Commit yang direview:** `750889abf3055cdc1019490b9f3e4bbd10eb8062`
- **Tingkat risiko:** Merah
- **Verdict:** TIDAK-BERSIH

Peninjau: sesi independen (bukan sesi penulis PR), hanya-baca. Kedalaman yang dijalankan sesuai
Jalur Merah: **L1 (ancaman & akses) + L2 (uang & jejak) + L4 (mutu uji)** — ketiganya dijalankan.
Semua eksekusi (gerbang, uji SQL, probe, uji mutasi, longgar-RLS, kalibrasi) dilakukan di salinan
tersendiri di `/tmp` (klon pada commit yang direview + worktree sekali-pakai yang dihapus setelahnya);
tidak ada satu pun berkas di repo kerja yang disentuh selain laporan ini.

## 1. Cakupan diff

Diff yang direview: `git diff origin/main 750889abf3055cdc1019490b9f3e4bbd10eb8062` =
**369 berkas · +47630 / −132 baris** (persis angka paket). Rincian: 361 berkas baru (A), 8 diubah (M).
Mesin membagi jalur: 66 Merah / 117 Kuning / 186 Hijau.

| # | File | Jalur risiko | Diperiksa | Bukti (perintah/temuan) |
|---|---|---|---|---|
| 1 | `supabase/migrations/0001…0012` (12 file, ±2.600 baris SQL) | Merah | ya — dibaca penuh + dieksekusi di PostgreSQL nyata (PGlite di Node) pada commit ini | `node alat/uji-sql.mjs` → `uji: 28 LULUS · 0 GAGAL`; probe bypass `/tmp/probe1…probe9` (lihat §2/§4) |
| 2 | `supabase/tes/*.sql` (28 file uji) | Merah | 8 dibaca penuh (`rls_pengguna`, `rls_semua_tabel`, `status_pesanan`, `isolasi_lintas_penyewa`, `izin` sebagian, `percobaan_pin_perangkat` via dok); 28 file semuanya dieksekusi | `node alat/uji-sql.mjs` → 28 LULUS; 24 file memakai `harap_gagal` (uji negatif nyata) |
| 3 | `supabase/functions/verifikasi_pin/index.ts` | Merah | ya — dibaca penuh | `python3 alat/periksa-fungsi-pin.py` → LOLOS (hanya POST, tanpa `console.*`, tanpa `service_role`, kunci dari env) |
| 4 | `.github/workflows/ci.yml` | Merah | ya — dibaca penuh | file baru (tidak ada CI di `main`); langkahnya dieksekusi ulang manual via `bash aplikasi/alat/periksa-semua.sh` — LOLOS |
| 5 | `_sistem/validate_system.py` (M) | Merah | ya — diff vs `main` + dieksekusi | `python3 _sistem/validate_system.py` → `SYSTEM-BUILDING-APLIKASI VALIDATOR: PASS`; diff = perluasan scan rujukan ke seluruh `docs/` + 2 pengecualian sempit terdokumentasi (lihat §2 klaim 10) |
| 6 | `alat/uji-sql.mjs` (pelakas uji SQL: peran/auth palsu + tiruan pgcrypto berlabel) | Merah | ya — dibaca penuh | dijalankan 4× (asli, longgar-RLS, kalibrasi, salinan baru) — perilaku konsisten |
| 7 | `alat/uji-mutasi-0012.py` | Merah | ya — dieksekusi penuh | `python3 alat/uji-mutasi-0012.py` → `RINGKASAN: 12/12 mutasi WAJIB terbukti MERAH` + kontrol hijau (fenced §3) |
| 8 | `alat/review-pr.py` (mekanisme paket + pemeriksa laporan + penilai kalibrasi) | Merah | ya — logika `--periksa-laporan`/kalibrasi dibaca; `--uji-diri` dieksekusi | `python3 alat/review-pr.py --uji-diri` → LOLOS (6 contoh: 1 bagus, 5 buruk — semua ditolak/diterima sesuai harapan) |
| 9 | `alat/audit-independen.py` | Merah | ya — `--uji-diri` dieksekusi | `python3 alat/audit-independen.py --uji-diri` → LOLOS |
| 10 | `alat/periksa-buku-uji.py`, `alat/periksa-bersih.py`, `periksa-fungsi-pin.py`, `periksa-panduan.py`, `periksa-roadmap.py`, `periksa-rujukan.py`, `periksa-temuan-audit.py`, `periksa-fondasi-independen.py`, `periksa-rahasia.py`, `bantu_uji_diri.py`, `mulai-sesi.py`, `pulihkan-git.sh`, `kalibrasi-cacat.json` | Merah | logika guard yang relevan dibaca (`_cek_paket_disebut`, `uji_perintah_buku_uji`); semuanya dieksekusi (termasuk `--uji-diri`) | `bash aplikasi/alat/periksa-semua.sh` → `SEMUA PEMERIKSAAN LOLOS.` |
| 11 | `aplikasi/alat/periksa-semua.sh`, `periksa-struktur.py`, `periksa-kerapatan.py`, `periksa-komponen-env.py`, `periksa-uji.py`, `uji-kontras.py`, `pratinjau.sh` | Merah | `periksa-semua.sh` + `periksa-struktur.py` dibaca penuh; semuanya dieksekusi | periksa-semua.sh di klon baru → LOLOS; `uji-kontras` → `166 lolos, 0 gagal` |
| 12 | `aplikasi/src/**` (34 file: komponen, hook, lib, layar kerangka + 9 file uji) | Kuning | `lib/env.ts` dibaca penuh; selebihnya dieksekusi | vitest → `Test Files 9 passed (9)` · `Tests 66 passed (66)`; `npm run build` + `npm audit --audit-level=low` LOLOS |
| 13 | `aplikasi` config: `package.json` + lock, `tsconfig*`, `vite.config.ts`, `eslint.config.js`, `.prettierrc.json`, `.env.example` | Kuning | `.env.example` + lock dieksekusi (`npm ci`); struktur dibaca | `npm ci` sukses; `.env.example` hanya memuat 2 nilai publik (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`), rahasia sengaja dikomentari |
| 14 | `docs/uji/**` (protokol, BUKU_UJI_PEMILIK, paket putaran 5–11, laporan putaran 8, AUDIT_RIWAYAT, TEMUAN_LUAR_CAKUPAN, kalibrasi) | Merah/Kuning | PROTOKOL_REVIEW_PR_INDEPENDEN + BUKU_UJI_PEMILIK + TEMUAN_LUAR_CAKUPAN + bahan kalibrasi dibaca; bahan kalibrasi dieksekusi di salinan /tmp | `git apply docs/uji/kalibrasi/pr-bahan-2026-09-17.diff` + `node alat/uji-sql.mjs` → 4 GAGAL (lihat §5) |
| 15 | `docs/KEAMANAN.md`, `TECH_SPEC.md`, `PRD.md`, `ROADMAP.md`, `DISCOVERY.md`, `DECISIONS_LOG.md`, `SPESIFIKASI_UI.md`, `TERTANGGUH.md`, `AGENT_OPERATING_GUIDE.md`, `docs/ops/**`, `docs/teknis/**` | Kuning | bagian yang menyatakan perilaku keamanan/uang dibaca (KEAMANAN §1–4b, DECISIONS_LOG F-11/T1-23, TECH_SPEC §8–9, ROADMAP T1-12/15/19/20/23); semuanya dijaga `periksa-panduan`/`periksa-roadmap`/`periksa-rujukan` | ketiga penjaga → LOLOS; temuan klaim basi di `PANDUAN_PENGGUNA.md` (lihat PR-04) |
| 16 | `PANDUAN_PENGGUNA.md` (M), `ACCEPTANCE_TESTS.md` (M), `PROFIL_PENGGUNA.md` (M), `PROMPT_ENTRI_UNIVERSAL.md` (M), `STATUS.md` (M), `docs/README.md` (M) | Kuning | diff dibaca penuh | diff = pemutakhiran isi (buku induk v2, catatan AT-05/08); `periksa-panduan.py` → LOLOS kecuali klaim basi PR-04 |
| 17 | `prototipe/**` (38 file: HTML, CSS, JS, aset gambar/font) | Hijau | tidak dibaca baris demi baris — dijaga gerbang | `prototipe/alat/periksa-halaman.py` + `aplikasi/alat/uji-kontras.py` di dalam periksa-semua → LOLOS (166 pemeriksaan) |
| 18 | `docs/desain/**` (±60 file gambar referensi/mockup) | Hijau | tidak dibaca (aset biner) | diluar jangkauan semantik; tidak ada klaim perilaku yang bergantung padanya |
| 19 | font woff2 + `LISENSI-*.txt` (aplikasi & prototipe), `aplikasi/public/**` | Hijau | tidak dibaca baris demi baris | `periksa-kerapatan.py`/`uji-kontras` menghitung berat huruf (461 KB) → LOLOS; `periksa-bersih.py` → tidak ada artefak build di Git |
| 20 | `_log-sesi/LOG_SESI_2026-09-16/17.md`, `_salinan-meta/STATUS_SISTEM_v0.2.0_HISTORI.md` | Hijau | tidak dibaca baris demi baris (catatan sesi/arsip) | `periksa-rujukan.py` → LOLOS (rujukan di dalamnya hidup) |
| 21 | `package.json` + `package-lock.json` (akar), `.gitignore` | Hijau | dieksekusi | `npm audit --audit-level=low` → 0 kerentanan (di dalam periksa-semua) |

Jujur soal batasan: 186 file jalur Hijau (aset desain/font, prototipe, log sesi) tidak saya baca satu
per satu — saya andalkan gerbang otomatis yang memang dirancang untuk lapisan itu dan yang saya
jalankan sendiri sampai hijau. File jalur Merah/Kuning yang menyatakan perilaku (migrasi, uji, guard,
dokumen keamanan) saya baca dan/atau eksekusi langsung.

## 2. Klaim yang dibantah

| # | Klaim (paket §3) | Cara membantah | Hasil nyata |
|---|---|---|---|
| 1 | U-04 kini berbunyi: bilang `Siapkan review PR.`, lalu salin berkas (paling baru, folder `docs/uji/review-pr/`) | baca `docs/uji/BUKU_UJI_PEMILIK.md` baris U-04 pada commit ini | tidak terbantah — baris U-04: "1) Buka chat BARU … 2) Bilang `Siapkan review PR.` — agent menyebutkan nama berkas **paling baru** (berkas SIAP-TEMPEL di folder docs/uji/review-pr/); salin SELURUH isi berkas itu" |
| 2 | Penjaga `alat/periksa-buku-uji.py` menerima pola "paling baru" hanya bila menyebut folder | baca fungsi `_cek_paket_disebut` + jalankan `python3 alat/periksa-buku-uji.py --uji-diri` | tidak terbantah dengan satu nuansa: pola itu hanya diterima bila teks memuat `review-pr` **dan** `paling baru` (dan folder memuat minimal 1 paket SIAP-TEMPEL). Jika folder TIDAK disebut, guard diam (tidak error pun) — jadi "menerima hanya bila menyebut folder" benar, tetapi kasus tanpa folder lolos tanpa pemeriksaan. `--uji-diri` → LOLOS (mutasi "paket disebut lebih tua → ditolak" terbukti) |
| 3 | `bash aplikasi/alat/periksa-semua.sh` memasang `aplikasi/node_modules` dan pustaka alat uji sendiri | jalankan di klon baru tanpa node_modules (`/tmp/rb`, checkout 750889a) | tidak terbantah: log memuat `== memasang pustaka aplikasi (sekali saja per salinan) ==` dan `== memasang pustaka alat uji SQL (sekali saja per salinan) ==`, diakhiri `SEMUA PEMERIKSAAN LOLOS.` (fenced §3) |
| 4 | Perintah uji database dibungkus `bash alat/uji-database.sh` (memasang sendiri pustakanya) | jalankan `bash alat/uji-database.sh` di klon baru lain (`/tmp/fresh`, tanpa node_modules) | tidak terbantah: `== memasang pustaka alat uji SQL (sekali saja per salinan) ==` → `added 1 package in 510ms` → `uji: 28 LULUS · 0 GAGAL` (fenced §3) |
| 5 | Penjaga baru di `aplikasi/alat/periksa-struktur.py`: kedua skrip WAJIB memeriksa/memasang pustaka sebelum dijalankan | baca fungsi `uji_perintah_buku_uji` + jalankan `python3 aplikasi/alat/periksa-struktur.py` | tidak terbantah: guard mensyaratkan penanda spesifik (`$APLIKASI/node_modules/.bin/prettier`, `$REPO/alat/node_modules/@electric-sql/pglite`, `npm ci --prefix alat`) dan menolak skrip `set -euo pipefail` yang tidak memeriksa node_modules; keluaran `perintah buku uji: dua perintah uji memasang pustakanya sendiri` |
| 6 | Buku Uji U-02/U-03 + log buku disegarkan; PANDUAN Bagian E menambah baris | baca baris U-02/U-03 + catatan 2026-09-17 di `BUKU_UJI_PEMILIK.md` dan tabel `PANDUAN_PENGGUNA.md` §Bagian E | tidak terbantah: U-02 = `Uji semuanya.`, U-03 = `Uji database.` (keduanya jalur chat, bukan terminal); log buku memuat 2 baris 2026-09-17; Bagian E punya baris `bash alat/uji-database.sh` ↔ `Uji database.` (baris 521) dan `bash aplikasi/alat/periksa-semua.sh` ↔ `Uji semuanya.` (baris 522) |
| 7 | `Uji semuanya.` = kalimat biasa di chat → agent menjalankan periksa-semua | cek pengikatan kalimat→perintah di `PANDUAN_PENGGUNA.md` | tidak terbantah pada lapisan dokumen: baris 522 mengikatkan kalimatnya ke `bash aplikasi/alat/periksa-semua.sh`, dan catatan baris 531 menyatakan keduanya "kalimat biasa di chat". Perilaku agent itu sendiri tidak bisa saya verifikasi (lihat §6) |
| 8 | `Uji database.` = kalimat biasa → agent menjalankan `node alat/uji-sql.mjs` | cek baris 521 + isi `alat/uji-database.sh` | tidak terbantah secara fungsional, dengan presisi berbeda: pengikatannya ke `bash alat/uji-database.sh`, dan skrip itu berakhiran `exec node alat/uji-sql.mjs "$@"` — perintah yang dieksekusi pada akhirnya identik |
| 9 | Uji otomatis membuktikan perilaku baru/bebas regresi **pada commit ini** (bukan commit sebelumnya) | checkout persis `750889a` di salinan /tmp, jalankan seluruh suite di atas pohon itu | tidak terbantah: di pohon 750889a sendiri → SQL `uji: 28 LULUS · 0 GAGAL`, unit 66/66, mutasi 12/12 MERAH + kontrol hijau. Baris-baris uji baru (diskon_cap, harga_item, persetujuan_void, jejak_pengaturan, stok_arah, izin_efektif_untuk, cabang_sesi) mengevaluasi perilaku migrasi 0011/0012 yang ada di commit ini |
| 10 | Tidak ada gerbang keamanan/CI yang dilemahkan (ambang diturunkan, uji dimatikan, revoke/hak dicabut dihapus) | diff 8 file M vs `main`; grep `revoke` di seluruh diff; cek CI (file baru); cek uji disabled (`skip`/`xit`/`todo`); bandingkan grant 0006→0011→0012 | tidak terbantah, dengan dua catatan. (a) `main` tidak punya CI — CI di PR adalah file baru yang ketat (npm audit level low, mutasi 12/12, semua guard + `--uji-diri`). (b) 0012 menambah revoke (`izin_efektif_untuk`, `boleh_untuk` dicabut dari `anon,authenticated` — lebih ketat dari 0006). Tidak ada uji disabled. Satu pengecualian scope pada `_sistem/validate_system.py` (M): scan rujukan mengecualikan `docs/uji/kalibrasi/bahan-*` dan `docs/uji/{audit,review-pr}/LAPORAN_*` — terdokumentasi dengan alasan (barang bukti tidak boleh disunting), sempit, dan bukan gerbang keamanan; saya catat sebagai pengamatan mekanisme, bukan pelemahan |
| 11 | Perubahan pada jalur uang/keamanan/data pelanggan tidak bisa dilewati lewat pemanggilan langsung (RPC/API) | probe SQL langsung sebagai klien (`set local role authenticated`) pada commit ini: insert pesanan status `lunas`/`batal`, insert diskon `voucher` 100%, insert pembatalan dengan `nilai_kerugian` dikarang, panggil fungsi pemicu langsung, periksa `peran_peladen()` sebagai klien | **TERBANTAH (bagian)**: 3 celah terverifikasi — PR-01 (diskon `voucher` tanpa voucher, K-2), PR-02 (status `lunas`/`batal` bisa disisipkan langsung, K-3), PR-03 (`nilai_kerugian` bisa dikarang, K-3). Yang TIDAK bisa dilewati (terbukti): angka uang pesanan (trigger menolak insert/update non-peladen), harga item (harus `harga_berlaku` kecuali izin `ubah_harga`), overpay, status via UPDATE, fungsi pemicu (error saat dipanggil klien), `peran_peladen()` sebagai klien = `false` |
| 12 | Dokumen yang menyatakan perilaku (fondasi, buku induk, panduan Lee) sudah ikut diperbarui — tidak ada klaim basi | bandingkan angka yang diklaim dokumen hidup dengan keadaan commit: `ls supabase/tes/*.sql \| wc -l` vs teks `PANDUAN_PENGGUNA.md` & `docs/TERTANGGUH.md`; cek klaim PIN/perangkat di DECISIONS_LOG/KEAMANAN | **TERBANTAH (bagian)**: `PANDUAN_PENGGUNA.md:63` (A2 "peta kejujuran") dan `docs/TERTANGGUH.md:27` (T-018) masih menulis "21 berkas uji" padahal commit ini punya **28** (7 uji baru dari 0012 tidak ikut memperbarui angka); `periksa-panduan.py` tidak memeriksa angka itu (LOLOS padahal basi). Sisanya jujur: KEAMANAN.md §2 bahkan menulis sendiri lapisan per-perangkat PIN yang belum berlaku (F-11), DECISIONS_LOG mencatat perubahan PIN 4–6→6 (T1-23) |

## 3. Pemeriksaan gerbang

| # | Perintah | Hasil nyata (ringkas) |
|---|---|---|
| 1 | `bash aplikasi/alat/periksa-semua.sh` (klon baru, commit 750889a) | lihat fenced A — seluruh pemeriksa repo + aplikasi, `SEMUA PEMERIKSAAN LOLOS.` |
| 2 | `node alat/uji-sql.mjs` | lihat fenced B — `uji: 28 LULUS · 0 GAGAL` |
| 3 | `git diff --shortstat origin/main 750889abf3055cdc1019490b9f3e4bbd10eb8062` | lihat fenced C — 369 berkas, cocok dengan paket; diff `750889a..tip` hanya 7 file dok/paket (§8) |
| 4 | `python3 _sistem/validate_system.py` + `python3 alat/periksa-roadmap.py` + `python3 alat/periksa-panduan.py` | lihat fenced D — `PASS` / `LOLOS` / `LOLOS` |
| 5 | `python3 alat/audit-independen.py --uji-diri` + `python3 alat/review-pr.py --uji-diri` | lihat fenced E — keduanya `HASIL: LOLOS` (pemeriksa terbukti bisa menolak) |
| 6 | uji mutasi pada gerbang perubahan ini: `python3 alat/uji-mutasi-0012.py` | lihat fenced F — 12/12 mutasi MERAH + kontrol hijau (wajib Jalur Merah) |
| 7 | uji longgar RLS (dikerjakan peninjau, bukan bawaan repo): longgar policy `pengguna_pilih` (hapus cek `penyewa_id = penyewa_saya()`) di worktree /tmp, lalu `node alat/uji-sql.mjs supabase/tes/rls_pengguna.sql` | lihat fenced G — GAGAL saat dilonggarkan (exit 1), hijau (exit 0) di commit asli |
| 8 | `bash alat/uji-database.sh` di klon baru tanpa node_modules | lihat fenced H — pasang sendiri + 28 LULUS |
| 9 | bahan kalibrasi: `git apply docs/uji/kalibrasi/pr-bahan-2026-09-17.diff` di salinan /tmp, lalu `node alat/uji-sql.mjs` | lihat fenced I — 4 GAGAL (detail §5) |

A. `bash aplikasi/alat/periksa-semua.sh` (klon baru `/tmp/rb`, tanpa node_modules):

```
== memasang pustaka aplikasi (sekali saja per salinan) ==
== memasang pustaka alat uji SQL (sekali saja per salinan) ==
 Test Files  9 passed (9)
      Tests  66 passed (66)
uji: 28 LULUS · 0 GAGAL
HASIL: LOLOS
== bukti mutasi pagar migrasi 0012 (kontrol hijau + 12/12 WAJIB MERAH) ==
RINGKASAN: 12/12 mutasi WAJIB terbukti MERAH (+ 2 mutasi tunggal pada penjaga bertumpuk:
  hijau = sesuai dugaan; lapisan itu dibuktikan lewat mutasi gabungan M3k)
SYSTEM-BUILDING-APLIKASI VALIDATOR: PASS
HASIL: LOLOS — semua pemeriksaan roadmap terpenuhi.
HASIL: LOLOS — buku induk lengkap, rujukan hidup, prompt identik dengan sumbernya.
SEMUA PEMERIKSAAN LOLOS.
(exit 0)
```

B. `node alat/uji-sql.mjs` (commit 750889a):

```
  LULUS supabase/tes/rls_pengguna.sql
  LULUS supabase/tes/rls_penyewa.sql
  LULUS supabase/tes/rls_semua_tabel.sql
  LULUS supabase/tes/status_pesanan.sql
  LULUS supabase/tes/stok_arah.sql
----------------------------------------------------------------------
uji: 28 LULUS · 0 GAGAL
HASIL: LOLOS
```

C. `git diff --shortstat origin/main 750889abf3055cdc1019490b9f3e4bbd10eb8062`:

```
 369 files changed, 47630 insertions(+), 132 deletions(-)
```

D. pemeriksa dokumen:

```
$ python3 _sistem/validate_system.py
SYSTEM-BUILDING-APLIKASI VALIDATOR: PASS
$ python3 alat/periksa-roadmap.py
HASIL: LOLOS — semua pemeriksaan roadmap terpenuhi.
$ python3 alat/periksa-panduan.py
HASIL: LOLOS — buku induk lengkap, rujukan hidup, prompt identik dengan sumbernya.
```

E. mekanisme tidak tumpul:

```
$ python3 alat/audit-independen.py --uji-diri
HASIL: LOLOS — pemeriksa laporan & penilai kalibrasi terbukti bisa MENOLAK yang buruk dan
MENERIMA yang baik, dan jalur pulang laporan (auditor → sesi kerja) terbukti bekerja.
$ python3 alat/review-pr.py --uji-diri
  [OK ] buruk-klaim-kurang.md: hasil=1 harapan=1 (klaim dibantah & gerbang terlalu sedikit)
  [OK ] buruk-verdict-longgar.md: hasil=1 harapan=1 (verdict bukan TIDAK-BERSIH padahal ada
        K-1/K-2 TERVERIFIKASI)
  [OK ] buruk-kedalaman-kata.md: hasil=1 harapan=1 (jalur Merah hanya cocok kata di prosa,
        tanpa blok bukti)
  [OK ] buruk-paket-hilang.md: hasil=1 harapan=1 (paket review yang dirujuk tidak ada)
HASIL: LOLOS — pemeriksa laporan review PR terbukti bisa MENOLAK yang buruk dan MENERIMA yang baik.
```

F. uji mutasi (wajib Jalur Merah — sengaja rusak → MERAH → pulihkan → hijau):

```
UJI MUTASI — penutup celah review putaran8 (0012)
  OK  M1 diskon 'promo' dibuka lagi (tanpa cabang izin): MERAH (diskon_cap.sql)
  OK  M2 cap total resto tidak dibaca: MERAH (diskon_cap.sql)
  INFO M3a diskon boleh ditanam saat subtotal 0: HIJAU (sesuai dugaan: penjaga kembarnya masih menahan)
  INFO M3b diskon boleh melebihi subtotal (penjaga kedua ikut dimatikan): HIJAU (sesuai dugaan)
  OK  M4 harga item bebas diisi klien: MERAH (harga_item.sql)
  OK  M5 subtotal dari klien dipercaya: MERAH (harga_item.sql)
  OK  M6 bukti PIN boleh dipakai berulang: MERAH (persetujuan_void.sql)
  OK  M7 pelaku pengaturan boleh dikarang: MERAH (jejak_pengaturan.sql)
  OK  M8 keluar menambah stok lagi: MERAH (stok_arah.sql)
  OK  M9 pemeriksaan keanggotaan cabang dimatikan (izin orang lain): MERAH (izin_efektif_untuk.sql)
  OK  M9b keanggotaan cabang NONAKTIF tetap dianggap berizin: MERAH (izin_efektif_untuk.sql)
  OK  M10 pembatas PIN kembali menghitung atas nama korban: MERAH (pin_kunci_silang.sql)
  OK  M11 akun nonaktif tetap dapat cabang: MERAH (cabang_sesi.sql)
  OK  M3k diskon boleh ditanam pada subtotal 0 (dua penjaga dimatikan sekaligus): MERAH (diskon_cap.sql)
KONTROL PENUTUP (setelah dipulihkan harus hijau)
  OK  semua uji hijau lagi setelah pemulihan
RINGKASAN: 12/12 mutasi WAJIB terbukti MERAH
```

G. uji izin/RLS GAGAL saat dilonggarkan (dikerjakan peninjau): di worktree /tmp, policy
`pengguna_pilih` (`supabase/migrations/0004_pola_rls.sql`) dilembutkan — cek
`penyewa_id = public.penyewa_saya()` dihapus dari cabang `owner_pusat`/`admin_cabang`:

```
# longgar: policy pengguna_pilih tanpa cek penyewa (simulasi regresi)
$ node alat/uji-sql.mjs supabase/tes/rls_pengguna.sql
  GAGAL supabase/tes/rls_pengguna.sql
        HARAPAN TIDAK TERPENUHI: owner pusat melihat 5 pegawai restonya (dapat 7, harap 5)
----------------------------------------------------------------------
uji: 0 LULUS · 1 GAGAL
HASIL: GAGAL
(exit=1)
# kondisi commit asli:
$ node alat/uji-sql.mjs supabase/tes/rls_pengguna.sql   # → LULUS, exit=0
```

H. `bash alat/uji-database.sh` di klon baru (`/tmp/fresh`, tanpa `node_modules`):

```
== memasang pustaka alat uji SQL (sekali saja per salinan) ==
added 1 package in 510ms
UJI SQL LOKAL (PostgreSQL di dalam Node — tanpa server, tanpa akun)
----------------------------------------------------------------------
uji: 28 LULUS · 0 GAGAL
HASIL: LOLOS
(exit 0)
```

I. bahan kalibrasi (fenced penuh di §5).

## 4. Temuan

### [PR-01] Diskon `jenis='voucher'` bisa dicatat kasir hingga 100% subtotal — tanpa voucher, tanpa PIN, di atas batas diskon manualnya
- **Tingkat:** K-2
- **Artefak:** `supabase/migrations/0012_penutup_celah_review.sql:72-75` (cabang `voucher` di `picu_diskon_batas` hanya memeriksa `boleh('pakai_voucher')`), didukung `supabase/migrations/0010_pembayaran.sql:120,531` (tanpa constraint `voucher_id`; policy insert untuk kasir) dan `supabase/migrations/0005_izin_berjenjang.sql:107` (bawaan kasir `pakai_voucher = true`)
- **Klaim yang dilanggar:** paket §3 klaim 11 ("jalur uang tidak bisa dilewati lewat pemanggilan langsung") — dan bertentangan dengan aturan desain 0012 itu sendiri: untuk jenis `promo` yang mesinnya (T1-19/T1-20) belum ada, 0012 menutup jalur secara gagal-aman (`raise exception 'Diskon promo otomatis belum aktif…'`), padahal mesin `voucher` (T1-12/T1-19/T1-20) sama-sama belum ada di commit ini (tabel voucher tidak ada — `grep "create table.*voucher" supabase/` → kosong)
- **Bukti:** probe sebagai kasir resto A (`90000000-…-0004`) pada commit 750889a, pesanan `eeee0000-…-0010` (subtotal 54000):

```
$ node alat/uji-sql.mjs /tmp/probe2_voucher.sql
  LULUS /tmp/probe2_voucher.sql
uji: 1 LULUS · 0 GAGAL
   (isi probe: insert diskon_transaksi (pesanan_id, jenis, nilai)
    values ('eeee0000-…-0010','voucher',54000) → TERCATAT;
    select sum(nilai) = 54000 terverifikasi)
# kontrol — jalur 'manual' kasir untuk nilai yang sama DITOLAK:
$ node alat/uji-sql.mjs /tmp/probe7b.sql
  GAGAL /tmp/probe7b.sql
        Diskon ini melebihi batas izin Anda. Minta persetujuan atasan (PIN).
```

- **Skenario gagal:** kasir (bawaan `pakai_voucher=true`) mencatat "diskon voucher" sebesar subtotal penuh pada pesanan yang sudah punya subtotal — tanpa ada voucher yang dicek, tanpa PIN, tanpa persetujuan; barisnya append-only (tidak bisa dihapus/diubah klien). Cap resto tidak menolong: `batas_maks_potongan_persen` default 100% (`0002:92`) sehingga pemeriksaan cap dilewati. Saat `hitung_total` (T1-15) mendarat, potongan itu akan mengurangi total pembayaran — uang resto keluar tanpa wewenang. Batas kasir untuk diskon manual (25.000 / 5%) bisa dilewati total hanya dengan menulis `jenis='voucher'`.
- **Dugaan penyebab:** 0012 menutup celah `promo` (PR-01 putaran 8) dengan prinsip "jenis yang belum punya mesin = gagal-aman", tetapi cabang `voucher` hanya diberi pemeriksaan izin memakai voucher — padahal mesin voucher (tabel `voucher`, RPC `cek_voucher`/`pakai_voucher`, T1-12/T1-19/T1-20) belum mendarat; kolom `voucher_id` masih tanpa kunci asing sehingga tidak ada yang merujuk voucher nyata.
- **Cara membuktikan perbaikan:** tambahkan uji `supabase/tes/diskon_voucher.sql`: (a) kasir insert `jenis='voucher'` dengan `voucher_id NULL` (atau id voucher yang tidak ada) → harus ditolak; (b) kasir insert voucher dengan nilai di atas batas manualnya → harus ditolak; kemudian `node alat/uji-sql.mjs supabase/tes/diskon_voucher.sql` → LULUS, dan suite penuh tetap `28+1 LULUS · 0 GAGAL`. Uji lama juga harus tetap hijau (tidak ada relaksasi lain).
- **Status verifikasi:** TERVERIFIKASI

### [PR-02] State machine status pesanan hanya dijaga di UPDATE — klien bisa INSERT pesanan langsung berstatus `lunas`/`batal`
- **Tingkat:** K-3
- **Artefak:** `supabase/migrations/0009_pesanan.sql:186-187` (`create trigger pesanan_jaga_status … before update on public.pesanan` — tidak ada penjaga di INSERT) + policy `pesanan_tambah` yang tidak membatasi nilai `status` saat insert
- **Klaim yang dilanggar:** paket §3 klaim 11; juga klaim dokumen `supabase/tes/status_pesanan.sql` sendiri ("→ lunas: HANYA peladen · → batal: HANYA lewat jalur pembatalan") yang hanya diuji pada jalur UPDATE
- **Bukti:** probe sebagai kasir pada commit 750889a:

```
$ node alat/uji-sql.mjs /tmp/probe1_lunas_insert.sql
  LULUS /tmp/probe1_lunas_insert.sql     # insert pesanan dengan status='lunas' DITERIMA
$ node alat/uji-sql.mjs /tmp/probe1b_batal_insert.sql
  LULUS /tmp/probe1b_batal_insert.sql    # insert status='batal' + dibatalkan_pada DITERIMA;
                                         # select count(*) from public.pembatalan = 0
```

- **Skenario gagal:** kasir menandai pesanan `batal` dengan satu INSERT — tanpa catatan `pembatalan` (tidak ada alasan wajib, tidak ada `nilai_kerugian`, tidak ada pemeriksaan PIN), sehingga Aturan Bisnis 7 ("pembatalan berjejak") bisa dilewati untuk pesanan yang belum dikirim ke dapur; pesanan juga bisa lahir langsung sebagai `lunas` (tanpa pembayaran apa pun, total 0) yang merusak state machine `draf → dikirim → … → lunas` dan laporan penjualan yang kelak menghitung baris `lunas`.
- **Dugaan penyebab:** penjaga status ditulis sebagai tanggapan audit AUD-3 K-2 yang kasus konkretnya adalah UPDATE (`kasir memindahkan pesanan menjadi lunas`), sehingga jalur INSERT tidak pernah dikunci ulang; policy insert cukup memeriksa peran, bukan nilai status awal.
- **Cara membuktikan perbaikan:** perluas `supabase/tes/status_pesanan.sql`: kasir INSERT `pesanan` dengan `status` selain `draf` (mis. `lunas`, `batal`, `dikirim`) → harus ditolak; setelah perbaikan uji baru LULUS dan suite penuh tetap 0 GAGAL; jalur peladen (insert sebagai pemilik/service_role) harus tetap bebas.
- **Status verifikasi:** TERVERIFIKASI

### [PR-03] `nilai_kerugian` pada pembatalan bisa dikarang klien (non-nol apa pun diterima)
- **Tingkat:** K-3
- **Artefak:** `supabase/migrations/0012_penutup_celah_review.sql:438-440` (`if new.nilai_kerugian = 0 then new.nilai_kerugian := coalesce(v_nilai, 0); end if;` — nilai non-nol kiriman klien diterima apa adanya, tidak pernah dibanding dengan `v_nilai` hasil hitung peladen)
- **Klaim yang dilanggar:** paket §3 klaim 11 pada sisi jejak/uang (L2): komentar migrasi mengklaim "Nilai kerugian dihitung dari SALINAN HARGA", tetapi itu hanya berlaku bila klien mengirim 0
- **Bukti:** probe sebagai kasir (punya `void_sebelum_dapur`) pada commit 750889a:

```
$ node alat/uji-sql.mjs /tmp/probe3_kerugian.sql
  LULUS /tmp/probe3_kerugian.sql
   (isi probe: insert pesanan draf, lalu insert pembatalan (tahap='sebelum_dapur',
    alasan='barang salah', nilai_kerugian=1) → TERCATAT;
    select nilai_kerugian from public.pembatalan = 1)
```

- **Skenario gagal:** kasir membatalkan pesanan bernilai tinggi (sebelum dapur) dan menulis `nilai_kerugian` kecil (mis. 1 rupiah) sehingga laporan kerugian under-report, atau menulis angka besar sesukanya — kolomnya append-only, jadi angka yang dikarang bertahan selamanya sebagai "jejak resmi". Nilai riil seharusnya deterministik dari salinan harga (`v_nilai`), bukan pilihan klien.
- **Dugaan penyebab:** trigger menganggap nilai non-nol sebagai "sudah dihitung peladen" (pola `if … = 0 then isi`) yang aman untuk jalur RPC masa depan, tetapi tidak membedakan sumber nilai saat insert langsung dari klien.
- **Cara membuktikan perbaikan:** trigger menolak `nilai_kerugian` yang berbeda dari `v_nilai` (atau mewajibkan equality untuk jalur klien: `new.nilai_kerugian in (0, v_nilai)`); uji baru: kasir insert pembatalan dengan `nilai_kerugian` ≠ nilai hitung → ditolak; suite penuh tetap 0 GAGAL.
- **Status verifikasi:** TERVERIFIKASI

### [PR-04] Klaim basi: dokumen hidup menulis "21 berkas uji" padahal commit ini punya 28
- **Tingkat:** K-4
- **Artefak:** `PANDUAN_PENGGUNA.md:63` (bagian A2 "peta kejujuran": "…(21 berkas uji, semua LULUS)") dan `docs/TERTANGGUH.md:27` (T-018: "(21 berkas uji LULUS)")
- **Klaim yang dilanggar:** paket §3 klaim 12 ("dokumen yang menyatakan perilaku sudah ikut diperbarui — tidak ada klaim basi")
- **Bukti:**

```
$ ls supabase/tes/*.sql | wc -l
28
$ node alat/uji-sql.mjs | tail -2
uji: 28 LULUS · 0 GAGAL
HASIL: LOLOS
# penjaga buku induk tidak memeriksa angka ini:
$ grep -n "berkas uji" alat/periksa-panduan.py
(kosong) → python3 alat/periksa-panduan.py tetap "HASIL: LOLOS"
```

- **Skenario gagal:** Lee membaca "peta kejujuran" di buku induk dan mengira cakupan uji database = 21 berkas; 7 uji baru yang justru menutup celah putaran 8 (diskon_cap, harga_item, persetujuan_void, jejak_pengaturan, stok_arah, izin_efektif_untuk, cabang_sesi) tidak terlihat sebagai bagian dari klaim. Dokumen yang dijaga mesin "supaya tidak bisa basi" basi untuk angka yang paling gampang dicek.
- **Dugaan penyebab:** baris A2/T-018 ditulis saat hitungan masih 21 (sebelum 0012 menambah 7 uji) dan tidak ada guard yang mengikat angka jumlah uji ke isi folder `supabase/tes/`.
- **Cara membuktikan perbaikan:** segarkan kedua baris menjadi 28, dan tambahkan cek di `periksa-panduan.py` (atau `periksa-bersih.py`) yang membandingkan angka "N berkas uji" di dokumen hidup dengan `ls supabase/tes/*.sql | wc -l`; setelah itu `python3 alat/periksa-panduan.py --uji-diri` tetap LOLOS dan mutasi mengubah angka → ditolak.
- **Status verifikasi:** TERVERIFIKASI

**Rencana pemulihan & sisa risiko (bahasa sederhana, wajib Jalur Merah).**
Jika PR ini ternyata salah dan sudah terlanjur di-merge ke `main`: langkah pemulihan =
`git revert` commit merge-nya (seluruh PR adalah berkas baru di atas `main` yang masih kosong,
sehingga revert kembali ke `main` bersih tanpa konflik), lalu jalankan `bash aplikasi/alat/periksa-semua.sh` di `main` untuk membuktikan titik dasar kembali hijau (ini juga menutup L-04). Selama
PR belum di-merge, rekomendasi sederhana: **jangan merge dulu** — perbaiki PR-01 (K-2 terverifikasi;
menurut protokol, adanya K-1/K-2 terverifikasi melarang merge) dan PR-02/PR-03 sebelum merge;
PR-04 bisa menyusul tetapi sebaiknya ikut. Sisa risiko yang tetap ada walau PR ini bersih:
(L1) lapisan kedua pembatas PIN per-perangkat (12×/15 menit) belum berarti sampai perangkat
terdaftar (T1-24) — sudah diakui jujur di `KEAMANAN.md` §2 dan `TEMUAN_LUAR_CAKUPAN` L-06, dan
sejak 0012 penghitung per-perangkat hanya berlaku per (perangkat, pencoba), jadi 12 pegawai
berbeda di satu tablet tidak memicu kunci perangkat — pembatas yang benar-benar efektif saat ini
tetap 5×/15 menit per akun; (L2) `hitung_total` (T1-15) belum ada, jadi uang belum bisa tercatat
sama sekali — perilaku gagal-aman (pembayaran ditolak), bukan celah; (L4) uji lokal memakai
PostgreSQL WASM (PGlite) + tiruan bcrypt berlabel, jadi kekuatan KDF dan perilaku Supabase nyata
(Data API, `request.headers`) belum teruji — tugas T0-08/T0-09 milik Lee.

## 5. Kalibrasi cacat tanaman

Bahan: `docs/uji/kalibrasi/pr-bahan-2026-09-17.diff`. Cara kerja: salinan terpisah (`git worktree`
dari klon /tmp, `node_modules` disymlink agar tidak mengotori klon lain), `git apply` bahan, lalu
review diff + jalankan `node alat/uji-sql.mjs`. Saya tidak membuka kunci jawaban
(`alat/kalibrasi-cacat.json`) dan tidak mencari tahu isi katalog — cacat ditemukan dari diff dan
perilaku.

**Ditemukan: 4 dari 4 · temuan palsu: 0**

Daftar cacat yang saya temukan:
1. `0004_pola_rls.sql` — policy `pengaturan_pilih` diganti `using (penyewa_id is not null)` →
   baris pengaturan (pajak, service, struk, jam buka) SEMUA resto terbaca siapa pun yang masuk.
   Terbukti: `rls_semua_tabel.sql` GAGAL ("Tabel public.pengaturan membocorkan 1 baris milik
   resto lain") + `rls_pengguna.sql` GAGAL ("owner pusat hanya melihat pengaturan restonya
   (dapat 2, harap 1)").
2. `0012_penutup_celah_review.sql` — cabang `jenis='promo'` di `picu_diskon_batas` diganti
   `raise exception` → `null;` → celah PR-01 putaran 8 terbuka lagi (diskon promo tanpa
   pemeriksaan izin). Terbukti: `diskon_cap.sql` GAGAL ("perintah tidak ditolak (diskon jenis
   promo DITOLAK…)").
3. `0005_izin_berjenjang.sql` — baris `revoke all on function public.boleh(text, uuid) from
   public;` dihapus → `EXECUTE` bawaan PUBLIC tetap pada `boleh(text, uuid)`, sehingga `anon`
   bisa memanggilnya. Terbukti: `izin.sql` GAGAL ("anon tidak boleh memanggil boleh()").
4. `0003_helper_identitas.sql` — cek `and p.aktif` dihapus dari join fungsi `cabang_saya()`
   (versi 0003 yang memakai klaim JWT). **Catatan jujur:** hunk ini TIDAK membuat uji apa pun
   merah, karena migrasi 0012 menulis ulang `cabang_saya()` (versi tabel `sesi_cabang` yang
   tetap memeriksa `p.aktif`) sehingga fungsi lama tidak terpakai lagi di keadaan akhir. Saya
   tetap menghitungnya sebagai cacat tanaman karena ia memang mengikis penjagaan di berkas
   identitas — tetapi saya tidak bisa membuktikannya sebagai cacat hidup; kalau kunci jawaban
   tidak menghitung hunk ini, baris ini yang harus dianggap temuan ekstra, bukan kesalahan
   cacat lain.

Keluaran bahan (fenced):

```
$ git apply docs/uji/kalibrasi/pr-bahan-2026-09-17.diff
$ node alat/uji-sql.mjs
  GAGAL supabase/tes/diskon_cap.sql
        HARAPAN TIDAK TERPENUHI: perintah tidak ditolak (diskon jenis promo DITOLAK (mesin promo belum ada — dulu lolos tanpa pemeriksaan))
  GAGAL supabase/tes/izin.sql
        HARAPAN TIDAK TERPENUHI: perintah tidak ditolak (anon tidak boleh memanggil boleh())
  GAGAL supabase/tes/rls_pengguna.sql
        HARAPAN TIDAK TERPENUHI: owner pusat hanya melihat pengaturan restonya (dapat 2, harap 1)
  GAGAL supabase/tes/rls_semua_tabel.sql
        Tabel public.pengaturan membocorkan 1 baris milik resto lain
----------------------------------------------------------------------
uji: 24 LULUS · 4 GAGAL
HASIL: GAGAL
```

Worktree salinan sudah dihapus setelahnya; klon asal tidak berubah.

## 6. Yang tidak bisa saya verifikasi

- **Status CI GitHub (Actions) pada commit PR ini.** `gh` di sesi ini mengembalikan
  `HTTP 401: Bad credentials`, sehingga saya tidak bisa melihat hasil run Actions untuk
  `750889a`/tip. Pengganti seadanya: seluruh langkah `ci.yml` saya jalankan manual di klon
  commit ini (`bash aplikasi/alat/periksa-semua.sh` = kumpulan langkah yang sama) dan hijau —
  tetapi itu bukan bukti run Actions hijau.
- **Perilaku di Supabase nyata** (bcrypt asli pgcrypto, paparan Data API, jalur
  `request.headers` untuk perangkat, klaim JWT): uji di sini memakai PostgreSQL WASM (PGlite)
  dan tiruan bcrypt berlabel; proyek sendiri menunda verifikasi ini ke T0-08/T0-09 (butuh akun
  Lee).
- **Perilaku agent terhadap kalimat `Uji semuanya.` / `Uji database.`** — saya hanya bisa
  memverifikasi pengikatan kalimat→perintah di dokumen (PANDUAN Bagian E), bukan eksekusinya
  oleh agent di sesi baru.
- **186 berkas jalur Hijau** (aset desain/font, prototipe, log sesi) tidak saya baca baris demi
  baris — saya andalkan gerbang otomatis yang saya jalankan sendiri (periksa-halaman,
  uji-kontras, periksa-kerapatan, periksa-bersih, periksa-rujukan) sampai hijau.
- **Hunk kalibrasi ke-4** (0003) — lihat catatan jujur di §5: ditemukan sebagai edit, tetapi
  dampaknya pada keadaan akhir tidak bisa saya buktikan (fungsi tergantikan oleh 0012).

## 7. Pernyataan tidak mengubah apa pun

Saya peninjau independen — **bukan** sesi penulis PR ini — dan selama review saya tidak mengubah,
memperbaiki, menambah, atau menghapus satu pun berkas di repositori kerja: satu-satunya berkas
yang saya buat adalah **laporan ini**. Semua eksekusi yang mengubah sesuatu (uji mutasi bawaan
repo, pelemahan policy RLS, `git apply` bahan kalibrasi, probe bypass) dilakukan di salinan
terpisah di `/tmp` (`/tmp/rb` = klon pada commit yang direview, `/tmp/fresh` = klon kedua, plus
worktree `/tmp/rb-rls` dan `/tmp/rb-cal` yang sudah dihapus); file probe SQL (`/tmp/probe*.sql`)
juga berada di luar repo. Tidak ada commit lain yang saya buat selain commit laporan ini, dan
tidak ada push selain push laporan ini.

## 8. Temuan di luar cakupan diff

| # | Temuan | Kenapa di luar cakupan | Bukti | Saran tindak lanjut |
|---|---|---|---|---|
| 1 | Kepala cabang PR (`742721d`) bergerak **2 commit** melampaui commit yang direview (`d29c4de`, `742721d`) — keduanya hanya menyentuh berkas paket/log/dokumen: `PROJECT_STATE.md`, `_log-sesi/LOG_SESI_2026-09-17.md`, `docs/AGENT_OPERATING_GUIDE.md`, `docs/teknis/REKAM_PESAN_PEMILIK.md`, `docs/uji/REVIEW_PR_RIWAYAT.md`, `docs/uji/review-pr/PKT-2026-09-17-pr-01-putaran11{,-SIAP-TEMPEL}.md` (+81/−30) | di luar diff `origin/main…750889a` yang menjadi target paket | `git diff --stat 750889a origin/arena/01a0a8a2-resto-barokah` → 7 berkas, tidak ada `supabase/`/`aplikasi/`/`alat/` | gerbang merge tetap harus dinilai pada kepala terakhir (pola L-01 di `TEMUAN_LUAR_CAKUPAN_REVIEW.md`): disegarkan paket menunjuk tip sebelum Lee memutuskan |
| 2 | Paket yang tersimpan di commit yang direview sendiri masih menunjuk commit LAMA (`79e7c93`, 365 berkas); paket yang dipakai review ini (versi tip) sudah menunjuk `750889a` | perubahan paket terjadi di commit setelah target | `git show 750889a:docs/uji/review-pr/PKT-…putaran11.md` → "Commit yang direview: 79e7c93…" vs `git show origin/…:…` → "750889a…" | sesuai aturan ayam-dan-telur, perubahan pasca-target pada berkas paket tidak membatalkan paket — tetapi mekanisme "siapkan ulang paket tiap tip bergerak" harus berjalan sebelum merge supaya tidak ada PR yang dinilai pada paket basi |
| 3 | Cabang `main` (`253d129`) belum pernah melewati CI, jadi gerbang baru pertama kali berlaku saat merge | `main` bukan bagian dari diff PR ini | sudah terlacak sebagai L-04 (`DITUNDA`) di `docs/uji/TEMUAN_LUAR_CAKUPAN_REVIEW.md` + tugas `T1-44` | tidak perlu tindakan baru — pastikan L-04/T1-44 dikerjakan setelah merge (jalankan `bash aplikasi/alat/periksa-semua.sh` di `main` dan simpan keluarannya) |