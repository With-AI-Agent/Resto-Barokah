# LOG SESI — 2026-09-18 (putaran 14)

> Berkas ini dibaca otomatis oleh `alat/mulai-sesi.py` (baris "LOG_SESI terbaru").
> **Keadaan:** CLOSED — sesi ditutup bersih, seluruh pekerjaan ter-commit & ter-push.

## Keadaan Sesi

- **Sesi:** kerja (coding/pemeliharaan) di cabang `arena/01a0a8a2-resto-barokah`.
- **Konteks masuk:** Lee menjalankan 4 sesi independen (2 review PR putaran13 + 2 audit AUD-3
  2026-09-18) atas commit `d1f11d7`, lalu mengirim hasilnya dengan pesan: periksa dulu kemungkinan
  laporan tertimpa sebelum menyimpulkan jumlahnya.
- **Sesi ini ditutup karena:** permintaan Lee — sesi sudah berat (pemakaian memori besar karena
  percakapan panjang); lanjutan dikerjakan di chat baru.

## Kronologi ringkas

1. **Inventaris & penyelamatan laporan** — 4 sesi menghasilkan 7 berkas laporan; satu versi laporan
   hidup hanya di riwayat commit (tertimpa). Semua ditarik lewat `--ambil-laporan` lalu dibaca penuh.
2. **Bantah-balik semua temuan dengan probe sendiri** (bukan mempercayai kalimat laporan):
   12 temuan review + 15 temuan audit dinyatakan **NYATA**; sisanya dijelaskan/dicabut.
3. **Perbaikan** — migrasi baru `supabase/migrations/0014_penutup_celah_putaran13.sql` (10+ bagian)
   + **10 berkas uji SQL baru**, 6 uji lama diselaraskan. Suite: **41 LULUS · 0 GAGAL**.
4. **Bukti pagar** — `alat/uji-mutasi-0014.py` (17 mutasi) dan `alat/uji-mutasi-0012.py` diperluas
   (16 mutasi + mutasi gabungan M3k/M5k/M8k): semuanya WAJIB MERAH, dan terbukti MERAH.
5. **Alat audit sendiri diperkuat** — `alat/periksa-paket.py` (F-11/F-12), `alat/periksa-angka-bukti.py`
   (F-14), `aplikasi/alat/periksa-komponen-env.py` (F-15) + `--uji-diri`, `rls_semua_tabel.sql`
   (arah timbal balik), `alat/uji-sql.mjs` (ringkasan job CI).
6. **2 cacat tertangkap CI, bukan gerbang lokal** — field wajib `STATUS.md` sempat berubah dan
   T-019 belum ditandai di ROADMAP. Keduanya diperbaiki → **CI hijau** (push & PR).
7. **Mekanisme lanjut-sesi dibuat** (permintaan Lee pesan ke-35): `alat/lanjut-sesi.py`
   (`--siapkan` / periksa / `--di-ci` / `--uji-diri`) + `docs/ops/SIAP-LANJUT.md` (handoff untuk
   agent baru) + `docs/ops/SIAP-TEMPEL-SESI-BARU.md` (satu berkas siap tempel untuk Lee).

## Cacat jujur sesi ini

- `berkas_berlaku()` di alat mutasi sempat menunjuk repo asli (bukan salinan kerja) → semua mutasi
  terbaca "hijau" palsu. Diperbaiki; mutasi yang dilewati kini **dilaporkan**, tidak pernah dicap hijau.
- Ruang kerja sempat kehilangan Git & `node_modules` (sandbox di-reset 1× lagi); karya `0014`
  diselamatkan dari salinan lab sebelum hilang.
- Satu langkah CI sempat merah **tanpa bisa direproduksi** dan **log mentah GitHub tidak bisa dibaca**
  dari lingkungan kerja → `alat/uji-sql.mjs` kini menulis ringkasan ke halaman Summary job.

## Yang harus dibaca sesi berikutnya

`docs/ops/SIAP-LANJUT.md` (keadaan + rencana) → `PROJECT_STATE.md` → `docs/DECISIONS_LOG.md`
(keputusan putaran13) → `docs/TERTANGGUH.md` (8 butir terbuka) → `docs/uji/AUDIT_RIWAYAT.md`.

### 2026-09-18 (lanjutan) — menutup putaran verifikasi handoff

- Dijalankan pemeriksa penuh; **CI MERAH** (run 35328530232) karena cacat tulisanku sendiri:
  baris `**Waktu pembaruan:** 2026-09-18 (putaran 14) — ...` di `STATUS.md` membuat tanggal tidak
  lagi langsung diikuti `—`, sehingga kontrak `_sistem/validate_system.py` (dijalankan CI) menolaknya.
  Dibetulkan → `2026-09-18 — putaran14: ...`. Pelajaran: **jalankan pemeriksa penuh SEBELUM menutup**,
  dan **CI adalah sumber kebenaran**, bukan ingatan.
- Mekanisme lanjut-sesi ditambah: bila CI terakhir bukan success, handoff menulis baris
  **PERHATIAN** dan pemerkisa mencetak catatan (peringatan, bukan penolakan — penolakan akan
  memacetkan mekanisme saat memperbaiki CI merah).
- **Bukti CI bisa MENOLAK handoff cacat** (bukan asumsi): commit sementara `195b708` menduplikasi
  baris commit di handoff → CI **MERAH** di run 35329424533 & 35329430980, tepat pada langkah 15
  "Pemeriksa fondasi, roadmap, struktur, komponen, uji & kontras" (langkah yang memuat pemeriksa
  handoff). Commit `cacdff3` mengembalikan pohon ke bentuk benar → CI **HIJAU**
  (run 35329851024 & 35329856536).
- Uji-diri pemeriksa handoff: **13 kasus** (mutasi teks + 2 repo Git uji + 2 kasus peringatan CI),
  termasuk bukti handoff **basi** ditolak pada repo Git nyata.

### 2026-09-18 (penutup) — pertanyaan Lee: apa yang WAJIB dan TIDAK WAJIB saat pindah sesi

Lee bertanya: "apakah cukup pakai berkas tempel di sesi baru, tanpa atur base branch dan tanpa
kirim prompt penutup ke sesi lama dulu?" Jawaban yang benar (dan sekarang masuk buku):
- **base branch: tidak perlu disentuh** — cabang dibuat otomatis; base branch hanya dipakai saat
  Pull Request dibuka (PR #1 sudah terbuka).
- **prompt penutup: disarankan 1 kalimat, TIDAK wajib** — kalau sesi lama mati, langsung pindah;
  agent baru diperintah memeriksa keadaan repo lebih dulu. Risiko satu-satunya: pekerjaan yang
  belum di-commit/belum di-push.
- **berkas siap-tempel harus versi terbaru** (berubah setiap batch).
Ditulis ke `PANDUAN_PENGGUNA.md` (AL-13) + `docs/PANDUAN_PEMILIK.md` (versi tanya-jawab), dijaga
`alat/periksa-panduan.py` (4 topik wajib baru) dan `alat/lanjut-sesi.py` (blok CARA PAKAI di
berkas tempel + mutasi uji-diri ke-16).

Insiden sandbox (ke-4): ref Git kembali mundur ke `253d129` (main) dengan seluruh pekerjaan masih
di disk; pulih lewat resep baku: `git cat-file -e` → `git fetch origin <cabang>:refs/remotes/origin/kerja-terakhir`
→ `git add -A && git reset --soft b542871`. Tidak ada pekerjaan yang hilang.

### 2026-09-18 (koreksi Lee) — bahasa komunikasi

Lee menegur: agent sempat menjawab dengan bahasa yang salah. Aturan yang benar: **semua komunikasi
dengan Lee memakai bahasa Indonesia yang sederhana**. Aturannya sudah ada di
`docs/AGENT_OPERATING_GUIDE.md` (baris 62: "semua laporan dalam bahasa Indonesia"), tetapi TIDAK
ada di Prompt Pembuka/kanonik — sehingga sesi baru bisa tidak pernah membacanya.

Perbaikan (mekanisme, bukan sekadar niat):
- `PROMPT_ENTRI_UNIVERSAL.md` + `PANDUAN_PENGGUNA.md`: baris pertama blok Prompt Pembuka sekarang
  **ATURAN BAHASA (wajib, jangan dilanggar)** — dijaga tetap identik oleh `alat/periksa-panduan.py`.
- `alat/periksa-panduan.py`: topik wajib baru "aturan bahasa ke Lee" (pola `ATURAN BAHASA`).
- `alat/lanjut-sesi.py`: berkas siap tempel wajib memuat ATURAN BAHASA + butir 6 pada blok
  CARA PAKAI; pemeriksa MENOLAK berkas tempel yang kehilangan aturan itu.

### 2026-09-18 (buktinya nyata) — mekanisme pindah sesi DIPAKAI sesi baru; 3 cacat mekanisme ditutup

Lee benar-benar membuka sesi baru dengan berkas siap-tempel, lalu mengirimkan jawaban sesi baru itu
untuk dinilai. Hasil penilaian (diperiksa ulang oleh sesi ini, bukan percaya klaim):

**Bekerja sesuai harapan:**
- Sesi baru mulai dari `main` (1 commit, template) → mengenali "basis salah" → menjalankan resep
  `git fetch origin <cabang>:refs/remotes/origin/kerja-terakhir` + `merge --ff-only` → mendarat di
  ujung cabang `51feb34` (135 commit di atas main). Inti mekanisme = TERBUKTI.
- Melaporkan KARTU SESI (cabang, commit, CI hijau, tree bersih, PR #1 terbuka + JANGAN MERGE),
  membaca `PROJECT_STATE`/`STATUS`/`TERTANGGUH` (8 butir), dan TIDAK menulis apa pun sebelum
  tujuan dikonfirmasi. Bahasa Indonesia dipakai — ATURAN BAHASA bekerja.
- Sesi baru menemukan 4 hal dengan jujur (bukan mengaku semua beres) — inilah perilaku yang diminta.

**Cacat mekanisme yang ditemukan sesi baru, lalu diperbaiki di sini (semua TERBUKTI sebelum-sesudah):**
1. `alat/mulai-sesi.py` buta pada status jeda: `CODING_DIJEDA_SADAR` tidak cocok baris mana pun di
   `PHASE_SKILLS` → KARTU SESI hanya mencetak **1** skill padahal fase coding (sekarang: **15**).
   Perbaikan: pencocokan persis → terkandung → awalan fase, + `--uji-diri` (5 kasus) + didaftarkan
   di CI & gerbang CI.
2. Handoff §3 MENGKLAIM paket peninjau "sudah disegarkan ke commit keadaan", padahal paket menunjuk
   `cdd80b6` (12 commit di belakang, diperiksa sendiri). Perbaikan: baris "Paket peninjau terbaru"
   ditulis MESIN dari berkas paketnya (tidak bisa dibohongi lagi) + §3 tidak lagi mengklaim.
3. Handoff tidak menjelaskan bahwa PR #1 menunjuk cabang sesi LAMA → sesi baru bisa mengira
   pekerjaannya otomatis masuk PR #1. Perbaikan: §2c + catatan cabang di berkas siap-tempel.
4. (Bonus) Catatan ruang kerja: `node_modules` hilang setiap ruang kerja di-reset → ditulis di §2.

Temuan sesi baru yang lain (nomor migrasi ROADMAP basi; paket belum menunjuk tip) tetap terbuka
sebagai pekerjaan, bukan cacat mekanisme — sudah tercatat di §3 handoff.

