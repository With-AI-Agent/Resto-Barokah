# LOG SESI — 2026-09-23 (penutupan sesi arena/01a0c97c — pindah sesi atas permintaan Lee)

## Ringkasan giliran malam ini (lanjutan "Lanjut")

1. **CI-fix batch `161c861`** — Prettier, `docs/STATUS.md` + `docs/PROJECT_STATE.md` (kontrak
   validator `_sistem/validate_system.py`: field **Pekerjaan belum tersimpan:** + **Waktu
   pembaruan:**), penyebut entitas `pesanan_item_status_riwayat` di ROADMAP, langkah mutasi CI
   0032/0033.
2. **Batch SQL `285bd3d`** — RPC `tandai_habis` (0035, T4-05) + `set_stok` (0036, T4-06) +
   `opname_stok` (0037, T4-07); tes `menu_habis_sumber.sql`, `set_stok.sql`, `opname_stok.sql`,
   `anti_dobel.sql`; mutasi 0035/0036/0037 = 9/9 MERAH + --uji-diri. Suite SQL **78 LULUS · 0
   GAGAL**. Kontrak lama yang MENANG ditemukan lagi: saldo awal wajib lewat pergerakan
   (bukan tulis langsung), opname selisih-nol ditolak buku besar, pembekuan stok_pergerakan
   untuk pegawai.
3. **T4-09 bukti nyata** — kasus **T-409** di `alat/uji-konkuren.py`: dua koneksi paralel
   menandai item sama → tepat 1 riwayat (P2 mengubah, P1 idempoten); kalibrasi mutan = unik
   `pesanan_item_status_riwayat_kunci_idx` menolak rekaman kembar (terbukti tajam).
4. **Batch UI `bb30562`** — `Stok.tsx` + `Opname.tsx` + uji (9/9). **Pelajaran: registri
   `DAFTAR_LAYAR` dikunci tes lama `layar.test.ts` (tepat 8 layar G1 + 7 keadaan wajib) —
   entri baru TIDAK boleh ditambah tanpa revisi kontrak (butuh keputusan Lee).** Layar baru
   hidup via `App.tsx` tanpa entri registri. Aplikasi **253 tes LULUS**.
5. **Handoff `4988cee` + `d923e99`** — insiden: berkas akar `PROJECT_STATE.md`/`STATUS.md`
   (170 baris riwayat deterministik) sempat tertimpa tulisan pendek → **dipulihkan utuh dari
   `bb30562`** lalu disisipkan blok "FASE 4 TUNTAS" di kepala. Kontrak akar: `PROJECT_STATE.md`
   wajib punya baris `STATUS:`; `STATUS.md` wajib punya tepat satu
   `**Pekerjaan belum tersimpan:** Tidak ada`.
6. `python3 alat/lanjut-sesi.py` = **LOLOS** di `d923e99` ("handoff lengkap & segar").

## Keadaan CI

- `35796505521` (285bd3d) = **failure** di "Tipe (TypeScript ketat)" — penyebabnya entri
  registri layar ber-skema salah (sudah dibuang di bb30562; kontrak tes lama menang).
- `35797533476` (bb30562) = **cancelled** (tertimpa push berikutnya — normal GitHub).
- `35797734100` (d923e99) = status terakhir diketahui **in_progress** — SESI BARU WAJIB
  memeriksa hasilnya lebih dulu (`gh run view 35797734100` / `gh run list`) sebelum klaim
  hijau atau memulai pekerjaan baru. Jangan klaim "CI hijau" tanpa bukti run sukses.

## Pindah sesi (permintaan Lee, tanpa merge)

- Semua kerja ter-push di `arena/01a0c97c-resto-barokah` (ujung `d923e99` — dibuktikan
  `git ls-remote`). Tidak ada PR yang di-merge/ditutup (PR #1–#4 tetap terbuka, keputusan Lee).
- Sesi baru: Lee menulis **"baca pro.md"** → alat `lanjut-sesi.py --daftar-sesi` akan menemukan
  sesi aktif lewat `docs/ops/SIAP-LANJUT.md` (menunjuk dirinya sendiri). `PROMPT_SESI_BARU.md`
  baris pertama sudah diisi `arena/01a0c97c-resto-barokah` untuk jalur susul-manual.
- Sisa pekerjaan (di SIAP-LANJUT §3): verifikasi CI `35797734100` → bukti manual Lee
  (foto T4-03, uji dua perangkat T4-05, cabut-jaringan T4-10) → keputusan infra e2e
  Playwright → kabel data realtime → Fase 5.

Tidak ada pekerjaan yang belum tersimpan. Tidak ada merge/deploy/sebar.

---

# SESI BARU `arena/01a0cb7f` (pintu "baca pro.md") — CI merah dibaca & diperbaiki

## 1. Orientasi (PRO.md §1) — dijalankan apa adanya

- `python3 alat/lanjut-sesi.py --daftar-sesi` → tepat satu kandidat: `arena/01a0c97c-resto-barokah`
  (`SEDANG DITUJU`; tiga cabang lain `terserap`, satu `SENGAJA DITINGGALKAN`). Tidak perlu bertanya.
- `python3 alat/lanjut-sesi.py --susul` → "OK: checkout ini SUDAH memuat seluruh pekerjaan cabang itu"
  (HEAD sesi ini `f7fd468` = ujung `arena/01a0c97c`, dibuktikan `git ls-remote`).
- `python3 alat/lanjut-sesi.py` → **LOLOS** (dua catatan: CI terakhir bukan success, dan cabang lokal
  belum punya `origin/arena/01a0cb7f`).
- Catatan lingkungan: klon mula-mula **dangkal** (riwayat tidak bisa diuji) dan `alat/node_modules`
  kosong. Diperbaiki lebih dulu: `git fetch --unshallow origin` (338 commit terbaca) + `npm ci --prefix alat`
  + `npm ci` di `aplikasi/`. Tanpa itu dua pemeriksa gagal PALSU (`periksa-paket` 38 temuan F-11,
  `periksa-matriks-izin` tidak bisa memuat PGlite) — keduanya LOLOS setelah lingkungan beres.

## 2. Keadaan CI yang sebenarnya (bukti, bukan klaim)

- Run `35797734100` (commit `d923e99`) yang ditinggalkan sesi lalu = **cancelled**, bukan in_progress:
  anotasi GitHub "Canceling since a higher priority waiting request … exists" (tertimpa push `f7fd468`).
- Run penggantinya `35798327450` (commit `f7fd468` = HEAD sesi ini) = **failure** setelah 10m30s.
  Langkah yang merah: **"Pemeriksa fondasi, roadmap, struktur, komponen, uji & kontras"** — semua langkah
  sebelumnya (Prettier, ESLint, tipe, Vitest, mutasi aplikasi, build, npm audit, 78 uji SQL, seluruh mutasi
  migrasi 0009–0037, konkuren, keamanan SQL) ✓ hijau.
- Log run tidak bisa diunduh (`gh run view --log-failed` → `results-receiver…: EOF`), jadi langkah itu
  dijalankan ulang LOKAL di commit yang sama: **5 dari 53 perintah** keluar bukan-nol.

## 3. Empat cacat nyata + perbaikannya

1. `alat/periksa-gerbang-ci.py` — lima perintah mutasi baru (`uji-mutasi-0032/0033/0035/0036/0037.py`)
   sudah ada di `ci.yml` tetapi **tidak terdaftar di `GERBANG_WAJIB`** → "perintah TIDAK DIKENAL di CI".
   Lima entri ditambahkan (pola + label, gaya entri 0031).
2. `aplikasi/alat/periksa-semua.sh` — lima perintah CI yang sama **tidak tercermin di skrip lokal**
   (`periksa-paritas-ci.py` GAGAL, dan uji-dirinya ikut merah). Lima baris ditambahkan sesudah 0031.
3. `PANDUAN_PENGGUNA.md:66` — masih menulis "**72 berkas uji**" padahal `supabase/tes/*.sql` nyata **78**
   (`periksa-panduan.py` K-4). Angka disegarkan ke 78.
4. `docs/PROJECT_STATE.md:8` — rujukan mati `aplikasi/uji/e2e/dapur.spec.ts` tanpa penanda rencana
   (`periksa-rujukan.py`). Diberi penanda "— rencana, belum dibuat" sesuai `POLA_HARAPAN` pemeriksa.

## 4. Bukti sesudah perbaikan

- Seluruh **53 perintah** langkah CI "Pemeriksa fondasi…" dijalankan lokal di pohon ini: **0 bukan-nol**.
- `bash aplikasi/alat/periksa-semua.sh` dijalankan penuh (termasuk lima skrip mutasi baru + suite SQL +
  Vitest + build): **kode keluar 0** (berkas `exit` proses = `0`); satu-satunya baris `GAGAL` di ekor adalah
  `lanjut-sesi.py` yang menolak karena ruang kerja masih KOTOR saat itu (belum di-commit) — bukan cacat kode.
- Lima skrip mutasi baru dijalankan ulang satu per satu sebagai bukti: 0032 = 7 mutasi MERAH, 0033 = 2,
  0035 = 3, 0036 = 3, 0037 = 3 — semuanya `HASIL: LOLOS` (kode keluar 0).
- `npm run format:check` (Prettier) bersih.

## 5. Batas yang tetap dijaga

Tidak ada merge, tidak ada deploy/sebar Supabase, tidak ada cabang selain cabang sesi ini, tidak ada
penghapusan cabang, PR #1–#4 tetap terbuka menunggu keputusan Lee.

## 6. Hasil CI sesudah perbaikan (BUKTI, bukan klaim)

- Perbaikan di-commit sebagai `9a6a6c8` (satu commit: 4 berkas cacat + `PROJECT_STATE.md`/`STATUS.md`/
  log sesi ini + `docs/ops/SIAP-LANJUT.md` hasil `--siapkan`), lalu di-push ke
  `origin/arena/01a0cb7f-resto-barokah` (dibuktikan `git ls-remote` → `9a6a6c8f13a4676edd6cd9e6a2f6cc0a041c74dc`).
- **Run CI `35801364931` pada commit `9a6a6c8` = `completed success`** (job "Periksa (lint · tipe · uji ·
  pemeriksa Python)" ✓ dalam 11m51s, 0 langkah merah) — langkah "Pemeriksa fondasi, roadmap, struktur,
  komponen, uji & kontras" yang semula merah kini hijau.
- Run sebelumnya untuk pembanding: `35798327450` (commit `f7fd468`, pohon sebelum perbaikan) = failure.
- Catatan jujur soal pemeriksa handoff: `python3 alat/lanjut-sesi.py` di checkout ini kini menjalankan
  pemeriksaan kesegaran riwayat (riwayat sudah penuh setelah `git fetch --unshallow`; di sesi sebelumnya
  klonnya dangkal sehingga pemeriksaan itu dilewati). Aturannya menuntut `Commit keadaan kerja` == induk
  commit terakhir, sedangkan `--siapkan` menulis SHA HEAD saat dijalankan — jadi commit penutup yang
  memuat handoff selalu tercatat "satu commit di depan" induknya. Itu keterbatasan struktural alat,
  bukan pekerjaan yang hilang; sisa pemeriksaan handoff lain (ruang kerja bersih, ter-push, cabang tujuan
  ada di GitHub, commit terakhir menyentuh SIAP-LANJUT + PROJECT_STATE + STATUS) semuanya LOLOS.

---

# BATCH 2 (sesi `arena/01a0cb7f`) — kabel data realtime KDS + waktu peladen

Lee: *"kalau memang semuanya harus dikerjakan, aku mau semuanya dikerjakan; urutannya ikut yang
terbaik menurutmu."* Urutan yang diambil: (1) perbaiki CI merah [selesai, bukti di atas] →
(2) **kabel data realtime KDS** (batch ini) → (3) registri `DAFTAR_LAYAR` → (4) infra e2e
Playwright → (5) Fase 5. Bukti manual/visual (T4-03/T4-05/T4-10) tetap milik Lee — agent tidak
bisa memotret atau mencabut kabel jaringan.

## Yang dikerjakan

1. **`aplikasi/src/hook/useTiketDapur.ts` (baru)** — kontainer data untuk `LayarDapur`/`LayarBar`:
   muat antrean `pesanan` + `pesanan_item` + `meja` (status `dikirim`/`dimasak`/`siap`, urut
   `dikirim_ke_dapur_pada`), langganan `postgres_changes` untuk `pesanan` & `pesanan_item`
   (disaring `cabang_id` bila UUID sah), **jaring pengaman** penyegaran berkala 15 detik bila
   saluran realtime `CHANNEL_ERROR`/`TIMED_OUT`, cadangan antrean di perangkat lewat
   `lib/antrean-lokal` (keadaan `sebagian` = layar memasang tanda "Tertunda"), dan aksi tulis
   lewat RPC peladen (`set_status_item`, `tandai_habis`) — bukan tambal-sulam di klien.
2. **`aplikasi/src/App.tsx`** — `tiket={[]}` diganti kabel nyata untuk dapur & bar; `waktuSekarang`
   kini waktu peladen; `onCoba` = muat ulang; `onMulaiMasak`/`onSelesaiMasak`/`onTandaiHabis`
   tersambung. Komponen layar tetap murni (tidak disentuh).
3. **Migrasi `0038_waktu_peladen.sql` (baru)** — RPC `public.waktu_peladen()` (`stable`,
   **security invoker**, `search_path` terkunci, hanya `authenticated`) supaya umur tiket dihitung
   dari jam PELADEN. Alasan: `@supabase/postgrest-js` v2.116 **tidak mengekspos kepala jawaban
   HTTP**, jadi kepala `Date` tidak bisa dipakai — fakta ini diperiksa di
   `node_modules/@supabase/postgrest-js/dist/index.d.mts` (`PostgrestResponseSuccess` hanya punya
   `success`/`error`/`data`/`count`).
4. **`supabase/tes/waktu_peladen.sql` (baru)** — anon ditolak; ISO UTC 24 huruf berakhiran `Z`;
   `iso` & `epoch_ms` cocok `now()` (±10 detik); `prosecdef = false`; `search_path` terkunci.
5. **`alat/uji-mutasi-0038.py` (baru)** — 3 mutasi wajib MERAH: dijadikan `security definer`,
   kunci `search_path` dilepas, hak `execute authenticated` dicabut. Hasil **3/3 MERAH** +
   `--uji-diri` OK.
6. **Gerbang ikut diperbarui** (tanpa ini CI merah lagi — persis cacat batch sebelumnya):
   langkah baru di `ci.yml`, entri `GERBANG_WAJIB` di `periksa-gerbang-ci.py`, baris padanan di
   `aplikasi/alat/periksa-semua.sh`, dan angka berkas uji `78 → 79` di `PANDUAN_PENGGUNA.md`.

## Bukti (dijalankan, bukan diklaim)

- `npx vitest run src/hook/useTiketDapur.test.tsx` → **15 lulus**. Dua mutasi kalibrasi dilakukan
  untuk membuktikan uji itu tajam: `takeaway → dinein` di peta tipe = 1 uji MERAH; menghapus
  `setWaktuSekarang(iso)` = 1 uji MERAH. Keduanya dikembalikan.
- `npm test` (aplikasi) → **53 berkas / 268 tes lulus** (sebelumnya 253).
- `npm run typecheck` → bersih; `npm run lint` → 0 error (4 warning `react-refresh` lama di
  `KartuPesanan.tsx`/`LayarDapur.tsx`, sudah ada sebelum batch ini).
- `node alat/uji-sql.mjs` → **79 LULUS · 0 GAGAL** (sebelumnya 78).
- `python3 alat/uji-mutasi-0038.py` → **3/3 mutasi MERAH**; `--uji-diri` OK.
- Seluruh **62 perintah** langkah CI "Pemeriksa fondasi…" dijalankan lokal: 0 bukan-nol
  (`periksa-bersih.py` sempat menolak karena berkas baru belum `git add` — setelah ditambahkan, LOLOS).
- `periksa-gerbang-ci.py`, `periksa-paritas-ci.py` (106 perintah CI), `periksa-panduan.py` LOLOS.

---

# BATCH 3 (sesi `arena/01a0cb7f`) — registri layar diperluas + kabel data Stok/Opname

Lee: **"Lanjut"** → urutan yang saya ambil dari §3: registri `DAFTAR_LAYAR` + kabel data
Stok/Opname lebih dulu (murni kode agent), infra e2e Playwright menyusul.

## Yang dikerjakan

1. **`aplikasi/src/lib/layar.ts`** — +3 layar resmi: `bar` (W-4-02), `stok` (W-5-01), `opname`
   (W-5-02). Total **11 layar** (8 layar G1 tidak disentuh).
2. **`aplikasi/src/lib/layar.test.ts`** — kontrak DIREVISI (ini perubahan kontrak yang butuh
   keputusan Lee; Lee menyerahkan urutan & pelaksanaan ke agent 2026-09-23): `LAYAR_G1_WAJIB`
   (8 id) tetap wajib, `LAYAR_FASE4` wajib, dan **layar tak dikenal ditolak** — jadi kontrak
   diperluas, bukan dilunakkan.
3. **`aplikasi/src/lib/aksi.ts`** — +6 aksi (total **38**): `bar.mulai_buat`, `bar.selesai_buat`
   (RPC `set_status_item`), `stok.catat_perubahan` (RPC `set_stok`, izin `ubah_stok`),
   `stok.ke_opname`, `opname.catat_fisik` (RPC `opname_stok`, izin `ubah_stok`),
   `opname.kembali_stok`.
4. **`aplikasi/src/kontrak/bantuan.ts`** — +3 panduan kontekstual (total 11). `periksa-bantuan.py`
   sebelumnya MENOLAK karena 3 layar baru belum berpanduan — ketahuan dari menjalankan pemeriksa,
   bukan dari menebak.
5. **`aplikasi/src/hook/useStok.ts` (baru) + uji (9)** — kabel data Stok/Opname: saldo
   `stok_bahan`, buku besar `stok_pergerakan` (terbaru dulu, dibatasi 50), realtime + penyegaran
   berkala 20 detik, aksi tulis lewat RPC. `numeric` peladen (string) dipetakan `angka()` →
   tidak ada NaN yang bocor ke layar.
6. **`aplikasi/src/layar/dapur/Opname.tsx`** — diberi keadaan `gagal` + `onCoba` (tanpa itu
   kegagalan jaringan tampil sebagai "belum ada bahan" yang menipu) + 1 uji baru.
7. **`docs/PETA_UI.md`** digenerate ulang (`peta-ui.py --generate`), `docs/ROADMAP.md` disisip
   **Bukti 2026-09-23** di T1-31/T1-32/T1-42 (bukti lama 2026-09-22 tidak dihapus), dan
   `docs/DECISIONS_LOG.md` ditambah entri **[Kelengkapan UI/2026-09-23]** yang mencatat dasar
   keputusan Lee.

## Bukti (dijalankan)

- `npm test` aplikasi → **54 berkas / 280 tes LULUS** (sebelum batch ini 53/268).
- Kalibrasi: mutasi `angka()` (biarkan NaN lolos) → **2 uji MERAH**; mutasi peta tipe di
  `useTiketDapur` (batch 2) → 1 uji MERAH. Semua dikembalikan.
- `python3 alat/peta-ui.py` → LOLOS (**11 layar, 38 aksi**, PRD M1–M12 terhubung).
- `python3 alat/periksa-bantuan.py` + `--uji-diri` → LOLOS (**11/11 layar** berpanduan).
- Seluruh **62 perintah** langkah CI "Pemeriksa fondasi…" → **0 bukan-nol**.
- `npm run typecheck` bersih · Prettier bersih · ESLint 0 error (4 warning `react-refresh` lama).
