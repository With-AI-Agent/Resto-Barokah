# LOG SESI — 2026-09-23 (sesi kedua hari ini, cabang `arena/01a0cca9-resto-barokah`)

## Keadaan Sesi

- **Keadaan:** CLOSED
- **Scope:** MODE MARATON atas rencana `docs/ops/SIAP-LANJUT.md` §3 — Fase 5 (Bayar & Struk).
  Lee memilih "lanjut": kerjakan berurutan tanpa bertanya, berhenti hanya pada Stop Conditions.
- **Posisi:** Fase 5 — **T5-01, T5-03, T5-04, T5-05 SELESAI** di sesi ini. Fase 5 tersisa mulai
  **T5-06** (void sebelum dapur mulai), lalu T5-07…T5-12 di `docs/ROADMAP.md`.
- **Sudah disepakati:** modal bayar keras-kode di `LayarKasir.tsx` dibongkar dan diganti layar
  `Bayar.tsx`/`useBayar` (satu pintu uang: RPC `bayar_pesanan` 0039) · struk memisahkan pajak &
  service dan TIDAK PERNAH menghitung uang sendiri (hanya menampilkan angka dari peladen) ·
  T5-04 tidak memakai migrasi baru karena pagarnya sudah ada di `0019`.
- **Masih terbuka:** **T-026** (infra e2e Playwright) dan **T-027** (pajak & service keranjang
  masih perkiraan layar) — keduanya menunggu pilihan Lee. Juga PR #1/#2/#3 masih OPEN (cabang
  sesi lama, base `main`) — belum disentuh sesuai larangan Lee.
- **Langkah berikutnya:** T5-06 (void sebelum dapur mulai, alasan wajib). Catatan: pagar
  `picu_pembatalan_sah` sudah ada di `0015` — **periksa dulu apa yang sudah hidup** sebelum
  menulis migrasi baru (pelajaran T5-04 & T5-05).

---

## Kronologi

1. **Orientasi.** `PRO.md` → `alat/mulai-sesi.py` (KARTU SESI) → `alat/lanjut-sesi.py` LOLOS.
   Catatan handoff "CI terakhir BUKAN success" ternyata **basi**: run `35819761987` ter-*cancel*
   karena tertimpa push, penggantinya `35819916392` **success**.
2. **Lee memilih "lanjut"** → mode maraton, tanpa pertanyaan di tengah jalan.
3. **Batch 1 — T5-01 sambungan kasir (commit `e8baf2c`).** Modal 3-metode keras-kode di
   `LayarKasir.tsx` dibuang, diganti layar `Bayar`. Uji penjaga `LayarKasirBayar.test.tsx`
   memastikan metode bayar tidak boleh dikeraskan lagi di layar kasir.
   **Jebakan yang ditemukan:** penjaga berbasis `?raw` ikut mencocokkan teks di dalam komentar —
   komentar harus dibuang dulu sebelum diperiksa, kalau tidak ujinya hijau/merah karena alasan palsu.
4. **Batch 2 — T5-03 struk (commit `66312f9`).** `Struk.tsx` + `Struk.test.tsx` + CSS +
   `supabase/tes/pajak_service.sql`. Pajak & service dicetak **terpisah** dan **selalu** muncul
   (termasuk Rp0), supaya pelanggan bisa mencocokkan angka. Selisih pembulatan ditampilkan apa
   adanya. Mutasi aplikasi ditambah 5 → 14/14 MERAH.
5. **Infra e2e Playwright — DITUNDA, bukan dikerjakan.** Dicoba dua cara hari ini:
   `npx playwright install --with-deps chromium` gagal (paket sistem `libxdamage1`,
   `libxkbcommon0`, `xvfb` tidak tersedia, apt keluar 100) dan `npx playwright install chromium`
   gagal ("Download failure code=1"). Ini menegaskan catatan 2026-09-17.
   **Keputusan:** TIDAK menambah langkah CI yang tidak bisa saya jalankan lokal — itu melanggar
   aturan paritas CI dan akan membuat CI merah sendiri. Dicatat sebagai **T-026** di
   `docs/TERTANGGUH.md`; `T11-01`/`T11-11` di ROADMAP ditandai **❓ T-026**.
6. **Batch 3 — T5-04 diskon (commit `227a51b`).** Temuan utama: **pagarnya sudah ada** sejak
   `0019_pesan_diskon_jujur.sql` (`picu_diskon_batas()`, cap kumulatif dari `0014`). Jadi tidak
   ada migrasi baru — membuat aturan uang kedua di tempat kedua justru berbahaya. Yang kurang
   adalah **buktinya**: ditambah `supabase/tes/diskon_tumpuk.sql` + `alat/uji-mutasi-0019.py`
   (**5/5 mutasi MERAH**), didaftarkan ke `ci.yml` + `GERBANG_WAJIB` + `periksa-semua.sh`.
7. **Koreksi jujur di dalam batch 3 (layak diingat).** Asersi pertama untuk "total diskon melebihi
   subtotal" ternyata **tidak menguji apa pun** — satu baris diskon besar lebih dulu ditahan pagar
   **batas izin** (owner pun berbatas 20 %), sehingga pagar subtotal tak pernah tersentuh.
   Ketahuan karena mutasi ke-5 tetap hijau. Ujinya diperbaiki (tumpuk menyala + cap 100 % +
   penambahan bertahap) sampai pagar subtotal benar-benar yang menahan. **Mutasinya tidak dibuang
   demi hasil hijau** — pola yang sama pernah terjadi pada `diskon_cap.sql` dan temuan audit A-17/F-06.
8. **Bukti akhir sesi:** aplikasi **330 tes LULUS** · suite SQL **83 LULUS · 0 GAGAL** ·
   `uji-mutasi-app.mjs` 14/14 MERAH · `uji-mutasi-0019.py` 5/5 MERAH · gerbang CI & paritas CI
   LOLOS (109 perintah) · `periksa-rujukan.py`, `periksa-bersih.py`, `periksa-roadmap.py` LOLOS ·
   `bash aplikasi/alat/periksa-semua.sh` hijau pada semua pemeriksa kode.
9. **Kendala penutup: token GitHub kedaluwarsa.** `gh auth status` → "The github.com token in
   GH_TOKEN is no longer valid"; `git push` menolak. Pekerjaan **aman sebagai commit lokal
   `227a51b`** di cabang sesi. Push menunggu Lee menyambungkan ulang GitHub di Arena.
   Tidak ada pekerjaan yang hilang, dan tidak ada cabang/PR yang saya sentuh.

10. **Batch 4 — T5-05 diskon + PIN atasan.** Pola yang sama seperti T5-04 terulang, tetapi kali
    ini dengan kesimpulan berbeda: sebagian pagar sudah ada, **satu bagian benar-benar hilang**.
    `picu_diskon_batas()` memeriksa izin PEMANGGIL; `picu_diskon_setuju_jujur()` (0016) sudah
    membuktikan atasan menekan PIN — tetapi keduanya tidak pernah berbicara, sehingga alur
    "di atas batas → PIN atasan" yang dijanjikan PRD M3 **mustahil dijalankan**. Ditutup migrasi
    `0041_diskon_pin_atasan.sql`, dengan syarat kupon yang sama persis seperti 0016 dan batas
    atasan tetap berlaku (PIN bukan cek kosong).
11. **Voucher keras-kode dibuang.** `LayarKasir.tsx` punya `BAROKAH10K` yang langsung memotong
    Rp10.000: tanpa izin, tanpa alasan, tanpa jejak pemberi, tanpa voucher apa pun di database,
    dan kodenya terlihat siapa pun yang membuka berkasnya. Diganti `DiskonManual.tsx` + uji
    penjaga `LayarKasirDiskon.test.tsx` supaya tidak kambuh.
12. **Kejadian penting: bukti mutasi lama tergeser diam-diam.** Sesudah menambah `0041`, mutasi
    **M6** di `uji-mutasi-0012.py` turun dari 16/16 → 15/16. Saya TIDAK menganggapnya kebetulan:
    dicek dengan `git stash` (pohon lama = 16/16, pohon baru = 15/16), ketahuan bahwa M6 mencari
    "migrasi terbaru yang memuat pola" dan pola `and pp.dipakai_pada is null` kini juga ada di
    `0041` — jadi mutasinya mengenai berkas saya, bukan pagar void. Diperbaiki dengan menyebut
    berkas `0015` secara eksplisit. Pagar void sendiri tidak pernah tumpul.
13. **T-027 dibuka, T3-02 dikoreksi jujur.** Saat membongkar `LayarKasir.tsx` ketahuan pajak &
    service masih dihitung layar (10 %/5 %). Tidak membahayakan uang (struk & layar Bayar memakai
    angka peladen), tetapi DoD T3-02 "tidak ada rumus uang di klien" belum sepenuhnya ditepati.
    Dicatat di ROADMAP apa adanya (❓ T-027) — centangnya tidak dicabut karena pekerjaan aslinya
    ada, tetapi kekurangannya tidak disembunyikan.
14. **T5-06 dikerjakan dengan memeriksa dulu, bukan langsung menulis migrasi.** ROADMAP
    menjadwalkan `0041_void_pra.sql`; sebelum membuatnya saya membaca isi database dan menemukan
    `picu_pembatalan_sah()` (`0015`) sudah memenuhi SELURUH DoD-nya — tahap dari dua tanda,
    alasan wajib, sekali batal, nilai dari salinan harga, tanpa penghapusan data. Migrasi baru
    tidak dibuat: selain mubazir, itu persis resep jebakan butir 12.
15. **Cacat sungguhannya di layar, dan cukup serius.** Tombol "Hapus item" di keranjang kasir
    membuang baris dengan `filter` untuk semua keadaan. Untuk draf itu benar; untuk item yang
    sudah tercatat itu berarti barang hilang tanpa alasan, tanpa pelaku, tanpa jejak — dan
    tabel `pembatalan` yang sudah dijaga ketat itu **tidak pernah dipanggil siapa pun**, jadi
    laporan pembatalan harian yang dijanjikan ke pemilik selalu kosong. Ditutup `VoidItem.tsx`
    (alasan wajib; spasi saja tetap dianggap kosong) dan percabangan `onBatalkanItem` di
    `LayarKasir.tsx` yang baru membuang item setelah peladen menjawab berhasil.
16. **Bukti T5-06:** VoidItem 8 tes + LayarKasirVoid 7 tes; aplikasi 62 berkas / **368 tes
    LULUS**; `uji-mutasi-app.mjs` **24/24 MERAH** dengan 4 mutasi baru (alasan tidak wajib,
    penolakan disulap berhasil, hapus tanpa alasan, item lenyap walau ditolak); suite SQL 84
    LULUS; `tsc` bersih; lint 0 error. Tidak ada perintah CI baru — uji baru ikut terbawa
    `vitest run` dan `uji-mutasi-app.mjs` yang sudah terdaftar di gerbang.
17. **Catatan pemulihan:** sandbox sempat ter-reset di tengah sesi (riwayat Git lokal hilang,
    `node_modules` + paket pip terhapus, commit `1d62cec` lenyap) — isi berkas selamat.
    Dipulihkan dengan `git fetch origin` + `git reset FETCH_HEAD`, pasang ulang dependensi
    (`psycopg[binary]` karena PEP 668), verifikasi ULANG semua angka bukti, lalu T5-05
    di-commit ulang sebagai `8cc6c18` dan **ter-push**.
18. **T5-07 dikerjakan dengan kebiasaan baru: periksa dulu.** Dugaan awal (pagar PIN sudah ada)
    benar, tetapi pemeriksaan menemukan sesuatu yang lebih serius dan tidak ada di rencana:
    kolom `pembatalan.bahan_terbuang` — **satu-satunya** penanda yang memisahkan "pembatalan
    biasa" dari "kerugian bahan" di laporan pemilik — tidak pernah diperiksa satu pemicu pun
    sejak `0010`. Yang dijaga hanya `nilai_kerugian`.
19. **Kenapa itu mahal:** kasir bisa membatalkan pesanan yang makanannya sudah dimasak sambil
    mengirim `bahan_terbuang = false`. Barisnya sah, PIN atasannya benar, nilainya benar —
    tetapi kerugian bahannya tidak pernah muncul di laporan, dan karena tabel ini append-only,
    angka itu bertahan selamanya. Ditutup `0042_bahan_terbuang_jujur.sql`: penanda dihitung
    peladen dari tahap, kiriman bawaan ditimpa, kiriman bertentangan ditolak.
20. **Jebakan yang sama kambuh dan saya kenali lebih cepat.** Suite SQL hijau (85 LULUS) tetapi
    `uji-mutasi-0012.py` turun 16/16 → 15/16 di M6. Tidak dianggap kebetulan: `0042` menulis
    ulang `picu_pembatalan_sah()`, jadi memutasi `0015` tidak berpengaruh lagi. Jangkar berkas
    M6 dipindah `0015` → `0042`, pulih 16/16, dan aturan umumnya ditulis di komentar berkas itu
    supaya orang berikutnya tidak perlu menemukannya untuk ketiga kalinya.
21. **Pendaftaran CI:** `alat/uji-mutasi-0042.py` didaftarkan di `.github/workflows/ci.yml`,
    `GERBANG_WAJIB` (`alat/periksa-gerbang-ci.py`), dan `aplikasi/alat/periksa-semua.sh` —
    gerbang & paritas LOLOS. Angka "84 berkas uji" di `PANDUAN_PENGGUNA.md` disegarkan ke 85
    (ketahuan `periksa-panduan.py`, bukan dibiarkan basi).
22. **T5-08: berhenti di batas yang benar, bukan berhenti bertanya.** Tugas ini menyentuh data
    pribadi pelanggan, jadi sebelum menulis kode saya cari keputusan terkuncinya — dan memang
    ada: T-011 (disetujui Lee 2026-09-21) menyatakan data pelanggan baru boleh DISIMPAN setelah
    kebijakan privasi + halaman persetujuan ada, dengan migrasinya dijadwalkan T8-15. Karena
    keputusannya jelas dan tidak bertentangan, ini bukan Stop Condition; yang benar adalah
    mengerjakan bagian yang memang boleh dikerjakan.
23. **Maka yang dibuat hanya layarnya** (persis kolom File di ROADMAP): `DataPelanggan.tsx`,
    tanpa tabel/migrasi/RPC sama sekali. Tidak ada satu pun nomor pelanggan yang bisa tersimpan
    sebelum kebijakannya siap. Yang dikunci & diuji: persetujuan eksplisit (tombol simpan mati
    sampai dicentang, dan persetujuan ikut sebagai data `setuju: true` — bukan diasumsikan),
    minimalisasi (hanya HP + nama panggilan opsional), penjelasan kegunaan data DI LAYAR bukan
    hanya di halaman kebijakan, dan tombol Lewati yang selalu hidup supaya bagian ini tidak
    pernah menghambat pembayaran (DoD eksplisit tugas ini).
24. **Satu pilihan yang saya jelaskan terbuka:** nomor tidak divalidasi ketat (cukup 8 angka).
    Menolak format tak biasa hanya membuat kasir mengarang nomor supaya bisa lanjut, dan data
    karangan di basis pelanggan lebih buruk daripada tidak ada data sama sekali.
25. **Angka bukti penutup sesi:** aplikasi 63 berkas / **377 tes LULUS** · suite SQL **85
    LULUS** · `uji-mutasi-app.mjs` **26/26 MERAH** · `uji-mutasi-0042.py` **4/4 MERAH** ·
    `uji-mutasi-0012.py` **16/16** · `tsc` bersih · lint 0 error · format bersih · gerbang &
    paritas CI LOLOS. Fase 5 kini T5-01…T5-08 selesai; berikutnya T5-09 (struk digital).
26. **Paritas CI penuh menangkap satu kegagalan nyata yang hampir lolos.** `periksa-semua.sh`
    dijalankan sampai habis dan berhenti di `alat/uji-mutasi-0015.py`: mutasi **F-05**
    (idempotensi pembatalan) dilaporkan "pagar TUMPUL". Setelah diselidiki, ini jebakan yang
    SAMA dengan butir 20 di tempat kedua — `0042` menulis ulang `picu_pembatalan_sah`, jadi
    memutasi salinannya di `0015` tidak berpengaruh apa-apa. Diperbaiki dengan menambah
    konstanta `MIG42` dan mengarahkan F-05 ke sana; `uji-mutasi-0015.py` kembali LOLOS.
27. **Kalau saya hanya mengandalkan suite SQL, CI akan merah dan saya tidak akan tahu
    sebabnya.** Suite SQL hijau sempurna (85 LULUS) sementara dua penilai mutasi lumpuh.
    Aturannya saya tulis permanen di `docs/DECISIONS_LOG.md` [Mutu gerbang/2026-09-23] dan di
    `SIAP-LANJUT.md` butir 0o1: sesudah menambah migrasi, jalankan SELURUH uji mutasi, bukan
    hanya suite SQL.
28. **Memeriksa CI GitHub menemukan kegagalan KEEMPAT dari jebakan yang sama — dan yang paling
    serius.** `gh run list` menunjukkan CI merah 4× berturut-turut sejak T5-05. Langkah yang
    gagal: `alat/uji-mutasi-0019.py` (pagar diskon T5-04). Sebabnya sama: `0041` menulis ulang
    `picu_diskon_batas` utuh, sehingga SELURUH 5 mutasi 0019 lumpuh sejak T5-05. Pesannya
    ("pagar tumpul!") gampang disalahartikan sebagai pagar diskon yang jebol; kenyataannya
    sesudah jangkar diarahkan ke `0041` hasilnya **5/5 MERAH** — pagarnya sehat sepanjang waktu.
29. **Seluruh 26 penilai mutasi disisir satu per satu** untuk memastikan tidak ada yang lumpuh
    lagi. Sempat muncul beberapa "GAGAL" palsu karena penilai memakai direktori kerja tetap di
    `/tmp` dan saya tanpa sadar menjalankannya bersamaan dengan `periksa-semua.sh` di latar;
    setelah dijalankan berurutan semuanya LOLOS (`0012` 16/16, `0014` 17/17, `0015` LOLOS,
    `0016` LOLOS, `0019` 5/5, `0041` 6/6, `0042` 4/4). Catatan alat ini ditulis di SIAP-LANJUT
    butir 0o3 supaya tidak menipu orang berikutnya.
30. **`periksa-semua.sh` penuh: 43 HASIL LOLOS, tanpa satu pun langkah CI gagal.** Sisa
    `[X]`-nya hanya pemeriksa handoff `lanjut-sesi.py` (ruang kerja belum di-commit saat itu),
    yang memang dibereskan di langkah penutup sesi.
31. **Token GitHub pulih (Lee menyambungkan ulang) → 2 commit tertahan ter-push**, dan
    `alat/lanjut-sesi.py` akhirnya **LOLOS** (sebelumnya mengeluh cabang belum ada di GitHub).
32. **T5-09 struk digital.** Periksa dulu: `Struk.tsx` sudah ada sejak T5-03, jadi yang belum
    ada bukan tampilan struknya melainkan cara MENYERAHKANNYA saat printer mati.
    `StrukDigital.tsx` membungkus `<Struk>` yang sama — bukan menggambar ulang — karena DoD
    menuntut "isi identik dengan struk cetak", dan satu-satunya cara menjamin itu selamanya
    adalah memastikan hanya ada SATU tempat yang menggambar struk. Dijaga dua uji penjaga
    `?raw`: dilarang beraritmetika atas medan uang, wajib tetap memuat `<Struk`.
33. **Tiga jalan penyerahan berlapis:** `navigator.share` (lembar berbagi HP → WhatsApp),
    `window.print` ("Simpan sebagai PDF" ada di Android & desktop), dan `navigator.clipboard`
    sebagai jaring pengaman. Tombol yang tidak didukung peramban DISEMBUNYIKAN, bukan
    ditampilkan lalu gagal — kasir yang diburu antrean tidak boleh menebak tombol mana yang
    bekerja. `@media print` menyembunyikan tombol supaya PDF-nya hanya berisi struk.
34. **Bukti T5-09:** StrukDigital 15 tes; aplikasi 64 berkas / **392 tes LULUS**;
    `uji-mutasi-app.mjs` **28/28 MERAH** (2 mutasi baru: tombol Bagikan tampil walau tak
    didukung, ringkasan mengaku LUNAS padahal belum dibayar); `tsc` bersih; lint 0 error;
    format bersih. Verifikasi manual di Android & desktop tetap milik Lee.

